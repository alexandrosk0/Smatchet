# Unanchored plan-delivery claims — grandfathered baseline

_Generated by `agents/scripts/core/test-plan-claim-anchors.sh --baseline`.
Do not hand-edit: regenerate and commit._

Each row is a § Deviations / § Implementation log line asserting that something
ALREADY existed, with no `file:line` / `#PR` / sha to check it against. They are
grandfathered so `--all` is usable as a gate at all; NEW claims must anchor.
Burn one down by adding the citation (or deleting the claim), then re-run
`--baseline` to shrink this file. Adding a row here does not make a claim true.

- `docs/plans/active/agentic-backlog-campaign.md` — `- **Phase 0 (staleness sweep) became the dominant first deliverable, not B1-execution.** Verifying premises before coding revealed **14 prioritized entries already shipped** — including 5 of 6 P1s (B1 CI merge-base, B2 merge-watcher GH_API_DOWN, relay, tooling long-running-polls, bug code-color). Executing them as written would have produced redo-work PRs. This is the "verify file-claims before acting" process rule paying out at campaign scale.`
- `docs/plans/active/backlog-pr-roadmap.md` — `- **2026-07-14 · 3 of 6 deferred groups had already shipped** (PR-10 all-but-user-info, PR-11, PR-18) — the doc's status rows were stale by ~3 weeks. Reconciled above; remaining open set is PR-14, PR-19, PR-21, plus the PR-10 user-info-window bucket-E residue.`
- `docs/plans/shipped/ai-assistant-side-panel.md` — `- **AppController_LuaStubs.cpp gains documentation only — no new stub.** Plan packet's claim "mirror stub implementations" assumed the OFF build needs ai.* Lua-callable no-op stubs. But when SMATCHET_WITH_LUA_AUTOMATION=0, the entire Lua interpreter is absent — no Lua script can run, so no ai.* name needs to resolve. The genuine parity is at the C++ AppController member level (AddAiContext / ClearAiContext / PromptAi), which Phase B already shipped as always-on stubs in AppController.cpp. LuaStubs.cpp gets a top-of-file docstring explaining this so future contributors don't try to add a stub mirror that wouldn't serve any link target.`
- `docs/plans/shipped/ai-assistant-side-panel.md` — `- **No LayoutSchemaVersion bump.** Phase C is additive: new files + new toggles that already shipped Phase A'. The single bump for the whole feature lands in Phase E per plan rule.`
- `docs/plans/shipped/ai-chat-textedit-markdown.md` — `- **Superseded by [markdown-language-definition-for-textedit.md] (markdown-language-definition-for-textedit.md) § Slice 5 (2026-05-19)**. The original constraint that drove this design ("ImGui has no rich-text-with-selection widget and writing one from scratch is multi-week") was eventually addressed by Slice 4 of that follow-up plan — a SelectableTextRun overlay that adds drag-select + Ctrl+C on top of any ImGui draw path. Slice 5 then moved the AI chat off AiChatTextEditorView onto MarkdownPreviewRender + the new overlay, fully retiring the TextEditor-as-prose-view experiment that landed here. Files deleted in that slice: AiChatTextEditorRender.{h,cpp}, plus the MarkdownChat() LD + SetTokenColor / DisableColorizerPasses vendored-patch methods on TextEditor. The original AiChatMarkdownTokens scanner was already retired in Slice 1 (LD-based coloring).`
- `docs/plans/shipped/appcontroller-god-object-decomposition.md` — `- Phase 3: per the triple-check, the TrackerConnectivityState enum was already relocated to Types/ConnectivityTypes.h by the appcontroller-fan-in plan — no enum work needed (kept the ITicketSyncDeps::ConnectivityState lockstep cast). The N→1 adapter fan-in is automatic (one adapter class, N instances share the 3 re-pointed setter bodies). The connectivity service's probe-state-transition unit tests needed a probe-rate-limit test seam: PumpUntilStateSettled calls the existing SetNextProbeAt(now) each tick to clear the ~90s healthy-probe gate (a wall-clock-bounded unit pump can't otherwise reach the next scheduled probe) — no production change, test-harness only.`
- `docs/plans/shipped/ci-falsepositive-hardening.md` — `- **coverage-quarantine false-red — regression-pin (NEW).** The coverage.sh fix itself (the --test-case-exclude=*[quarantined:*]* on both captures + the test-binary-vs-tooling verdict split via classify_capture_failure) was already applied; the missing action-item was the fixture. Added a coverage_gate.bats case pinning that the quarantine-exclude flag is defined AND handed to both capture invocations, so a silent removal (the exact false-red regression) reds a discoverable test.`
- `docs/plans/shipped/core-include-dag.md` — `Phase 0 (gate + baseline of 5 edges) was already merged on develop before this PR.`
- `docs/plans/shipped/coverage-sanitizer-required-contexts.md` — `- vs the original (pre-recon) plan: there is **no new UBSan job** — it already exists (Sanitizer (UBSan via Clang)). The real Phase-1 product of this slice is the ASAN Run ctest under ASAN deterministic-red fix + escape hatches + config promotion, not a job stand-up. The α/β preset fork is moot (α was already the implementation).`
- `docs/plans/shipped/dev-onboarding-first-run-quickstart.md` — `- **The persisted-flag item became a reuse of the already-shipped BackendHasBeenReachable** — dropping § Files to modify items **7, 8, 9** (the new TrackerSetupCompleted config key, its serialize/deserialize rows, and the first-launch migration) **and the § Verification bucket-A ShouldGrandfatherExistingUser matrix**. The re-audit found [ConfigManager] (../../../Source/Core/src/Config/ConfigManager.cpp) already persists BackendHasBeenReachable, set on the first successful backend round-trip — semantically the same "this user has a working tracker" bit the plan wanted to add, and already migration-safe for existing users (an existing config with a reachable backend already has it true), which is exactly what ShouldGrandfatherExistingUser existed to compute. Adding a second flag would have been two sources of truth for one fact. **Design-level divergence from the approved plan — surfaced to the user in the ship report.**`
- `docs/plans/shipped/dx12-standalone-win-arm64.md` — `- (this PR) · ARM64 advisory CI leg + Unreal ARM64 guard. build-and-test.yml gains windows-msvc-arm64: a compile-only amd64_arm64 cross job (msvc-dev-cmd arch: amd64_arm64 sources the cross vcvars → ninja-iter-msvc-arm64 builds SmatchetStandalone), continue-on-error advisory, no ctest (the cross-built ARM64 binary can't execute on the x64 runner). SmatchetImGuiPlugin.Build.cs throws a BuildException when a Win64 target's architecture is ARM64 (packaged Win64 libs are x64-only). Row 12 (installer) already shipped in Phase 2; row 13 (perf arch key) deferred — see Deviations.`
- `docs/plans/shipped/fuzz-smoke-gate-promotion.md` — `develop it was ALREADY corrected (Bucket- noted dropped 2026-06-15). So the AGENTS.md edit was`
- `docs/plans/shipped/god-file-splits.md` — `- **PR 4 seam reuse** — extended the pre-existing AppController_LuaBindings_detail.h (already shared with _Draw.cpp) instead of adding a new _Internal.h; the plan's "helpers → _Internal.h" (SanitizeLogText/TruncateForTrace/LuaTruthy/AsciiLowerCopy) was already largely satisfied by that header. Kept the whole automation/MCP runtime in the spine (unassigned by the plan) → spine ~710 (over the ~350 target, well under the 1,200 ceiling).`
- `docs/plans/shipped/lua-host-friend-drop.md` — `- **No deviations.** Sketch in § Next slice sketch executed verbatim. The class AppController; forward declaration was already absent from the new LuaAutomationHost.h (defaulted ctor + no friend means no need for the forward-decl); plan called for its removal — outcome equivalent.`
- `docs/plans/shipped/mobile-app-fuller-integration.md` — `../plans/shipped/…; test-docs.sh green). No code change — the code plan was already 100 % complete on`
- `docs/plans/shipped/mobile-app-fuller-integration.md` — `P1.1, P1.4 were already shipped (and P1.2 / P1.5 partially) before this plan was re-opened. See`
- `docs/plans/shipped/mobile-app-fuller-integration.md` — `trackerTypeBuf; the case-insensitive match it reads was already present). If the Test-delta gate FAILs,`
- `docs/plans/shipped/mobile-mvp-completion.md` — `- **WS4 item 16 — fix landed in the field-editor UI commit-trigger, not tracker-backend.** § Files-to-modify routed the stray-PUT fix to tracker-backend, on the assumption the wrong write should be stopped at the HTTP-PUT layer. The actual root cause is upstream of the backend: RenderTextInlineEdit (Source/Core/src/TicketFieldEditor.cpp) commits via QueueEdit on submitted || (!editJustStarted && IsItemDeactivatedAfterEdit()), and on-device *every* focus-loss (Back / tap-away / IME dismiss) is a deactivation while the desktop ImGuiKey_Escape-cancel guard is unreachable (imgui_impl_android emits no Escape key). A backend-layer fix would have to distinguish a user-intended PUT from a stray one *after* the edit was already queued — impossible without the UI-frame intent. The correct seam is the commit *decision* itself: a new pure header Source/Core/include/TicketFieldEditorCommitPolicyPure.h (ShouldCommitInlineFieldEdit(explicitSubmit, deactivatedAfterEdit, isMobile)) commits on explicit-submit-only when kMobileInlineEditBuild, leaving the desktop branch (isMobile=false) byte-identical to the prior submitted || deactivatedAfterEdit. The widget plumbing splits the explicit-Enter return from the focus-loss deactivation (new outExplicitSubmit out-param on DrawDurationFieldWithSuggestions; IsItemDeactivatedAfterEdit() for the plain-text branch). **No Source/Core/src/Tracker/ change.** The explicit long-press / commit-button affordance stays Phase-1 (item 23) per the "broad: no implicit commit on mobile" scope decision.`
- `docs/plans/shipped/msvc-build-onboarding-hardening.md` — `- Docs slice (2026-06-03): closed the previously-deferred plan files 5 + 6. README.md build section gains the blessed one-command (build_and_run.ps1 -Preset ninja-iter-msvc, no Developer Prompt needed) + the MSYS2-retirement note. AGENTS.md § Build rule gains the "never propose MSYS2 for building" guard (the canonical ninja-iter-msvc command was already present). **Remaining: only the optional/conditional file 9** (test-build-warnings.sh first-error summary — gated on "if build logs remain noisy under MSVC", not observed; effectively droppable).`
- `docs/plans/shipped/msvc-build-onboarding-hardening.md` — `- build_standalone.ps1 (plan file 1) already had the MSVC bootstrap from slice 1. This slice only added the retirement throw for *-msys2 presets (replacing the Use-Msys2Ucrt64Environment call) — the overall function was not removed to preserve historical context in Use-Msys2Ucrt64Environment for the function body itself (it is now unreachable but documents what it did).`
- `docs/plans/shipped/p4-gated-ship-loop.md` — `- **ADR slot 0006 → 0008**: plan said docs/adr/0006-p4-gated-ship-loop.md. Slot 0006 was already taken by 0006-orchestrator-pr-stays-draft-by-default.md (and slot 0007 by 0007-audit-trail-actor-column.md) when implementation started. New ADR landed at docs/adr/0008-p4-gated-ship-loop.md; the plan's frontmatter + § Files-to-modify entry updated to match. No semantic change.`
- `docs/plans/shipped/reduce-agent-prompt-bloat.md` — `5. **config sub-block**: budgets nest in the **existing** agents block (which already had enabled_project), not a new block — per-block additionalProperties:false required editing agents.properties specifically.`
- `docs/plans/shipped/solo-merge-review-gate.md` — `- **Core change was already live** (grill finding): develop already had required_pull_request_reviews: null (0 reviews), so the count-flip was not needed — this PR only **codifies** the existing state. The live-apply is therefore an idempotent *confirmation*, not a fix. § Context rewritten to reflect this.`
- `docs/plans/shipped/spawn-mcp-auth.md` — `lifetime already has same-user local access (out of the threat model). Tracked for`
- `docs/plans/shipped/tracker-interface-split.md` — `- **ITrackerIssueReader.h includes CachedTicketTypes.h instead of LocalCacheManager.h**: CachedTicketTypes.h is the SQLite-free header that defines CachedTicket. The plan said to include LocalCacheManager.h but the lighter include is strictly better and was already the right call per CachedTicketTypes.h's own header comment.`
