# Plan — build-quality + velocity hardening

> **Slug**: `build-quality-velocity-hardening` (matches this file's basename without `.md`).
>
> **Status**: `active`

## Context

Triggered by a "would vlang help?" question. A 13-dimension, 27-agent evidence-backed audit of the C++14 dual-target build, plus a read-only round-2 that resolved three Sprint-2-gating unknowns.

**Verdict:** a language/std swap is rejected — `Source/Core/` compiles into both `SmatchetStandalone` (OpenGL/GLFW) and `SmatchetCore_DX12` (Unreal); Unreal pins C++14 and forbids a non-C++ module, so the swap is impossible *and* the audit shows it unnecessary. **31 of 40 findings are fixable in place; only 9 are genuine intrinsic C++14/dual-target tax, most still mitigable in-language** (pImpl, fwd-decl, per-target PCH, `static_assert`).

After this lands, the highest-ROI build-quality + velocity fixes are sequenced, and the "build/CI reports GREEN while real breakage escapes" correctness classes are closed. Several findings are direct **Pillar-3** violations / real bugs: a sol2 patch that fails open into heap corruption on a dep bump, an OfflineQueue field-edit replay that wedges permanently, a silently-desyncable ImWchar ABI, and a bare swallow of untrusted Plane JSON.

Origin: session audit workflows `wf_8406f84f-757` (40-finding synthesis) + `wf_e18e1d41-69f` (round-2). The raw 40-finding JSON, per-bug fix specs, and round-2 evidence are staged in session scratch (not committed here); the actionable roadmap with evidence refs is inlined below, and every line-number claim was re-verified directly against the tree.

## Approach

Fix in place — no language/std change. Three phases, weighted to protect **both** code-quality and production-speed, never trading a quality gate for speed:

1. **Sprint 1 — stop false-green / fail-open.** All S-effort; each closes a class where the build or CI is green while a real defect or a dropped correctness patch escapes. Includes the four real bugs. Highest quality-per-hour, and several also remove agent/CI round-trips (speed).
2. **Sprint 2 — structural.** Attack the two root multipliers (the `json.hpp` / sol2 / `AppController` header choke; the missing automated safety nets) plus throughput (a merge queue + gate-correctness).
3. **Backlog / accept.** The irreducible C++14/dual-target floor — pay down ride-along, never as a dedicated sprint.

Round-2 corrected two would-be-wasted Sprint-2 efforts (the `json_fwd` swap is ineffective in isolation; ThreadSanitizer cannot run on the Windows runners), captured inline so the structural work targets the real levers rather than the audit's first guess. Trade-off that shaped the sequencing: the four bugs + the dependency-patch hardening lead because they are correctness (Pillar-3), not optimisation — speed work waits behind them.

## Roadmap

### Sprint 1 — stop false-green / fail-open (all S-effort)

> **Status: Sprint 1 FULLY SHIPPED (verified against the tree 2026-06-07).** Every bullet
> below is DONE — per-item commits/PRs in § Implementation log. Highlights: #16 → #905
> (latch release at `OfflineQueueService.cpp:752-759` + pinning runtime test
> `OfflineQueueServiceRuntime.test.cpp:721-749`), #37 → #906 (`PlaneIssueMutation.cpp:327`
> `LOG_WARN` handlers), #23 → #907 (`SmatchetImGuiFonts.cpp:24`), #3/#5/#6 → #915
> (include gone from `AppController.h`; glob-vs-list assert `tests/CMakeLists.txt:603`;
> `scripts/dev/is-exe-fresh.sh`), #4/#15/#24 → #911 (`project.config.json:93` +
> `test-config-globs.sh`; `function_size_audit.py --assert-absent`;
> `no-glfw-in-core-headers` in `test-lint-rules.sh:270`), #33 → #909
> (`build.md:49` § Stale-PCH recovery (C2859)), #38 → `6537dc3` (remaining
> `ninja-iter-msys2` mentions are intentional: migration notes in BUILD.md/README.md +
> the rejection regression test `scripts/dev/local/test-build-wrapper.ps1`). Do not re-do.

- **[#1] `smatchet_patch_or_die`** — route all vendored-source patches (sol2 metatable token, cpr, sqlitecpp, ghc, luaconf) through one helper that `string(FIND)`s its target and `FATAL_ERROR`s if absent, then SHA-pin cpr/sqlitecpp/sol2. Today the sol2 patch's only guard is a change-detector (`CMakeLists.txt:609`); a dep bump silently no-ops it → heap corruption returns. **Top recommendation — do first.**
- **[#16] OfflineQueue latch reset** — `OfflineQueueService.cpp:666-668` returns on null `Mutations()` without releasing the in-flight latch the other three early-returns release → field-edit replay wedges permanently → offline edits silently lost.
- **[#23] ImWchar `static_assert`** — emit `static_assert(sizeof(ImWchar)==4)` from a DX12 host TU; WCHAR32 parity is hand-synced across three points (`SmatchetImConfig.h:15`, `Build.cs:12`, packaged `imconfig.h:70` commented out) with no compile-time guard → silent ABI/text-memory corruption.
- **[#37] Plane empty-catch marker** — `PlaneIssueMutation.cpp:327` bare `catch(...){}` swallows untrusted-JSON parse with no log/marker (strict-zone CRITICAL).
- **[#3]** delete dead `#include "JiraClient.h"` (`AppController.h:40`) — the only `JiraClient` occurrence in the header is the include itself; drags cpr+json into its 105 direct includers.
- **[#4]** point the `**/Locales/*.json` visual-validation glob (`project.config.json:91`) at the real localization source (it matches zero tracked files) + add a zero-match selftest.
- **[#5]** `tests/CMakeLists.txt` is a hand-maintained `add_executable` list (no GLOB) — add a configure-time glob-vs-list assert so a new test `.cpp` can't be silently uncompiled (false green).
- **[#6]** add `scripts/dev/is-exe-fresh.sh` source-vs-exe mtime preflight wired into the run path — exe-staleness is prose-only today.
- **[#15]** decomposition verification must assert the function is ABSENT from the absolute size list, not just pass `--diff` (a still-over-cap partial reduction currently passes).
- **[#24]** add a `no-glfw-in-core-headers` grep delta rule to `test-lint-rules.sh` (the rule is prose-only).
- **[#33]** document the C2859 / stale-PCH ~4s targeted regen + the `SMATCHET_USE_PCH=OFF` escape in `build.md`.
- **[#38]** prune the retired `ninja-iter-msys2` build dir + codex debris.

### Sprint 2 — structural (estimates use the round-2-corrected numbers)

> **Resuming?** This is the original roadmap (historical). For the CURRENT state —
> what's shipped / parked / remaining 🟢 / escalated 🔴 — read **§ Verification
> (actual) → "Sprint-2 status"** below; it is authoritative. Don't re-do items the
> status block marks shipped.

Root multiplier A — the `json.hpp` / sol2 / `AppController` header choke:

- ~~**[#2]** `json_fwd` swap of the 3 named headers~~ — **NO-GO in isolation** (round-2): relieves only 4 of 298 json-pulling TUs because `Commands/Command.h` (201 TUs) + `AppController.h` (185 TUs, `parametersSchema`) hold `nlohmann::json` **by value** → can't forward. Unblock via #19 first.
- **[#19]** pImpl `AppController` (8,022 LOC across `AppController*.cpp`; 105 direct / ~114 transitive includers; lifts sol2's 22k-line header out of the public graph) — also the prerequisite for any `json_fwd` payoff.
- **[#20/#28]** PCH json: Standalone = NO-GO ("net wash" confirmed at `SmatchetPch.h:17-21`); **Core_DX12 json-PCH = GO for a one-shot trial** — PCH-less today (the `SMATCHET_USE_PCH` block in `Source/Standalone/CMakeLists.txt` is Standalone-only) yet 64% of its 225 TUs pull json, never measured. ghc-in-PCH = NO-GO (6-7% reach). Tests get a `REUSE_FROM` PCH.

Root multiplier B — missing automated safety nets:

- ~~**[#10]** TSan on existing runners~~ — **NO-GO** (round-2): clang-cl has no Windows TSan runtime; `Sanitizers.cmake:86-92` hard-returns under MSVC/clang-cl; no Linux C++ build exists. Redirect to its own scoped slice: a new `ubuntu-latest` nightly TSan job over the portable pure-logic + threading subset — requires standing up a Linux Clang preset (none today) + confirming Core/Sync/Tracker Linux-portability.
- **[#11]** per-PR UBSan (runs under clang-cl today) + a nightly Whisper/Lua-ON sanitizer.
- **[#12]** bucket-E render coverage for the 8 remaining data-dependent UI windows.
- **[#8/#13]** revive the perf gate — commit `ci-windows-latest` baselines, fix the headless launch, enforce 6.94 ms + p99.

Throughput (protect speed without weakening checks):

- **[#14]** ~~GitHub merge queue for develop~~ — **DONE (alt path)**: queue is org-only / unavailable on this user-owned repo; the O(n) per-PR rebuild tax under `strict=true` was instead removed by turning **strict off** (PRs merge on own green head, post-merge CI backstop). See §Deviations.
- **[#7]** tighten the comment-noise gate (the #1 build-green-CI-red cause) + wire `pre-ship.sh`'s format-then-gate order into the Stop hook.
- **[#9/#18]** de-dup the coverage / perf cold-rebuilds (redundant whole rebuilds — the higher win).
- **[#29]** split the serial windows-msvc 2-preset configure — **LOW** (~100s/run).
- **[#21]** project-local `Optional<T>` / `Result` for the 122 `outError` + 19 `Try*` patterns. **#21b (tracker backend `ITrackerBackend` virtual-interface layer) — SHIPPED 2026-06-08** via [`tracker-result-migration.md`](docs/plans/tracker-result-migration.md) Slices 1–7 (PRs #1020/#1022/#1023/#1029/#1030/#1033/#1035): all `ITracker*` sub-interface `bool + outError` virtuals → `Result<T, TrackerError>` / bare `TrackerError`, zero behaviour change. **Remaining #21:** the `AppController` public-API flip (that plan's scoped-but-unexecuted Slices 8–9) + the non-tracker `outError` sites (Whisper / P4 / config / texture cache).
- **[#22]** SHA-pin remaining deps + a CI smoke for the load-bearing Lua mirror.
- **[#25]** a CI step exercising the `SmatchetPackageUnrealLibs_DX12` packaging graph.
- **[#31/#32]** extract cpr-free `*Pure` Tracker logic for unit tests; fix the flaky-test env gate + add a quarantine tag.

### Backlog / accept — irreducible floor (ride-along only)

- **[#26]** 116 ImGui-draw monoliths — immediate-mode, not a C++14 defect.
- **[#40]** 449 per-backend clones — ISP-correct; extract only the AI streaming-parse.
- **[#34]** publish LTO link (publish-only, correctly scoped).
- **[#35/#36]** configure re-surgery stamp; dynamic perf-scope-name map growth.

## Files to modify

Sprint 1 (the first PR or two — split along seams if the diff exceeds the per-PR ceiling):

1. [`cmake/SmatchetThirdParty.cmake`](../../../cmake/SmatchetThirdParty.cmake) — add `smatchet_patch_or_die` helper (#1).
2. [`CMakeLists.txt:564`](../../../CMakeLists.txt) — route the sol2 + cpr/ghc/luaconf patches through the helper; SHA-pin cpr/sqlitecpp/sol2 (#1).
3. [`Source/Core/src/Sync/OfflineQueueService.cpp:666`](../../../Source/Core/src/Sync/OfflineQueueService.cpp) — reset the latch under the schedule mutex before the null-Mutations return (#16).
4. [`Source/Core/src/Tracker/PlaneIssueMutation.cpp:327`](../../../Source/Core/src/Tracker/PlaneIssueMutation.cpp) — add the `// catch-all-ok:` marker (#37).
5. A DX12 host TU ([`Source/Core/src/Ui/SmatchetImGuiHost.cpp`](../../../Source/Core/src/Ui/SmatchetImGuiHost.cpp), confirmed to include `imgui.h`) — `static_assert(sizeof(ImWchar)==4)` (#23). Route via `unreal-bridge`.
6. [`Source/Core/include/AppController.h:40`](../../../Source/Core/include/AppController.h) — delete the dead `JiraClient.h` include (#3).
7. [`project.config.json:91`](../../../project.config.json) — fix the visual-validation glob (#4).
8. [`tests/CMakeLists.txt:12`](../../../tests/CMakeLists.txt) — glob-vs-list configure assert (#5).
9. [`agents/scripts/project/test-lint-rules.sh`](../../../agents/scripts/project/test-lint-rules.sh) — `no-glfw-in-core-headers` rule (#24); `--scan-file` absolute-list assert (#15).
10. `scripts/dev/is-exe-fresh.sh` (new) + the relaunch path — exe-staleness preflight (#6).
11. [`docs/agent-rules/build.md`](../../agent-rules/build.md) — C2859 / stale-PCH recovery note (#33).

Sprint 2 file set (interface headers, `AppController.{h,cpp}`, the PCH headers + `CMakeLists.txt`, `.github/workflows/*`, `cmake/Sanitizers.cmake`, `tests/ui/`, `docs/perf/baselines/`) is scoped per-slice at execution time — each slice is its own PR.

## Existing utilities reused

- `WarnIfPackagedLibsAreStale()` mtime-comparison pattern in `SmatchetImGuiPlugin.Build.cs` — reuse the shape for `is-exe-fresh.sh` (#6).
- The existing grep-rule pattern in `test-lint-rules.sh` (`no-printf-stderr`, `define-imgui`) — clone for `no-glfw-in-core-headers` (#24).
- `Config/ConfigManager.h:21` + `Config/ToolbarConfig.h:15` already use `json_fwd` — the in-repo precedent for #2 once unblocked.
- `function_size_audit.py --scan-file` / `--list` — the absolute-list assert for #15.
- The three sibling early-returns in `OfflineQueueService.cpp:648/653/658` — the exact reset pattern bug #16 must mirror.
- The `// catch-all-ok: <reason>` marker convention (e.g. `Config/ConfigManager.cpp:633`) — the fix shape for #37.

## UX Pillar callouts

- **Pillar 1 (perf, 144 Hz / 6.94 ms)**: #13 makes the budget enforced (it is not today); the pImpl/header changes (#2/#19/#20) are compile-time only — no runtime delta. Each Source/Core slice runs its mapped perf scenario before merge.
- **Pillar 2 (UI never blocks > 100 ms)**: no new sync I/O on the UI thread is introduced; #11 adds the sanitizer coverage that protects the existing dispatch model.
- **Pillar 3 (never crash)**: the headline of Sprint 1 — bugs #16 (data loss), #23 (memory corruption), #37 (swallowed error), and #1 (fail-open heap corruption) are all Pillar-3 fixes.
- **Pillar 4 (accessibility)**: N/A — no accessibility surface is touched by this plan.

## Perf-review-system gates (mandatory when diff touches `Source_Core/`; else `N/A — <reason>`)

**Fires** — Sprint-1 #16/#23/#3 and all of Sprint-2's structural items touch `Source/Core/`.

1. **PR-fast CI** — the header/pImpl slices (#2/#19/#20) touch the `AppController` / `Command` hub → scenario **`idle`** (`command-palette-fuzzy` is bucket-C-only — requires `--screenshotPath` — per `agents/core/perf-gatekeeper.md` § Curated diff → scenario map); a slice that also touches grid files adds `priority-grid-scroll`. The Sprint-1 bug fixes are off any hot path.
2. **Pillar 2 static scanner** — no new sync-I/O reachable from `ImGui::*` is added by any slice.
3. **Dispatcher drain** — bug #16 touches `OfflineQueueService` replay scheduling, not `MainThreadDispatcher::Drain()`; no change.
4. **Visible-cue bucket-E harness** — no new > 100 ms sync-stall code path.
5. **Marker inventory** — #36 (backlog) would change a `SMATCHET_UI_PERF_SCOPE` name; regen `docs/perf/MARKER_INVENTORY.md` in that PR. #13 is itself a perf-system change → re-baseline intentionally with golden-approval.

**Pre-push local check** (self-contained): `bash scripts/dev/perf-run.sh <scenario>` then `python scripts/dev/perf-compare.py docs/perf/baselines/<scenario>.<host>.json build/perf-runs/<scenario>-<ts>.json` for the scenario(s) named above, before opening any Source/Core PR (full procedure: `docs/guides/perf-workflow.md` § Gate-check).

## Risks / non-goals

- **Risk — deleting `AppController.h:40` (#3) breaks a transitive consumer** that relied on `AppController.h` to pull `JiraClient.h`. Mitigation: the dual-target build (`SmatchetStandalone` + `SmatchetCore_DX12`) catches it; the 3 `.cpp` that use the type already include it directly.
- **Risk — the Core_DX12 json-PCH trial (#20) may not pay off.** Mitigation: it is a measurement, not a commitment — revert if there's no wall-clock win.
- **Risk — `smatchet_patch_or_die` (#1) over-fires** if a future dep legitimately already matches. Mitigation: the helper returns early when the replacement text is already present (idempotent).
- **Non-goal — any language/std change.** This is the rejected premise of the whole audit.
- **Non-goal — the backlog/accept tier as a dedicated sprint.** Ride-along only.
- **Non-goal — applying the staged bug fixes inside this PR.** This PR is the plan only; the four fixes ship as their own subsystem-routed PRs.

## Verification

- **Bucket A (pure-logic ctest, `test-rig`)**: bug #16 — a `TickOfflineFieldEdits` test with null `Mutations()` asserting a later tick is not latched out; #5/#15/#24 each ship a `--selftest`/bats case proving the gate fails on bad input and passes on good.
- **Bucket E (ImGui Test Engine)**: #12 adds boot-open-assert-widget smokes for the 8 uncovered windows.
- **Bash-driver scenario / screenshot / sanitizer**: #11/#13 wire the per-PR UBSan + perf gate; #1 — a CI smoke asserting the sol2 metatable token is present after configure.
- **Build gate**: `cmake --build --preset ninja-iter-msvc --target SmatchetStandalone SmatchetCore_DX12` (dual-target) on every Source/Core slice — the #23 `static_assert` must compile in both worlds.
- **Doc validation (blocks plan-doc PRs — keep this bullet)**: the canonical `scripts/dev/test-docs.sh` suite green (anchors / agent-contract / plan-index / ref-integrity / portable-purity / md_lint). A red doc-validation job blocks merge even though non-required.
- **Plan stress-test — `grill-with-docs` (keep this bullet)**: stress-test this plan against the domain model + sharpen terms before executing Sprint 1; record the outcome.
- **Manual residue**: none introduced. Each slice's verification is automated per the bucket above; the only judgment step (TSan Linux-portability for #10) is a scoped slice, not silent residue.

## Out of scope (flagged, not designed)

**Deferral residue-sweep (keep this note)** — before finalising, grep `**/CONTEXT*.md`, `docs/adr/`, `agents/*.md`, and `docs/self-improvement/categories/` for stray references to anything deferred here, and revise or delete them.

- Any language/std swap (vlang, C++17/20) — the rejected premise.
- The backlog/accept tier (#26/#40/#34/#35/#36) — ride-along, no dedicated work.
- Applying the four staged bug fixes — separate subsystem PRs (`offline-sync`, `tracker-backend`, `unreal-bridge`, `build-doctor`).
- The raw 40-finding audit JSON + per-bug fix specs — session scratch, not committed; this plan inlines the actionable subset.
- The 4 developer-owned `SmatchetPreferencesUi*` / UI WIP files — untouched.

## Implementation log
*Sprint 1 FULLY shipped (incl. hygiene #3/#5/#6 via #915); Sprint 2 partial → plan stays `active` (not archived).*

- `4cc7c4a6` (#905) · #16 OfflineQueue: reset the in-flight latch on the null-`Mutations()` early-return (mirror the 3 sibling returns) + a discriminating runtime test (count 0→1 across the reset).
- `57789552` (#906) · #37 Plane `CreateIssue`: `LOG_WARN` instead of an empty `catch(...){}` on response-parse (Network/API tier) + `catch-all-ok` markers on the 2 sibling parse-fallbacks.
- `909aa2a8` (#907) · #23 ImWchar: `static_assert(sizeof(ImWchar)==4)` in `SmatchetImGuiFonts.cpp` guarding `IMGUI_USE_WCHAR32` parity (compiled in both targets).
- `8a06ae74` (#908) · #1 `smatchet_patch_or_die` helper routing all vendored patches (FATAL on a vanished target) + SHA-pin cpr/sqlitecpp/sol2 + a sol2-token configure-time assert.
- `c3c9a72b` (#909) · #33 `build.md`: MSVC toolset-pin (canonical `STL1001` / `with-msvc-env.sh` / `msvc_toolset_pin`) + stale-PCH (C2859) recovery.
- `272dbabc` (#911) · #24 `no-glfw-in-core-headers` lint rule + #15 `function_size_audit.py --assert-absent` + #4 repoint dead `Locales/*.json` glob at the real source (+ `test-config-globs.sh`).
- #38 (prune retired msys2 debris): already removed in `6537dc3` (`bootstrap-msys2.ps1` + `*-msys2` presets); leftover untracked `build/*msys2*` dirs cleared locally.
- `0f3e6ab5` (#915) · Sprint-1 hygiene finish: #3 drop the dead `JiraClient.h` include from `AppController.h` (add the 5 cpr-free role interfaces it transitively provided — drops cpr from ~105 includers) · #5 tests glob-vs-list configure assert + `check-test-list.sh` · #6 `is-exe-fresh.sh` exe-staleness preflight.
- `37562965` (#917) · #22 SHA-pin remaining FetchContent deps + `test-lua-mirror-smoke.sh`.
- `c2287c02` (#918) · Test-delta gate auto-exempts no-new-runtime-surface diffs (the test-light exemption filed in the 2026-06-06 postmortem).
- `e1bd3d38` (#921) · #7 prose-guard the comment-noise gate (`comment_audit.py` `is_prose_not_code` + `--selftest`, regression-guarded by `lint_rules.bats`) + wire the CI delta-lint gate into the Stop hook (`pre-ship-stop-gate.sh` — fires only in the ready-to-ship state, non-trapping, `SMATCHET_SKIP_STOP_GATE` escape).
- `5a45dc49` (#922) · #11 per-PR UBSan job (`Sanitizer (UBSan via Clang)`, clang-cl) + #25 `SmatchetPackageUnrealLibs_DX12` packaging-graph smoke. Both non-required, additive. **Caveat (filed `infra.md` P2)**: the UBSan job is path-gated to `source_core_cpp` and merged without ever executing — validate on the next Source/Core C++ PR.
- `20f63bb3` (#923) · #31 verified the Tracker `*Pure` extraction is already comprehensive (all 9 units have substantive tests); filled the one real gap — `IssueTableSerializer` (35 tests / 108 assertions). #32 doctest-native flaky-test quarantine (`SMATCHET_QUARANTINED_TEST_CASE` + `SMATCHET_QT_CHECK`, env-flip via `SMATCHET_RUN_FLAKY`).
- `ea4017fa` (#927) · coverage fast-follow: `coverage.sh --no-breaks`. OpenCppCoverage attaches as a debugger, so doctest broke into it on #923's intentional WARN-on-false → `0x80000003` (all tests passed; the process crashed at teardown). `--no-breaks` disables only the break; real failures still fail. Fixes a latent coverage-job fragility. (Required because #923 auto-merged past the resulting red **non-required** Coverage check — gate-escape postmortem `postmortems.md` 2026-06-06 "#923".)
- `9f5e56a6` (#929) · #8/#13 **PARKED**: `docs/plans/perf-gate-revival.md` — full investigation + 8-step playbook. The gate is a guaranteed-pass no-op today (zero `ci-windows-latest` baselines; perf workflows provision no Mesa software-GL so the headless `--spawn` launch is unproven; 6.94 ms is not encoded in `perf-compare.py`/`regression-policy.json`). SAFE-NOW steps (Mesa wiring, scenario-set fix, mean-ceiling code) + PARKED steps (live CI run → human-approved golden baselines → flake calibration → required-wiring) split in the doc; blocked on human baseline approval + multi-run flake calibration.
- (#937 + baselines PR) · #8/#13 **UN-PARKED → GATE ARMED** (2026-06-07): Mesa software-GL fixed end-to-end (root cause: Mesa ≥ 22 thin-loader split — `opengl32.dll` alone dies at process start; same bug was live-but-masked in bucket-C/E, postmortem owed), `command-palette-fuzzy` dropped from the PR-fast set, `mean_abs_ceiling_ms` knob landed (null until calibration), `perf-run.sh` result-file-authoritative fix, headless launch **proven on the runner** (4/4 scenarios, `run_failure_count=0`), 4 human-approved `ci-windows-latest` baselines committed, `Perf PR-fast` added to the merge-gates meant-to-block allow-list (+ bats). Remaining in `perf-gate-revival.md`: 5-calibration, skip-companion (7), GitHub-required flip (6b, user-gated). Found: `scenario.run` emits no `p99Ms` — p99 ceiling inert until the emitter adds it.
- (2026-07-23, #21 Tier-2 `Result<T>` batch — SubprocessCapture::Run + P4AnnotateFile + HttpGetBinary + DownloadAttachmentToLocalFile) · **Flipped four Tier-2 `bool + out-param(s)` sites onto payload-carrying `Result<T>` (user "do tier 2").** (1) **`SubprocessCapture::Run`** (`SubprocessCapture.h` now `#include`s `SmatchetResult.h`) → `Result<CaptureResult>` — reuses the existing `CaptureResult` struct; the internal `RunWindows`/`RunPosix` helpers stay on `bool + out + outError` and `Run` wraps them (`Ok(std::move(out))` / `Err(std::move(outError))`). Sole prod caller `P4RunCommand` (`P4Annotate.cpp`) reads `has_value()`/`error()`/`std::move(value())`; the failure log dropped its stale `cap.durationMs` reference (unavailable on the Err branch). 7 test sites (`SubprocessCapture.test.cpp`) updated. (2) **`P4AnnotateFile`** (`P4Annotate.h` — already `Result`-wired) → `Result<std::vector<P4AnnotatedLine>>`; empty-vector-is-success preserved (only spawn/exit failures → `Err`). Sole prod caller (`AnnotateAnalysisUi_Worker.cpp` `std::async` detail loader) branches `has_value()` into `DetailPack{Lines,Error}`. 5 test sites (`P4AnnotateE2E.test.cpp`) updated. (3) **`HttpGetBinary`** (file-local in `SmatchetFieldIconRender.cpp`) → `Result<std::vector<unsigned char>>`; sole caller (a `LaunchBackgroundTask` worker whose result is captured into a second `PostToMainThread` lambda) routes through the shared **`UnpackResult<T>`** helper into its `fetchOk`/`bytes`/`fetchError` locals. (4) **`DownloadAttachmentToLocalFile`** (file-local in `AttachmentAppUpdateService.cpp`) — the one genuine multi-payload collapse: the two out-params (`outFilePath` + `outMime`) fold into a new file-local **`struct DownloadedAttachment { filePath; mime; }`** → `Result<DownloadedAttachment>`; all 3 callers (`OpenAttachment` / `OpenAttachmentInSystemViewer` / `DownloadAttachmentForPreview`, all same TU) read `has_value()`/`value().filePath`/`error()`. **Excluded (documented non-goal): `SmatchetImGuiHost::TakeCommandResultJson`** — its `bool + std::string& outJson` has NO `outError`; the `bool` means "result ready?" (a polling miss paired with `IsCommandResultReady`), i.e. the **`Optional<T>` value-or-absent shape, not the `bool + outError` contract #21 targets** — AND it is re-exported across the stable Unreal C ABI (`SmatchetHost_TakeCommandResultJson`, `char*`/`nullptr`), so a `Result<T>` would both misrepresent "not ready" as an error and force a boundary change. Left as-is (matches the earlier "`Try*` probes are `Optional<T>`, out of #21 scope" ruling). **Dup gate:** the de-`outError`'d attachment download prologue re-surfaced the pre-existing `McpPlugin.cpp` capped-cpr-download clone (grandfathered at base, tripped only because the function is now in the diff) — added the same ADR-0015 `SMATCHET_DEVIATION(rule=duplication)` marker the twin site at `:513` already carries (each download site inlines its capped writer deliberately; a shared helper would couple Core↔Mcp). Gates: `coverage-delta-gate` PASS (prod + `SubprocessCapture`/`P4AnnotateE2E` test deltas both present → **no `tests-out-of-band`**), `test-lint-rules.sh --diff` PASS (incl. include-cycle + fan-in ratchet), `dup_audit --diff` PASS, clang-format clean. Two sites are unit-tested (SubprocessCapture 7 cases + P4AnnotateFile 5 cases, updated in-place); the two cpr network paths (`HttpGetBinary`, `DownloadAttachmentToLocalFile`) stay untested (cpr/network — as before). **Uncompilable locally → CI verifies the dual-target MSVC build + the POSIX core compile + ctest.** **This clears Tier 2 of the #21 forward map.** Remaining #21 is Tier 3 (structural blockers — do-not-flip), Tier 4 (scenario `void + outErr` test-owned sweep), the Whisper-plugin adjacents, and the Tracker-internal residuals; `SaveAutomationScriptContent` stays unmigrated (no live caller — likely dead).
- (2026-07-23, #21 Tier-1 leftovers `VoidResult` — TryBeginLuaAiPromptTurn + DownloadAndLaunchInstallerUpdate) · **Flipped the two remaining clean Tier-1 `bool + outError` sites onto `VoidResult` (user "do Tier 1 leftovers"), closing the Tier-1 forward-map row.** (1) **`AppController::Impl::TryBeginLuaAiPromptTurn`** (`AppControllerImpl.h` decl + `AppController_LuaBindings_Ai.cpp` impl) — the AI-prompt rate-limit/consent gate. The two reject branches (`RejectReentrant` / `RejectTooSoon`) → `VoidResult::Err(...)` with their verbatim messages, the accept path → `VoidOk()`. Sole caller `LuaAiPromptGlue` reads `!gate.has_value()` → `luaL_error(L, "%s", gate.error().c_str())`; the `EndLuaAiPromptTurn` RAII release guard is untouched. (2) **`AttachmentAppUpdateService::DownloadAndLaunchInstallerUpdate`** (`AttachmentAppUpdateService.h` — now `#include`s `SmatchetResult.h`) + the `AppController` delegator (`AppController.h`/`AppController.cpp`). All six failure branches (empty-URL, temp-dir resolve, create-file, download-fail accumulation, trust-fail, launch-fail, `#else` unsupported-platform) → `VoidResult::Err(...)`; cancel → `Err("Download cancelled.")`; launched → `VoidOk()`. The Tier-3 `ShouldLaunchDownloadedInstaller` helper stays on its `bool + trustError&` shape (do-not-flip) and its message is forwarded verbatim through `Err(trustError)`. Sole caller — the `DrawAppUpdateModal` "Download and Install" `LaunchBackgroundTask` worker (`Ui/SmatchetUI.cpp`) — unpacks `r.has_value()`/`r.error()` into the same `ok`/`err` locals its `PostToMainThread` post-back already captured (drives `appUpdateActionStatus`). Behaviour-preserving (exact strings, the partial-file `std::remove`, the app-quit-on-launch). Gates: `test-lint-rules.sh --diff` PASS (incl. include-cycle + fan-in ratchet), `dup_audit --diff` PASS, clang-format clean. Neither method is unit-testable (Lua/sol2 gate needs a live `Impl`; the installer path is Windows-only cpr/ShellExecute + WinVerifyTrust) → `coverage-delta-gate` FAILs prod-without-test by construction → **`tests-out-of-band`** (the sanctioned behaviour-preserving-refactor case). **Uncompilable locally → CI verifies the dual-target MSVC build + both callers.** **This clears Tier 1 of the #21 forward map** — remaining actionable #21 is Tier 2 (`Result<T>`: `SubprocessCapture::Run`, `DownloadAttachmentToLocalFile`, `TakeCommandResultJson`, `HttpGetBinary`, `P4Annotate`); `SaveAutomationScriptContent` stays unmigrated (no live caller — likely dead).
- (2026-07-22, #21 Tier-1 `VoidResult` batch — RecreateLocalCacheDatabase + SetStoragePreference, first non-collaboration slice) · **Flipped two clean Tier-1 `bool + outError` sites onto `VoidResult` (user "do 2" — the Tier-1 batch from the forward map below).** (1) **`IAppSync::RecreateLocalCacheDatabase`** (Sync) — interface virtual (`IAppSync.h` now `#include`s `SmatchetResult.h`) + `AppController` override (`AppController_LocalCacheDb.cpp`, incl. the file-local `RemoveLocalCacheDbFiles` helper → `VoidResult`) + the `FakeSync` fake (`BuiltinFacetCommands.test.cpp`) + both callers: the `sync.full` builtin command (`!has_value()` → the `warning` field) and the Preferences → Local-data recreate-db modal (`has_value()`/`error()` → toast). The existing `sync.*` test (`RecreateError` → `warning`, `RecreateCalls`) passes UNCHANGED through the fake. (2) **`ConfigManager::SetStoragePreference`** (Config static; `ConfigManager.h` now `#include`s `SmatchetResult.h`) + its sole caller (the storage-mode combo). Behaviour-preserving (exact error strings, the recovery-reopen-on-remove-failure branch, the deferred success side-effects). Gates: `coverage-delta-gate` PASS (prod + `FakeSync` test delta → **no `tests-out-of-band`**), `test-lint-rules.sh --diff` PASS (incl. include-cycle + fan-in ratchet), `check-test-list.sh` PASS, clang-format clean. **Uncompilable locally → CI verifies the dual-target MSVC build + the callers.** Deferred Tier-1/2 sites (per the forward map): `SaveAutomationScriptContent` (no live caller — likely dead, not migrated), `TryBeginLuaAiPromptTurn`, `DownloadAndLaunchInstallerUpdate` (2-layer/installer), `SubprocessCapture::Run`, `DownloadAttachmentToLocalFile`, `TakeCommandResultJson`, `HttpGetBinary`, `P4Annotate`.
- (2026-07-22, #21 — collaboration surface COMPLETE + forward map for the remaining non-collaboration sites) · **The #21 collaboration surface is fully migrated** — mutations→`VoidResult` (`AddIssueWatcher` #1899 · `AddIssueCommentAnnotateContext` #1900 · `AddIssueCommentPlain` #1901 · `SubmitWorklog` #1903); reads→`Result<T>` (`FetchIssueComments`/`FetchUserGroupNames`/`FetchPaneGroupMembers` #1904 · `FetchIssueWatchers`/`FetchIssueVotes`/`SearchUsersByQuery` #1905 · `FetchPaneUserActivity` #1906); field-edit a decided non-goal (#1907). Reusable seams `collab::CollaborationResultToResult<T>` + `UnpackResult<T>` are unit-tested. **Remaining #21 `bool + outError` surface (read-only sweep 2026-07-22), by tractability:** **Tier 1 — clean `VoidResult` (~9):** `RecreateLocalCacheDatabase` (`IAppSync` virtual, no fake, 2 callers), `ConfigManager::SetStoragePreference` (static, 1 caller), `SaveAutomationScriptContent` (plain, UI caller), `TryBeginLuaAiPromptTurn`, `DownloadAndLaunchInstallerUpdate`, + file-local helpers (`RemoveLocalCacheDbFiles` / `EnsureFieldIconsCacheDir` / `Read`+`WriteEntireFile`). **Tier 2 — clean `Result<T>` (~5):** `SubprocessCapture::Run` (→`Result<CaptureResult>`), `DownloadAttachmentToLocalFile` (2 payload outs → struct), `SmatchetImGuiHost::TakeCommandResultJson`, `HttpGetBinary`, the `P4Annotate` at-changelist path. **Tier 3 — structural blockers (do NOT mechanically flip):** `FetchRecentCommitsForUser` (accumulates `outError` on partial-success — same both-branch shape as the field-edit non-goal), the two `IAppAttachments` methods (`OpenAttachmentInSystemViewer` / `DownloadAttachmentForPreview` — optional-pointer `outError` + `outFellBackToUrl` multi-out), `ExecuteLuaConsoleSnippet` (two-branch payload), the `SmatchetImGuiHost` boot cluster (re-exported through the C ABI / Unreal plugin — boundary-owned). **Tier 4 — scenario `void + outErr` (~31 edits, test-owned):** `IScenario::OnStart` across ~28 dev/test scenarios → one contract, large mechanical sweep, low product value. **Adjacent:** Whisper `ModelDownloader::Start` / `WindowsAudioCapture::Start` live in `Source/Plugins/Whisper` (not Core); the Core-side `DispatchDownload` wrapper is a clean flip but forwards into the plugin. **Tracker-internal residuals (~12):** internal helpers under `Tracker/` still on `bool + outError` (`JiraClient::FetchUsers`, `FieldCatalogCache` persist/load, `EnsureTrackerAuthConfig`, `IssueDraft::FromJson`, Linear GraphQL helpers, …) — judgment call whether #21 reaches into the layer already declared "done". **NB:** the ~19 `Try*` value-or-fail probes are `bool + payload` (→ `Optional<T>`), NOT the `outError` contract — out of #21 scope. **Net actionable clean Core work ≈ 14–19 sites (Tiers 1+2)**; the rest is test-owned sweep, boundary-owned, or genuinely blocked.
- (2026-07-21, #21 DECISION — field-edit sites kept on the rich-struct shape; collaboration surface CLOSED) · **User decision (option 1): the two remaining #21 field-edit sites are DELIBERATELY NOT migrated to `Result<T>` — they are not debt.** `TryPrepareOfflineFieldEdit` (`bool + FieldEditResult& outResult + std::string& outFieldsPayloadJson + std::string& outError`) and `SubmitFieldEditNetworkOnly` (already returns `FieldEditResult` **by value** — tidied 2026-07-18, not an `outError` method) both hinge on **`FieldEditResult`** (`Types/FieldEditTypes.h`), which carries `Ok` + `Error` + **`ErrorTransient`** (drives OfflineQueue replay eligibility) + the optimistic **`UpdatedDisplayValues` map** — data the caller (`RunCommitWorker` / the grid pipeline) needs on **both** the success and failure branches. A success-XOR-error `Result<T>` cannot represent that "partial success with a recoverable error" state without dropping the transient flag / display map the offline-replay path depends on, so the rich struct is the correct model, not a stale pattern. **This CLOSES the #21 collaboration surface**: every collaboration mutation is on `VoidResult` (`AddIssueWatcher` #1899 · `AddIssueCommentAnnotateContext` #1900 · `AddIssueCommentPlain` #1901 · `SubmitWorklog` #1903) and every collaboration read is on `Result<T>` (`FetchIssueComments`/`FetchUserGroupNames`/`FetchPaneGroupMembers` #1904 · `FetchIssueWatchers`/`FetchIssueVotes`/`SearchUsersByQuery` #1905 · `FetchPaneUserActivity` #1906). **Future agents: do NOT re-attempt the field-edit `Result<T>` flip — it was evaluated on the merits and rejected.** Remaining #21 `outError` paydown, if ever resumed, lives only in non-collaboration subsystems (Whisper / config / attachment) noted in earlier log rows.
- (2026-07-21, #21 AppController flip — read-side `Result<T>`, slice 3: FetchPaneUserActivity, the last clean read) · **Flipped `AppController::FetchPaneUserActivity` off `bool + out-param + outError` onto `Result<std::vector<TrackerActivityEntry>>` (user "merge and continue autonomously") — COMPLETING the collaboration read-side migration.** The live **`TrackerActivityProgress& progress` streaming ref STAYS a by-ref param** (the worker streams progress into it across the multi-second fetch); only the entries payload + error fold into the `Result`. Plain `AppController` method (no interface/fake). Reuses the `collab::CollaborationResultToResult<T>` seam (`Activity()`-based; "Tracker backend is not initialized." + "does not support activity features.", verbatim). Sole caller — `SmatchetUserInfoUi`'s `launchActivityFetch` `std::async` worker — reads `has_value()`/`value()`/`error()` into the `ActivityPayload` (`p.Entries` / `p.Error` with the "Activity fetch failed." fallback), matching its `launchGroupsFetch`/`launchMembersFetch` siblings. The #1457 map-mutex context-latch + ADR-0012 backend latch are preserved verbatim. Behaviour-preserving; the branch logic is the already-tested seam (`CollaborationResultToResult` in `CollaborationPreconditionPure.test.cpp`) and `AppController.cpp` is not in the doctest rig → adds no new runtime surface → `coverage-delta-gate` FAILs prod-without-test by construction → dismissed with **`tests-out-of-band`** (the sanctioned behaviour-preserving-refactor case, as with `AddIssueWatcher` #1899). Gates: `test-lint-rules.sh --diff` PASS, `check-test-list.sh` PASS, clang-format clean. **Uncompilable locally → CI verifies the dual-target MSVC build + the one UI caller.** **#21 collaboration read-side is now COMPLETE.** The only remaining #21 `outError` sites structurally resist `Result<T>`: `TryPrepareOfflineFieldEdit` / `SubmitFieldEditNetworkOnly` (their `FieldEditResult` carries state on both success and failure branches).
- (2026-07-21, #21 AppController flip — read-side `Result<T>`, slice 2: the `IAppUsers` read virtuals FetchIssueWatchers / FetchIssueVotes / SearchUsersByQuery) · **Flipped the three `IAppUsers` interface-virtual reads off `bool + out-param + outError` onto `Result<T>` (user "merge and continue autonomously").** `FetchIssueWatchers` / `SearchUsersByQuery` → `Result<std::vector<TrackerUser>>`; **`FetchIssueVotes` → `Result<TrackerIssueVotes>`** — the multi-out (`vector<TrackerUser>& outVoters` + `int*`/`bool*`/`bool*`) collapses into the existing `TrackerIssueVotes` payload (Voters/VoteCount/HasVoted/VotersArrayInResponse), which the backend `ITrackerCollaboration` already returns. Full vertical: the **`IAppUsers` virtuals** (rank-0 leaf — now `#include`s `SmatchetResult.h` and forward-declares `TrackerIssueVotes`; the by-value `Result<T>` returns need only forward decls at the pure-virtual declaration, verified by a standalone `clang -fsyntax-only` of the header), the `AppController` overrides (reuse the #1904 `collab::CollaborationResultToResult<T>` seam), the **`FakeUsers`** fake (`BuiltinFacetCommands.test.cpp`, + its new `ITrackerCollaboration.h` include), and **9 caller sites** across 4 TUs: `BuiltinCommands_Users.cpp` (users.search/watchers/votes, + `ITrackerCollaboration.h` for the votes payload), `TrackerGridFieldDisplay.cpp` (watchers + votes render-workers), `AnnotateAnalysisUi_Modals.cpp` (×3 search) and `SmatchetAutocompleteUi.cpp` (JQL user-search). **DRY:** the async-worker `Result`-unpack idiom (introduced in slice 1's `SmatchetCommentsModalUi`) recurred → extracted a generic **`UnpackResult<T>(Result<T>&&, bool&, T&, std::string&)`** into `SmatchetResult.h` (+ 2 `SmatchetResult.test.cpp` cases: Ok moves payload / Err copies message, each leaving the other out-param untouched) and routed the four worker sites through it — clears the `dup` (DRY Pillar 5) gate without a deviation and de-inlines the idiom. Behaviour-preserving (exact strings, deferred success-notify, the vote-count/has-voted/voters-in-response surfacing). Gates: `coverage-delta-gate` PASS (prod + `SmatchetResult`/`BuiltinFacetCommands` test delta → **no `tests-out-of-band`**), `test-lint-rules.sh --diff` PASS (incl. `dup` after the helper extraction), `check-test-list.sh` PASS, clang-format clean. **Uncompilable locally → CI verifies the dual-target MSVC build + interface/fake + 9 callers.** Remaining read-side #21: `FetchPaneUserActivity` (retains the live `progress&` streaming ref — the last clean read); `TryPrepareOfflineFieldEdit` / `SubmitFieldEditNetworkOnly` structurally resist `Result<T>`.
- (2026-07-21, #21 AppController flip — read-side `Result<T>`, slice 1: FetchIssueComments / FetchUserGroupNames / FetchPaneGroupMembers) · **Opened the #21 collaboration READ-side phase: flipped the three plain read delegators off `bool + out-param + outError` onto `Result<T>` (user "do 1-3 into one").** `FetchIssueComments → Result<std::vector<TrackerIssueComment>>`, `FetchUserGroupNames → Result<std::vector<std::string>>`, `FetchPaneGroupMembers → Result<std::vector<TrackerUser>>` — all **plain `AppController` methods** (no `IApp*` mirror/fakes). New shared seam **`CollaborationResultToResult<T>`** in `Tracker/CollaborationPreconditionPure.h` — the payload-carrying read-side sibling of `CollaborationErrorToVoidResult`: maps the backend's `Result<T, TrackerError>` onto `Result<T>`, surfacing `err.Detail` verbatim (the historical `outError = err.Detail` contract). Unlike the mutation family, the read delegators keep their backend/capability GUARDS **inline** rather than routing through `ClassifyCollaborationPrecondition`: pure reads (no read-only gate) with divergent capability/message pairs (`Collaboration()` + "collaboration features" for comments; `Activity()` + "activity features" for the two group fetches) and divergent backend-absent wording. Behaviour-preserving: exact user-facing strings, the `FetchPaneGroupMembers` group-roster cache short-circuit + post-fetch write-back, and the deferred success-notify all preserved verbatim. **4 callers** (`SmatchetCommentsModalUi`, `SmatchetUserInfoUi` ×2, `AnnotateAnalysisUi_Modals`) read `has_value()`/`value()`/`error()` into the same locals their `PostToMainThread` / future post-backs already captured. **4 new seam tests** (`CollaborationPreconditionPure.test.cpp`): the mapper's Ok pass-through by move, Detail passthrough (Server/InvalidRequest), and empty-Detail-still-Err. `coverage-delta-gate` PASS (seam header + seam test + prod cpp all changed → **no `tests-out-of-band`**). Gates: `test-lint-rules.sh --diff` PASS, `check-test-list.sh` PASS, clang-format clean. **Uncompilable locally → CI verifies the dual-target MSVC build + the 4 UI callers.** Remaining read-side #21: `FetchIssueWatchers` / `FetchIssueVotes` / `SearchUsersByQuery` (interface+fake — `IAppUsers`), `FetchPaneUserActivity` (retains the live `progress&` streaming ref); `TryPrepareOfflineFieldEdit` / `SubmitFieldEditNetworkOnly` structurally resist `Result<T>` (`FieldEditResult` carries state on both success AND failure).
- (2026-07-18, #21 AppController flip — SubmitWorklog, collaboration sibling #4 / completes the mutation family) · **Flipped `AppController::SubmitWorklog` off `bool + std::string& outError` onto `VoidResult` (user "continue") — the last `bool+outError` collaboration MUTATION.** Second `IAppTicketMutations` interface-virtual flip (same two-interface shape as `AddIssueCommentPlain` #1901): the interface virtual, the `AppController` override (reuses the #1896 seam with its historical `"Jira backend is not initialized."` message → behaviour-preserving; the **`PrefetchIssueTicketsForKeys({issueId}, true)` success side-effect is preserved** verbatim after the success path), the `FakeMutations` fake (`WorklogOk ? VoidOk() : VoidResult::Err("worklog rejected")`, keeping the `LastWorklogTimeSpent` recorder), and both **callers**: the `ticket.add_worklog` builtin command (maps `!has_value()` → `Failure(BackendError, "Worklog failed: " + r.error())`, `timeSpent` payload unchanged) and the `TicketFieldEditor.cpp` worklog modal (`has_value()` → close popup; else `ErrorMsg = "Failed: " + worklogResult.error()`). The `AddWorklog` hits in `*IssueMutationHttp.test.cpp` are the `ITrackerCollaboration` client method (`TrackerError`) — untouched. Existing `BuiltinFacetCommands` test covers the `ticket.add_worklog` timeSpent formatting through the command→fake path. Behaviour-preserving; `coverage-delta-gate` PASS (prod + fake test delta → **no `tests-out-of-band`**). Gates: `test-lint-rules.sh --diff` PASS, `check-test-list.sh` PASS, clang-format clean. **Uncompilable locally → CI verifies.** **Collaboration MUTATION family now fully on `VoidResult`** (AddIssueWatcher/AddIssueCommentAnnotateContext/AddIssueCommentPlain/SubmitWorklog). Remaining #21 collaboration surface is READ-side only: `FetchIssueWatchers`/`FetchIssueComments`/`SearchUsersByQuery`/`FetchIssueVotes` — payload returns → `Result<T>` (a different shape; the seam's precondition would feed a `Result<T>::Err`, not a `VoidResult`), and `FetchIssueVotes` additionally has the rank-0/rank-3 layering block noted earlier.
- (2026-07-18, #21 AppController flip — AddIssueCommentPlain, collaboration sibling #3 / first IAppTicketMutations virtual) · **Flipped `AppController::AddIssueCommentPlain` off `bool + std::string& outError` onto `VoidResult` (user "continue").** Third collaboration delegator onto the #1896 seam, and the **first interface-virtual** of the collaboration family — a full two-interface vertical (vs the plain-method AddIssueWatcher/AddIssueCommentAnnotateContext): the **`IAppTicketMutations::AddIssueCommentPlain` virtual** (header already carries `SmatchetResult.h` from the `SubmitFieldEdit` flip), the `AppController` override (reuses the seam with its historical `"Jira backend is not initialized."` message param → behaviour-preserving), the **`FakeMutations` fake** (`BuiltinFacetCommands.test.cpp` — `CommentOk ? VoidOk() : VoidResult::Err("comment rejected")`), and all **3 callers**: the `ticket.add_comment` builtin command (`BuiltinCommands_TicketMutations.cpp` — maps `!has_value()` → `CommandResult::Failure(BackendError, "Comment failed: " + r.error())`) and the two UI async posters (`SmatchetCommentsModalUi.cpp` + `AnnotateAnalysisUi_Window.cpp`'s quick-comment-template lambda, both reading `has_value()`/`error()` into the same `ok`/`err` capture). NB: the many `AddIssueCommentPlain` hits in `*IssueMutationHttp.test.cpp`/`GitHubClientHttp`/`JiraIssueMutationCommentHttp` are the **`ITrackerCollaboration`** client-level method (returns `TrackerError`) — a different method, untouched. Existing `BuiltinFacetCommands` test (dispatch `ticket.add_comment`, assert success + `CommentOk=false` → `BackendError`) covers the flip through the command→fake path. Behaviour-preserving; `coverage-delta-gate` PASS (prod + the fake test delta → **no `tests-out-of-band`**). Gates: `test-lint-rules.sh --diff` PASS (fan-in + include-cycle + comment-noise + oversized), `check-test-list.sh` PASS, clang-format clean. **Uncompilable locally → CI verifies the interface + fake + dual-target MSVC + 3 callers.** Remaining collaboration `bool+outError`: `SubmitWorklog` (the other `IAppTicketMutations` virtual) + the read-side `FetchIssueWatchers`/`FetchIssueComments`/`SearchUsersByQuery` (`Result<T>`, not `VoidResult`).
- (2026-07-18, #21 AppController flip — AddIssueCommentAnnotateContext, collaboration sibling #2) · **Flipped `AppController::AddIssueCommentAnnotateContext` off `bool + std::string& outError` onto `VoidResult`, reusing the #1896 seam (user "continue").** Second collaboration delegator onto `Tracker/CollaborationPreconditionPure.h`. **Wrinkle:** this delegator (like `SubmitWorklog`/`AddIssueCommentPlain`) historically worded its backend-absent error **"Jira backend is not initialized."** — the seam emits the tracker-agnostic **"Tracker backend is not initialized."** To stay behaviour-preserving without changing user-facing text, `ClassifyCollaborationPrecondition` gained an **optional `backendAbsentMessage`** param (defaulted to the tracker-agnostic string, so the already-merged `AddIssueWatcher`'s 4-arg call is byte-for-byte unaffected); this call site passes `"Jira backend is not initialized."`. **Plain `AppController` method** (no `override`, no `IApp*` mirror, no fakes) — unlike `SubmitWorklog`/`AddIssueCommentPlain`, which ARE `IAppTicketMutations` virtuals (deferred: they carry fakes + more callers). Both callers — `AnnotateAnalysisUi_Window.cpp`'s two `LaunchBackgroundTask` async lambdas (the standalone annotate-comment + the chained assign-then-comment) — now read `has_value()`/`error()` into the same local `err`/`ok` the `PostToMainThread` capture already used. **2 new seam tests** (`CollaborationPreconditionPure.test.cpp`): the custom `backendAbsentMessage` is surfaced, and it does NOT leak past the read-only gate (ordering preserved). Behaviour-preserving; `coverage-delta-gate` PASS (seam header + seam test + prod cpp all changed → **no `tests-out-of-band` needed** this time, unlike the AddIssueWatcher flip). Gates: `test-lint-rules.sh --diff` PASS (fan-in + include-cycle + comment-noise + oversized), `check-test-list.sh` PASS, clang-format clean. **Uncompilable locally → CI verifies the dual-target MSVC build + the two UI callers.** Remaining collaboration `bool+outError` siblings: `AddIssueCommentPlain` + `SubmitWorklog` (both `IAppTicketMutations` virtuals — two-interface flips), plus the read-side `FetchIssueWatchers`/`FetchIssueComments`/`SearchUsersByQuery` (payload returns → `Result<T>`, not `VoidResult`).
- (2026-07-18, #21 AppController flip — AddIssueWatcher, step 2 of the two-step) · **Flipped `AppController::AddIssueWatcher` off `bool + std::string& outError` onto `VoidResult`, landing on the seam verified in step 1 (#1896).** The public method now returns `VoidResult` (`has_value()` success; `error()` carries the user-facing message); the guard ordering + exact strings live in the already-unit-tested `Tracker/CollaborationPreconditionPure.h`, so this is a behaviour-preserving signature change on verified logic. **Plain `AppController` method** — no `IApp*` interface mirror (no fakes), and the AppController.h decl is not part of the delegator-mirror deviation block (a standalone method), so no duplication marker needed. Sole caller — `TrackerGridFieldDisplay.cpp`'s watch-self `std::async` lambda — now does `const VoidResult r = app.AddIssueWatcher(issueKey); return r.has_value() ? "" : r.error();` (same `std::string` future shape the UI reads into `watchSelfError`). `AppController.h` already carries `SmatchetResult.h` (slice 1). Behaviour-preserving; the flip adds **no new runtime surface** (the branch logic is the seam, covered by `CollaborationPreconditionPure.test.cpp`), so `coverage-delta-gate` FAILs prod-without-test by construction → dismissed with **`tests-out-of-band`** (the template's sanctioned "behaviour-preserving refactor already covered by existing tests" case — this is precisely why step 1 added the coverage first). Gates: `test-lint-rules.sh --diff` PASS (fan-in + include-cycle + comment-noise + oversized), `check-test-list.sh` PASS, clang-format clean. **Uncompilable locally → CI verifies the dual-target MSVC build + the one UI caller.**
- (2026-07-18, #21 groundwork — collaboration-delegator preflight seam) · **Extracted + unit-tested the shared collaboration preflight so a pending `AddIssueWatcher` #21 flip becomes CI-verified (step 1 of the "groundwork then flip" two-step; user chose "groundwork").** The `AppController` collaboration delegators (`AddIssueWatcher`/`FetchIssueWatchers`/`FetchIssueVotes`/`AddIssueCommentPlain`/`AddWorklog`/…) are **untestable in the doctest rig** — `AppController.cpp` drags the UI/Lua/MCP chain and is not linked (per `AppControllerFieldCatalogLock.test.cpp`'s own note), which is exactly why these `bool + outError` leaves were flagged "test-gapped" and not clean CI-verified flip targets. New header-only pure seam `Source/Core/include/Tracker/CollaborationPreconditionPure.h` (`namespace smatchet::collab`): `ClassifyCollaborationPrecondition(readOnlyMode, requireWritable, hasBackend, hasCollaboration) → VoidResult` (encodes the **load-bearing guard ordering** — read-only reported before missing-backend before missing-collaboration — and the exact user-facing strings) + `CollaborationErrorToVoidResult(const TrackerError&) → VoidResult` (Ok → `VoidOk()`, else `err.Detail` verbatim, matching the historical `outError = err.Detail` contract). Both already return `VoidResult`, so the follow-up public flip is a thin signature change landing on a verified seam. `AppController::AddIssueWatcher` **rewired to call the seam** (behaviour-preserving; still returns `bool` this PR — the pre-flight now also LOG_WARNs the backend-null/collab-null cases the old code returned silently, a strict-superset observability nudge, no contract change). New `tests/Core/CollaborationPreconditionPure.test.cpp` — 10 cases pinning every precondition branch + ordering (read-only-wins, backend-before-collab) + the error map (Ok→VoidOk, Server/InvalidRequest Detail passthrough, empty-Detail still-Err). `requireWritable` is parameterised so the read-only fetch siblings can adopt the seam later without a readonly gate. Header includes only `SmatchetResult.h` + `Tracker/TrackerError.h` (rank-3, layer-clean); no `.cpp` to link. Gates: `coverage-delta-gate.sh` PASS (prod+test), `test-lint-rules.sh --diff` PASS (fan-in + include-cycle + duplication), `check-test-list.sh` PASS, clang-format clean. **Uncompilable locally → CI verifies; the pure seam itself IS now doctest-covered.**
- (2026-07-18, #21 follow-up — SubmitFieldEditNetworkOnly return-by-value) · **Tidied the `SubmitFieldEditNetworkOnly` signature (adjacent to #21 but not an `outError`→`Result` flip).** The method returned `bool` AND filled a `FieldEditResult& outResult` out-param, where the `bool` was **exactly `outResult.Ok`** in every branch (both the sprint/timetracking helpers and the regular path set `Ok=true` on success) — a redundant bool. Dropped it: the method now **returns `FieldEditResult` by value** (`FieldEditPipelineService` impl + header, `AppController` decl + delegator — plain method, no interface/fakes). This is NOT a `Result<T>` migration and deliberately so: on failure the caller (`RunCommitWorker`) reads `ApplyResult.ErrorTransient` to drive the offline-queue fallback, so the rich result struct must survive failure — a `Result<FieldEditResult>` with a string `Err` would drop `ErrorTransient`. The private branch helpers keep their `FieldEditResult& outResult` refs (now bound to the impl's local `result`). Caller updated to `result.ApplyResult = app.SubmitFieldEditNetworkOnly(...); if (result.ApplyResult.Ok)`; the 4 `FieldEditPipelineService.test.cpp` sites assert `r.Ok` instead of the dropped bool. Behaviour-preserving; no `tests-out-of-band`. `FieldEditResult` is a rank-0 `Types/FieldEditTypes.h` type → return-by-value is layer-clean. Gates: `coverage-delta-gate.sh` PASS (prod+test), `test-lint-rules.sh --diff` PASS (incl. duplication — the AppController.h delegator-mirror deviation still covers it), clang-format clean. **Uncompilable locally → CI verifies.**
- (2026-07-18, CI reliability — LocalCacheManagerContention de-flake, SECOND root cause) · **Fixed the *second*, distinct `LocalCacheManagerContention` flake mode surfaced by the #1895 Coverage run: `duplicate column name: has_merge_conflict` (`CHECK(3 == 8)`).** The #1894 write-path busy-retry (entry below) fixed the *write* half; this is a **schema-init** race, orthogonal. `InitSchema` migrates additively with a `PRAGMA table_info` guard before each `ALTER TABLE … ADD COLUMN`, but the check-then-act is **not atomic across connections**: when N threads open the same on-disk cache concurrently, two can both read a column as absent past the guard, and the loser's `ALTER` fails with `duplicate column name` — a plain `SQLITE_ERROR` (code 1, *not* a BUSY, so the ctor's transient-busy retry never caught it) → the ctor re-threw → the stampede thread failed. Fix (Approach A — harden production, not relax the test): a file-local `AddColumnIfMissing(db, table, col, columnDef)` replaces all 10 `if (!SqliteTableHasColumn) db.exec(ALTER…)` blocks (both queue-table families + the ticket/pending-creates families). It keeps the `table_info` guard as the fast path (already-migrated cache runs no ALTER, throws nothing) and adds a catch that **absorbs only the benign race** via a new pure, unit-tested predicate `IsDuplicateColumnRace(code, message)` (`SqliteOpenRecoveryPure.h`: `SQLITE_ERROR` **and** message contains SQLite's "duplicate column name" text — the failure carries no dedicated result code, so message-matching is unavoidable; a null message or any other `SQLITE_ERROR` propagates so a genuine SQL bug is never swallowed). The loser's post-state is exactly what its migration wanted (the column now exists), so it continues idempotently. Closes the TOCTOU at the source rather than papering it with a whole-`InitSchema` retry; also net-reduces the TU (10 guard blocks → 10 one-liners). **Test delta:** 5 new `IsDuplicateColumnRace` cases in `SqliteOpenRecoveryPure.test.cpp` (race message hit; other-`SQLITE_ERROR`/empty-message miss; non-`SQLITE_ERROR` codes + null-message miss; disjoint from the transient-busy/corruption axes). The `[high-risk]` `LocalCacheManagerContention` open-stampede already forces the concurrent-migration path. Gates: `coverage-delta-gate.sh` PASS (prod+test), `test-lint-rules.sh --diff` PASS (incl. comment-ratio after trim), `check-test-list.sh` PASS, clang-format clean. **On #1895's branch by the single-branch constraint (unblocks its Coverage run); [high-risk] flaky-lane-only → CI is the verifier, cannot reproduce locally on Linux.**
- (2026-07-18, CI reliability — LocalCacheManagerContention de-flake) · **Fixed the chronic `LocalCacheManagerContention` flake that intermittently reddened a run on three #21 PRs (#1880 ASAN, #1882 UBSan, #1892 Coverage).** Root cause: the Slice-G/G2 bounded busy-retry (5 attempts, capped exp backoff) wrapped **only `InitSchema`** (the ctor open); the **write path had none** — `SaveTicket`/`SaveTickets` opened a `SQLite::Transaction`, committed, and re-threw on any exception. `busy_timeout=5000ms` waits on lock *acquisition* but does NOT absorb an *immediate* BUSY (a WAL snapshot / write-lock conflict returned without waiting) — exactly the code `InitSchema`'s retry exists for. Under the ~10× ASAN/UBSan/Coverage slowdown the concurrent-write window widened and a writer's immediate BUSY escaped `SaveTicket` as an uncaught throw, failing the test's `succeeded==kThreads` / `failures[i].empty()` write-half assertions (the opens were already retry-protected). Fix (Approach A — harden production, not relax the test): a file-local `RunWriteTxnWithBusyRetry` wraps the write transaction in the **same** bounded retry the ctor uses, reusing the shared unit-tested pure policy (`IsTransientBusyCode` + `BusyRetryBackoffMs`); a transient code rolls the attempt back (Transaction dtor) and retries with backoff, ticket upserts being idempotent → replay-safe; non-transient errors and an exhausted budget propagate unchanged. `SaveTicket`/`SaveTickets` now route through it. The helper is templated on the write body (no `std::function` type-erasure) and takes `stmtMutex_` **per-attempt**, wrapping the whole `BEGIN…writeRows…COMMIT` (both the cached statements and the single-connection transaction are instance-serialized — SQLite forbids a nested transaction on one connection) and releasing it on scope exit **before** the backoff sleep, so a contending writer's retry never holds the mutex while sleeping. Real robustness for genuine multi-process cache writes, not just the test. **Test delta:** the existing `[high-risk]` open-stampede case is strengthened so each of the 8 threads persists several tickets (racing the write lock repeatedly, not just the ctor's CREATE TABLE) — this pins the write-path retry without adding a new on-disk test setup (the file's on-disk WAL contention is by necessity: `:memory:`/shared-cache is per-connection and has no WAL, and `LocalCacheManager` opens filenames, not URIs). Gates: `coverage-delta-gate.sh` PASS (prod+test), `test-lint-rules.sh --diff` PASS, `check-test-list.sh` PASS, clang-format clean. **The contention suite is [high-risk] flaky-lane-only → CI (the flaky lane) is the verifier; cannot reproduce locally on Linux.**
- (2026-07-15, #21 AppController flip — slice 4) · **#21 escalated tier — flip increment 4 (`SubmitFieldEdit`, the two-interface slice; explicit user go-ahead "do 1").** Flipped the online field-edit entry point off `bool + std::string& outError` onto `VoidResult` across the **full two-interface vertical**: **both** interface virtuals — `IAppTicketMutations::SubmitFieldEdit` (ticket.* command surface) and `ILuaBindingHost::SubmitFieldEdit` (Lua `Ticket:set_field`/`:transition`) — each header now `#include`s `SmatchetResult.h`; the `AppController` override (`AppController.h`; the pre-existing `SMATCHET_DEVIATION(rule=duplication)` on the delegator mirror still covers it) + delegator (`AppController_CatalogAndFieldEdit.cpp`); the `AppController::Impl` `ILuaBindingHost` forwarder (`AppControllerImpl.h` + `AppController_LuaBindings_Tickets.cpp`); and the service impl (`FieldEditPipelineService.{h,cpp}`) — which **removed the now-dead `VoidResultToBool` adapter** (the private Sprint/Timetracking/Regular branch helpers were already `VoidResult` from an earlier slice, so the public method now returns their result directly). **Callers migrated:** 2 Lua glue sites (`AppController_LuaBindingsCore.cpp` — rebuild the `{ok, err}` Lua tuple from `has_value()`/`error()`), 3 `ticket.*` command sites (`BuiltinCommands_TicketMutations.cpp`), 2 off-UI-thread UI sites (`AnnotateAnalysisUi_Window.cpp` — keep a local `ok`/`err` for the downstream PostToMainThread capture + the chained `AddIssueCommentAnnotateContext` which is still bool+outError). **Fakes:** `FakeLuaBindingHost` + `BuiltinFacetCommands.test.cpp`'s `FakeMutations` (control fields `SubmitFieldEditReturn`/`Error`/`SubmitOk` kept; only the return re-wrapped). **Tests:** the 8 `FieldEditPipelineService.test.cpp` `SubmitFieldEdit` cases → `.has_value()`/`.error()`; the Lua `Ticket:set_field` tests need **no change** (they drive through the glue and assert the same `{ok, err}` tuple). Scoped deliberately to `SubmitFieldEdit` only — `SubmitFieldEditNetworkOnly` is `bool + FieldEditResult&` (a value-out-param pattern, not `bool + outError`) and stays for a later value-`Result` migration. Behaviour-preserving; no `tests-out-of-band`. Gates: `coverage-delta-gate.sh` PASS (prod+test), `test-lint-rules.sh --diff` PASS (fan-in + include-cycle + duplication), clang-format clean. **Uncompilable locally → CI verifies (two interface contracts + fakes + dual-target MSVC).**
- (2026-07-15, #21 AppController flip — slice 3) · **#21 escalated tier — flip increment 3 (`EnsureIssueEditMetaLoaded`/`RefreshIssueEditMeta`, "batch several" go-ahead).** Flipped the editmeta-cache load/refresh pair off `bool + std::string* outError` onto `VoidResult` across the full vertical: the **`EditMetaCacheService`** methods (`.h` decls + `.cpp` defs; `EditMetaCacheService.h` now `#include`s `SmatchetResult.h`), the **`AppController` mirror delegators** (`AppController.h` decls + `AppController_CatalogAndFieldEdit.cpp` — plain non-virtual methods, no `IApp*` interface/fakes; `AppController.h` already carries `SmatchetResult.h` from slice 1), the service-internal self-calls (`RefreshIssueEditMeta` → `EnsureIssueEditMetaLoaded`; the two `Warm*` background warmup callers now discard the `VoidResult` — fire-and-forget, issue stays optimistic on fetch failure), and the 4 `FieldEditPipelineService.cpp` call sites (all already discarded the bool). The `outError` param sat mid-signature (param 2 of 4) so every call site dropped it and shifted `issueTypeKeyOverride`/`configSnapshot` left. **Test delta** (`EditMetaCacheService.test.cpp`): the ~14 direct `svc.*` calls migrated to `.has_value()`; the two `outError`-asserting blocks now read `.error()` (`CHECK(r.error() == "HTTP 503…")`). `Result` is not `[[nodiscard]]`, so the fire-and-forget sites need no `(void)`. Behaviour-preserving; no `tests-out-of-band` (real test delta). Gates: `coverage-delta-gate.sh` PASS (prod+test), `test-lint-rules.sh --diff` PASS (fan-in + include-cycle), clang-format clean. **Uncompilable locally → CI verifies.**
- (2026-07-14, #21 AppController flip — slice 2) · **#21 escalated tier — flip increment 2 (`Approve`/`RevokeLuaScript`, user "batch several" go-ahead).** Flipped the Lua-script consent-gate pair off `bool + std::string& outError` onto `VoidResult` across the full vertical: the **`IAppAutomation` interface virtuals** (`IAppAutomation.h` now `#include`s `SmatchetResult.h`), the `AppController` overrides + impls (`AppController_LuaScriptFiles.cpp`), the **member-function-pointer command helper** `RegisterLuaScriptConsentCommand` (`BuiltinCommands_Automation.cpp` — the fn-pointer type `bool (IAppAutomation::*)(const std::string&, std::string&)` → `VoidResult (IAppAutomation::*)(const std::string&)`, call adapts to `has_value()`/`error()`), and the `FakeAutomation` test overrides. **Added real coverage** (previously the fakes were set-but-never-asserted): a new block in the `automation.*` test dispatches `lua.approve-script`/`lua.revoke-script` through the registry → fn-pointer helper → fake, asserting the success path + the empty-script guard. Interface + AppController-override + fn-pointer helper + test-fake all migrated; behaviour-preserving. Gates: `coverage-delta-gate.sh` PASS (prod+test), `test-lint-rules.sh --diff` PASS (fan-in + include-cycle), clang-format clean. **Uncompilable locally → CI verifies.**
- (2026-07-14, #21 AppController flip — slice 1) · **#21 escalated tier — first AppController public-API-flip increment (user go-ahead 2026-07-14).** Flipped the `ApplyFieldEditResult` vertical off `bool + std::string& outError` onto `VoidResult`: the service method (`FieldEditPipelineService::ApplyFieldEditResult`, impl + header), the public `AppController::ApplyFieldEditResult` (AppController.h decl + delegator; **`AppController.h` now `#include`s `SmatchetResult.h`** — fan-in-ratchet + include-cycle lint both still PASS), and the 2 `SmatchetGridFieldEditPipeline.cpp` call sites (adapt via a documented file-local shim; the grid pipeline still branches on bool for its toast/cell-feedback — direct-VoidResult conversion is a follow-up). Chosen as the smallest clean first slice: `ApplyFieldEditResult` is a **plain (non-virtual) AppController method** — no `IApp*` interface mirror (no fakes), no member-fn-pointer dispatch, no dev-owned-WIP callers (unlike `RecreateLocalCacheDatabase` → SmatchetPreferencesUi, or `Approve/RevokeLuaScript` → fn-pointer helper). Behaviour-preserving; the 3 existing service tests updated to `has_value()`/`error()` (real test delta, no `tests-out-of-band`). **Uncompilable in this Linux env → CI is the compile verifier.** Gates: `coverage-delta-gate.sh` PASS (prod+test), `test-lint-rules.sh --diff` PASS (incl. fan-in + include-cycle), clang-format clean. Remaining flip surface: the public `SubmitFieldEdit`/`SubmitFieldEditNetworkOnly`, `EnsureIssueEditMetaLoaded`/`RefreshIssueEditMeta`, and the `IAppAutomation`/`IAppSync` no-payload virtuals.
- (2026-07-14, #21 field-edit branch helpers) · **#21 continue — first VoidResult consumer.** Migrated `FieldEditPipelineService`'s three private branch helpers (`SubmitFieldEditSprint`/`SubmitFieldEditTimetracking`/`SubmitFieldEditRegular`) off `bool + std::string& outError` onto `VoidResult` (`VoidOk()` / `Err(reason)`). Each keeps a function-local `outError` for its logging/audit side-channel. **Public `SubmitFieldEdit` API unchanged** (`bool + outError`) — it adapts the helpers' `VoidResult` back via a new file-local `VoidResultToBool`. Isolated: the helpers are private, called only from the 3 dispatch sites; **no AppController surface touched** (the reason this is a clean slice while the public no-payload methods await the AppController flip). First real consumer of the #1873 `VoidResult` groundwork. Behaviour-preserving; 2 new test cases (`FieldEditPipelineService.test.cpp`) pin the migrated Sprint/Timetracking `Err` paths through the public contract + adapter (existing success cases cover the `VoidOk()` paths). Gates: `coverage-delta-gate.sh` PASS (prod+test), `test-lint-rules.sh --diff` PASS, `check-test-list.sh` PASS, clang-format clean.
- (2026-07-14, #21 VoidResult groundwork) · **#21 continue — no-payload enabler.** Added the no-payload Result shape to `SmatchetResult.h`: `struct Unit {}` + `using VoidResult = Result<Unit>` + `inline VoidResult VoidOk()`. Rather than a fiddly `Result<void>` partial specialisation (C++14-hard, aligned-storage machinery), "no payload" is modelled as an empty `Unit` value — it works with the *existing* `Result<T,E>` template unchanged (Unit is empty, trivially-copyable, nothrow-movable → satisfies the copy-assign nothrow-move `static_assert` with zero storage overhead). This is the enabler for the remaining `bool + outError` **no-payload** #21 tranche (edit-meta / field-edit) — those methods are verbatim-mirrored on `AppController`'s public API, so their migration lands with the escalated AppController public-API flip; this groundwork unblocks it. Fully unit-tested (4 new `VoidResult` cases in `SmatchetResult.test.cpp`: VoidOk success, Err reason, copy/move state, wrong-state `error()` contract) — no `tests-out-of-band` (header + test only; coverage-delta-gate sees no production `.cpp` change). Gates: `test-lint-rules.sh --diff` PASS, `check-test-list.sh` PASS, clang-format clean.
- (2026-07-14, #21 texture-cache slice) · **#21 continue — persistence/renderer tier.** Migrated `SmatchetImageTextureCache::GetOrLoadFromMemory` and `GetOrLoadFromFile` off `bool + SmatchetLoadedIconTexture& out + std::string& outError` onto `Result<SmatchetLoadedIconTexture>` (`SmatchetResult.h`). Clean value-returning fit (POD payload). The 3 `SmatchetFieldIconRender.cpp` callers were all fire-and-forget (`(void)` return, `loaded`/`err` discarded — cache-warming for a later frame), so they collapse to the 2-arg call with the `loaded`/`err` locals removed; `GetOrLoadFromFile` had zero callers. Internal static helpers (`DecodeWithStb`/`CreateTextureFromRgba`) keep their `outError` shape; the public fns wrap their errors into `Err`. Mechanical, behaviour-preserving. **`tests-out-of-band`**: the functions call `ImGui::GetIO()`/`ImTextureData::Create`, so their TU cannot link into the doctest rig (ImGuiLib → GLFW/X11/OpenGL is deliberately not configured on the CI test runner — see `tests/CMakeLists.txt` header) — genuinely not unit-testable, label applied. Gates: `test-lint-rules.sh --diff` PASS, clang-format clean. Remaining #21 non-tracker sites: attachment download (`Result<struct>`, network-coupled), the `bool + outError` no-payload tranche (edit-meta / field-edit — needs a `Result<void>`/bare-error convention decision), plus the `AppController` public-API flip.
- (2026-07-14, #21 audit-trail slice) · **#21 continue — persistence tier.** Migrated `BackendAuditTrail::ReadRecentEvents` off `std::vector<json>` + nullable `std::string* outError` onto `Result<std::vector<nlohmann::json>>` (`SmatchetResult.h`) — the Result shape makes the previously-muddled "no events" vs "read failed" distinction explicit (Ok(events) with an empty vector when there's no audit file yet; Err(reason) on a filesystem/parse-lock exception). 4 prod callers rewired (`AiContextBuilder`, `AiAssistantController`, `BugReportService` all ignored the error → `value_or({})`; `SmatchetAuditUi` surfaces it in the UI → `has_value()`/`value()`/`error()`) + 3 test sites (`BackendAuditTrail.test.cpp` ×2, `OfflineQueueServiceRuntime.test.cpp`). Behaviour-preserving except one pathological edge: a mid-loop (OOM-only) exception previously returned partial events alongside the error; Result is ok-XOR-error so that now returns Err. Gates: `coverage-delta-gate.sh` PASS (prod+test), `test-lint-rules.sh --diff` PASS, `check-test-list.sh` PASS, clang-format clean. Remaining #21 non-tracker sites: config (`SetStoragePreference` — dev-owned WIP caller), attachment/edit-meta/field-edit, texture cache (renderer-coupled), plus the `AppController` public-API flip.
- (2026-07-14, re-sweep + #21 P4 slice) · **Double-check pass.** Re-verified all Sprint-1 + shipped Sprint-2 claims against the current tree (`smatchet_patch_or_die`, OfflineQueue latch release, `static_assert(sizeof(ImWchar)==4)`, sol2-free `AppController.h` + `AppControllerImpl.h`, `SmatchetPchCoreDx12.h`, `ninja-tsan-linux` + `tsan-linux-nightly.yml`, `is-exe-fresh.sh`, `SmatchetResult.h`) — all present, no drift. **Status reconcile:** #8/#13 perf gate was stale-marked "Parked" here but is actually **SHIPPED — GATE REQUIRED** per `perf-gate-revival.md` (COMPLETE 2026-06-07; step-5 calibration RESOLVED 2026-07-06; `p99Ms` emitter landed) — moved to Shipped. **#21 P4 slice:** migrated `P4FirstSubmittedChangelistOnCalendarDay` (→ `Result<std::string>`) and `P4ChangesForUser` (→ `Result<std::vector<P4ChangeSummary>>`) off the `bool + outChangelist/outChanges + outError` shape onto `Result<T, std::string>` (`SmatchetResult.h`); both callers rewired (`SmatchetUserInfoUi.cpp`, `AnnotateAnalysisUi_Window.cpp` — the async lambda's `pair<cl,err>` contract preserved verbatim). Mechanical, behaviour-preserving; `test-lint-rules.sh --diff origin/develop` PASS, clang-format clean. Advances #21's non-tracker `outError` paydown (2 of the ~80 sites; Whisper/config/attachment/edit-meta/field-edit remain, plus the `AppController` public-API flip).
- (2026-06-07, quick-batch audit) · A batch re-execution of #16/#37/#3/#23/#4/#6/#15/#38 found **all eight already shipped** (commits above); re-verified each against the tree at `c742847d`: latch release + `[high-risk]` pinning test present (#16), no bare `catch(...){}` remains in `PlaneIssueMutation.cpp` (#37), zero `JiraClient` includes in `AppController.h` (#3), `static_assert(sizeof(ImWchar) == 4)` at `SmatchetImGuiFonts.cpp:24` (#23), Locales glob repointed + `test-config-globs.sh` zero-match selftest (#4), `is-exe-fresh.sh` present (#6), `--assert-absent` mode + selftest in `function_size_audit.py` (#15), and #38's residual `ninja-iter-msys2` grep hits are all intentional (migration notes + the preset-rejection regression test) — no debris remains. Outcome of the batch: this audit note + the Sprint-1 SHIPPED banner (resume-accuracy), no code change needed.

## Deviations from plan (Sprint 2 additions)
- **#37**: planned shorthand was "add the `// catch-all-ok:` marker"; upgraded to a policy-mandated `LOG_WARN` (`exception-handling-policy.md` Network/API tier — a 2xx-response parse failure is not an escape-hatch case).
- **#23**: planned home was `SmatchetImGuiHost.cpp`; relocated to `SmatchetImGuiFonts.cpp` (leaner DX12+GLFW TU with no `SmatchetUI.h`, clears the strict clang `-Wmicrosoft-include` lint, and is the semantic home — it builds the ImWchar glyph-range arrays). Compiles in both targets (broader than the planned DX12-only TU).
- **#4**: there are **no tracked `Locales/*.json`** (runtime override files placed next to the exe); repointed the glob at the real tracked source (`SmatchetLocalization.cpp` + `SmatchetLocaliz*.h`) and added `test-config-globs.sh` (fail-closed zero-match).
- **#15**: implemented as a new `function_size_audit.py --assert-absent <name>` mode (exit 1 if still over-cap) rather than a bare `--diff` check.
- **Merge mechanics**: a 6-PR-per-feature split (vs the PR-batching "one PR per feature") exhausted CodeRabbit's hourly quota → `cr-out-of-band` ×4 (#905-908) + `tests-out-of-band` (#906/#907) + 2 strict-`BEHIND` admin force-merges (#908/#911). Postmortem: `postmortems.md` 2026-06-06.
- **#29 (serial configure split) — skipped**: the two configure presets (`ninja-test-msvc` and `ninja-iter-msvc`) share `.fetchcontent-src` and their FetchContent subbuilds embed the absolute source-dir path in `CMakeCache.txt`. Backgrounding/parallelising within the same job would cause subbuild CMakeCache path collisions. Splitting into two separate jobs doubles the fixed overhead (checkout ~10s + MSVC setup ~30s + cache-restore ~15s = ~55s fixed cost per job × 2 = ~110s added overhead vs ~100s saved) — net-negative. Leave serial; the ~100s is the irreducible FetchContent populate cost on a cold run.
- **#9/#18 (coverage cold-rebuild) — partial**: coverage uses `ninja-test-msvc` with an instrumented OpenCppCoverage run; the build flags (`/Zi` debug info, OpenCppCoverage hooks) differ from `ninja-iter-msvc`. The compiled artifacts CANNOT be reused across the two workflows — the coverage rebuild is irreducible. The genuine win was fixing the wrong FetchContent cache path (see implementation log entry above) so the dep downloads are not re-fetched on every run.
- **#14 (merge queue) — blocked by platform; goal met another way**: the GitHub merge queue is an **org-only feature** (Team/Enterprise), absent from both classic branch protection AND rulesets on this user-owned repo, and there is no classic-protection API field to enable it. The actual goal (kill the `strict=true` O(n) per-PR force-merge dance) was achieved instead by turning **strict off** on develop's required-status-checks (`strict=false`, the 5 required checks preserved) — PRs now merge on their own green head; post-merge CI is the backstop. The `merge_group` workflow triggers + watcher `--auto` from #919 are kept (harmless, future-proof if the repo ever moves under an org). AGENTS.md / merge-gates.md updated to describe strict-off (not "develop is behind a queue").
- **#12 (batch 1 of 2 — 5 of 8 windows covered)**: PR #926 (`scripts/dev/test-ui-data-dependent-windows.sh`) covers Views Dashboard, Bulk Export, Plan Doc Viewer, Performance Window, and Annotate Analysis. Three windows deferred to batch 2: (6) Attachment Preview — needs a helper to inject synthetic `AttachmentCollectionRequest` entries before the guard passes; (7) New Issue Draft row — inline inside `drawActiveProjectWindow` (no separate `ImGui::Begin`), needs fixture data + `newIssueDraftActive=true` with a field catalog seeded; (8) Offline Queue panel — also inline. Batch 2 follow-up: `test/bucket-e-data-dependent-windows-batch2`.
- **#12 (batch 2 of 2 — completes 8/8) — SHIPPED**: branch `feat/hardening-12-batch2` adds the last 3 to `tests/ui/data_dependent_windows_smoke.test.cpp` (TEST-ONLY, no product change): (6) **Attachment Preview** — inject one synthetic non-image entry into `g_ui.attachmentWindowEntries` + `attachmentPreviewWindowOpen`; the draw fn's pre-`Begin` entries-guard means `WindowIsLive("Attachment Preview")` itself proves past-guard render (empty path covered too). (7) **New Issue Draft row** — fixture-coupled (same recipe as `funcsize_grid_render`): boot fixture → sync → focus `Smatchet - Active Project` → `newIssueDraftActive=true` + synthetic draft so `RenderNewIssueDraftRow`'s active branch ticks; SKIPS without the fixture env. (8) **Offline Queue panel** — host-window-live asserts the panel's `total==0` empty-guard branch every frame (always covered); `QueueCreateOffline` opportunistically forces the populated body when a live local cache exists. Local result: Attachment + Draft PASS, Offline empty-path PASS (populated path skips under the current `--spawn` harness — `OfflineQueueService` only constructed with a live LCM; backlogged in `tooling.md`). The pre-existing batch-1 Performance test fails on local discrete-GPU GL but is green under CI Mesa (the gating `Bucket-E UI tests` job). Driver `scripts/dev/test-ui-data-dependent-windows.sh` updated to inject the fixture + isolate user-data.

## Verification (actual)
- **Dual-target build** (`ninja-iter-msvc`, `SmatchetStandalone` + `SmatchetCore_DX12`) green for the bug fixes (#16/#23/#37); #1 verified configure-idempotent + negative-test FATAL + dual-target build.
- **ctest** (`ninja-test-msvc`) 100%; the #16 discriminating case runs (8/8 assertions).
- **#24/#15/#4**: `test-lint-rules.sh --selftest` + `lint_rules.bats` (27) + `function_size.bats` (19) + `function_size_audit.py --selftest` + `test-config-globs.sh` + doc suite — all green.
- **#20/#28 — Core_DX12 json-PCH = measured KEEP (2026-06-08).** Isolated from-scratch compile of the `SmatchetCore_DX12` target (229 TUs, `ninja-iter-msvc`, MSVC RelWithDebInfo, deps pre-warmed, best-of-2): **baseline 32.85 s → with json-PCH 28.34 s = 13.7 % faster** (PCH cluster 28.03/28.34/30.96 s across 4 runs; one 62 s machine-noise outlier discarded). New `Source/Core/include/SmatchetPchCoreDx12.h` carries `nlohmann/json.hpp` ONLY — no imgui.h/GLFW/`<windows.h>` (DX12-backend + `no-glfw-in-core-headers` safe); wired via `target_precompile_headers(SmatchetCore_DX12 …)` under the existing `if(SMATCHET_USE_PCH)` gate (separate from the Standalone PCH; publish presets keep PCH OFF, so the iter/publish split is preserved). Dual-target build green; doctest 1428/1428. **`REUSE_FROM` N/A for `SmatchetTests`** — the doctest rig compiles a curated subset of `Source/Core/src/*.cpp` directly and links `nlohmann_json::nlohmann_json`; it never compiles against `SmatchetCore_DX12`, so a `REUSE_FROM SmatchetCore_DX12` PCH does not apply (a separate test-rig PCH was out of scope).
- **#33**: `scripts/dev/test-docs.sh` 9/9.
- **Sprint-1 hygiene #3/#5/#6**: SHIPPED in #915 (`0f3e6ab5`) — see implementation log. (Sprint 1 now fully complete.)
- **Sprint-2 status (as of 2026-06-06):**
  - **Shipped:** #7 (#921), #11+#25 (#922), #22 (#917), #31+#32 (#923), #9/#18-partial + #29-skipped (#925), #14-via-strict-off (#920), coverage `--no-breaks` (#927), **#20/#28 (Core_DX12 json-PCH — measured KEEP, see below)**, **#8/#13 (perf gate — GATE REQUIRED; see below)**. #12 batch-1 = 5/8 windows (#926); **#12 batch-2 = 3/3 → 8/8 windows complete** (`feat/hardening-12-batch2`).
  - **Parked (human-gated):** none. #8/#13 has since **SHIPPED — GATE REQUIRED** (status reconciled 2026-07-14): per [`perf-gate-revival.md`](docs/plans/perf-gate-revival.md) the headless launch is proven on CI, 4 human-approved `ci-windows-latest` baselines are committed, `mean_abs_ceiling_ms = 6.94` was calibrated 2026-07-06, the `p99Ms` emitter follow-up landed, and `Perf PR-fast (windows-2022)` is now a required context.
  - **Remaining 🟢:** none — #12 batch-2, #20/#28, and **#10 TSan (MVP shipped — see below)** all landed by 2026-06-08; **#8/#13 perf gate is REQUIRED** (2026-07-14 reconcile). The escalated 🔴 tier is now **#19 FULLY SHIPPED (19a + 19b + 19c — sol2 lifted off `AppController.h`)** + #21 (`Optional<T>`) as the sole remaining item.
  - **Escalated 🔴 (await user):** #19 (pImpl `AppController`) — **19a + 19b + 19c SHIPPED** (see #19 entry below); #21 (`Optional<T>`/`Result`) — being paid down incrementally. **The #21 COLLABORATION surface + the `AppController` public-API flip are now COMPLETE** (2026-07-22 — collaboration mutations→`VoidResult`, reads→`Result<T>`; edit-meta done; field-edit a decided non-goal). Remaining #21 is **non-collaboration only** — a tiered forward map is in the 2026-07-22 implementation-log row below (Tier-1/2 clean flips ≈14–19 Core sites; the rest is test-owned/​boundary-owned/​genuinely-blocked).
- **#19 — pImpl `AppController` (sol2 header lift): step 19a SHIPPED (2026-06-08, PR #1016, branch `feat/appcontroller-pimpl`).** 19a is the pImpl scaffold only — forward-declared nested `struct Impl`, `std::unique_ptr<Impl> impl_` declared LAST, empty `Impl{}` + out-of-line ctor/dtor in `AppController.cpp` (incomplete-type discipline compile-verified). **Zero behaviour change.** Verified: dual-target build (`SmatchetStandalone` + `SmatchetCore_DX12`, `ninja-iter-msvc`) green; `SmatchetTests` 1460/14678 + `SmatchetLuaTests` 30/167 green; `test-lint-rules.sh --diff origin/develop` PASS; clang-format clean. Perf-neutral by construction (`impl_` touched only in ctor/dtor; no hot accessor de-inlined).
  - **sol2-lift baseline (authoritative `ninja -t deps`):** objects depending on `sol/sol.hpp` — `SmatchetStandalone` **102** (98 non-Lua-binding Ui/Commands), `SmatchetCore_DX12` **91**. 19a does not change these (header still includes sol2). **Finding:** `SmatchetCore_DX12` is **not** Lua-off in `ninja-iter-msvc` — it links `SmatchetCoreInterface` which sets `SMATCHET_WITH_LUA_AUTOMATION=1` as an INTERFACE define, so DX12 also pulls sol2 today. After 19c, sol2 should remain only in `AppController_Lua*.cpp` (+ LuaConsole/Mcp Lua) object depfiles in both targets.
  - **19b SHIPPED (2026-06-08, PR #1016, commit `6427423e`).** Migrated the COLD sol2-/subsystem-heavy member STORAGE off the public header into `struct AppController::Impl`, now defined in the new src-only `Source/Core/src/AppControllerImpl.h` (included only by the `AppController*.cpp` TUs): `sol::state lua` + the three `sol::protected_function` maps, `luaFieldCache_`/`luaWindows_`/`pendingLuaWindowOps_`, `scenarioPrior*`, `luaMcpTools_`(+mutex), `luaTicketActions_`/`luaGlobalActions_`(+mutex), `fieldIconMaps*`(+mutex), `luaHost_`, `aiAssistant_`, the MCP activity log, and the automation queue + worker. **Destruction-order invariant preserved verbatim inside Impl** (`sol::state lua` declared FIRST → tears down LAST, after the protected_function containers). **Shutdown UAF safety unchanged**: `~AppController` still flips `impl_->automationWorkerShuttingDown_` + joins `impl_->automationWorker_` in the dtor BODY (impl_ alive throughout) BEFORE any member destruction; `impl_->aiAssistant_.reset()` stays at the top of the body. Hot per-cell scalars (`luaProviderGen_`/`luaWindowDataGen_`/`inDrawLuaWindows_`, MCP HTTP-traffic atomics) stay inline; only `HasAiAssistantController()` de-inlined. ~3 member-access TUs rewired to `impl_->…`; the bare `sol::state` member uses a function-local `sol::state& lua = impl_->lua;` alias. **In 19b the header STILL includes `<sol/sol.hpp>` + `ILuaBindingHost.h`** (sol-typed methods + nested types remain on the class) — so the sol2-lift counts are unchanged from the 19a baseline; the lift lands in 19c. Verified: dual-target build green; `SmatchetTests` 1460/14678 + `SmatchetLuaTests` 30/167 green; `test-lint-rules.sh --diff origin/develop` PASS; clang-format clean.
  - **19c SHIPPED (2026-06-08, PR #1016).** The actual sol2 lift landed: `AppController::Impl` now `public ILuaBindingHost` (gains an `AppController& app_` back-ref threaded through the ctor — `make_unique<Impl>(*this)`), the `McpToolDefinition` type was **relocated to `namespace smatchet::lua`** in `ILuaBindingHost.h` (+ a new `virtual GetLuaMcpTools() const = 0` interface method), and the ~23 sol-typed methods moved off the public class onto `Impl` (`InitLuaCore`/`InitLuaUi`/`PrepareFreshLuaState`/`ReplayActiveSetupScripts`, the sol-typed `Lua*Bind` overrides + the UI-side `LuaRegister*`/`LuaUiRegisterWindowBind`/`LuaUiInvalidateFieldCacheBind`/`ParseMcpToolDef`/`ResolveLuaFieldProvider`, `RunAutomation{Job,AutoScript,FlatScript,ActionCall}`). `state["__smatchet_app"]` now holds the `Impl`-as-`ILuaBindingHost*`; `__smatchet_app_ui` holds the concrete `Impl*`. `AppController.h` **drops `<sol/sol.hpp>`, `ILuaBindingHost.h`, AND `AppController_LuaTypes.h`** (the third silently re-pulled sol2 via `sol::protected_function` in its value types) + the 4 `using` aliases + the nested `McpToolDefinition`; it now forward-declares `class ILuaBindingHost;` + the `smatchet::lua::*` value types and exposes a sol-free `ILuaBindingHost* GetLuaBindingHost()`. The sol-FREE Lua API (`ExecuteLua*`, `RunAutoScript`, `Execute*Action`, `LuaGetActiveTicketsBind`, `FindFieldById`, `SubmitFieldEdit`, `LuaCommands`, `AppForCommandContext`, `TryRenderCachedLuaField`, `DrawLuaWindows`) stays on `AppController`; `Impl` forwards to it via `app_`. `McpPlugin.cpp` reads tools via `GetLuaBindingHost()->GetLuaMcpTools()` (3 sites, guarded). `FakeLuaBindingHost` gained the `GetLuaMcpTools` override. **sol2-lift PROOF (`ninja -t deps`, before → after):** total objects depending on `sol/sol.hpp` **197 → 9**; `SmatchetStandalone` **106 → 5** (non-Lua-binding Ui/Commands **103 → 2** — the residual 2 are `AppController.cpp` + `McpPlugin.cpp`, both of which now legitimately need sol2); `SmatchetCore_DX12` **91 → 4**. The ~100 Ui/Commands includers no longer compile `<sol/sol.hpp>`. **Verified:** dual-target build (`SmatchetStandalone` Lua-ON + `SmatchetCore_DX12`) green; `SmatchetLuaTests` 30/30 (167/167 assertions) + `SmatchetTests` 1475/1475 (14731 assertions) green; Lua-OFF stub path (`AppController_LuaStubs.cpp` + `AppController.cpp` + `AppControllerImpl.h` no-Lua branch) syntax-clean (`cl /Zs` with `SMATCHET_WITH_LUA_AUTOMATION` off); `test-lint-rules.sh --diff origin/develop` PASS; clang-format clean. Destruction-order invariant preserved (Impl member order verbatim; `app_` is a reference → no teardown effect); shutdown UAF safety unchanged. No hot accessor de-inlined (the per-cell scalars + `TryRenderCachedLuaField` stayed on `AppController`).
- **#10 — TSan Linux subset: MVP SHIPPED (2026-06-08), expansion staged.** The NO-GO-in-place was lifted by standing up the missing Linux Clang preset. New `_smatchet-clang-linux-base` + `ninja-tsan-linux` configure presets (native `clang++`/`clang`, Ninja) drive a new headless `SmatchetTsanTests` target (`tests/CMakeLists.txt`, gated `if(UNIX AND SMATCHET_SANITIZER STREQUAL "tsan")`) under `-fsanitize=thread` (`cmake/Sanitizers.cmake`'s tsan branch — reachable for the first time now there is a non-MSVC compiler). A new `SMATCHET_BUILD_APP` option (default ON; **pure-additive — every existing preset is byte-for-byte unchanged**) flipped OFF only by `ninja-tsan-linux` skips GLFW/ImGui/Lua/whisper/Standalone/DX12 so the Linux runner needs no X11/OpenGL `-dev` packages. Nightly `.github/workflows/tsan-linux-nightly.yml` (cron + `workflow_dispatch` + paths-scoped `pull_request`) runs `ctest` under `TSAN_OPTIONS=halt_on_error=1`. **ADVISORY / non-required** — deliberately NOT added to `project.config.json` required_contexts.
  - **Curated subset shipped (MVP):** `*Pure` units (`TrackerLabelsPure`, `TrackerDateTimePure`) + **`LocalCacheManager`** (SQLite-backed cache carrying `std::mutex`/`std::atomic` — real race surface; ImGui-free closure = `LocalCacheManager.cpp` + `IssueDraft.cpp` + `Logger.cpp`). This is what TSan now covers on every nightly + relevant PR.
  - **Expansion DEFERRED (follow-up):** the `GridLiveContext` / `TicketSyncService` / `OfflineQueueService` sync layer is ImGui-header-coupled — `TicketSyncService.cpp` calls `SmatchetToastManager::Push` and `SmatchetToast.h` `#include`s `<imgui.h>`, so that path cannot link without `ImGuiLib` (i.e. without GLFW/X11 on the runner). Pulling it into the TSan subset needs Toast decoupled from the sync layer (a genuine layering smell — a background sync thread reaching directly into a UI singleton) or `ImGuiLib` built on Linux. Filed as debt; out of scope for this pure-additive infra PR.
- **Merge-gates decision — RESOLVED (option B, #933):** the "never merge past ANY red" (AGENTS.md) ⇄ "non-required → pass" (tested impl) contradiction surfaced after #923 is fixed — `merge-gates.sh` now blocks on non-required reds that match a curated allow-list (`Coverage|Sanitizer|Bucket-`, non-advisory); other non-required reds still pass. Postmortem: `postmortems.md` 2026-06-06 "#923"; filed `infra.md` P1 → applied.
- Sprint 2 #9/#18 (partial) — fixed wrong FetchContent cache paths in `perf-pr-fast.yml`, `perf-full.yml`, and `coverage.yml` (all three cached `build/<preset>/_deps`, which is the subbuild CMakeCache dir, not the downloaded sources at `.fetchcontent-src`). Added sccache cross-job restore-key fallback in `perf-pr-fast.yml`. #29 skipped (justified below in Deviations).

## Archive (post-ship — DO IN THIS PR, never a follow-up)
*The `git mv` is the step that reliably gets dropped (empirically ~62% of post-ship plans drifted stale-in-place). Bind it to the impl-log write: in the SAME PR that populates the three sections above —*
1. *flip the § Status header to `shipped`,*
2. *`git mv docs/plans/active/<slug>.md docs/plans/shipped/<slug>.md`,*
3. *regen the index: `bash agents/scripts/core/test-plan-index.sh --fix`.*

*No ref-sweep — references use the tier-less form `docs/plans/<slug>.md` (the gates resolve it against any tier; PR #890), so the move can't break them. Write new plan references tier-less.*

*(Delete this `## Archive` block as part of step 2 — once moved to `shipped/`, the file is reference material and the checklist has served its purpose.)*
