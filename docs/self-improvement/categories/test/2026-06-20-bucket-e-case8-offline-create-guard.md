- 2026-06-20 · orchestrator (roadmap PR-9 follow-up) · [test] · P2 — bucket-E case-8 (offline-create populated path) still SKIPs even with the new `SMATCHET_UITEST_WITH_LOCAL_CACHE=1` opt-in — the blocker is a DIFFERENT guard than the missing-cache one the original entry named
  Details: PR-9 (#1518) shipped the `UiTestScenario` local-cache opt-in + `EnsureLocalCacheForUiTest()` per the `bucket-e-uitestscenario-no-live-local-cache` entry, but probing showed `app->Cache` is ALREADY live in the spawn child (JiraDeterministic sync populates it), so the cache opt-in is a no-op on that branch. `QueueCreateOffline` still returns 0 — from a deeper guard (`ConfigManager::Load().ReadOnlyMode` or an `EnqueuePendingCreate` constraint), not the missing-cache one. The original backlog premise was stale; the harness opt-in shipped (harmless), but case-8 self-activation needs the real guard cleared.
  Concrete next action: a `debug-detective` pass to identify which guard fires in `QueueCreateOffline` under the bucket-E spawn child — the `LOG_WARN`/`LOG_ERROR` it emits does NOT reach the ImGui-TTY spawn log, so it needs Logger-sink instrumentation to surface. Once identified, either relax the guard for the UI-test harness or document case-8 as covered-by-a-different-path. Cross-ref: PR-9 #1518; `bucket-e-uitestscenario-no-live-local-cache` (applied #1518).
  Status: applied (2026-06-21 P3 sweep — shipped #1521)
  Last-reviewed: 2026-06-21
