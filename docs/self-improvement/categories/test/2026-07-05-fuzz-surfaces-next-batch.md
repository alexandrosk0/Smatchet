- 2026-07-05 · orchestrator (expand-fuzz-targets Slice E2c) · [test] · P2 — two untrusted-input fuzz surfaces pre-scoped for the next batch after E2c shipped `github_map` / `plane_map` / `linear_map` (the 3-target session cap). Both were validated against the code this session and deliberately NOT taken — each needs a prerequisite before it is cleanly fuzzable, so they are queued rather than forced.
  Details:
  (1) **MCP JSON-RPC dispatch/validation** (untrusted stdin/HTTP from arbitrary MCP clients). The raw parse is already depth/node-bounded — `McpPlugin.cpp:567,888` route `req.body` through `smatchet::json_safe::ParseBounded` (1 MiB cap) — so the parse-crash class is closed and a raw-parse fuzzer would only re-exercise `ParseBounded`. The residual surface (method routing + `tools/call` arg validation) lives inline in the `handleMcpJsonRpcPost` lambda (`McpPlugin.cpp:877-972`) and the `HandleJsonRpc*` members, all bound to a live `AppController` (`impl_->app->Commands().Dispatch`, the Lua host), so it cannot be linked standalone. There is no stdin/length-prefix framer (MCP is httplib-only, no `AiSseParser::Feed` analogue). **Prerequisite:** extract a pure `DispatchMcpRequest(const nlohmann::json&, ToolInvoker&) -> nlohmann::json` seam that takes the already-`ParseBounded` json plus an injected tool-invoker interface, then fuzz method-routing/validation against a stub invoker.
  (2) **Config + locale override parsing** (`smatchet_config.json`, `Locales/<lang>.json`). Both SECURITY_AUDIT findings on this surface are already remediated: the format-string vuln (#1) is guarded by `FormatSpecifiersMatch` (`SmatchetLocalization.cpp:1415`, shipped #1566 2026-06-27, extended af5536f 2026-07-02) and the POSIX read-size asymmetry (#33) is fixed with a 64 MiB cap + `ParseBounded` in `ConfigManager::LoadJsonFile` (`ConfigManager_PathUtils.cpp:781,798`, #1566). The pure ingress everything flows through is the shared `json_safe::ParseBounded` (already covered by `tests/Lua/BoundedJsonParse.test.cpp`), and the override-load/`Format` semantics only reach through the heavy `ConfigManager_PathUtils.cpp` (DPAPI/flock) + global-singleton closure. The one genuinely-fresh sub-surface is the `FormatSpecifiersMatch` / `ConversionSpecifiers` printf-specifier extractor (new hardening code that parses attacker-controlled strings), but it is file-static. **Prerequisite:** expose those two helpers behind a testable shim + add a differential harness asserting a specifier-mismatched override falls back to the trusted English literal (regression fuzz, not a crash finder).
  Also noted (lower priority, within the tracker surface E2c took): the **Jira issue-mapper** (`JiraIssueMappingPure.cpp`) and the **Jira field-catalog builders** (`TrackerFieldCatalogPure.cpp` — deepest structural nesting via `/createmeta`) are the natural next tracker targets, but both link `TrackerFieldValueParser.cpp -> TrackerFieldValueUtils.cpp -> ConfigManager.h`, a cascade that fights the fuzz preset's ImGui/ConfigManager-free-Core closure design (tests/CMakeLists.txt:1330). Needs that cascade validated under `ninja-fuzzer-linux` or a lighter seam extracted first. Views/saved-query (owner-only, already `ParseBounded`) and the SQLite cache (claimed by `slice-g-db-corruption` Phases 1-2) are deprioritized/out-of-scope.
  Concrete next action: a follow-up fuzz slice (E2d) that first lands the two source seams above (MCP dispatch extraction; locale specifier-guard shim), then adds the corresponding harnesses. Cross-ref: SECURITY_AUDIT.md #1/#33, tests/fuzz/README.md, docs/guides/testing-surface.md §6 Gap 3.
  **E2d update (2026-07-05):** surface (2) SHIPPED — the `FormatSpecifiersMatch`/`ConversionSpecifiers` guard was extracted into the header-only `smatchet::l10n` unit (`Source/Core/include/SmatchetLocalizationFormatGuard.h`, behaviour-identical to the file-statics it replaced) and fuzzed by `fuzz_locale_format_guard` (nightly-monkey Layer 3 pattern; header-only closure, seed corpus of realistic %-format pairs). Surface (1) MCP dispatch is ON HOLD: after #1637 the maintainer's `docs/plans/nightly-monkey-tester.md` scopes the MCP `tools/list`/`tools/call` surface under **Layer 4 (deferred pending infra + governance sign-off)**, so a strict-zone `Source/Plugins/Mcp` seam refactor there is now governance-gated — escalated to the maintainer rather than taken autonomously. The lower-priority Jira mapper / field-catalog cascade note still stands.
  Status: partially-applied (locale guard shipped E2d; MCP dispatch escalated/governance-gated)
  Last-reviewed: 2026-07-05
