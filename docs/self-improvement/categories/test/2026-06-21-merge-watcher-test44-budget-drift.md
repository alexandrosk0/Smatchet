- 2026-06-21 · orchestrator (P3-sweep surfaced) · [test] · P3 — `merge_watcher.bats` test #44 (`handle_blocked_cr_triage falls back to legacy counter when gh returns no head_sha`) fails on a budget-3→1 test-vs-code drift
  Details: surfaced during the P3-sweep (PR #1530 region). The test pre-populates `triage_attempts: 2` and expects the legacy path to increment to `3`, but `MERGE_WATCH_TRIAGE_BUDGET`'s default was later changed `3 → 1` (see the sibling test ~line 1478), so with budget 1 the code early-returns `BUDGET_EXHAUSTED` BEFORE the legacy increment path — the assertion never reaches `3`. The test is ASCII-named + unrelated to the gh-stub-windows fix that shipped in #1530; it fails on the unmodified file too (verified). The full `test-merge-watcher-bats.sh` driver exits 1 on this single case.
  Concrete next action: decide the intended budget-vs-fixture semantics and align them — either update the test's pre-populated `triage_attempts` to match budget 1 (assert the `BUDGET_EXHAUSTED` early-return path), OR if the legacy-increment path is still meant to be reachable, pin the test's `MERGE_WATCH_TRIAGE_BUDGET` env explicitly so it doesn't inherit the changed default. Small, contained. Cross-ref: #1530; `tests/bats/merge_watcher.bats` test #44 + the sibling at ~:1478.
  Status: open
  Last-reviewed: 2026-06-21
