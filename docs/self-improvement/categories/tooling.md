# Agent self-improvement — tooling

> Format / categories / workflow / priority / triage: see
> [`../AGENT_SELF_IMPROVEMENT.md`](../AGENT_SELF_IMPROVEMENT.md) (index + spec).
> Sibling categories: bug · process · tooling · infra · test · security · external-blockers · applied.
> Live entries only. `applied` entries archive immediately to `applied.md`.

## Triage log

- 2026-06-18 · orchestrator (sec-consent-modal #1400 ship session) · [tooling] · P2 — `comment-blank-run-flags-single-doc-paragraph-separator`: the `comment-blank-run` lint rejects a SINGLE bare `//` line used as a paragraph separator inside one contiguous doc-comment block — there is no allowance for "blank comment line between two prose paragraphs of the same block," so the idiomatic way to break a long header doc into readable paragraphs is a hard CI failure that a green local build does not surface.
  Details: hit on PR #1400 (first-send outbound-context consent modal). The new pure header `Source/Core/include/AiOutboundConsent.h` used two bare `//` lines as paragraph breaks inside an otherwise-contiguous doc-comment block (between the "what it does" prose and the "why it's header-inline" prose). `comment_audit.py` flagged both as new comment-noise vs `origin/develop` and the `test-lint-rules.sh` "Run non-UI bucket-A tests" step (the `Windows + MSVC` required context) failed — a full CI round-trip for two readability separators, while BOTH builds were green locally (the lint does not run in the targeted build-verify path — see the sibling `build-verify-shortcut-bypasses-comment-noise-and-lint-gate`, this file/date). Root cause: `comment_audit.py:58` `DECORATIVE_RE = re.compile(r"^\s*(//|/\*|\*)[\s/*=#~_>-]*$")` matches a bare `//` (the alternative is empty after the comment opener) → bucket `cut-blank` (one of `CUT_BUCKETS`) → rule `comment-blank-run` (name mapping ~line 281). The classifier has no notion of "this bare `//` sits between two non-blank comment lines of the same block" — it treats every bare `//` as strippable noise regardless of context. `tests/` is excluded, so the same separators in the test file were NOT flagged; only `Source/`-tree headers trip it. The fix this session was to delete both separators and let the prose run together (commit 782e23ce), which is strictly worse for readability than the intended paragraph break.
  Concrete next action: relax the `cut-blank` classification so a SINGLE bare `//` (or `*`) line that sits BETWEEN two non-blank comment lines of the SAME contiguous block is treated as an allowed intra-block paragraph separator, not noise. Keep flagging (a) runs of 2+ consecutive bare comment lines, (b) decorative banners (the `[=#~_>-]` fill variants), and (c) a bare `//` that brackets commented-out code. Implementation: in the line-walk that assigns `cut-blank`, look at the prev/next non-empty line — if both are in-block comment lines and this is the only blank-comment line in the run, skip the cut. Add a `--selftest` fixture pinning: single separator between two prose lines = PASS; 2+ run = still cut; leading/trailing block separator = still cut. Est ~1–1.5 h (classifier tweak in `comment_audit.py` + selftest cases + re-run `test-lint-rules.sh --selftest`). Cross-ref: PR #1400 (the incident + commit 782e23ce that worked around it by deleting the separators); `build-verify-shortcut-bypasses-comment-noise-and-lint-gate` (the shift-left half — why a green local build hid this); `agents/scripts/core/comment_audit.py:58` (`DECORATIVE_RE`) + ~line 281 (bucket→rule map).
  Status: applied (2026-06-20 roadmap campaign — shipped #1509)
  Last-reviewed: 2026-06-20

- 2026-06-18 · orchestrator (sec-consent-modal #1400 ship session) · [tooling] · P2 — `build-verify-shortcut-bypasses-comment-noise-and-lint-gate`: the standard build-verify loop (a targeted `cmake --build` / `build_and_run.ps1 -BuildOnly` / the ship-loop build step) compiles + runs tests but does NOT run `comment_audit.py` or `test-lint-rules.sh`, so a fully-green local build can hide a lint-only CI failure (comment-noise, strict-zone, function-size, include-cycle) that only surfaces after push — a wasted CI round-trip on every lint-shaped miss.
  Details: hit on PR #1400 — both builds green locally (`ninja-test-msvc` 8/8, `ninja-iter-msvc` links clean) yet the FIRST CI run failed on the `comment-blank-run` lint (two bare `//` separators; see the sibling `comment-blank-run-flags-single-doc-paragraph-separator`, this file/date). The mechanism to catch it locally already exists but is gated behind the wrong entry point: `scripts/dev/pre-ship.sh` runs `python3 agents/scripts/core/comment_audit.py --fix "$base_ref"` (line ~227) AND `test-lint-rules.sh --diff <base>` (line ~46), and its own header note (line 5) states that a targeted `cmake --build` / `ctest` does NOT run the comment-noise / lint gate. So an agent that verifies via the fast build-only path (the common case mid-iteration) skips pre-ship entirely and only learns of a lint miss from CI. The comment-noise rule is the visible instance, but the same gap covers every `test-lint-rules.sh` check (strict-zone, function-size, comment-ratio WARN, include-cycle, fan-in ratchet) — all CI-only relative to a build-only verify.
  Concrete next action: shift-left — make the standard build-verify loop run a fast lint pre-flight so a green build cannot hide a lint CI failure. Preferred: have `build_and_run.ps1 -BuildOnly` (and the ship-loop build step) invoke `comment_audit.py --diff <base>` + `test-lint-rules.sh --diff <base>` (diff-only, no `--fix`, so it is read-only + fast — seconds against the changed files) after a successful build and fail the verify with the same finding CI would report; optionally offer `--fix` interactively. Cheaper interim: a `scripts/dev/verify.ps1` wrapper (or a documented one-liner in the ship-loop prompt) that chains build → diff-lint, so agents stop reaching for the bare `cmake --build`. Note: relaxing the comment-blank-run rule (the sibling entry) shrinks ONE class of these misses but does not close the gap — function-size / strict-zone / include-cycle stay CI-only until the lint runs in the local loop, so this entry is independently worth doing. Est ~45 min–1 h (wire the diff-lint into `build_and_run.ps1 -BuildOnly` + a smoke that a comment-noise diff fails the local verify). Cross-ref: PR #1400 (green-build-hid-lint incident); `comment-blank-run-flags-single-doc-paragraph-separator` (the rule-relax half); `scripts/dev/pre-ship.sh` lines ~5/46/227 (the gate that already exists but the build-only path skips).
  Status: applied (2026-06-20 roadmap campaign — shipped #1509)
  Last-reviewed: 2026-06-20

- 2026-06-18 · orchestrator · [tooling] · P2 — `plan-index-fix-wrong-cwd-silent-noop`: `test-plan-index.sh --fix` resolves its archive dir RELATIVE to `$PWD`, so running it from a different tree's CWD than the worktree being fixed silently no-ops ("index up to date") while the real INDEX drift reds CI
  Details: `test-plan-index.sh` sets `ARCHIVE_DIR=docs/plans/shipped` and its Python does `os.listdir(archive_dir)` — relative to the invocation `$PWD`, NOT the git root or the script's own location. Archiving 2 plans in worktree `b8-toolingsweep-archive`, I regenerated via `cd /c/Development/Smatchet && bash <worktree>/agents/scripts/core/test-plan-index.sh --fix` — so it indexed the MAIN integration tree's `shipped/` (which lacked my 2 git-mv'd plans), printed "index up to date (133 plans)", and never touched the worktree's `INDEX.md`. The drift surfaced only in CI's `Auto-sync plan INDEX` + `Doc anchors + agent contract` jobs (142 plans in archive, my 2 missing) → 2 red REQUIRED checks on #1394. Re-running the identical command from INSIDE the worktree (`cd <worktree> && bash agents/scripts/core/test-plan-index.sh --fix`) correctly rewrote the index (+2). Same silent-degrade class as the python-off-PATH `—`-date entry (applied.md 2026-06-07) but a different trigger (CWD vs PATH).
  Concrete next action: (1) make `test-plan-index.sh` (and sibling relative-path repo scripts — `test-backlog-counts.sh`, etc.) `cd "$(git rev-parse --show-toplevel)"` at the top, or resolve `ARCHIVE_DIR`/`INDEX_FILE` against the script's git root, so it can't index the wrong tree regardless of invocation CWD. (2) Orchestrator habit + a line in the plan-archival playbook: run repo-relative scripts FROM the worktree root, never via `cd <other-tree> && bash <worktree>/script` — relative paths bind to `$PWD`, not the script location. Est ~20 min (script) + doc.
  Status: applied (2026-06-20 roadmap campaign — shipped #1502)
  Last-reviewed: 2026-06-20

- 2026-06-18 · orchestrator (keystore-H2 #1357 session) · [tooling] · P3 — `gh-graphql-reviewthreads-invocation-gotchas`: four concrete Git-Bash/Windows gotchas turn a PR `reviewThreads` query + reply into several failed attempts — implementation notes for the proposed `pr-blocked-why.sh` helper
  Details: Fetching PR #1357's unresolved review threads to verify them against HEAD (see process.md, same date) and posting per-thread replies cost several failed attempts on the orchestrator's Git-Bash/Windows shell before working. Four reusable gotchas, none currently captured: (1) the GitHub repo slug is `alexandrosk0/Smatchet`, NOT the git author display name "Alexandros Konstantonis" — a guessed owner/name fails GraphQL with `Expected NAME, actual: (none)`; derive it once via `gh repo view --json owner,name`. (2) Inlining the multi-line GraphQL on the shell mangles it — write the query to a file and pass `gh api graphql -F query=@file.graphql -F owner=<o> -F name=<r> -F num=<n>`. (3) `gh api` REST endpoints must be passed WITHOUT a leading slash under Git-Bash/MSYS — `repos/<o>/<r>/pulls/<n>/comments/<cid>/replies`, not `/repos/...`; a leading slash is path-rewritten to `C:/Program Files/Git/repos/...` -> "invalid API endpoint". (4) Parsing the JSON in Python defaults to cp1252 on this host and dies on non-ASCII bytes in CR comment bodies (`UnicodeDecodeError ... 0x8f`) — open with `encoding='utf-8'` and ASCII-fold before printing to the cp1252 stdout.
  Concrete next action: encode all four in the `pr-blocked-why.sh` / `reviewThreads` helper proposed by `green-pr-blocked-no-merge-signal` (this file, same date) — slug from `gh repo view`, query via `-F query=@file`, REST paths slash-stripped, JSON read as utf-8 — so the helper runs portably in the orchestrator's Windows/Git-Bash shell. Folds into that helper; no separate work item. Cross-ref: `green-pr-blocked-no-merge-signal`; #1357.
  Status: applied (2026-06-21 P3 sweep — shipped #1525)
  Last-reviewed: 2026-06-21

- 2026-06-18 · orchestrator (PR-merge-drive session) · [tooling] · P2 — `green-pr-blocked-no-merge-signal`: a `mergeable=MERGEABLE` + `mergeStateStatus=BLOCKED` PR with EVERY check green/skipped gives no signal as to WHY it can't merge — diagnosing it took a multi-step manual GraphQL hunt; the blocker turned out to be an unresolved bot review-thread under develop's `required_conversation_resolution`.
  Details: #1354 (fleet-preflight, all required checks green) sat `BLOCKED` and neither GitHub-native auto-merge nor the merge-watcher daemon would land it; `gh pr merge` only said "base branch policy prohibits the merge". The forensic that finally found it: `required_status_checks` were all completed (skipped-required counts as pass), required reviews = 0 — the real blocker was develop's `required_conversation_resolution=true` + ONE unresolved Cursor Bugbot thread (which flagged a genuine selftest flake). Nothing in `gh pr view` / `gh pr checks` surfaces "blocked solely on an unresolved conversation"; you must GraphQL `reviewThreads{isResolved}`. Related nuance found the same session: GitHub-native auto-merge does NOT fire for docs/script-only PRs whose required CODE lanes SKIP (e.g. #1336/#1356) — those merge only via the merge-watcher daemon, so arming `gh pr merge --auto` on such a PR is a no-op that reads as a stall.
  Concrete next action: a `pr-blocked-why.sh <pr>` helper (or a `pr-status-watch.sh` extension) that, when `mergeable=MERGEABLE && mergeState=BLOCKED` with no failing checks, reports the precise blocker — (a) unresolved review threads via GraphQL `reviewThreads{isResolved,author,path}`, (b) skipped-required contexts, (c) required-reviews shortfall — and notes whether the PR's class merges via daemon vs native auto-merge. Optionally offer to resolve a bot thread already addressed by a later commit. Est ~1 h + a bats fixture. Cross-ref: this session #1354 (conversation-resolution stall), #1336/#1356 (daemon-only docs merges); `scripts/dev/pr-status-watch.sh`.
  Status: applied (2026-06-20 roadmap campaign — shipped #1503)
  Last-reviewed: 2026-06-20

- 2026-06-16 · orchestrator (dead-session salvage) · [tooling] · P2 — `merge-snapshot-ledger-uncommitted-on-shared-tree-loss-risk`: `smatchet-merge-watcher` appends each merged-PR audit row to `docs/self-improvement/merge-snapshots.jsonl` in the **working copy** of the shared integration tree (C:/Dev/Smatchet) on **whatever branch happens to be checked out there**, leaving the rows uncommitted; they only reach `develop` via an irregular **manual** `chore(ledger)` harvest PR — so a `git reset`/`checkout`/`clean` of that tree (which the concurrent-session worktree rules actively encourage for a stale shared tree) silently destroys every un-harvested row.
  Details: observed taking over the dead `codex/tooltip-wheel-router` session — the shared tree's `merge-snapshots.jsonl` carried **28 un-harvested rows** (PRs 1249–1315, dated 2026-06-15) sitting dirty on a feature branch that was 66 commits behind `develop`. The watcher's PASS handler appends a JSON row per merge to whatever working copy it runs against; nothing commits or pushes it. Harvesting is a human ritual (precedent: PR #1251, #1170, #1189 — `chore(ledger): commit accumulated merge-snapshot rows`), fired on no fixed cadence. The hazard is the collision with AGENTS.md § Concurrent interactive sessions: that section tells agents to `reset --hard`/`checkout` a stale shared tree to recover HEAD drift, and `git-janitor`/`merge-watcher` auto-act on it — any of those on a tree holding un-harvested rows is **unrecoverable audit-data loss** (the rows exist nowhere else; they are not on a branch, not pushed, not stashed). In this incident the 28 rows were rescued by copying them to a temp file before any reset and replaying them into a fresh worktree off develop (this PR carries them), but that only worked because a human noticed; the normal stale-tree-recovery path would have dropped them. Two independent gaps: (a) the rows live only in an uncommitted working copy on a rug-pullable shared tree; (b) the harvest is manual + cadence-less, so the backlog of un-committed rows grows unbounded between harvests, widening the loss window.
  Concrete next action: make the ledger write durable instead of relying on a manual harvest — preferred: the merge-watcher PASS handler commits the appended row directly to a dedicated, never-reset ledger branch (or opens/updates a standing `chore(ledger)` PR) so each row is pushed the moment it is written; cheaper interim: (1) a `git-janitor`/`merge-watcher` pre-reset guard that refuses to `reset`/`checkout`/`clean` a shared tree whose `merge-snapshots.jsonl` has uncommitted rows (stash-to-temp + warn instead), and (2) a scheduled harvest (issue-janitor cadence) that auto-opens the `chore(ledger)` PR at N≥threshold un-harvested rows so the backlog can't silently accumulate. Est ~1.5-2 h for the watcher-commits-directly path + a bats fixture (append row → assert committed+pushed, not left dirty); ~45 min for the pre-reset guard alone. Cross-ref: PR #1251 / #1170 / #1189 (the manual harvest precedent this would automate); AGENTS.md § Concurrent interactive sessions (the `reset --hard`/`checkout` stale-tree recovery this collides with); `process-rules.md` § Git/p4 discipline (the 5-step destructive-op pre-flight a guard would extend).
  Status: applied (2026-06-20 roadmap campaign — shipped #1505)
  Last-reviewed: 2026-06-20

- 2026-06-16 · orchestrator (PR-review sweep) · [tooling] · P3 — `appcontroller-fan-in-ratchet-count-based-evades-swap`: the new AppController include-fan-in ratchet gate is COUNT-based, not SET-based, so a count-neutral includer swap (drop one existing `#include "AppController.h"` site + add a different NEW one in the same PR) slips through silently — defeating the gate's own stated "block a new includer on sight" contract
  Details: surfaced re-reviewing PR #1308 (the PR that landed the gate). On origin/develop the script is `agents/scripts/core/appcontroller_fan_in_audit.py`; `regression()` lives at lines 181-194 (note: #1312 rewrote/robustified the selftest AFTER #1308 and bumped `BASELINE_FAN_IN` 114→115, which is why the live location no longer matches #1308's PR-diff line numbers). The body short-circuits to pass whenever `len(head_paths) <= len(base_paths)` (lines 186-187: `if len(head_paths) <= len(base_paths): return []  # ratchet down or flat — never a regression by count`). The true offender-listing loop below it already computes the set-difference `sorted(head_paths - base_paths)` (line 189) — but the count guard pre-empts it for any count-neutral change. So a PR that removes one already-counted includer and adds a different brand-new one (net count flat) returns `[]` before the set-diff loop is ever reached: the new dependency evades the gate. This contradicts the script docstring + the AGENTS.md enforcement-card intent ("a new includer is an exact signal" / "block a new includer on sight"). FAIL-OPEN — a fan-in regression-by-substitution lands unblocked. (Routing: not a user-observable product defect — an agent-system gate weakness → tooling, no GitHub Issue per ADR-0014.)
  Concrete next action: drop the count-based early return in `regression()` and ALWAYS evaluate the set-difference — `new = sorted(head_paths - base_paths)`, run each through the existing deviation-suppression check, and FAIL on any surviving new includer regardless of whether the total count rose, fell, or stayed flat. A genuine swap-and-stay-flat then either ratchets the baseline DOWN (the PR also removed one — fine) or is correctly caught as a new dependency. Add a selftest case mirroring the existing fixtures: `base={A}`, `head={B}` (equal cardinality, A dropped + B added) MUST still flag B. Est ~30-45 min (logic delta + selftest fixture + a quick `--selftest` run). Cross-ref: PR #1308 (introduced the gate); PR #1312 (`appcontroller-fan-in selftest robust… 114→115` — the rewrite that moved `regression()` to its current lines, NOT the bug source); the AGENTS.md enforcement contract-card row that documents the "new includer on sight" contract this swap defeats.
  Status: applied (2026-06-21 P3 sweep — shipped #1522)
  Last-reviewed: 2026-06-21

- 2026-06-16 · orchestrator (PR-review sweep) · [tooling] · P3 — `fuzz-readme-ctest-preset-nonexistent`: `tests/fuzz/README.md:27` instructs `ctest --preset ninja-fuzzer-linux`, but the fuzz-harness PR added `ninja-fuzzer-linux` ONLY as a configurePreset + buildPreset — there is no `testPresets` entry (CMakePresets.json has no `testPresets` section at all), and CMake test presets do NOT inherit from configure/build presets, so the documented command errors `No such test preset` for anyone following the README on Linux
  Details: surfaced re-reviewing PR #1296 (Slice E2a libFuzzer harness). The README "How it builds" block (lines 25-27) is `cmake --preset ninja-fuzzer-linux` / `cmake --build --preset ninja-fuzzer-linux` / `ctest --preset ninja-fuzzer-linux` — the first two resolve (configure + build presets exist) but the third has no matching testPresets entry, so `ctest --preset` fails before running anything. The CI lane itself is UNAFFECTED and correct: `fuzz-smoke.yml` uses bare `ctest --output-on-failure` from the build dir, and the smoke test IS registered via `add_test`, so the lane works — only the hand-run README instruction is broken. Doc-only / dev-workflow defect, no product or CI impact (the agent first-pass cited `README.md:807`, a stale line number — the README is 60 lines; verified the real location is line 27). (Routing: test-tooling doc defect, no user-observable product behaviour → tooling/backlog, no GitHub Issue per ADR-0014.)
  Concrete next action: pick one — (a) add a `testPresets` entry named `ninja-fuzzer-linux` with `"configurePreset": "ninja-fuzzer-linux"` to CMakePresets.json (then `ctest --preset` works as documented), or (b) change README:27 to the non-preset form `ctest --test-dir build/ninja-fuzzer-linux --output-on-failure` to match exactly what `fuzz-smoke.yml` runs. (b) is the lower-risk ~10-min fix and keeps the doc lock-step with CI. Cross-ref: PR #1296 (introduced the README + presets); `fuzz-smoke.yml` (the working CI invocation the README should mirror); the sibling `fuzz-target-include-closure-unresolved-invisible` entry below (same fuzz-harness review sweep — a different, complementary gap).
  Status: applied (2026-06-21 P3 sweep — shipped #1523)
  Last-reviewed: 2026-06-21

- 2026-06-15 · mcp-toolsmith · [tooling] · P2 — `ban-bare-json-parse-on-untrusted-ingress`: a lint/grep gate banning bare `nlohmann::json::parse(` (and `::accept`/`::from_*`) on attacker-controlled ingress not routed through `smatchet::json_safe::ParseBounded` — this depth-bomb class has now recurred at THREE distinct ingress families (the `decode_json` Lua sink, the Jira ADF parser `TrackerFieldValueParser.cpp` #1220, and the MCP REST/JSON-RPC/Lua-MCP-params sites #1271), each found by manual re-review AFTER the prior fix shipped, so the gap is the lack of a mechanical backstop, not a one-off miss.
  Details: nlohmann's recursive-descent `json::parse` recurses once per nesting level and stack-overflows (SEH/SIGSEGV — NOT a catchable C++ throw) on a deeply-nested payload BEFORE any field is read; a byte cap does not bound depth (~1 M nested arrays fit in ~1 MiB). PR #1271 extracted the shared `smatchet::json_safe::ParseBounded` (`Source/Core/include/Json/BoundedJsonParse.h`) and routed all known sinks through it, but nothing prevents the NEXT `req.body` / sol2-string / MCP-`arguments.dump()` ingress from reaching for a bare `nlohmann::json::parse(...)` again. The three confirmed sites were each caught only by a human reading the diff. A delta-gated grep (mirroring the existing strict-zone grep rules in `agents/scripts/project/test-lint-rules.sh`, e.g. `no-printf-stderr`) over changed first-party C++ that flags a bare `nlohmann::json::parse(` / `json::accept(` whose argument is a known-untrusted source (`req.body`, an MCP params string, a Lua-provided `std::string`) and is NOT `ParseBounded` would convert this recurring manual-review catch into a mechanical block. The hard part is the taint heuristic (which `json::parse` calls are on trusted, already-bounded internal data vs raw ingress) — start conservative: WARN-first (calibration, like `duplication`), scoped to the strict zones (`Source/Plugins/Mcp/`, `Source/Core/src/{Tracker,Sync,Commands}/`) + the Lua-binding TUs where ingress lives, with a `SMATCHET_DEVIATION(rule=bare-json-parse-untrusted; …)` escape for the genuinely-internal callers. Even a coarse "any bare `json::parse(req.body` / `json::parse(paramsJson`-shaped match in a strict zone" catches the exact three observed shapes.
  Concrete next action: add a grep rule to `test-lint-rules.sh` (+ `--selftest` token in the AGENTS.md enforcement contract-card if it graduates to blocking) — delta-gated vs `origin/develop`, WARN-first, strict-zone-scoped, with the `bare-json-parse-untrusted` deviation escape; a bats fixture: a bare `nlohmann::json::parse(req.body)` in `Source/Plugins/Mcp/` → WARN/flag; a `ParseBounded(req.body, …)` call → clean; an internal `json::parse(serialized_internal_state)` outside the ingress zones → clean. Est ~1.5-2 h (grep rule + heuristic calibration + bats). Cross-ref: PR #1271 (this fix — the 3rd site + the shared helper); PR #1220 (`TrackerFieldValueParser.cpp` ADF depth cap — the 2nd site); security.md 2026-06-13 `JSON->Lua decode sink lacks explicit depth/size bounds` row (the 1st site + the consolidated Resolution); `Source/Core/include/Json/BoundedJsonParse.h` (the canonical bounded parse the gate steers callers toward); the existing strict-zone grep rules in `test-lint-rules.sh` (`no-printf-stderr`/`define-imgui` — the pattern to mirror).
  Status: applied (2026-06-20 roadmap campaign — shipped #1510)
  Last-reviewed: 2026-06-20

- 2026-06-16 · orchestrator (PR-review sweep) · [tooling] · P2 — `fuzz-target-include-closure-unresolved-invisible`: a `smatchet_add_fuzz_target` (standalone `add_executable`) whose `INCLUDES` omits a subdir that one of its closure `.cpp`s reaches via a transitive **quote-include** compiles fine in the main build (resolved by `SmatchetCoreInterface`'s INTERFACE include dirs, which the standalone target does NOT inherit) but `fatal error: file not found` on the fuzz lane — and that lane (`fuzz-smoke.yml`) is **advisory** (not required, not on `MERGE_GATES_BLOCK_ALLOWLIST_RE`), so the break can land red-but-unblocked on develop
  Details: surfaced reviewing PR #1301 (Slice E2b fuzz drivers). Two of three new targets in `tests/fuzz/CMakeLists.txt` are wired to fail compilation: `fuzz_callstack` INCLUDES only `${_fuzz_core}/include`, but its closure pulls `CallstackParser.h:4` → bare `#include "ConfigManager.h"` (header at `Source/Core/include/Config/ConfigManager.h`) → unresolved with no `include/Config` on the path; `fuzz_markdown_adf` compiles `src/Privacy/TextRedaction.cpp:1` → bare `#include "TextRedaction.h"` (header at `include/Privacy/TextRedaction.h`) with INCLUDES = `include` + `include/Ui` only → unresolved. The main build resolves both via the interface include dirs at `CMakeLists.txt:1228` (`include/Config`) / `:1232` (`include/Privacy`); a standalone `add_executable` fuzz target inherits none of them, and there is no directory-scope `include_directories()` in the chain. This class is invisible to `test-lint-rules.sh` (no rule inspects CMake include-closure resolution) and only manifests on the advisory lane, so it slips the required-gate set entirely. (Routing note: the two #1301 breaks themselves are NOT logged — per the author's decision they are fixed in-PR before merge; this entry is only the systemic-gate gap.)
  Concrete next action: a lightweight gate that, for each `smatchet_add_fuzz_target` SOURCES `.cpp` closure, statically resolves first-hop quote-includes against that target's declared `INCLUDES` and warns on any unresolved header (fail-closed: emit a WARN, not a hard block, during calibration — the advisory lane stays the authoritative compile check). Lower-effort alternative: a checklist item in `tests/fuzz/README.md` + the code-review fuzz-driver checklist — "every closure `.cpp`'s bare quote-includes must resolve under the target's INCLUDES, not just the main-build interface dirs." Est ~1-1.5 h for the static-resolver + a bats fixture (target with a closure `.cpp` whose quote-include is outside INCLUDES → WARN; fully-covered target → clean); ~10 min for the checklist-only mitigation. Cross-ref: `bucket-lane-status-broken-sentinel-auditable` (sibling — another advisory-lane break invisible to the audit/poller layer).
  Status: applied (2026-06-20 roadmap campaign — shipped #1506)
  Last-reviewed: 2026-06-20

- 2026-06-15 · orchestrator (gate-escape-postmortem) · [tooling] · P2 — `bucket-lane-status-broken-sentinel-auditable`: the Bucket-E lane-integrity guard already classifies a Mesa software-GL harness death as `status=broken passed=0 failed=N` (CI log: `lane-integrity — bucket-E is BROKEN … dead harness, not a regression`), but that verdict is a log annotation only — NO machine-readable signal feeds the merge-poller or `postmortem-owed.sh`, so a silently-broken advisory lane is invisible to the audit tooling
  Details: #1259 (merged 2026-06-15) dropped `Bucket-*` from the poller block-list **"while Mesa-GL lanes can't boot"** (explicitly temporary) — develop's `MERGE_GATES_BLOCK_ALLOWLIST_RE` is now `Coverage|Sanitizer|Perf PR-fast|Android security gate`. So today a red Bucket-E owes nothing and blocks nothing, which is correct. But the lane-integrity guard's `status=broken` verdict lives ONLY in the workflow log: `merge-gates.sh` reads a check's *conclusion* (success/failure/cancelled), and `postmortem-owed.sh` triggers on a block-scope RED — neither distinguishes a BROKEN harness (infra outage, file infra) from a legitimate advisory red or a real regression. Two consequences: (a) #1259's removal is explicitly conditional ("while Mesa-GL lanes can't boot") — when the lane stabilizes (#1265/#1290) and `Bucket-*` is re-promoted to the block-list, a future dead-harness run would again block merges with NO auditable "this is infra, not your diff" trail, replaying the #1218 force-merge ceremony; (b) even now there is no rollup that flags "Bucket-E has been BROKEN for N consecutive runs," so the stabilization effort has no owed-style nudge tracking the outage. Observed 2026-06-15: 4 Bucket-E reds (#1264/#1268/#1269/#1271) all carried `status=broken passed=0 failed=73` and were correctly NOT owed postmortems — but only because `Bucket-` is off the block-list; the dead-harness state itself never reached the audit layer. `postmortem-owed.sh --list` at develop tip cleanly reports "no gate escapes owed" for exactly this reason.
  Concrete next action: surface the lane-integrity `status=broken` verdict as a machine-readable artifact (a check-run output field, or a committed sentinel line the poller can parse) and teach `postmortem-owed.sh` to treat a block-scope check that is RED-because-BROKEN as an auditable **WARN** ("infra-broken — file infra, not a postmortem"), NOT an owed escape — so the moment `Bucket-*` rejoins the block-list a dead harness surfaces as infra rather than silently blocking (replaying #1218) or silently passing. Cheaper standalone alt (decoupled from the block-list question): a periodic rollup (issue-janitor cadence) that counts consecutive BROKEN Bucket-E runs and nudges at N≥threshold, feeding the existing `bucket-mesa-exe-boot` (infra P1) stabilization. Est ~1-1.5 h for the WARN-classifier + bats fixture (block-scope check RED with `status=broken` artifact → WARN, not owed; RED without it → owed unchanged); ~45 min for the standalone rollup. Cross-ref: #1259 (the explicitly-temporary `Bucket-*` block-list removal); #1258 (advisory-red-owes-nothing regression guard); `bucket-ui-lane-out-of-band-label` (tooling, down-scoped — the raw-PUT residual, complementary not duplicate); `bucket-mesa-exe-boot` (infra P1 — the lane stabilization this nudge would track); postmortems.md 2026-06-14 PR #1218 (the original Bucket-E force-merge that motivated dropping `Bucket-*`).
  Status: applied (2026-06-20 roadmap campaign — shipped #1505)
  Last-reviewed: 2026-06-20

- 2026-06-14 · orchestrator (gate-escape-postmortem) · [tooling] · P3 — `bucket-ui-lane-out-of-band-label`: PARTIALLY SUPERSEDED by #1259 (merged 2026-06-15). The downgrade-label half is now MOOT — #1259 dropped `Bucket-*` from the poller block-list, so the poller no longer blocks a red Bucket-* lane at all (nothing left to downgrade). Only the general raw-PUT guard survives as a residual P3 (preventing gate from postmortems.md 2026-06-14 PR #1218)
  Details: ORIGINAL finding — `merge-gates.sh` blocked a red `Bucket-*` but gave it no `$downgraded` override hatch (only `Test-delta gate`/`Perf PR-fast` had one), so a confirmed Bucket-E env-outage forced a raw direct REST PUT that bypassed the ENTIRE poller block-list. **#1259 (merged 2026-06-15) removed `Bucket-` from the block-list regex** — develop's `MERGE_GATES_BLOCK_ALLOWLIST_RE` is now `Coverage|Sanitizer|Perf PR-fast|Android security gate` (the #1258 regression-guard direction: an advisory Bucket-* red owes nothing and no longer blocks the poller; the `$downgraded` set now carries only tests-out-of-band/perf-out-of-band). So the proposed `bucket-out-of-band` DOWNGRADE label is moot — there is nothing left to downgrade, a red Bucket-* already passes the poller, and adding a label for it would re-introduce a block to then relax. What #1259 did NOT close is the general raw-PUT hole: a direct `gh api …/merge` still consults ONLY the GitHub-required contexts and bypasses the poller's REMAINING block-list (`Coverage`/`Sanitizer`/`Perf PR-fast`/`Android security gate`) — the exact #1130/#1220 Coverage/Sanitizer non-poller escape class. That belt-and-suspenders guard is the residual, and it is general (not Bucket-specific). The original #1218 override DECISION was legitimate (confirmed env-outage, zero diff exposure); only the MECHANISM was unscoped.
  Concrete next action: (downgrade-label half — DROPPED, moot post-#1259). Residual only: a `gh api …/merge` wrapper / pre-merge guard that refuses a direct PUT while ANY remaining block-list member (`Coverage`/`Sanitizer`/`Perf PR-fast`/`Android security gate`) is non-green absent its matching out-of-band label — closes the raw-PUT hole for all block-list members, incl. the #1130/#1220 Coverage/Sanitizer non-poller path. NOTE this now overlaps the `sanitizer-required-context`/`coverage-required-context` required-promotion route: a *required* context blocks the GitHub-native merge button + `--auto` too, so once those land the residual hole shrinks to admin/direct-PUT only — fold this guard into whichever lands rather than building a standalone wrapper. Est ~30-45 min if built standalone (a bats case: direct-PUT path + red Coverage absent `coverage-out-of-band` → refuse; with the label → allow). Cross-ref: #1259 (Bucket-* block-list removal) + #1258 (advisory-red-owes-nothing regression guard); postmortems.md 2026-06-14 PR #1218; the #1130/#1220/#1229 direct-merge-path class; `coverage-required-context`/`sanitizer-required-context` (the required-promotion complement that also closes the GitHub-native button).
  Status: open (down-scoped 2026-06-15 — downgrade-label half closed by #1259; raw-PUT-guard residual only)
  Last-reviewed: 2026-06-15

- 2026-06-14 · orchestrator (gate-escape-postmortem) · [tooling] · P2 — `out-of-band-on-trust-boundary-owes-tracked-test`: a `tests-out-of-band`/`perf-out-of-band` override on a strict-zone / security-trust-boundary diff must auto-file a tracked deferred-test obligation at ship (preventing gate from postmortems.md 2026-06-14 PR #1212)
  Details: PR #1212 changed a security trust boundary (`TrackerHttpUtils.{h,cpp}` + `JiraIssueMutation.cpp` — disabling cross-host redirect-following so the API token can't leak) with NO `*.test.cpp` delta, the test-delta requirement dismissed by `tests-out-of-band`. The override may be legitimate (the regression test needs an HTTP-redirect fixture that didn't exist) but `tests-out-of-band` creates NO durable obligation to add the deferred test, so a security fix's regression test silently never gets written — same class as #1124 (a legitimate override whose self-declared follow-up never happened). The complement of `postmortem-owed-moot-override-false-positive` (which correctly SUPPRESSES a moot override): a LOAD-BEARING override on a trust boundary should not just be flagged once by the owed-nudge, it should mint a tracked test obligation.
  Concrete next action: at override-merge time (the same hook that writes the ADR-0017 snapshot — `mandatory-merge-snapshot-on-override-merge`), when a `tests-out-of-band` (or `perf-out-of-band`) label is LOAD-BEARING (the gate would not have passed without it) AND the diff touches a strict zone (`Source/Core/src/{Tracker,Sync,Persistence,Config,Commands}/`, `Source/Plugins/Mcp/`) or a known trust-boundary path, auto-file a tracked deferred-test obligation — a `test.md` entry (or a GitHub Issue) naming the PR + the untested behaviour — so it can't evaporate. The owed test for THIS PR is already filed (`tracker-redirect-no-follow-regression-test`, test.md); this entry makes the MECHANISM durable. Est ~1.5-2 h (wire into the snapshot/override path + a bats fixture: a strict-zone diff + load-bearing `tests-out-of-band` → one obligation row; a non-strict-zone diff or moot override → none). Cross-ref: postmortems.md 2026-06-14 #1212; #1124; `postmortem-owed-moot-override-false-positive`; `mandatory-merge-snapshot-on-override-merge` (the hook point).
  Status: applied (2026-06-20 roadmap campaign — shipped #1503)
  Last-reviewed: 2026-06-20

- 2026-06-14 · orchestrator (gate-escape-postmortem) · [tooling] · P3 — `coverage-quarantine-safe`: the `Coverage (windows-2022 + OpenCppCoverage)` capture reds the lane when the quarantine SYNTHETIC self-test hard-CHECKs, conflating a fixture failure with a coverage regression → false-red Coverage merged past on a non-poller path (preventing gate from postmortems.md 2026-06-14 PR #1227; salvaged from the closed duplicate PR #1236 — the sharp tooling complement to the broader `ci-infra-flake-reds-masquerade-as-real-breakage` in infra.md)
  Details: `coverage.sh` runs the full `SmatchetTests` binary under OpenCppCoverage (`coverage.sh:186`) and `exit 1`s if `RC_TESTS != 0` (`coverage.sh:197`, `FAIL: OpenCppCoverage returned non-zero (SmatchetTests=$RC_TESTS, …)`) — BEFORE the `--threshold 65` coverage gate is ever evaluated. The quarantine synthetic self-test `[quarantined:test-rig]` (`tests/Core/FlakySelfTest.test.cpp:64`, body `SMATCHET_QT_CHECK(1 == 2)`) is WARN-by-default (prints, does NOT fail the suite) UNLESS `SMATCHET_RUN_FLAKY=1`, which promotes `SMATCHET_QT_CHECK` to a hard `CHECK` (`tests/support/QuarantineTestCase.h`) → the test binary exits non-zero → `coverage.sh` conflates that with a coverage-tooling failure and reds the lane. On PR #1227 (`fix(config): harden POSIX secret-at-rest perms …`) the Coverage lane red exactly this way (job 81293411741: `[quarantined:test-rig] … Status: FAILURE! … FAIL: OpenCppCoverage returned non-zero (SmatchetTests=1)`) and #1227 merged past it (human-manual, alexandrosk0, no `coverage-out-of-band` label). grep did NOT surface where `SMATCHET_RUN_FLAKY=1` enters the coverage run (`coverage.yml` / `coverage.sh` don't set it explicitly) — the leak source needs confirming. NB the lane is intermittent (green on develop tip now) — exactly the profile that trains override-by-reflex.
  Concrete next action: (1) make the capture quarantine-safe at the source — pass `--test-case-exclude=*[quarantined:*]*` to the doctest binary on the capture line (`coverage.sh:186`), OR explicitly clear/assert-unset `SMATCHET_RUN_FLAKY` before the capture run. (2) Split the two verdicts at `coverage.sh:197`: a non-zero TEST-binary exit (a doctest failure) and a non-zero OpenCppCoverage exit (a coverage-tooling failure) are different signals — a doctest failure should surface as a Tests failure, not masquerade as a coverage regression the `coverage-out-of-band` label can't cleanly describe. (3) Add a bats/selftest fixture: a capture run with the quarantine self-test present → Coverage green (self-test WARN/excluded); a genuine <65% → red; confirm + pin the `SMATCHET_RUN_FLAKY` leak source. Est ~1-1.5 h. Cross-ref: postmortems.md 2026-06-14 PR #1227; `scripts/dev/coverage.sh:186-197`; `tests/support/QuarantineTestCase.h`; `tests/Core/FlakySelfTest.test.cpp:64`; `coverage-required-context` (once binding, a false-red here deadlocks-by-design → raises this toward P1); the broader `ci-infra-flake-reds-masquerade-as-real-breakage` (infra.md).
  Update (2026-06-19, Cluster-C reconcile): RECONFIRMED — the false-red has NO LIVE INGRESS on develop. `SMATCHET_RUN_FLAKY` is set NOWHERE in `.github/` / `scripts/` / `agents/` (grep clean), so the quarantine self-test runs WARN-only (suite passes); and PR #927 (`coverage.sh --no-breaks`) already neutralized the WARN-crash half. The filed P2 red cannot recur as described → downgraded P2→P3. The defense-in-depth hardening (`--test-case-exclude=*[quarantined:*]*` + splitting test-fail vs coverage-tooling-fail at `coverage.sh:197`) stays cheap+valid but guards a path nothing currently exercises — implement only if a `SMATCHET_RUN_FLAKY` ingress is ever added. Cross-ref: PR #927.
  Status: applied (2026-06-21 P3 sweep — shipped #1522)
  Last-reviewed: 2026-06-21

- 2026-06-13 · orchestrator · [tooling] · P2 — `deeper-audit-harness-buildout`: stand up the audit *tooling* the deeper-audit playbook (docs/security/DEEPER_AUDIT_PLAYBOOK.md, PR #1191) calls for but the repo does not yet have — the 8 audit-METHOD gaps from the 34-target cross-reference (Workflow wf_ccaf45b5-1bc) that are tooling, not vulnerabilities, so they do not belong in security.md.
  Details: the cross-reference of the playbook's 34 follow-up targets against the security ledger found 13 untracked; 5 are concrete finding-candidates (now filed in security.md as `deep-audit-xref` rows), the other 8 are *audit techniques we cannot currently run*. This epic groups them: (1) **libFuzzer/AFL harnesses** over the untested parse surfaces — `AiSseParser`/`AiNdjsonParser` (the streaming decoders), the Plane mappers (`PlaneIssueMappingPure`), the GitHub mappers (`GitHubIssueSearchMapping`), and the Jira ADF parser — corpus + OSS-Fuzz-style CI lane; (2) **MCP/AI ThreadSanitizer lane** — a CI job (or local script) that drives MCP dispatch + AI streaming under TSan to confirm/deny the two `deep-audit-xref` race candidates (targets 3 + 5) and catch future cross-thread g_ui access; (3) **CodeQL config-key negative-space matrix** — extend the CodeQL config to assert every security-relevant config key has a validation/canonicalization path (catches the next AgentsMdLoader-class arbitrary-read); (4) **second-tier dependency CVE/OSV match + SBOM lane** — `OSV-Scanner` over the lockfiles + an SBOM artifact in CI, beyond the first-tier curl/gradle pins already tracked; (5) **Unreal-embed prebuilt-lib supply-chain check** — verify the prebuilt DX12/ImGui libs packaged into the .uplugin against a pinned hash (the embed path skips the source build gates); (6) **DX12 C-ABI / ProjectSavedDir audit harness** — exercise the Unreal-only C-ABI boundary + the `ProjectSavedDir`-derived paths for path-traversal/handle-misuse; (7) **imgui_test_engine bare-HEAD pin** — `cmake/ImGuiTestEngine.cmake` fetches imgui_test_engine at a floating ref; pin to a sha (supply-chain). All 8 are internal tooling (`userVisible:false`) → backlog epic, not Issues.
  Concrete next action: split into per-item slices, sequence highest-leverage first — (a) the parser fuzz harnesses (items 1) share one harness scaffold + corpus dir, ship as one slice; (b) the MCP/AI TSan lane (item 2) unblocks confirming the two race candidates in security.md; (c) the OSV-Scanner + SBOM lane (item 4) is a self-contained CI add; (d) the imgui_test_engine sha-pin (item 7) is a ~15-min point fix, do it first. Items 3/5/6 are larger and can follow. Each slice that confirms a `deep-audit-xref` candidate should update that security.md row (candidate → confirmed/closed). **Ordering vs. the candidate-fix campaign:** this epic is a *prerequisite* of any campaign that fixes the security.md `deep-audit-xref` rows — most of those candidates carry a confirm-step (TSan / UBSan / fuzz / review) that cannot run until the matching lane here exists, so the harness lanes get built first. Concretely: the MCP/AI TSan lane (item 2) gates confirming the two race candidates (security.md targets 3 + 5, whose own next-action lines already say "Reproduce under the MCP/AI TSan lane"), and the parser fuzz harnesses (item 1) gate the Plane / GitHub mapper + JSON→Lua decode candidates (security.md targets 11/12/14). A fixer pointed at security.md should therefore stand up items 1-2 before treating those rows as actionable, not fix-then-confirm. Cross-ref: docs/security/DEEPER_AUDIT_PLAYBOOK.md (full per-target detail + the 7-tier ladder); docs/security/SECURITY_AUDIT_2026-06-13.md (the base audit); the 7 `deep-audit-xref` rows in security.md (the finding-candidates this tooling confirms). Est: total ~1-2 weeks across slices; (d) ~15 min, (c) ~half-day, (a)/(b) ~2-3 days each.
  Status: partially applied (2026-06-20 trap-sweep — shipped: fuzz-smoke.yml (item1), tsan-linux-nightly.yml (item2), imgui_test_engine SHA-pinned at cmake/ImGuiTestEngine.cmake:17 (item7); 2026-07-02 — shipped: OSV/SBOM lane (item4) via .github/workflows/dep-cve-sbom.yml + scripts/dev/gen-sbom.py (CycloneDX from the FetchContent pin set) + scripts/dev/osv-scan.py (OSV.dev commit-level query), advisory-on-introduction (no --fail-on); remaining: CodeQL config-key matrix (item3), Unreal prebuilt-lib hash (item5), DX12 C-ABI harness (item6))
  Last-reviewed: 2026-07-02

- 2026-06-13 · build-doctor · [tooling] · P2 — `fail-open-meta-gate-authoring-check`: deferred next-action (d) of `fail-open-gate-cluster-zero-match-and-transient-error` (applied 2026-06-13). The cluster's 8 sites were fixed individually, but no gate stops the NEXT fail-open driver/scanner from being authored.
  Details: a `--selftest`-style lint over `scripts/dev/test-*.sh` (+ embedded python scanners) that flags two anti-patterns at authoring time: (1) a driver with a `failed`-only success path (`if [ "$FAILED" != "0" ]; then exit 1; fi` as the LAST gate) with no preceding `passed=0 && failed=0` zero-match guard; (2) a python scanner using `os.listdir(<dir>)` (non-recursive) whose contract comment says "every … in `<dir>/`" — should use `os.walk`. Would have caught all 6+1 sites before merge. Pairs with the existing `test-gate-selftests.sh` convention-check (the "gate that gates the gates") — possibly an extension of it rather than a new gate.
  Concrete next action: extend `agents/scripts/core/test-gate-selftests.sh` (or a sibling) with the two greps above + a `# selftest: asserts-failure` synthetic-driver fixture; gate it on the lint-rules suite. Est ~2–3 h incl. bats. Cross-ref: `fail-open-gate-cluster-zero-match-and-transient-error` (applied, the 8 point-fixes); `tests/bats/fail_open_gate_cluster.bats`; `agents/scripts/core/test-gate-selftests.sh`.
  Recurrence (Batch 10, #117–#330): **5 more live siblings of the same fail-open family, one batch after #1192's 8-site point-fix — none of them touched by that fix** — which is the empirical justification for shipping this authoring meta-gate / a shared zero-match-guard helper instead of chasing drivers one by one. A NEW **sub-shape (D)** appeared: **gate greps a stale checked-in artefact, not the regenerated content.** `scripts/dev/test-perf-marker-inventory.sh:30` (#329, HIGH, 783b9946) greps the committed `docs/perf/MARKER_INVENTORY.md` (which `perf-marker-inventory.sh --check` writes to a temp file and *discards*, never updating the doc), so a `perf_temp:*` marker added in source but not regenerated into the doc leaks while the gate PASSES — defeating its sole purpose. The meta-gate's grep set should also flag a gate that greps a tracked/committed inventory/doc path on the success path rather than the freshly regenerated output. The 4 MEDIUM are pure shape-(B) zero-match false-PASS on drivers the point-fix never enumerated: `test-ui-sync-stall-visible-cue.sh:62` (#330), `test-ui-ai-assistant-enter-send.sh:56` (#214, **+ identical surviving logic in `test-ui-ai-assistant-panel-dock-swap.sh` and `test-ui-ai-prefs-autosave-flow.sh`**), `test-config-migration.sh:93` (#134 — doctest `--test-case` filter, asserts `FAILED -gt 0` only, never `PASSED -gt 0`). Plus a reachability-gap variant of shape (C): `pillar2-scan.sh:88` (#327, MEDIUM) `is_ui_reachable()` silently skips any UI-thread-reachable TU not named `*Ui*` / not directly `#include`ing `<imgui.h>` — sync I/O on the render path in a transitively-imgui helper passes with no CRITICAL. Takeaway: the shape-(B) guard belongs in the shared UI-test harness helper (so every driver inherits it), and the meta-gate grep set must cover (B) zero-match, (C) non-recursive/narrow-reachability scan, and (D) gate-on-stale-committed-artefact. Cross-ref: `docs/self-improvement/historical-review-findings.md` § Batch 10 (#117–#330, the 1 HIGH + 4 MEDIUM); `fail-open-gate-cluster-zero-match-and-transient-error` (applied — #1192's 8 point-fixes, which left these 5 untouched).
  Recurrence (Batch 11, #116–#1 — FINAL sweep, full #1–#1174 history now reviewed): **3 more live fail-open gates in the repo's oldest tail, incl. the lineage's first CRITICAL and its first in a REQUIRED CI check.** A NEW **sub-shape (E)** appeared: **gate greps the WRONG toolchain's output format → always-empty match → always green.** `scripts/dev/test-build-warnings.sh:46` (#86, **CRITICAL**, b68bf09a) greps only GCC's `warning: … [-Wunused-…]` tag form, but the default + CI preset is `ninja-iter-msvc` (MSVC emits `warning C4505/C4101/C4189/C4100` — numeric, no `[-Wunused-]` tag), and `SMATCHET_WARN_PRESET` is never set — so `OWNED_HITS` is always empty and this **required bucket-A** gate always prints `Passed: 1 Failed: 0` / exits 0, 100% blind to first-party unused-symbol warnings on the only toolchain it runs under (the regex was correct under #86's original MinGW build; a later default-preset flip to MSVC orphaned it). The meta-gate's grep set should also flag a warning/lint gate whose match pattern is a hardcoded single-toolchain literal (a `[-W…]` tag or a `gcc`/`clang`-only diagnostic shape) with no MSVC-code branch — high false-PASS risk on an MSVC-default repo. The other 2 are pure shape-(B) zero-run false-PASS on drivers neither #1192 nor Batch 10 enumerated: `test-theme-syntax-colors.sh:57` (#80, HIGH — doctest `SmatchetTheme*` filter, asserts `FAILED>0` only, green on `0 passed | 0 failed`) and `test-ui-views-columns-reorder.sh:68` (#77, HIGH — `ui_test.run` zero-match `passed=0/failed=0` reaches `exit 0`). Lineage takeaway (now that the FULL #1–#1174 history is swept): the fail-open family recurred in **every** batch that touched gate/test scripts — 8 (#439–#541), 10 (#117–#330), 11 (#116–#1) — and survived #1192's 8-site point-fix each time; that is the closed-form empirical case for the authoring meta-gate + a shared zero-match-guard helper over per-driver whack-a-mole. Final grep set the meta-gate must cover: **(B)** zero-match `passed=0&&failed=0`, **(C)** non-recursive / narrow-reachability scan, **(D)** gate-on-stale-committed-artefact, **(E)** single-toolchain-literal match pattern. Cross-ref: `docs/self-improvement/historical-review-findings.md` § Batch 11 (#116–#1, the 1 CRITICAL + 2 HIGH); `fail-open-gate-cluster-zero-match-and-transient-error` (applied — #1192).
  Status: applied (2026-06-20 roadmap campaign — shipped #1510)
  Last-reviewed: 2026-06-20

- 2026-06-13 · orchestrator · [tooling] · P3 — `fleet-verify-schema-over-required-field`: the fleet's verify-stage StructuredOutput schema marked `correctedLocation` as required even when the verdict confirmed the finding's original location unchanged, producing `Output does not match required schema: root: must have required property 'correctedLocation'` and a forced model retry.
  Details: only 1 occurrence in run `wf_62807bcd-dc8`, but every StructuredOutput retry re-runs the model over the full agent context, so a predictably-omitted required field is a latent retry-storm + compaction accelerant in a larger fleet. The field is only meaningful when a verifier *downgrades*/relocates a finding; on a plain "confirmed, location correct" verdict the model has nothing to put there and omits it.
  Concrete next action: in fleet verify schemas, make fields that only matter on a downgrade OPTIONAL (or give them a default / sentinel), and reserve `required` for fields the model can always supply (verdict, adjustedSeverity, rationale). Add a one-line schema-design note to `workflow-fleets.md` § Per-agent scoping (or a new § Verify-stage schema design). Est ~20 min. Cross-ref: `workflow-fleets.md`; run `wf_62807bcd-dc8`.
  Status: applied (2026-06-21 P3 sweep — shipped #1524)
  Last-reviewed: 2026-06-21

- 2026-06-12 · orchestrator · [tooling] · P2 — `markdown-links-local-passes-ci-fails-after-plan-archive`: `test-markdown-links.sh` diff-scope mode passes locally but fails on CI when a plan referenced by a new plan-doc was `git mv`'d to `shipped/` on develop after the feature branch was cut, because CI uses a GitHub synthetic merge commit where develop's deletion wins.
  Details: `pane-backend-picker.md:13` had \[multi-grid-tabs\]\(multi-grid-tabs.md\) — a relative markdown hyperlink resolving from `docs/plans/active/` to `docs/plans/multi-grid-tabs.md` (then at `active/`, archived to `shipped/` by #1155). Locally the link resolved fine (the file still existed in our branch, not yet rebased). On CI, the PR's synthetic merge commit = merge(our branch, develop HEAD after #1155). Develop had deleted the active-tier copy (via `git mv` in the mgt-s5-tsan branch) so 3-way merge resolution dropped the file from the synthetic commit — the link broke. The `guard-shared-tree` hook also blocked `git rebase origin/develop` in the worktree (cross-ref `guard-shared-tree-blocks-git-C-worktree` P3), so we couldn't rebase-first to pick up the `git mv` locally; the fix was: commit the corrected path (`../shipped/multi-grid-tabs.md`) and let CI verify on the synthetic merge. Note: `test-plan-ref-integrity.sh` (plain-text refs, NOT markdown hyperlinks) was never broken — it supports tier-less resolution; only `test-markdown-links.sh` (hyperlinks resolved from the file's directory) was affected.
  Concrete next action: (a) **Doc convention** — when writing \[label\]\(href\) markdown hyperlinks from `docs/plans/active/<slug>.md` to another plan, always use a tier-ful relative path (`../shipped/plan-name.md` or `./plan-name.md` for same-tier), never a bare `plan-name.md` that would silently fail once the target is archived; add one sentence to the plan template § Context boilerplate and to `docs/STRUCTURE.md` plan-link guidance. (b) **Pre-push local check improvement** — `test-markdown-links.sh` could optionally run against `git merge-tree $(git merge-base HEAD origin/develop) HEAD origin/develop` (synthetic merge tree) instead of the working tree, catching this class before push; or at minimum print a warning when the diff-scope run passes locally but the branch is behind develop by commits that touch the linked file. Est (a) ~15 min, (b) ~2–3 h. Cross-ref: `test-markdown-links.sh` (diff-scope, rule 2); `guard-shared-tree-blocks-git-C-worktree` (P3, blocked the rebase-first workaround); PR #1156 red doc-anchors run `27389579947`.
  Status: applied (2026-06-20 roadmap campaign — shipped #1502)
  Last-reviewed: 2026-06-20

- 2026-06-11 · orchestrator · [tooling] · P2 — `user-info-window-bucket-e-coverage`: Slice 5 of `user-info-window` shipped with its planned bucket-E (ImGui Test Engine) + screenshot-diff coverage deferred — the window's interaction contract is currently validated only by the one-time visual-validation pause, not by a repeatable gate.
  Details: `docs/plans/shipped/user-info-window.md` § Verification names the bucket-E set: window open/close (Escape + Close), Close/Escape triggers `ClearUserActivity` while a host-`isOpen` clear does not, load-button disable while loading, one-shot group member fetch per open, `VcsFeedLayout` toggle (unified ↔ separate) + config-key persistence, and the ~400px narrow-layout assertion (sections stack, card rows, no horizontal scroll); plus screenshot diffs at desktop AND ~400px widths covering the four sections + both VCS layouts. None of it landed in PR 4 — recorded as not-run in § Verification (actual) per the § Manual residue contract (visual-validation exception). Until covered, any regression in `SmatchetUserInfoUi`'s lifecycle (e.g. the relaunch-flag future handling or the close-edge `ClearPaneUserActivity`) lands silently.
  Concrete next action: add a bucket-E test TU driving the window via the staged-request latch (`UiDrawSession::userInfoRequestPending` + identity fields) against a fixture backend; assert the 7 named behaviours; add 4 screenshot baselines (desktop/narrow × unified/separate) to the existing screenshot-diff harness. Est ~0.5–1 d. Cross-ref: `docs/plans/shipped/user-info-window.md` § Verification (actual); PR 4.
  Status: open
  Last-reviewed: 2026-06-11

- 2026-06-08 · orchestrator · [tooling] · P2 — `android-emulator-mvp-smoke-harness`: Phase-0 mobile MVP has zero automated on-device coverage — boot/render/IME/data-path are manual residue (named in `docs/plans/shipped/mobile-app-jql-mvp.md` § Verification)
  Details: PR B (Slice 3b + 4) shipped the Android host shell + the live JQL→render→edit→PUT data path, but every runtime check (EGL/GLES3 boot, ImGui frame render, touch + soft-keyboard IME UTF-8 commit, surface-lost/context-recreate on background→foreground, the full Jira read-200/write-204 round-trip) was verified **manually** on an `x86_64` emulator + a physical device. CI runs only the `android-ndk-arm64` (configure+link the `.so`) and the new advisory `mobile-android-apk` (Gradle `assembleDebug`) jobs — neither boots the APK. So any regression in the host shell or the wired data path lands silently until a human re-runs the device smoke. The desktop bucket-E (ImGui Test Engine) + screenshot harness are structurally desktop-only and cannot cover the Android target.
  Concrete next action: stand up an emulator smoke job with `reactivecircus/android-emulator-runner` (KVM-accelerated `x86_64` AVD, API 24+) that (1) builds + installs `app-debug.apk` (`adb install -t -r`), (2) pushes a fixture `smatchet_config.json` (Domain/Email/Token from a CI secret + a fixture JQL like `project = SMTEST ORDER BY updated DESC`) into the app-private dir, (3) launches the Activity, (4) asserts via `adb logcat` markers that the EGL host reached its first rendered frame + the JQL fetch returned HTTP 200 + the grid built N>0 rows, and (5) optionally drives a scripted cell-edit → assert PUT 204. Gate it advisory first (emulator boots are flaky on shared runners). A throwaway Atlassian Cloud free-tier site + a scoped, revocable token in GitHub Actions secrets is the smoke backend (never a real user's token). Est ~1-2 d (most of it AVD-stability + the logcat-marker assertion contract). Until it lands, mobile MVP smoke = manual residue every PR touching `Source/Mobile/`. Cross-ref: `docs/plans/shipped/mobile-app-jql-mvp.md` § Verification (actual), PR B.
  Status: partially applied (2026-06-20 trap-sweep — shipped: mobile-emulator-smoke.yml + .github/scripts/mobile-emulator-smoke.sh boot/first-frame smoke (PR #1098); remaining: fixture-config push, JQL HTTP-200 assert, grid N>0 rows, PUT-204 cell-edit round-trip)
  Last-reviewed: 2026-06-20

- 2026-06-08 · orchestrator · [tooling] · P2 — `coverage-gate-platform-else-arm-exemption`: Test-delta gate trips on cross-compile-only Core edits that can't carry a desktop test delta (preventing gate from postmortems.md 2026-06-08 PR #1021, #1016)
  Details: PR #1021 (Phase-0 mobile build infra) touched `Source/Core/src/Tracker/TrackerHttpUtils.cpp` + `Source/Core/src/SubprocessCapture.cpp` with a `static_cast<std::int32_t>` (LP64 narrowing — an identity no-op on LLP64 desktop) and a `std::string(ptr,length)` ctor swap living in a Bionic-only `#else` arm — both unreachable/untestable on the desktop unit-test target, validated instead by the advisory `android-ndk-arm64` + `posix-core-check` jobs. `coverage-delta-gate.sh` `_classify_diff` counts production `.cpp` churn with zero `*.test.cpp` delta as a fail and has no exemption for a hunk confined to a platform `#else`/`#elif` arm, so it forced a `tests-out-of-band` override. Same override class hit #1016 (behaviour-preserving pImpl/sol2 header-lift refactor — logic moved between TUs, no new test warranted). The existing auto-exemptions (comment/log/`static_assert`/include/catch-scaffold/build-only) don't cover a real statement inside a platform `#else` arm.
  Concrete next action: extend `_classify_diff` with a platform-guard exemption — track `#if defined(<macro>)`/`#ifdef <macro>` / `#else` / `#elif` / `#endif` nesting while walking the diff, and auto-exempt added/removed lines that sit inside an `#else`/`#elif` arm of a guard keyed on a known cross-target macro set (`__ANDROID__`, `__APPLE__`, `__linux__`, and the non-`_WIN32` else of a `_WIN32`/`WIN32` guard) — unreachable on the desktop test target by construction, mirroring the existing comment/log/include auto-exemptions. Add a bats fixture: a Core `.cpp` diff confined to a `#else` arm + zero test delta must NOT fail; the same diff on the `#if` (desktop) side MUST. Est ~1.5-2 h. P3 residue (same entry, harder to classify mechanically — likely stays a manual override): (a) behaviour-preserving cross-TU refactors (#1016); (b) an identity cast on the desktop-reachable side — #1021's LP64 cast is compiled by both targets and is not `#else`-confined, so the platform-arm exemption won't cover it (would need a separate "no-op-on-test-platform" heuristic, probably not worth the complexity). Cross-ref: PR #1021, #1016, postmortems.md 2026-06-08. **2026-06-10 priority raise:** residue (a) behaviour-preserving cross-TU refactor recurred a 3rd time — #1096 (off-thread the toolbar per-tracker append disk read; logic moved to a worker, no semantic change, existing suite green) waved through with `tests-out-of-band` (postmortems.md 2026-06-10). Recurrence count for residue (a) now #1016, #1083, #1096 → bump residue (a) from **P3 to P2**: it is the dominant repeat-offender of the Test-delta-override class, more than the harder-to-classify identity-cast (b). A behaviour-preserving-refactor exemption (e.g. a `tests-out-of-band`-equivalent that requires the PR to assert "no behavioural change + existing suite green" and is auto-honoured) is now worth the design cost. **2026-06-16 4th recurrence:** residue (a) hit again — #1308 (`feat(core): AppController fan-in Phase 1`: `<nlohmann/json.hpp>`→`json_fwd.hpp` + `LocalCacheManager` include→fwd-decl across 3 `AppController*.cpp` TUs, no behaviour change, dual-target × dual-toolchain build green) waved through with `tests-out-of-band` (postmortems.md 2026-06-16). Recurrence set for residue (a) now #1016/#1083/#1096/#1308 — 4 instances, still the dominant repeat-offender of the Test-delta-override class. P2 unchanged (already the max for this calibration-phase backlog); the 4th hit reinforces that the auto-honoured behaviour-preserving-refactor exemption is the right design and overdue. A header-lift / fwd-decl-only diff (`#include` → forward-decl, `*.hpp` → `*_fwd.hpp`, no statement change) is the cleanest mechanical sub-case to auto-detect first. **2026-06-16 5th recurrence:** residue (a) hit again — #1317 (`refactor(debt): move dictation hook body out of hot localized-imgui header`: the `inline` definition of `HookDictationOnLastItem(char*, std::size_t)` in `SmatchetLocalizedImGui.h` becomes a bare declaration, body moved byte-identical into a new TU `SmatchetDictationHook.cpp` in the same namespace; ImGui-coupled Class-C, no headless test home, dual-target × dual-toolchain build green) waved through with `tests-out-of-band` (postmortems.md 2026-06-16). Recurrence set for residue (a) now #1016 / #1083 / #1096 / #1308 / #1317 — 5 instances. This adds a **second** clean mechanical sub-case alongside fwd-decl-only: a **header→cpp body relocation** — an `inline`/in-header function definition demoted to a bare declaration with its body moved unchanged into a new or existing `.cpp` TU in the same namespace, detectable by pairing a removed in-header definition with an added byte-identical out-of-line definition. Auto-detect both structural sub-cases first (fwd-decl-only #1308, body-relocation #1317) — neither needs a semantic diff.
  Status: partially applied (2026-06-20 trap-sweep — shipped: fwd-decl-only sub-case (PR #1310, coverage-delta-gate.sh:127-135); remaining: the primary platform-#else-arm exemption + the #1317 body-relocation sub-case)
  Last-reviewed: 2026-06-20

- 2026-06-08 · test-author · [tooling] · P2 — bucket-E `UiTestScenario` boot has no live local cache (`OfflineQueueService` null) → the Offline-Queue-panel populated-path smoke can't run, only its empty-guard path (plan #12 batch-2)
  Details: `tests/ui/data_dependent_windows_smoke.test.cpp` case 8 (`OfflineQueuePanel_RendersInlineInActiveProject`) wants to push the inline offline-queue panel PAST its `total==0` guard by enqueueing a synthetic create via `AppController::QueueCreateOffline`. Under the `cmd ui_test.run --spawn` harness that returns 0: `QueueCreateOffline` delegates to `offlineQueue_`, which `AppController::Initialize` only constructs when the local cache (`Cache`/LCM) is live (`AppController.cpp:1438`), and the minimal UiTestScenario boot doesn't initialise it. So the test asserts only the empty-guard branch (host-window-live ⇒ panel's early-return ticked) and logs the populated path as skipped — honest, but the toolbar/table body (header, `Copy selected##unifiedoff`, the unified table) gets no live bucket-E coverage. Same gap would block any future inline-panel test that needs real offline-queue/LCM rows (dead-letter restore UI, conflict-resolve modal).
  Concrete next action: give `UiTestScenario::OnStart` an opt-in that initialises a throwaway SQLite LCM (temp `SMATCHET_USER_DATA` dir) so `offlineQueue_` is constructed and `QueueCreateOffline`/`GetPendingCreates` work in-harness — then case 8's populated path (and the `IM_CHECK_NO_RET(!GetPendingCreates().empty())` + toolbar probe) self-activates with no test edit (the branch is already written behind the `queuedId>0` guard). Cheapest: a `SMATCHET_UITEST_WITH_LOCAL_CACHE=1` env gate that the data-dependent driver sets. Est ~1-2 h. Until then case 8 = empty-path coverage only.
  Status: partially applied (2026-06-21 final autonomous batch — shipped #1518)
  Last-reviewed: 2026-06-21

- 2026-06-07 · test-author (Slice-2 agent) · [tooling] · P2 — bucket-E failures are blind: spawned-child stdout is reliably 0 bytes on Windows, so a failing UI test reports nothing
  Details: When a bucket-E run fails under the spawned-child runner on Windows, the child's stdout reliably comes back 0 bytes — the test engine's per-test log (which names the failing step/assertion) never reaches the caller, so every failure triage starts blind. The per-test output already exists in memory in the engine (`ctx->Test->Output.Log`); it just has no file egress. The `ui_test.run` command (`Source/Core/src/Commands/Builtin/BuiltinCommands_UiTest.cpp:27`) currently exposes no output-log parameter.
  Concrete next action: add a `ui_test.run --outLog=<path>` parameter that dumps `ctx->Test->Output.Log` per test to the given file, so a failed bucket-E run leaves a readable per-test log regardless of spawned-child stdout loss; wire the bucket-E bash drivers to pass it and cat it on failure. Est ~1-2 h.
  Status: applied (2026-06-21 final autonomous batch — shipped #1518)
  Last-reviewed: 2026-06-21

- 2026-06-07 · code-review · [tooling] · P3 — `guard-head-drift.sh` residual fail-open shapes after the CR-953 regex hardening (quoted -C paths with spaces; no-jq sed fallback truncation)
  Details: Post-merge review of #947/#953 + the regex-hardening follow-up fixed `git.exe` / quoted-full-path / subshell forms, but two shapes still fail OPEN: (a) `git -C "C:\a b" commit` — `GIT_OPTS_RE`'s `-C[[:space:]]+[^[:space:]]+` can't span a quoted path containing spaces, so the whole invocation mismatches and no deny fires (low practical risk: no repo/worktree path contains spaces today); (b) the no-jq `json_field` sed fallback truncates `.tool_input.command` at the first escaped quote, silently degrading every downstream regex — if jq is a hard prerequisite for the hook set, the hook should deny (or at least warn) when jq is absent instead of parsing garbage. Also: the protected-branch list `develop|main` is hardcoded at 3 sites; per portable-purity it should eventually read `project.config.json`.
  Concrete next action: (a) extend `GIT_OPTS_RE` with a quoted-path alternation (`-C[[:space:]]+("[^"]*"|'"'"'[^'"'"']*'"'"'|[^[:space:]]+)`) + a bats case; (b) add an explicit `command -v jq || deny "jq required"` (or WARN-and-allow with a log line) + a bats case with jq shadowed; (c) source the branch list from project.config.json via a build/copy-time substitution. Est ~1 h together.
  Status: partially applied (2026-06-20 trap-sweep — shipped: (a) GIT_OPTS_RE quoted-path alternation (guard-head-drift.sh:99); remaining: (b) explicit jq-absent deny/WARN, (c) branch list sourced from project.config.json)
  Last-reviewed: 2026-06-20

- 2026-06-07 · code-review · [tooling] · P3 — FPS-measure report label can go stale mid-run now that vsync toggles live
  Details: `FpsMeasure` snapshots `vsyncOff` once at `Begin`; since #953 a mid-measurement Preferences/`config.set vsync` flip changes measurement conditions live while the report still prints the boot-time label. Acceptable behaviour, but a confusing perf report is plausible.
  Concrete next action: either re-read `smatchet::vsync::Enabled()` at report time and print `vsync=changed-mid-run` when it differs from the Begin snapshot, or one doc line in the fps-measure env-var docs. ~15 min.
  Status: applied (2026-06-21 P3 sweep — shipped #1529)
  Last-reviewed: 2026-06-21

- 2026-06-06 · test-author · [tooling] · P3 — bucket-E inline-panel smoke recipe missing for New Issue Draft row and Offline Queue panel (plan #12 batch 2)
  Details: Two of the 8 data-dependent windows targeted by plan #12 (`SmatchetNewIssueDraftUi` inline row and Offline Queue panel) are not top-level `ImGui::Begin` windows — embedded inside `drawActiveProjectWindow` / `drawSecondaryWindowsTail`. The current bucket-E smoke pattern (`YieldUntil(WindowIsLive(...))`) cannot drive them without: (a) activating the fixture backend so tickets load, (b) setting `g_ui.newIssueDraftActive = true` + seeding `newIssueDraft`, (c) waiting until a draft-row item renders via `ctx->ItemExists`. The Attachment Preview (window 6) needs a helper to push a synthetic `AttachmentCollectionRequest` into `g_ui.attachmentCollectionQueue` before the window guard passes.
  Concrete next action: batch-2 PR on branch `test/bucket-e-data-dependent-windows-batch2` — fixture-coupled smoke that (1) boots with `SMATCHET_TEST_JIRA_BACKEND_FIXTURE`, (2) syncs, (3) sets `g_ui.newIssueDraftActive = true`, (4) waits for `"##newIssueSummary"` in the active-project window item tree. Est ~2-3 h. Cross-ref: plan `build-quality-velocity-hardening.md` #12 §Deviations; PR #926.
  Status: applied (2026-06-20 trap-sweep — PR #1006)
  Last-reviewed: 2026-06-20

- 2026-06-06 · orchestrator · [tooling] · P3 — `subsystem-invariant-audit` workflow deferred — portable dir can't hold a project-path literal
  Details: adopt-workflow-orchestration slice 3 (`agents/_shared/workflows/subsystem-invariant-audit.js`) was authored (read-only `code-review` fan-out per `Source/Core/src/<ctx>/` leaf-AGENTS.md zone → aggregator) but removed from PR #887 because it hardcodes the `Source/Core` path — a project literal in the portable `agents/_shared/` dir, which `test-portable-purity` (correctly) fails. A portable saved-workflow can't name a project-specific path. The pre-merge-review workflow (slice 2) shipped fine (generic).
  Concrete next action: pick one — (a) a project-scoped workflows location outside the portable purity zone (e.g. `agents/project/workflows/`) + extend `setup-harness.sh`'s link loop to it; or (b) make the workflow discover its zones at runtime (the subagent globs `Source/Core/src/*/AGENTS.md`) so the `.js` embeds no project path. Then re-add the workflow + its plan-doc § Implementation-log line. Est ~1-2 h. The intended shape (read-only code-review per disjoint zone, respecting fan-out-safety Boundary 2) is captured in `docs/plans/shipped/adopt-workflow-orchestration.md` § Deviations.
  Status: applied (2026-06-21 P3 sweep — shipped #1530)
  Last-reviewed: 2026-06-21

- 2026-06-02 · orchestrator · [tooling] · P2 — `function_size_audit.py --diff` is grandfather-blind: a partially-reduced-but-still-over-cap function passes silently (CI green ≠ target resolved)
  Details: The function-size delta gate keys on `(rule, basename, qualified-name, arity)` and only fails on a NEW key vs the merge-base. So a function that a refactor REDUCES but leaves over the hard cap keeps the same key → `--diff` exits 0 and CI's delta gate passes, even though the function is still oversized. Observed during the `full-function-size-compliance` program: `RenderNewIssueDraftRow` passed `--diff` at 249L/78br mid-decomposition (still over the 200L/30br caps); a `grid-engine` agent caught it only by also running `--scan-file`. This means a decomposition agent (or the orchestrator) that "verifies done" with `--diff` alone can ship a partial reduction, and per-PR CI (which uses `--diff`) won't catch it — the oversized function silently becomes the new grandfathered baseline.
  Concrete next action: (1) every decomposition / refactor subagent prompt + the decompose playbook (`docs/guides/imgui-draw-pattern.md` § Verification) MUST require `python3 agents/scripts/core/function_size_audit.py --scan-file <file>` confirming the target is ABSENT from the absolute violation list — not just `--diff` exit 0. (2) The orchestrator's end-of-program verification must run the repo-wide `--list` on develop and assert it is EMPTY, rather than trusting per-PR CI green. (3) Consider a CI mode that fails if `--scan-file` on any touched file regresses, closing the grandfather-blind window. ~1h.
  Status: applied (2026-06-20 roadmap campaign — shipped #1510)
  Last-reviewed: 2026-06-20

- 2026-06-02 · orchestrator · [tooling] · P3 — `comment_audit.py` noise heuristics false-positive on ordinary prose (the #1 build-green-but-CI-red cause; ~10 fix-commits this program)
  Details: Across the function-size program the comment-noise gate caused ~10 separate fix-commits, every one a false positive on legitimate doc prose. `CODE_LIKE_RE` (in `comment_audit.py`) flags a comment when its body (a) contains a `word (` adjacency (e.g. `// Returns Apply (with working set)…`, `// match SmatchetMcpServerUi.cpp (the only caller)`), (b) ends in `;`/`)`, or (c) starts with a C++ keyword (`if`/`for`/`else`/`return`); and `comment-blank-run` flags ANY bare `//` separator line inside a multi-line comment block. Two aggravators: clang-format reflowing a comment AFTER a hand-run gate re-triggers the rule (the moved line newly matches), and both the comment-audit `--diff` and the manual balance-grep key on the FULL raw line INCLUDING indentation, so relocating an unchanged develop comment into a less-nested helper re-flags it as "new". Net effect: subagents repeatedly shipped build-green branches that failed CI on a comment, costing a CI round-trip + fix-commit each.
  Concrete next action: (1) tighten `CODE_LIKE_RE` — require higher code-token density, and treat a comment line ending in sentence punctuation (`.`/`!`/`?` after a word) as prose regardless of an interior `(`/`;`. (2) normalize leading whitespace before the `--diff` snippet comparison so a re-indented-but-byte-identical comment stays grandfathered. (3) Mandate `scripts/dev/pre-ship.sh` (shipped #746) as the last pre-push step everywhere — it enforces the correct clang-format-THEN-gate order so a reflow can't slip a comment past. (4) Decomposition prompts must say: keep new `_detail.h` header comments as ONE contiguous prose block (no bare-`//` lines), avoid `word (`/trailing-`;`/leading-keyword shapes; and verify positional-ImGui balance by grepping the `ImGui::` prefix (a bare `BeginCombo` token also matches the helper's doc-comment text and inflates the count). ~1-2h for the regex/normalize work.
  Update (2026-06-19, Cluster-C reconcile): SUBSTANTIALLY RESOLVED — rec (1) shipped: `comment_audit.py is_prose_not_code()` (PR #921 "prose-guard the comment-noise gate") demotes a `CODE_LIKE_RE` match to prose on a strong prose signal, pinned by `--selftest` prose fixtures (#1053); #1115 fixed the bare doc-opener FP; #1289 added pre-ship auto-strip. The relocation aggravator is covered by merge-base-scoped diffing (`comment_audit.py` scans only ADDED-vs-fork-point lines). Only rec (2)'s explicit leading-whitespace-normalize-before-compare is unshipped, and it's marginal (merge-base scoping already covers the relocation case). Cross-ref: PR #921, #1053, #1115, #1289.
  Status: partially applied (2026-06-20 trap-sweep — shipped: rec(1) is_prose_not_code (PR #921) + #1289 pre-ship auto-strip; remaining: rec(2) explicit leading-whitespace-normalize (subsumed by merge-base scoping))
  Last-reviewed: 2026-06-20

- 2026-06-02 · orchestrator · [tooling] · P3 — no headless probe verifying per-subsystem leaf-doc nearest-wins discovery across harnesses
  Details: The per-subsystem-agent-docs feature (plan `docs/plans/shipped/per-subsystem-agent-docs.md`, shipped PR #720) loads each `Source/Core/src/<ctx>/AGENTS.md` per harness — Claude Code via a gitignored `CLAUDE.md` shim that `setup-harness.sh`'s `gen_subsystem_claude_shims()` generates, Codex via native nearest-`AGENTS.md`, Cursor/generic via the `CONTEXT-MAP.md` pointer (`docs/harness/SETUP.md` § Per-subsystem leaf discovery). Claude Code's path was verified live this session (reading a file under `Source/Core/src/Ui/` auto-loaded `Ui/CLAUDE.md`'s `@AGENTS.md`), but Codex + Cursor were NOT verified, and there is no automated regression that a leaf's rules actually reach an agent touching that dir. The plan's § Verification flagged this as semi-manual residue.
  Concrete next action: script a headless probe — spawn a minimal agent session cd'd into `Source/Core/src/Tracker/`, ask it to cite the rule source for a Tracker invariant, assert it names the leaf `AGENTS.md` (not central `code-review.md`). Run per supported harness once a headless fixture exists; gate in `doc-validation.yml` if cheap. ~1-2 h once a headless harness fixture is available.
  Status: partially applied (2026-07-08 backlog campaign — shipped: `agents/scripts/core/test-leaf-doc-discovery.sh`: static shim-parity check (a `@AGENTS.md` shim beside every leaf on a provisioned checkout; auto-enrolled via the test-*.sh glob, skips unprovisioned/CI checkouts) + opt-in `--live` headless probe (spawns `claude -p` cd'd into `Source/Core/src/Tracker/`, asserts the leaf AGENTS.md is cited as the rule source — ran live in-container: PASS, answer was exactly `Source/Core/src/Tracker/AGENTS.md`) + regression-net note in `docs/harness/SETUP.md` § Per-subsystem leaf discovery; remaining: Codex/Cursor halves (no headless fixture available) and any doc-validation gating — the live probe spends tokens, so it stays opt-in rather than a CI gate)
  Last-reviewed: 2026-07-08

- **2026-05-18** — Triage sweep per [`docs/plans/shipped/agent-docs-improvements.md`](../../plans/shipped/agent-docs-improvements.md) § Action 2. Cadence trigger: category breached the ~20 open-items threshold (was 27).
  - Before: 27 entries
  - Dedup-merged: 1 (Bucket-E tooltip-content-identity helper — 2026-05-17 entry kept as survivor with `Supersedes: 2026-05-16` line; 2026-05-16 sibling dropped)
  - Moved-to-parked: 16 (all P3 entries — see `## Parked` section at the bottom)
  - Escalated-to-external-blockers: 0 (`Headless CI runners` + `Install gitleaks/semgrep/flawfinder` evaluated; neither qualified — both are in-repo workflow / setup-script edits, not upstream)
  - After live: 10 P2 entries; parked block holds 16 P3 entries

<!-- Latest first. Append new P0 / P1 / P2 entries at the top. Append new P3 entries to ## Parked. -->

- 2026-05-31 · mcp-toolsmith · [tooling] · P3 — `comment-commented-out-code` classifier flags relocated/re-indented comments whose text starts with `ident()` as commented-out code
  Details: During Slice 3 (`McpPlugin::OnStart` extraction) a pure-mechanical refactor that *moved* an unchanged comment block (lines beginning `OnStop()`, `svr.stop()`) into a helper tripped `comment-commented-out-code` in the delta gate — the classifier's `CODE_LIKE_RE` matches a leading `Identifier(...)` call shape, and a relocated comment line is an "added" line vs the base, so untouched prose reads as new commented-out code. Forces a reword on comments the refactor never authored.
  Concrete next action: either (a) note in the strict-zone refactor guidance that moved comment blocks with line-leading `ident()` tokens should be pre-scanned + reworded, or (b) soften `comment_audit.py`'s `CODE_LIKE_RE` call-shape heuristic to require a trailing `;`/`{`/`}` (prose like "OnStop() is called on teardown" wouldn't match), so a bare `ident()` mid-sentence isn't classified as code. ~20-30 min. Low priority — the reword workaround is cheap.
  Status: applied (2026-06-21 P3 sweep — shipped #1522)
  Last-reviewed: 2026-06-21

- 2026-05-31 · orchestrator · [tooling] · P2 — No automated archive-staleness check; shipped plans rot in `docs/plans/active/` (16 of 25 this audit)
  Details: `docs/agent-rules/process-rules.md` § Plan-doc family already mandates "Archive on revision" (the same PR that fills the post-ship sections must `git mv` the plan to `shipped/` + sed cross-refs). It's prose-only and has now been under-followed twice: the 2026-05-28 sweep found 33 of 48 archive-eligible, and a 2026-05-31 audit again found 16 of 25 active plans had already-shipped code (post-ship sections frequently still on the template stub). Across many parallel ship-loops the manual `git mv` step is the first thing dropped, so `active/` silently accumulates finished work and stops reflecting in-flight state. Root cause is no machine check — `git-janitor` *acts* but doesn't *report* archive-eligibility.
  Concrete next action: add advisory-only `agents/scripts/project/test-plan-staleness.sh`, auto-enrolled by `scripts/dev/test-all.sh` (WARN, never hard-fail): for each `docs/plans/active/<slug>.md`, (1) classify § Implementation log as stub vs populated (real `<sha> · #<PR>` bullets); (2) optionally `gh pr view <N> --json state` on cited PRs — all MERGED ⇒ strong archive signal; (3) print the archive-eligible list with the exact `git mv` + cross-ref-sed one-liners. Stub-log plans (genuine in-flight) are exempt. Hook the same list into `git-janitor`'s end-of-session cleanup so archiving becomes part of the ship-line, not a periodic rescue sweep. ~1.5–2 h (bash + `gh pr list --json` join; reuse the moved-slug sed pattern from this audit's PR). Wins: stops the recurring stale-in-place drift at the source instead of re-running the manual sweep every few weeks.
  Status: applied (2026-06-20 roadmap campaign — shipped #1511)
  Last-reviewed: 2026-06-20

- 2026-05-26 · orchestrator · [tooling] · P2 — Summarize current-head CodeRabbit findings separately from history
  Details: PR #460 had older CodeRabbit review bodies with actionable comment counts, but the current head had CodeRabbit `SUCCESS` and a latest comment saying no actionable comments were generated. Reading raw `gh pr view --json reviews,comments` made the historical comments look unresolved until the current-head check and merge-gates result were correlated manually.
  Concrete next action: add a helper, likely `scripts/dev/coderabbit-current-head.sh <pr>`, that reports current head SHA, latest CodeRabbit check state, latest CodeRabbit review/comment for that head, and "historical comments ignored" when older actionable counts belong to previous commits. Estimated cost 45 min.
  Status: applied (2026-06-20 roadmap campaign — shipped #1504)
  Last-reviewed: 2026-06-20

- 2026-05-16 · build-doctor · [tooling] · P2 — Phase 9 coverage threshold (≥70%) advisory soak → blocking flip
  Details: Phase 9 (`test-phase-9-coverage-gates`) ships `scripts/dev/coverage.sh` + `.github/workflows/coverage.yml` running with `--threshold 0` and `continue-on-error: true` for the first two weeks. Parent plan's § End-state targets calls for ≥70% line coverage on `Source/Core/src/` (excluding ImGui / UI files) as a hard gate. Same advisory→blocking lifecycle as Phase 7's screenshot-diff.
  Concrete next action: after two consecutive green weeks of `coverage.yml` runs, flip (a) `coverage.yml` `continue-on-error: true` → `false`; (b) `coverage.sh` invocation from `--threshold 0` → `--threshold 70`; (c) consider adding `--threshold 90` carve-out for the high-risk units (IssueCreatePipeline, IssueDraft, TrackerFieldValueParser, CallstackParser, LocalCacheManager, TicketSyncService, ConfigManager migrations, MCP dispatch, Lua bindings) per parent plan. Estimated cost 30 min once the soak baseline is collected.
  Status: applied (2026-06-20 trap-sweep — PR #834)
  Last-reviewed: 2026-06-20

- 2026-05-16 · build-doctor · [tooling] · P2 — Headless CI runners need GL context for spawn-mode UI tests (bucket C + bucket E)
  Details: `.github/workflows/build-and-test.yml` runs `windows-2022` GitHub Actions runners. The runners advertise no display, and `glfwInit` + `glfwCreateWindow` against a hidden window returns a context whose `glReadPixels(GL_FRONT, ...)` reads an undefined / empty buffer (zero or driver-noise). Bucket-C screenshot diff therefore can't gate on cloud CI today — the Phase 7 advisory step is `continue-on-error: true`. Same blocker applies to the existing bucket-E `test-ui-views-columns-reorder.sh` (already excluded from the CI step).
  Concrete next action: wire mesa (`opengl32sw.dll` on Windows runners) OR a headless GL context via ANGLE-D3D11. Either lets Standalone + ImGui Test Engine + screenshot capture run in CI. Estimated cost ~3-5 h to install mesa on the runner image + verify a screenshot-diff round-trip; or ~1 day to switch to ANGLE-D3D11 if mesa proves too lossy for the L∞ ≤ 4 tolerance. Until then, bucket-C + bucket-E gates run on dev machines only.
  Status: applied (2026-06-20 trap-sweep — PR #441)
  Last-reviewed: 2026-06-20

- 2026-05-16 · test-author · [tooling] · P2 — Phase 7 pink-clear dock-gap scan (deferred from Phase 7 scenario set)
  Details: AGENTS.md § Debug techniques documents the magenta-clear trick (`glClearColor(1, 0, 1, 1)`) for detecting dock-gap leaks. The Phase 7 `DockGapSentinelScenario` originally planned to flip the clear color during its warm-up frames so any visible pink in the captured PPM = real dock gap. Implementation required a new `UiDrawSession::requestClearColor` flag + a `Source/Standalone/main.cpp` consumer — non-trivial surface for marginal coverage given the L∞ diff against a clean golden already catches dock-shift regressions. `smatchet::test::CountPixels(img, 255, 0, 255, tol)` shipped in `tests/support/GoldenImage.h` to enable the scan once the clear-color toggle lands.
  Concrete next action: add `requestClearColor{R,G,B,A}` fields to `UiDrawSession` + restore-on-clear-after-frame consumer in main.cpp; extend `DockGapSentinelScenario` to set pink-clear during warm-up + bash script to run `CountPixels(img, 255, 0, 255, 8) == 0` as a hard assertion. Estimated cost ~1.5 h.
  Status: open
  Last-reviewed: 2026-05-18

- 2026-05-16 · test-author · [tooling] · P2 — lint-hook deferred-drain verification gaps (4 checks deferred)
  Details: `scripts/dev/test-lint-hook-split.sh` ships 14 assertions across 7 of 11 plan-spec checks. Four deferred for follow-up:
    - Test 4 — issue surfacing with a real cppcheck violation. Requires fault-injection into a real .cpp under `Source/Core/` (write a deliberate `if (x = 1)` and expect drain exit 2). Either author a `tests/fixtures/` first-party-path subtree + carve-out, or run in a real-source mutation harness.
    - Test 5 — chunked drain across > `SMATCHET_LINT_DRAIN_CHUNK` files. Synthesise 11+ distinct .cpp paths into the queue, run drain, assert remainder re-queued.
    - Test 6 — parallel-subagent per-PID isolation. Stage two `.lint-queue.<distinct-pids>` files with overlapping + disjoint paths, run drain, assert both consumed without data loss.
    - Test 10 — lockfile serialises concurrent drains. Spawn two `lint-cpp-drain.sh` invocations in parallel against a shared queue, assert exactly one processes + the other exits 0 without touching state.
  Concrete next action: 1 h for tests 5+6+10 (pure file synthesis); 2 h for test 4 (needs the production-source mutation discipline). Not blocking the deferred-drain ship — pipeline behaviour is exercised by the 7 covered checks plus the live Part 0 spike.
  Status: applied (2026-06-20 roadmap campaign — shipped #1506)
  Last-reviewed: 2026-06-20

- 2026-05-15 · test-author · [tooling] · P2 — perf-measure scenario `blame-open-entry-tab` does not exist; Pillar 1 regression gate uncovered
  Details: Item #4 from `~/.claude/plans/make-the-any-presentation-serene-oasis.md` § Verification — "Pillar 1 gate — `perf-measure` on `blame-open-entry-tab` scenario before/after — mean frame ≤ 6.94 ms" — references a scenario name that is not registered with `ScenarioRunner` (only `priority-grid-scroll` + `lua-recorder-fuzz` + `ui-test` exist today; see `Source/Core/src/Commands/Scenarios/`).
  Concrete next action: author `blame-open-entry-tab`: (a) a fake-callstack injection API on `AppController` so the scenario can prime `BlameAnalysisUi::State().callstackBuf` without going through the live Jira fetch path, (b) a scenario class (~100 LoC modelled on `PriorityGridScrollScenario.cpp`) that opens Blame UI → runs `blame.process` → switches to Entry tab → ticks N frames so `UiPerfMonitor` accumulates `DrawColoredCppLine` samples, (c) `OnCancel` cleanup that unwinds the injection. Estimated cost 3 h (1 h injection API, 1.5 h scenario, 0.5 h doc + scripts/dev/test-blame-perf.sh runner). Until then, the tokenizer hot-path lacks a regression gate.
  Status: partially applied (2026-06-20 trap-sweep — shipped: a registered scenario SHELL only (AnnotateOpenEntryTabScenario, PR #1328) which is a STUB; remaining: the fake-callstack injection API + the tab-switch core + a perf runner — the tokenizer hot-path is NOT exercised)
  Last-reviewed: 2026-06-20

- 2026-06-08 · orchestrator · [tooling] · P2 — session-registry liveness fix shipped (ppid=1 → authoritative pid)
  Details: the per-tree session registry (`<tree>/.claude/.active-sessions/<id>`) recorded `ppid=1` on every entry — MSYS/git-bash `$PPID` sentinel on Windows. `kill -0 1` always fails there, so the PID-liveness arm of the concurrent-session guards was dead: liveness collapsed onto the 30-min ts-freshness arm alone, over-blocking legitimate HEAD-moving git ops for up to 30 min after a sibling closes, and letting the registry accrete dead entries nothing prunes. Fixed by capturing the real parent pid in the SessionStart writer (`session-tree-banner.sh`), an authoritative-pid (`numeric AND >4`) Win32 liveness probe (`tasklist`) in the shared lib (`session-registry-lib.sh`), and a SessionStart prune that removes an entry ONLY when BOTH dead-pid AND ts-stale. Shipped PR #996 with 18 bats scenarios + a 20-assertion `--selftest`.
  Concrete next action: two follow-ups — (1) wire a git-janitor registry sweep that calls `sr_prune_dead_stale` over every worktree's `.active-sessions/` (today prune only fires on the writing tree's SessionStart); (2) when the planned `guard.mjs` Node rewrite lands (`docs/plans/active/session-guard-agnostic.md`), port-forward the authoritative-pid liveness + dead+stale prune, reusing the bats scenarios as acceptance.
  Status: applied (2026-06-21 final autonomous batch — shipped #1512)
  Last-reviewed: 2026-06-21

## Parked

> P3 entries with no immediate owner. Reassess when adjacent feature lands or when a P2 promotion is justified.

<!-- Latest first within Parked. -->

- 2026-06-08 · orchestrator · [tooling] · P3 — `coverage-gate-multiline-log-join`: `coverage-delta-gate.sh` classifier judges multi-line `LOG_*` continuation lines as real runtime surface (preventing gate from postmortems.md 2026-06-08 PR #991)
  Details: `_line_is_no_runtime_surface()` exempts a line that *starts* `LOG_ERROR(`/`LOG_WARN(`/… but its continuation patterns (lines 112-113) only match a lone closer (`");`|`);`) or a pure string-literal fragment. A `LOG_*` call wrapped across lines by `.clang-format` ColumnLimit 120 leaves an identifier-bearing tail — e.g. `r.error.message.c_str());` or `redactedBody.c_str());` — which carries no real new surface (it is the tail of one log statement) yet is classified as FALLTHROUGH → the whole diff trips the Test-delta gate. On #991 this was masked because commit `644b32be` added a real test, but in the original logging-only intent it would have forced a genuine `tests-out-of-band` override for a purely-untestable LOG_ line. The classifier is per-line and never reassembles a continued statement.
  Concrete next action: in `_classify_diff`, when an added line *opens* a `LOG_*(` call whose parens are unbalanced, accumulate following added lines until the parens balance and the statement terminates (`);`), then classify the joined statement as a single logging-only unit (exempt) — instead of judging each physical line alone. Keep it conservative: only join when the opener is a `LOG_*(` (not arbitrary calls), and a non-LOG token appearing mid-join (a `;` that isn't the call's terminator, an `=`, a new statement) breaks the join back to strict per-line. Add `--selftest` fixtures: a 2-line and 3-line wrapped `LOG_ERROR` with `.c_str()` tails must classify EXEMPT; a `LOG_INFO(...)` followed by a real `for(...)` must still FALLTHROUGH. Est ~1-2 h. Cross-ref: PR #991, `OllamaClient.cpp` transport block; `.clang-format` ColumnLimit 120.
  Status: applied (2026-06-21 P3 sweep — shipped #1522)
  Last-reviewed: 2026-06-21

- 2026-06-07 · debug-detective · [tooling] · P3 — no cdb/WinDbg locally — minidump triage needed a hand-rolled python minidump scan
  Details: This session's crash-dump triage had no native debugger on the box (no cdb/WinDbg/kd) — the debug-detective had to hand-roll a python minidump-stream parser to extract the exception record + module list, which is slow, lossy (no symbolized stacks), and re-derived per incident. The `SmatchetCrashHandler` dump path (`Source/Standalone/SmatchetCrashHandler.cpp`) makes dumps a recurring triage surface, so the tooling gap recurs.
  Concrete next action: either `winget install Microsoft.WinDbg` once on the dev box (and note the canonical `cdb -z <dump> -c "!analyze -v; q"` invocation in `docs/agent-rules/debug-techniques.md`), or commit a dump-triage script in `agents/scripts/` wrapping the python minidump scan so the next incident doesn't re-derive it. Est ~30-60 min.
  Status: applied (2026-06-21 P3 sweep — shipped #1530)
  Last-reviewed: 2026-06-21

- 2026-06-07 · 1b agent · [tooling] · P3 — no `testPresets` in `CMakePresets.json` — `ctest --preset ninja-test-msvc` doesn't exist, every agent rediscovers it
  Details: `CMakePresets.json` defines configure/build presets only (verified: zero `testPresets` entries), so the natural `ctest --preset ninja-test-msvc` invocation fails and each agent independently rediscovers the working form (`ctest --test-dir build/ninja-test-msvc` or the bash drivers). Recurring small token/time tax across agents.
  Concrete next action: add a `testPresets` section to `CMakePresets.json` (one per test-bearing configure preset), OR a one-line note in `docs/agent-rules/build.md` naming the canonical ctest invocation. Est ~20 min.
  Status: applied (2026-06-21 P3 sweep — shipped #1523)
  Last-reviewed: 2026-06-21

- 2026-06-07 · orchestrator · [tooling] · P3 — orphaned perf baselines with no scenario emitter (`agent-handoff-roundtrip`, `agent-triage-roundtrip`)
  Details: `docs/perf/baselines/agent-handoff-roundtrip.dev.json` + `agent-triage-roundtrip.dev.json` reference scenarios with no registered IScenario (the agentic-flow C++ runtime they measured was deleted 2026-05-21, see applied.md banner) — they will never gain `p99Ms` and never be compared again. (CR sweep CR-949-4; pre-existing, not introduced by #949.)
  Concrete next action: delete the two orphans in a baseline-janitor sweep (or fold into the next perf-baseline bump PR); optionally teach `perf-baseline.sh` to flag baselines whose scenarioId is absent from `scenario.list`. Est ~15 min.
  Status: applied (2026-06-21 P3 sweep — shipped #1523)
  Last-reviewed: 2026-06-21

- 2026-06-04 · offline-sync · [tooling] · P3 — no bucket-E ImGui-Test coverage for the offline-conflict modal's scalar / unverified panes (ADR-0016)
  Details: `feat(offline): ask the user on offline-replay conflict for all field edits` added two new conflict-modal branches in `DrawOfflineConflictModal` (`SmatchetOfflineQueueUi.cpp`) — `DrawConflictPaneScalar` (Use Mine / Use Theirs / editable value) and `DrawConflictPaneUnverified` (Force Mine / Discard). The kind-dispatch + resolve plumbing is covered by bucket-A runtime tests (`OfflineQueueServiceRuntime.test.cpp`: scalar suspends, permanent/transient fetch-fail → unverified, discard hard-deletes + audits, resolve clears both bases) and the pure classifier (`OfflineFieldConflictPolicy.test.cpp`). What's NOT automated is the widget-level assertion that each pane renders the right buttons and each button routes to the correct `ResolveFieldEditConflict(kind=...)` / `DeletePendingFieldEdits` call — the plan's bucket-E item. Deferred this round: no existing ImGui-Test-Engine harness drives `DrawOfflineConflictModal`, so wiring one is net-new scaffolding beyond this feature's scope.
  Concrete next action: add an ImGui Test Engine case that seeds `d.conflictContextJson` with a `kind:"scalar"` and a `kind:"unverified"` context, opens `ResolveMergeConflict`, and asserts (a) the expected buttons exist per kind and (b) clicking each invokes the matching app call (spy via a fake AppController seam or a call recorder). ~2-3h once an offline-queue ImGui-Test fixture exists.
  Status: applied (2026-06-21 P3 sweep — shipped #1531)
  Last-reviewed: 2026-06-21

- 2026-05-30 · orchestrator · [tooling] · P3 — `git-janitor` is end-of-session-only; parallel-agent merged-branch residue piles up mid-session
  Details: With multiple agents + the merge-watcher daemon active, merged-PR local branches accumulate fast — a mid-session sweep found ~11 local residue branches (PRs all MERGED: #553/#554/#557/#560/#569/#576/#540/#541) + 2 worktrees holding now-merged branches. `git-janitor` deletes merged branches + cleans worktrees but only "after the last PR of a work session lands and the user signals no more changes"; nothing prunes between. Related-but-distinct from the 2026-05-18 `worktree-prune.sh` entry (that ff-pulls + deletes `[gone]` branches per worktree; this is a PR-state-keyed local-branch prune on a periodic cadence).
  Concrete next action: add a bounded periodic light-prune (reuse the `merge-watcher-install-prune-task` cron, or a `git-janitor --light` flag): delete only local-only branches whose head-PR is MERGED; never touch open-PR / worktree-held / no-PR-ahead branches; classification supplied by `git-leftover-audit.sh` (paired P2). ~1 h once the audit script exists.
  Partially addressed: 2026-06-02 — PR #743 (`git-janitor` v5 § Stale-branch sweep) ships the exact safe-delete classification this entry wants (MERGED local+remote deleted; OPEN / NO-PR / worktree-held protected), so the *what-to-delete* decision is solved. Residual: this entry's distinct ask is the **mid-session periodic cadence** (a cron / `--light` flag), whereas #743's sweep is **end-of-session within `git-janitor`**. Re-scope to: "classification shipped #743; periodic-trigger wiring (cron / `--light`) still open."
  Status: partially applied (2026-06-20 trap-sweep — shipped: classification (PR #743); remaining: the mid-session local-branch light-prune (--light flag) — the watcher prune-task only unregisters merged PRs, doesn't prune local branches)
  Last-reviewed: 2026-06-20

- 2026-05-30 · orchestrator · [tooling] · P3 — No sweep for stale `origin/*` branches with no PR (pushed-then-abandoned)
  Details: This session's sweep found 5 `origin/*` branches pushed but with no PR and behind develop — `fix/cr-gate-empty-body-review`, `fix/setup-harness-agent-junction`, `gate-enforcement-cr-gate`, `plan/agentic-layer-project-independence`, and `bug-report-assets` (an epoch-stamped asset branch). Durable (on origin) so not "lost", but orphaned — invisible unless you `git branch -r`, easy to forget, and they re-trigger GitHub's "Compare & pull request" banner. `git-janitor` only deletes *merged* branches; nothing flags pushed-no-PR remotes.
  Concrete next action: extend `git-leftover-audit.sh` (or `git-janitor`) to flag `origin/*` with no associated PR + behind `origin/develop` + last-commit age > N days. Report only — never auto-delete (could be a sibling's in-flight WIP). ~30 min on top of the audit script.
  Partially addressed: 2026-06-02 — PR #743 (`git-janitor` v5 § Stale-branch sweep) enumerates remote (`git branch -r`) branches and classifies `NO-PR` ones as **keep-or-ask** (never auto-delete), which covers this entry's core "flag pushed-no-PR remotes, never auto-delete" intent. It also added the protected-branch guard so an infra branch like `bug-report-assets` (this entry's own example) is no longer mis-flagged. Residual: the explicit **`last-commit age > N days`** staleness heuristic + the report-only summary line are not yet encoded (the sweep asks the user per NO-PR branch instead of age-ranking them). Largely covered; downgrade.
  Status: partially applied (2026-06-20 trap-sweep — shipped: NO-PR keep-or-ask + protected guard (PR #743, git-leftover-audit.sh:40); remaining: the last-commit-age>N-days heuristic)
  Last-reviewed: 2026-06-20

- 2026-05-30 · orchestrator · [tooling] · P3 — No edit-time gate for portable-purity → NEW-literal leaks land in a PR and only fail at CI
  Details: `scripts/dev/test-portable-purity.sh` (blocks NEW project literals in portable dirs `agents/core`, `agents/_shared`, `docs/agent-rules`, `docs/harness`) IS discovered by `scripts/dev/test-all.sh:46` (globs every `test-*.sh`) and runs in the `Doc anchors + agent contract` CI job — so the coverage exists. The gap is **when it fires**: there is no PostToolUse lint hook that runs it at edit time, and an agent editing a portable doc does not reliably run the full `test-all.sh` before pushing (the orchestrator pushed PR #557 without it). Result: a leak (`SMATCHET_*` env names + `Source/Core` example in `docs/agent-rules/memory-drain.md`) only surfaced at CR/CI → fix → re-push, a full round it would have caught in 5 sec at edit time. Distinct from the existing infra entry about *shrinking* the baseline.
  Concrete next action: add a PostToolUse hook that runs `test-portable-purity.sh` when an edited path is under a portable dir (`agents/core/`, `agents/_shared/`, `docs/agent-rules/`, `docs/harness/`) — same trigger shape as the C++ lint hook. (`test-all.sh` already covers it for full-suite runs; the hook closes the edit-time fast-feedback gap, and is robust to the self-mod-guard case where the orchestrator can't run it inline.) ~20 min. Watch: the `split-scripts-build-vs-agentic` plan moves `test-portable-purity.sh` to `agents/scripts/core/`, so `test-all.sh`'s `scripts/dev/test-*.sh` glob must be widened to keep discovering it.
  Status: applied (2026-06-21 P3 sweep — shipped #1530)
  Last-reviewed: 2026-06-21

- 2026-05-30 · memory-triage · [tooling] · P3 — Document the two Windows/git-bash seams when stubbing `gh` in bats suites
  Details: bats suites that PATH-stub `gh` for a native-Windows-Python consumer (e.g. `merge-watcher.py`, which pins `GH_BIN = shutil.which("gh")` at import) have two silent-failure seams; both already bit `merge_watcher_integration.bats` (0/4 on Windows until gated). (1) Native CreateProcess can't exec the driveless POSIX `gh` script git-bash prepends, so `shutil.which` skips it and returns the real `gh.EXE` — fix is a `gh.cmd` forwarder (`@echo off` + `bash "%~dp0gh" %*`) next to the bash stub so PATHEXT resolution picks the stub dir first. (2) `LOCALAPPDATA="$(mktemp -d)"` is a `/c/...` path native Python `open()`/`json.load` mis-resolves — convert with `cygpath -m` to mixed `C:/...` form (keep raw POSIX for teardown `rm`). Also: route gh `api -X PUT` fixtures on the REST path token (`*"/merge"*` vs `*"/update-branch"*`), not on `"$1 $2"`. These suites run under no CI gate (windows-2022 runner has no bats) so they rot silently.
  Concrete next action: capture this as a comment block in a shared bats helper (or `docs/agent-rules/` test note) so the next bats author doesn't re-derive it; optionally add a windows-bats CI lane. ~30 min for the note. Source: memory `project-bats-gh-stub-windows` (drained 2026-05-30).
  Status: applied (2026-06-21 P3 sweep — shipped #1524)
  Last-reviewed: 2026-06-21

- 2026-05-18 · orchestrator · [tooling] · P3 — `git worktree remove --force` leaves the directory on disk on Windows
  Details: End-of-session cleanup removed 35 agent worktrees via `git worktree unlock` + `git worktree remove --force` against each path. Git's metadata was correctly cleared (subsequent `git worktree list` showed only the main checkout), but every directory under `.claude/worktrees/agent-*` persisted on disk — 38 stale dirs requiring a manual `rm -rf agent-*` sweep. Likely cause: open file handles (lint hook caches, MSYS2 stat handles, antivirus scanning) prevent `RemoveDirectoryW` even with `--force`; git unregisters the worktree but logs no error when on-disk removal fails. Net effect: `worktree list` looks clean while disk usage remains, which is easy to miss for weeks.
  Concrete next action: wrap the `worktree remove` step in `agents/core/git-janitor.md`'s end-of-session checklist with a follow-up `for d in .claude/worktrees/agent-*; do [ -d "$d" ] && rm -rf "$d"; done` sweep — and emit a clear log line per directory deleted so the post-cleanup output reflects on-disk reality, not just git metadata. ~10 min doc + tiny script tweak.
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-17 · orchestrator · [tooling] · P3 — Lazy-load AI clients to drop spawn-ready timeout (architectural follow-up)
  Details: The cheap fix from the original P1 entry shipped — `--spawn` ready-timeout bumped 15s→30s with `SMATCHET_SPAWN_READY_MS` env override (`Source/Standalone/CliCommandRunner.cpp:670`). Bucket-E gates unblock. Architectural follow-up remains: profile AI-client init paths (`OpenAiClient`, `AnthropicClient`, `OllamaClient`, `AiNdjsonParser`, Lua glue) and lazy-load so MCP server publishes ready in <15s again, then drop the bump.
  Concrete next action: instrument `AppController` ctor + `IAiClient` subclass init with `SMATCHET_UI_PERF_SCOPE` markers; identify which init paths can defer past MCP-ready; refactor. Once mean spawn-ready is <10s on dev machines, revert the timeout to 15s. ~3-4 h.
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-17 · test-author · [tooling] · P3 — Preferences "Test connection" async button deferred from PR #174
  Details: PR #174 (`ai-debug-cli-and-prefs-validation`) planned a "Test connection" button in `SmatchetPreferencesUi.cpp` Assistant tab that would call `IAiClient::ProbeReachability` on a worker thread + post the result back via `MainThreadDispatcher`. Agent deferred at implementation time because the existing Preferences tab uses **per-field autosave** (no single Save button), so the async-result-display pattern would have fought the existing flow. Workaround for user: run `bash scripts/dev/manual-ai-anthropic-probe.sh` or `Smatchet.exe cmd ai.probe --provider anthropic` directly. Cost-to-add: ~30 min if folded into the broader Preferences UI refactor that gives the Assistant tab its own Save button (would also unblock other staged-validation UX). Independent worth alone: lower; CLI command + bash script already provide a clean equivalent.
  Concrete next action: either (a) add a self-contained Assistant-tab Save button + the async test button, or (b) leave the CLI path as the canonical reachability test and remove the button from any future plan docs. Decide at the next AI-feature-touching PR.
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-17 · security-review · [tooling] · P3 — Install gitleaks + semgrep + flawfinder in MSYS2 dev image (security-review fallback is grep)
  Details: Current `security-review` agent attempts gitleaks / semgrep / flawfinder when present, falls back to grep heuristics + cppcheck security warnings otherwise. On the MSYS2 UCRT64 runner none of the three are installed, so cross-language secret scans + AST-aware vuln patterns silently degrade to text-search.
  Concrete next action: add a `scripts/dev/install-security-tools.sh` (mirror of `doctor.sh` shape) that pacman-installs `gitleaks` (or `go install` if not packaged), `pipx install semgrep`, `pacman -S mingw-w64-ucrt-x86_64-flawfinder`. Document in `docs/harness/SETUP.md`. ~1 h.
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-17 · code-review · [tooling] · P3 — `MainThreadDispatcher::PostUiTask` sugar for typed worker→UI hand-off
  Details: `MainThreadDispatcher::PostToMainThread(Task)` takes `std::function<void()>` per `Source/Core/include/MainThreadDispatcher.h:33`. Phase B (PR #163) had to use the pattern "outer lambda captures AppController*, inner lambda references `g_ui` via TU-local `extern`" to reach UI state from a worker callback (`AiAssistantController.cpp` delta + error paths). The shape works but the discoverability is poor — Phase B agent's packet sketched the wrong signature (`function<void(AppController&)>`) on a guess. A typed sugar layer like `PostUiTask([](UiDrawSession& d){ ... })` (or two-arg `(AppController& app, UiDrawSession& d)`) would (a) make worker→UI hand-off self-documenting + (b) centralise the `g_ui` extern shim that AI/MCP/sync currently each replicate.
  Concrete next action: add `MainThreadDispatcher::PostUiTask(std::function<void(UiDrawSession&)>)` as a thin wrapper that resolves `g_ui` once at the dispatch boundary; deprecate raw `PostToMainThread` for worker callbacks. ~1 h including in-tree replacements of the 3 known worker→UI sites (sync, audit, AI).
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-17 · code-review · [tooling] · P3 — `Source/Core/include/IAiClient.h:14` `virtual ~IAiClient() {}` should be `= default`
  Details: Defaulted destructor preferred for trivial-destruct interfaces; rule-of-three compliance.
  Concrete next action: `virtual ~IAiClient() = default;` + add rule-of-three (copy/move ctor + assign defaults). Surfaced by retrospective code-review sweep on PR #140.
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-17 · code-review · [tooling] · P3 — `Source/Core/src/AiClientFactory.cpp:34,47` fallthrough returns after switch without `default:` will warn `-Wswitch` if `AiProvider` enum grows
  Details: Future-proof against an enum extension going unhandled.
  Concrete next action: add `default:` arm returning a null-client or assertion. Surfaced by retrospective code-review sweep on PR #140.
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-17 · code-review · [tooling] · P3 — `Source/Core/src/OpenAiClient.cpp:18-22` `JoinUrl` does not handle `base` ending `//` or non-leading-slash `path`
  Details: All call sites safe today; defensive note in case of future refactor.
  Concrete next action: comment or `CHECK` invariants at the function head. Surfaced by retrospective code-review sweep on PR #140.
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-17 · code-review · [tooling] · P3 — `tests/support/ScreenshotDiffMain.cpp:32` three-positional CLI with no `--help`
  Details: Discovery friction for a one-off contributor; positional args undocumented.
  Concrete next action: add a `--help` arm printing the three positional names + an example invocation. Surfaced by retrospective code-review sweep on PR #146.
  Status: applied (2026-06-20 trap-sweep — PR #165)
  Last-reviewed: 2026-06-20

- 2026-05-17 · code-review · [tooling] · P3 — `CMakePresets.json:154` `RelWithDebInfo` + `--coverage` may strip `gcov` notes via `-fdata-sections`
  Details: Coverage instrumentation can interact with dead-section stripping.
  Concrete next action: verify `*.gcno` existence with an acceptance test in `scripts/dev/test-coverage-gcno.sh`. Surfaced by retrospective code-review sweep on PR #148.
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-16 · test-author · [tooling] · P3 — Phase 7 mutation-sanity demo (bootstrap → mutate → revert)
  Details: Per AGENTS.md plan-revision contract + plan-locks packet, each high-risk verification should ship a mutation-sanity recipe: introduce a deliberate one-pixel offset in the production path, observe the gate fail, then revert before commit. Phase 7's two new scenarios + bash gate need the **first golden capture on the user's machine** before mutation-sanity can be meaningful — a freshly-bootstrapped golden is byte-equal to its own capture, so the mutation has to follow the bootstrap. `scripts/dev/test-screenshot-diff.sh` documents the recipe inline in its header.
  Concrete next action: dedicated demo session: (1) `bash scripts/dev/test-screenshot-diff.sh --bootstrap` to capture clean goldens, (2) nudge ImGui dock-spacing or palette padding by 1px in `SmatchetUI.cpp` / `SmatchetTheme.cpp`, (3) rerun the gate, observe diff helper reporting `L∞ > 4`, (4) revert before commit. Estimated cost ~20 min when adjacent to the next dev session that touches dock layout.
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-16 · orchestrator · [tooling] · P3 — `gh pr merge --delete-branch` fails when local worktree owns the branch
  Details: After auto-merge of Wave A2 PRs, `gh pr merge 119 --squash --delete-branch` and siblings emitted `failed to delete local branch <branch>: failed to run git: error: cannot delete branch '<branch>' used by worktree at 'C:/Dev/Smatchet/.claude/worktrees/agent-<id>'`. The merge **does** succeed remotely; only the local-branch deletion silently fails. Subsequent `gh pr merge` calls on later PRs sometimes also fail because the local clone still thinks the branch is alive.
  Concrete next action: document the right order in AGENTS.md § Project rules (alongside the existing § Destructive git ops in shared worktrees sub-section) + `agents/core/git-janitor.md`: worktree-remove first, then merge, then branch-delete. Estimated cost 15 min doc edit.
  Partially addressed: 2026-06-02 — PR #743 documented the **worktree-remove-before-`branch -D`** ordering in `agents/core/git-janitor.md` § Stale-branch sweep (the stale-cleanup context). Residual: the specific **`gh pr merge --delete-branch` collision** (merge succeeds remotely but local-branch delete fails when a worktree owns the branch) is not explicitly called out in the merge flow, and the AGENTS.md § Project rules cross-note isn't added. Ordering principle is now in the agent; the merge-time-specific note remains.
  Status: parked
  Trap-sweep note (2026-06-20): adjacent PR #743 added stale-branch-sweep ordering to git-janitor.md; this entry's own deliverable (the gh-pr-merge --delete-branch collision call-out + AGENTS.md cross-note) remains absent.
  Last-reviewed: 2026-06-20

- 2026-05-15 · test-author · [tooling] · P3 — `MarkdownPreviewLangTag` covered (bucket A); rendered-output coverage deferred
  Details: Added `tests/Core/MarkdownPreviewLangTag.test.cpp` (5 cases / 40 assertions) over the inlined `MarkdownPreviewRender::IsCppLikeLangTag` classifier — covers the C/C++ canonical spellings, case-insensitivity, non-cpp languages (python/js/rust/…), substring rejection (cppreference/ccache/cxxabi must not match), and whitespace-only / empty tags. This proves the decision predicate; what still needs automation is "given a markdown document containing a ` ```cpp ` fence, the leave-block handler actually iterates `codeBuffer` line-by-line through `DrawColoredCppLine`."
  Concrete next action: Bucket B — scenario `markdown-preview-fence-render` that builds a `MarkdownPreviewRender::Render(fixtureMd)` against a fixed input + screenshot-diff the rendered child region (gated on bucket-C harness). Bucket C — pixel-class count assertion against known keyword RGB. Bucket E — ImGui Test Engine fixture that opens the long-text editor modal with a fixed markdown source, asserts the colorized child renders ≥ N pixels of the active theme's keyword color. Estimated cost 30 min once bucket B/C harness from the parent theme entry lands.
  Partially addressed: 2026-06-01 — PR #651 (`99318756`) added the `code-syntax-coloring` bucket-C scenario, which renders fenced code blocks through `DrawColoredCodeBlock` (the polymorphic successor to `DrawColoredCppLine`) and golden-diffs the result — exercising the markdown fenced-block render path this entry asked to cover, across six languages. Residual: a dedicated `markdown-preview-fence-render` scenario over a real `MarkdownPreviewRender::Render(fixtureMd)` fixture (vs the scenario's own window) + the bucket-E long-text-editor-modal fixture remain unwritten.
  Status: parked (residual: dedicated fence-render fixture + bucket-E editor-modal)
  Trap-sweep note (2026-06-20): adjacent PR #651 shipped a code-syntax-coloring bucket-C scenario + golden; this entry's own deliverable (a markdown-preview-fence-render scenario + bucket-E editor-modal fixture) remains absent.
  Last-reviewed: 2026-06-20

- 2026-05-15 · test-author · [tooling] · P3 — Blame UI raw-callstack `showRaw=true` colored-display verification fully deferred
  Details: Item #3 from `~/.claude/plans/make-the-any-presentation-serene-oasis.md` § Verification — "raw callstack panel with `showRaw=true` is read-only colored; `showRaw=false` still editable" — has no testable seam at the pure-logic layer. The branch is a 2-line `if (State().showRaw)` at `Source/Core/src/BlameAnalysisUi_Window.cpp:316`; one arm calls `DrawColoredCppText(callstackBuf)` inside `BeginChild`, the other calls `InputTextMultiline(... 0)` for editable input. No algorithmic decision to unit-test. Bucket E (ImGui Test Engine) is the right home — open Blame UI with a pre-populated `callstackBuf`, drive the showRaw toggle, snapshot the panel, assert (a) `showRaw=true` panel has zero `InputText`-cursor item by walking the ImGui ID stack, (b) `showRaw=true` paints ≥ N keyword-color pixels in the panel rect, (c) `showRaw=false` panel has an `InputText` item with `ReadOnly=false`. Blocker today: no Blame UI fixture in `tests/ui/`.
  Concrete next action: 2 h (fixture + 3 cases). Filing so the gap accumulates evidence; bucket-A path proven by the parent theme test catches the underlying tokenizer + theme switch, so the residue is only the branch-routing layer.
  Status: parked
  Last-reviewed: 2026-05-18

- 2026-05-15 · test-author · [tooling] · P3 — bucket-B/C for theme syntax-highlight verification deferred — no `theme.*` CLI command + no golden-image screenshot diff
  Details: Manual step "cycle theme via Settings menu and eyeball that keyword/string/comment/number colors change in the Blame Entry tab" is partially covered by `tests/Core/SmatchetThemeSyntaxColors.test.cpp` (bucket A — 7 cases / 28 assertions over the file-static round-trip per theme + pairwise cross-theme keyword inequality). Pixel-level "DrawColoredCppLine actually paints those colors on screen" is not yet automated.
  Concrete next action: Bucket B requires a `theme.apply <ThemeId>` command in `Source/Core/src/Commands/BuiltinCommands.cpp` (~30 LoC: enum-arg parser + dispatch to `SmatchetTheme::ApplyStyle`) plus a `theme-cycle-blame` scenario that runs `theme.apply` × 5 with `blame.open` + `debug.window.screenshot` in between; pass condition "scenario exits 0 across all 5 themes, no warnings". Bucket C extends B with a pixel-class count assertion ("≥ N pixels match the theme's known keyword RGB in the Blame Entry region"). Estimated cost 1 h for bucket B, +1 h for bucket C with golden PPMs.
  Partially addressed: 2026-06-01 — PR #651 (`99318756`) landed the golden-image screenshot-diff capability + a `code-syntax-coloring` bucket-C scenario (six languages via `DrawColoredCodeBlock`, golden `tests/golden/code-syntax-coloring.png`). That closes the "no golden-image screenshot diff" half of this entry. The "no `theme.*` CLI command" half + the specific `theme-cycle-blame` pixel-class assertion in the Blame/Annotate Entry tab are NOT done (#651 covers the markdown code-block path, not the theme-cycle). Residual: ~2 h (theme.apply command + theme-cycle scenario + pixel assertion).
  Status: parked (residual: theme.apply CLI + theme-cycle-blame pixel assertion)
  Trap-sweep note (2026-06-20): adjacent PR #651 shipped the golden-image diff + code-syntax-coloring scenario; this entry's own deliverable (a theme.apply command + theme-cycle-blame scenario) remains absent.
  Last-reviewed: 2026-06-20
- 2026-05-22 · orchestrator · [tooling] · P3 — `lock-claim-update-p4.sh` not implemented; p4-counter backend forces release+re-claim for write-set growth
  Details: `agents/scripts/core/lock-claim-update.sh` (git-ref backend) supports in-place lock update via `--force-with-lease` push. The Perforce-counter sibling at `agents/scripts/core/lock-claim-p4.sh` ships claim + release only — no update path. Today `SMATCHET_LOCK_BACKEND=p4-counter bash agents/scripts/core/lock-claim-update.sh ...` exits 2 with a diagnostic pointing the caller at release-then-re-claim. Loses the atomic "grow this lock without releasing it" guarantee that the git-ref path has.
  Concrete next action: author `agents/scripts/core/lock-claim-update-p4.sh` modelled on `lock-claim-p4.sh`. Read existing `<lock_prefix><slug>_meta` counter, rebuild claim JSON with updated write-set + bumped `updated`, write back via `p4 counter --from=<old-json> --to=<new-json>` (CAS on the meta counter itself preserves atomicity). Then flip `agents/scripts/core/lock-claim-update.sh` p4-counter dispatch from `exit 2` to `exec` of the new script. Estimated cost ~1 h. Surfaces only when a p4-backend session needs in-place lock growth — until then the release+re-claim workaround is fine.
  Status: parked
  Last-reviewed: 2026-05-22

- 2026-05-31 · orchestrator · [tooling] · P2 — `SmatchetMergeWatcher` daemon can notify the *human* (desktop toast) but has no channel to notify/wake the *agent* of PR state / CodeRabbit feedback; the orchestrator must hand-poll
  Details: The watcher daemon (`agents/scripts/core/merge-watcher.py`, 60 s poll) detects CI / CodeRabbit / review-comment state changes and, on terminal states, fires `maybe_notify` → `smatchet-notify.sh` → a Smatchet in-app toast + Windows BurntToast fallback — i.e. it pings the **human**. There is no path that surfaces a state change into the agent/orchestrator session. So when a PR the orchestrator just opened gets CR actionable findings, the agent learns only by (a) the user relaying it, or (b) polling `gh pr checks` / the review threads itself. Hit repeatedly while shipping the split-scripts PRs (#609/#610/#615) — each needed a manual `gh pr checks --watch` + a `gh api graphql` thread fetch. Compounding: a freshly-opened PR is **not** auto-registered with the watcher (`merge-watcher-cli.py list` is empty for it), so the daemon's own `coderabbit-triage` doesn't even run unless registered — and that triage posts an advisory comment, it doesn't notify the agent or apply fixes. Net: the agent has *zero* passive signal and silently falls back to ad-hoc polling, which is easy to forget and burns turns.
  Concrete next action: pick one (likely 2 + 3) — (1) **agent-reachable notify sink**: extend `maybe_notify` (or add a state-transition hook) to also write a structured event the harness can surface to the session — e.g. append to `.claude/.agent-events.jsonl` that a SessionStart/poll reads, or drive a `RemoteTrigger`-style wake. (2) **`merge-watcher-cli.py await <pr> [--until=blocking|terminal]`** — a thin blocking command the orchestrator runs right after opening a PR; returns on the next blocking/terminal state with a structured summary (CR findings, failing checks). No daemon change; directly replaces the ad-hoc `gh pr checks --watch` + thread-fetch dance. (3) **encode the reality in the orchestrator prompt / AGENTS.md ship-loop**: "the watcher pings the human, not the agent — after opening a PR you MUST either `await` it (option 2) or poll; never assume passive notification." ~2-3 h for (2); (3) is a doc edit.
  Status: applied (2026-06-20 roadmap campaign — shipped #1505)
  Last-reviewed: 2026-06-20

- 2026-05-31 · orchestrator · [tooling] · P2 — subagent eval harness MVP is advisory; the live `code-review` smoke + judge-vs-human calibration are manual residue with no automation yet
  Details: The Phase-1 subagent eval harness (`scripts/dev/agent-eval-{run.sh,score.py}`, `docs/agent-eval/*`, plan `docs/plans/shipped/subagent-eval-harness.md`) proves the scoring contract against fixtures — the bats suites (`tests/bats/agent_eval_score.bats`, `agent_eval_run.bats`) run fully deterministic with a fake judge + `--fake-runner`, no live tokens. Two things remain inherently manual: (a) one live `code-review` end-to-end smoke off-CI (real `claude -p` adapter against a curated case, real judge) to confirm the live path produces a conformant result; (b) judge-vs-human calibration — the corpus of agreement data needed before the advisory WARN can graduate to a blocking gate. Until (b) exists, a quality regression only WARNs (malformed artifacts still FAIL).
  Concrete next action: (1) run the live smoke once and record the result JSON + any adapter-parse gaps (free-text→findings parsing in `agent-eval-run.sh` is best-effort regex today). (2) Stand up a calibration loop: collect N judge scores vs human labels on the same runs, measure agreement, then set BLOCK thresholds. (3) The trace flywheel — now Phase 2 of the unified `docs/plans/subagent-eval-agentic-coverage.md` plan (the former standalone `subagent-eval-flywheel.md` is superseded) — is the prerequisite for replacing the seed cases with harvested real traces; that unified plan's Phase 0 is the judge-calibration loop this entry's item (2) asks for. Smoke ~30 min; calibration is the unified plan.
  Status: applied (2026-06-20 roadmap campaign — shipped #1507)
  Last-reviewed: 2026-06-20

- 2026-05-31 · orchestrator · [tooling] · P2 — memory-budget-and-lifetime-hardening S1–S5 has 3 dev-machine-only verification residues (sanitizer live-swap repro, bucket-E thumbnail cue, S4 bounded-read unit test)
  Details: The memory-hardening PR (S1–S5, plan `docs/plans/shipped/memory-budget-and-lifetime-hardening.md`) shipped with dual-target build + a live `perf.memory` smoke + the full `no-detach` lint/bats green. Three verification items can't run on a headless CI runner and were not automated: (a) **bucket-D ASan/TSan live-swap repro for site #6** — kick the project-picker `ListProjects()` fetch, fire a live `SetBackend` Jira→Plane, assert no UAF/data-race against the freed backend (validates ADR 0012's shared ownership); the sanitizer preset + a GL spawn are dev-machine-only. (b) **bucket-E "loading thumbnails" cue** — the S5 cue is a passive `TextDisabled` driven off `pendingThumbnailUploads`; asserting it needs the ImGui Test Engine harness + an artificially-slow decode. (c) **bucket-A S4 bounded-read unit test** — `ParseImageDimensions` is a `static` TU-local; a real test needs it exposed (or an in-TU doctest) to feed a >64 KB fixture and assert bytes-read ≤ ceiling.
  Concrete next action: on a dev machine (or once headless GL/Mesa + the sanitizer preset are wired into CI): (1) add the site-#6 live-swap sanitizer repro as a bucket-D case; (2) wire the thumbnail-cue bucket-E test (or a screenshot-diff fallback) per `agents/core/test-author.md`; (3) expose `ParseImageDimensions` (or add a sibling pure helper) + a doctest. ~2-3 h total once the harness machine is available.
  Status: partially applied (2026-06-20 trap-sweep — shipped: (b) attachment_thumbnail_loading_cue.test.cpp (PR #1394), (c) ImageDimensionsPure.h ParseImageDimensionsFromBytes + ParseImageDimensions.test.cpp; remaining: (a) the project-picker ListProjects live-swap sanitizer case)
  Last-reviewed: 2026-06-20

- 2026-06-05 · orchestrator · [tooling] · P2 — `test-plan-index.sh --fix` (dev machine) emits a `—` shipped-date placeholder that CI's auto-sync then "corrects" → a guaranteed local-fix-vs-CI drift cycle
  Details: archiving the `reduce-agent-prompt-bloat` plan to `shipped/`, I ran `test-plan-index.sh --fix` locally and committed the regenerated `docs/plans/INDEX.md`. It passed `--check` locally but the `doc-validation.yml` "Doc anchors + agent contract" job FAILED with `test-plan-index: DRIFT`. Cause: local `--fix` wrote `| — |` for the new row's shipped-date column, while CI's "Auto-sync plan INDEX" job derives the real date (`2026-06-04`) and rewrote the row — so any commit of local `--fix` output is drift-by-construction, and the date column is environment-dependent. This is also the upstream trigger for the merge-gates poller false-pass filed above (the auto-sync bot's GITHUB_TOKEN correction commit didn't re-trigger CI).
  Concrete next action: make `test-plan-index.sh --fix` derive the shipped-date deterministically the SAME way the CI auto-sync job does (git log of the plan file's add-to-shipped commit, or the file's first commit date), so local `--fix` output is byte-identical to CI's regen and `--check` can't disagree across environments. If the date genuinely can't be derived offline (no network/tags), have `--fix` refuse + print "run CI auto-sync" rather than emit a placeholder that guarantees drift.
  Status: applied (2026-06-20 roadmap campaign — shipped #1502)
  Last-reviewed: 2026-06-20

- 2026-06-05 · build-doctor · [tooling] · P2 — `lint-syntax-both.py` FP-filter misses toolchain-bump PCH-version drift → false syntax-check FAIL on every touched .cpp
  Details: The Stop-hook dual-target syntax-check `_FP_PATTERNS` (`docs/harness/claude-code/hooks/lint-syntax-both.py:95-98`; regenerated into the gitignored `.claude/hooks/`) filters `Cannot open precompiled header` + `PCH file .* built from a different branch`, but NOT the toolchain-point-bump variant `Microsoft Visual C/C++ Version differs in precompiled file` / `was compiled for the target ...msvc19.NN`. When MSVC point-bumps (this session: 19.38→19.50), the `ninja-iter-clang` PCH goes stale and EVERY touched `.cpp` hard-FAILs the local syntax-check until the clang dir is manually refreshed — recurred repeatedly; build-doctor cleared it by regenerating only the PCH (`ninja -C build/ninja-iter-clang CMakeFiles/SmatchetStandalone.dir/cmake_pch.cxx.pch`, ~4s). Same stale-PCH noise class the filter already handles.
  Concrete next action: add `Microsoft Visual C/C\+\+ Version differs in precompiled file` + `was compiled for the target` to `_FP_PATTERNS` in the tracked `docs/harness/claude-code/hooks/lint-syntax-both.py` (`.claude/` is regenerated by setup-harness) so the version-drift case degrades to a silent skip — the MSVC `ninja-iter-msvc` build is the real correctness gate. Trade-off to weigh: a skip could mask a genuinely-stale clang dir, but blocking all edits on a toolchain bump is strictly worse.
  Status: applied (2026-06-20 roadmap campaign — shipped #1506)
  Last-reviewed: 2026-06-20

- 2026-06-10 · orchestrator · [tooling] · P2 — `coderabbit-plan-ref-convention-path-instruction`: `.coderabbit.yaml` has no `path_instructions` for `docs/plans/**`, so CodeRabbit re-flags the tier-less move-proof plan-ref convention on every plan-archive PR
  Details: From the gate-escape postmortem of PR #1110 (`cr-out-of-band`, 2026-06-10 ledger entry). Same class as the 2026-06-09 `tests/**` path-instruction drift above, different path. The repo's intentional plan-ref convention is **tier-less**: a `docs/plans/<slug>.md` reference omits the `active/`|`shipped/`|`deferred/` segment so the ref survives a plan moving between tiers (`agents/scripts/core/test-plan-ref-integrity.sh` resolves a tier-less ref against any tier — it is the *valid*, gate-blessed form). `docs/plans/INDEX.md` is auto-synced by `doc-validation.yml`'s "Auto-sync plan INDEX" job, not hand-maintained. CodeRabbit knows neither rule, so on #1110 it raised two false positives — (F1) "`docs/plans/mobile-mvp-completion.md` is missing `shipped/`", (F2) "`INDEX.md` out of sync, gate RED" — both refuted deterministically on the head (`test-plan-ref-integrity.sh` exit 0 "all 126 referenced plan paths resolve"; `test-plan-index.sh --check` exit 0; live "Doc anchors + agent contract" = success). The same false positives recur on every `active/`→`shipped/` archive PR, each forcing a manual `cr-out-of-band`.
  Concrete next action: add a `path_instructions` entry to `.coderabbit.yaml` for `path: 'docs/plans/**'` teaching CodeRabbit: (1) a `docs/plans/<slug>.md` reference WITHOUT an `active/`|`shipped/`|`deferred/` segment is the intentional move-proof convention and must NOT be flagged as "missing a tier" — `test-plan-ref-integrity.sh` resolves it; (2) `docs/plans/INDEX.md` is CI-auto-synced (the "Auto-sync plan INDEX" job) — do not assert it RED/out-of-sync from a stale pipeline view; the plan-ref-integrity + plan-index gates are the source of truth for these two concerns. ~20 min; pure-config, no code. Verifies by re-running CR on a plan-archive PR and confirming zero plan-path findings.
  Status: applied (2026-06-20 roadmap campaign — shipped #1504)
  Last-reviewed: 2026-06-20

- 2026-06-10 · orchestrator · [tooling] · P2 — `cr-review-skipped-pure-docs-auto-downgrade`: a CR `review-skipped` caused by CodeRabbit's own rate limit hard-blocks a pure-docs PR until a human hand-applies `cr-out-of-band`
  Details: From the gate-escape postmortem of PR #1095 (`cr-out-of-band`, 2026-06-10 ledger entry). #1095 was a pure-docs PR (ADR-0019 + a shipped-plan archive, 2 files). CodeRabbit hit its **"Review limit reached"** rate limit and emitted `review-skipped` — which the merge-gate CR condition (AGENTS.md § Merge gates) treats as a hard block. A `review-skipped` from a rate limit is an upstream infra condition, not a signal about the diff; on a pure-docs change there is nothing for CR to find, yet the gate still required a manual `cr-out-of-band`. The override was correct but mechanical/recurring — exactly the deterministic-safe case a gate should auto-handle.
  Concrete next action: in `agents/scripts/core/merge-gates.sh` (CR condition), when CodeRabbit's state is `review-skipped` AND the skip cause is a rate limit (CR's "Review limit reached" / `rate limited by coderabbit.ai` marker) AND `agents/scripts/core/is-pure-docs-diff.sh` returns true for the PR's diff, auto-downgrade the CR block to WARN (same effect as `cr-out-of-band`) without requiring the label — log the auto-downgrade reason. Scope STRICTLY to pure-docs: on any code diff a CR rate-limit `review-skipped` must still block (CR review is load-bearing there). Add a `tests/bats/merge_gates.bats` case: `review-skipped(rate-limit) + pure-docs` → WARN/pass; `review-skipped(rate-limit) + code diff` → block. ~1-2 h. Net removes a recurring manual override on the safest diff class.
  Status: applied (2026-06-20 roadmap campaign — shipped #1504)
  Last-reviewed: 2026-06-20

- 2026-06-11 · git-janitor · [tooling] · P2 — merged-branch worktrees accumulate unreaped in the shared multi-session tree; no session-end hook removes a worktree whose branch already merged
  Details: End-of-session git-janitor cleanup (this session) found ≈33 worktrees / ≈31 confirmed-MERGED local branches accumulating — every branch's `origin` ref was already auto-deleted post-merge, but the local branch + its worktree under `C:/Dev/trees|.codex/worktrees` were never reaped. The single-session `git-janitor` is structurally unable to clear this: the concurrent-session confinement rule forbids it from touching sibling-owned worktrees (~11 live sessions share the integration tree `C:/Dev/Smatchet`), and nothing runs when an individual session ENDS to remove a worktree whose PR has since merged. Result: monotonic growth of stale merged worktrees, each a future `git pull`/checkout collision risk (exactly the `tools/sourcetrail/*` untracked-overwrite block hit this session) plus disk cost. Distinct from `p4-janitor` (p4 task streams) and from the per-session janitor (which self-excludes by design).
  Concrete next action: add a `worktree-reap.sh` (coordinated periodic sweep, runnable when sessions are idle — NOT per-session) that reaps a worktree only when ALL hold: (a) its branch's PR is MERGED and the `origin` ref is gone; (b) the worktree is clean (no uncommitted/untracked user work); (c) no recent HEAD activity (idle threshold, e.g. > 24h) so it can't race an active session; (d) the branch is not protected/allowlisted (`bug-report-assets`) nor code-referenced. Action per match: `git worktree remove <path>` then `git branch -D <branch>`. Dry-run by default; explicit flag to act. Keep it OUT of the per-session `git-janitor` (which must stay self-excluding) — this is a separate idle-time tool. Surfaced by git-janitor during the vexp-removal closeout session.
  Status: partially applied (2026-06-20 trap-sweep — shipped: scripts/dev/worktree-prune.sh (PR #1164, MERGED-only + clean + protected + dry-run/--apply); remaining: sub-guard (c) the idle-threshold/recent-HEAD-activity (>24h) check)
  Last-reviewed: 2026-06-20

- 2026-06-11 · orchestrator · [tooling] · P2 — `workflow-watchdog.sh`: no automated stall detection for background Workflow fleets (crawl vs frozen is diagnosed by hand)
  Details: During the 2026-06-10/11 audit-fleet debacle and its salvage waves, "stalled" fleets had two opposite causes — crawl (TPM starvation: transcripts/results still advancing slowly; remedy = reduce concurrency / pin smaller model, do NOT kill) vs frozen (permission deny or agent death: newest transcript mtime static ≥ ~10 min; remedy = kill + salvage). Each diagnosis was manual transcript-mtime archaeology mid-incident. The reading pattern is now written down in `docs/agent-rules/workflow-fleets.md` § Stall watchdog, but nothing automates it.
  Concrete next action: `agents/scripts/core/workflow-watchdog.sh <fleet-slug>` — poll `build/<fleet-slug>/results/*` count + newest agent-transcript mtime in the session dir, classify crawl/frozen per the rule-doc table, emit a one-line verdict (and a nudge when frozen). Dry/read-only; no kill action. Bats: fixture dirs with advancing vs static mtimes.
  Status: applied (2026-06-20 trap-sweep — agents/scripts/core/workflow-watchdog.sh)
  Last-reviewed: 2026-06-20

- 2026-06-11 · orchestrator · [tooling] · P2 — fleet pre-flight check: nothing mechanically validates a Workflow fleet launch against the `workflow-fleets.md` checklist
  Details: All five 2026-06-10/11 fleet deaths (session-limit all-or-nothing, TPM starvation from an inherited 1M-context model, 200K-context overflow on an oversized input file, out-of-workspace permission deny, run-dir deletion on kill) were violations of now-written rules (`docs/agent-rules/workflow-fleets.md` § Pre-launch checklist) — every one detectable BEFORE launch from the workflow script + staged inputs: `model` unset on `agent()` calls, input files > ⅓ context window, paths outside the workspace, no repo-file checkpoint step in prompts, concurrency > 5 with live siblings.
  Concrete next action: `agents/scripts/core/fleet-preflight.sh <workflow-script> <fleet-dir>` — static checks: grep script for `agent(` calls missing `model:`; stat every file under `<fleet-dir>` against the ⅓-window byte cap; flag `~/.claude`/`%TEMP%`/absolute out-of-workspace paths in script + prompts; warn when prompts lack a results-file write step; check `.claude/.active-sessions` live count vs declared concurrency. Advisory (WARN lines, exit 0) first; promote to a gate after calibration. Bats over fixture scripts.
  Status: applied (2026-06-20 trap-sweep — PR #1354)
  Last-reviewed: 2026-06-20

- 2026-06-11 · code-review · [tooling] · P3 — shell-lint has no NUL-byte guard for first-party `*.sh`; an embedded NUL silently corrupts a script and evades every gate
  Details: During `feat/postmortem-owed-cleanup` a tool-edit dance left two NUL bytes inside `postmortem-owed.sh`'s jq `group_by` key (`"C\0"`/`"S\0"` instead of `"C "`/`"S "`). Functionally benign there (the keys still differed), but it (1) made `grep`/ripgrep classify the file as **binary** — every future text-search/review over it silently degrades to "binary file matches", (2) is a latent landmine for any future NUL-truncating edit, and (3) is caught by NOTHING — shellcheck runs clean on a NUL-bearing script, and the comment/format gates don't look for it. Found only by chance (the reviewer's Grep returned "binary file matches"). Same evade-every-gate class as the comment-audit false-negatives.
  Concrete next action: add a rule to `test-shell-lint.sh` (or a pre-ship grep) that fails when a tracked first-party `*.sh` contains a NUL byte — e.g. `LC_ALL=C grep -lP '\x00' "$f"` (or `git grep -lI` "is this binary" check) over the shell zone, with a "re-save as UTF-8 text" hint. ~15–20 min incl. a bats fixture (a NUL-bearing fixture script → gate red). Cheap guard for a corruption class with zero current coverage.
  Status: applied (2026-06-21 P3 sweep — shipped #1522)
  Last-reviewed: 2026-06-21

- 2026-06-15 · test-author · [tooling] · P2 — `keybindings-editor-rebind-bucketE-residue`: the click-to-rebind→dispatch integration seam + quick-bind modal capture interior have no bucket-E coverage — blocked by a private dirty-flag trigger + headless docked-window content clipping
  Details: PR2 of `docs/plans/keyboard-shortcuts-rebindable.md` (the rebindable-shortcut editor UX) ships bucket-E `tests/ui/keybindings_editor_rebind.test.cpp` with two real-UI assertions — (A) `EditorTabRendersWithLiveConflict` seeds a live `Ctrl+B` collision on `app.dock_debug.toggle`, opens Preferences, clicks the locale-stable `Keyboard Shortcuts###prefsTabKeybindings` tab, and YieldUntil-asserts `g_ui.preferencesActiveTab == Keybindings` (proves `DrawKeybindingsPreferencesTab`'s body — incl. the per-row `FindKeybindingConflict` + "conflicts with <action>" branch + the searchable table + System-shortcuts header — ticks crash-free with a conflict present, IM_ASSERT-trapped); (B) `DefaultComboDispatchesToCommand` presses `Ctrl+Alt+D` and asserts the public `g_ui.showDockDebug` flips (proves the live dispatch chain: cache → `smatchet::ui::MatchHotkey` → registry `Dispatch` → observable flag). What is NOT automated: the end-to-end **click a row's capture widget → capture a new combo → `MarkKeybindingsDirty` → `rebuildKeybindingCache` → the NEW combo fires the command** transition, and the **quick-bind modal's capture interior** (right-click "Set shortcut…" → capture → Set/Clear). Two hard blockers: (1) `SmatchetUI::MarkKeybindingsDirty()` is a **private** member with **no global accessor** reachable from a bucket-E TU (the dispatch-cache rebuild can only be triggered through the real editor/quick-bind mutating UI, not poked directly); (2) the editor's mutating widgets (per-row capture control, "Reset all to defaults" below a ~320px scroll-table, the quick-bind popup body) sit in the docked Preferences window's **clipped content region** — this repo's bucket-E suite has documented (see `funcsize_preferences_tabs.test.cpp`) that `ItemExists`/`ItemClick` on in-tab content below the fold is unreliable in the headless app (short content region clips lower widgets out of the Test Engine item table). The constituent logic IS covered at bucket-A: `tests/Core/KeybindingsConfig.test.cpp` (SetBindingHotkey upsert, FindKeybindingConflict, RemoveBinding) + `tests/Core/ImGuiHotkey.test.cpp` (Parse/Stringify round-trip, MatchHotkey exact-modifier match). So the residue is the UI-glue seam between them, not the logic.
  Concrete next action: pick one of — (a) expose a test-only trigger for the dirty-flag rebuild (a `// test-only` `extern "C"` shim or a friend hook that calls `MarkKeybindingsDirty()` + `rebuildKeybindingCache()` on the live `SmatchetUI`), letting a test mutate `g_ui.cfg.Keybindings` then force a rebuild and assert the new combo dispatches — closes the rebind→fire seam without needing clickable editor widgets; OR (b) drive the editor body in a REPLICA window (the repo's `Begin("SmatchetTest::…")` pattern from `ai_assistant_preferences_save_discard.test.cpp`) that hosts `DrawHotkeyRebindControl` + `QuickBindPopup::Draw` directly at full height (no docked clipping), so `ItemClick` on the capture control + a synthesized `KeyPress` exercises the capture→commit path; OR (c) widen/undock the headless Preferences window so its content region fits the full table (removes the clipping blocker for direct ItemClick). Est ~2-3 h for (a) (smallest, most deterministic); (b) is more faithful but re-hosts editor internals. Until then, the seam is logic-covered (bucket-A) + render-covered (the two shipped bucket-E tests) but not click-through-covered. Surfaced while authoring the PR2 bucket-E suite.
  Status: open
  Last-reviewed: 2026-06-15

- 2026-06-18 · claude (dx12-arm64 Phase 4 session) · [tooling] · P2 — `plan-index-autosync-PAT-403`: the `Auto-sync plan INDEX` job cannot self-heal — its `PLAN_INDEX_PAT` push 403s, so any genuine shipped-index drift reds every PR's doc gate
  Details: the "Auto-sync plan INDEX (PR branch)" job regenerates `docs/plans/INDEX.md` (`test-plan-index.sh --fix`) and, on drift, commits + pushes the result via `PLAN_INDEX_PAT`. Observed on PR #1389: the push failed `remote: Permission to alexandrosk0/Smatchet.git denied to alexandrosk0 ... 403`, so the auto-fix path dies and the companion `Doc anchors + agent contract` job (which runs `test-plan-index.sh` non-fix) reds the doc gate. In #1389 the index was actually in sync — the apparent drift was a self-inflicted shallow-clone regen (the index sorts by `git log --follow` first-commit dates, which a shallow checkout computes wrong) — so it did not block. But a real shipped-plan add/rename would wedge every open PR until someone regenerates from FULL history and pushes by hand. The PAT is likely expired, missing `Contents: write`, or not granted to this repo.
  Concrete next action: rotate/re-scope `PLAN_INDEX_PAT` (fine-grained PAT, Contents: Read+write, this repo) so the auto-sync push succeeds; OR convert the job to fail-with-remedy only (no push) and add a header note that the author must regenerate locally with full history (`git fetch --unshallow` first — a shallow regen corrupts the date sort). ~15 min once PAT access is decided.
  Status: open
  Last-reviewed: 2026-06-18

- 2026-06-18 · claude (dx12-arm64 ship session) · [tooling] · P2 — `test-plan-index-shallow-clone-corrupts-date-sort`: in a shallow checkout the plan-index generator emits a wrong-but-self-consistent INDEX, so `--fix` writes drift INTO the tree and local `test-docs.sh` passes while CI reds
  Details: `agents/scripts/core/test-plan-index.sh` orders rows + fills the "Approx. date" column from `git log --follow --format=%ad` first-commit dates (:51, sorted at :99). The web/remote execution environment clones SHALLOW (`git rev-parse --is-shallow-repository` = true; ~50 commits here), so `--follow` returns the shallow-boundary date instead of the true first-add for any plan whose history predates the boundary → a different row order than CI computes with full history. Lived this session on PR #1389: a clean checkout's committed INDEX looked "drifted" to the shallow generator, I ran `--fix` and committed a 138-line-reordered CORRUPT index; local `test-docs.sh` passed (the shallow regen is internally self-consistent) while CI's `Doc anchors + agent contract` + `Auto-sync plan INDEX` jobs (full history) red'd it. Recovery was `git fetch --unshallow` → re-run → "index up to date (140 plans)" → drop the bad commit + force-push. The high-integrity delta gate already guards this exact class (build-and-test.yml unshallows before `git merge-base`, ~:304), so the index generator is the unguarded sibling — and the agent-side lesson is: never regenerate a `git log`-reading artifact from a shallow clone, and don't "repair" a pre-existing-looking doc-gate red without first confirming it on full history.
  Concrete next action: make `test-plan-index.sh` detect `git rev-parse --is-shallow-repository == true` and either auto-`fetch --unshallow` (matching the CI delta-gate pattern) or hard-refuse with "shallow clone: first-commit dates unreliable — unshallow first" instead of silently writing a mis-sorted index; the same guard belongs on any history-reading doc gate. Cheapest: a ~3-line shallow check at the top of the generator + a bats fixture (shallow fixture repo → refuse).
  Status: applied (2026-06-20 roadmap campaign — shipped #1502)
  Last-reviewed: 2026-06-20

- 2026-06-18 · orchestrator (pr-intent-capture-hardening session) · [tooling] · P2 — `shallow-clone-corrupts-git-log-date-generators`: `test-plan-index.sh --fix` (and the whole class of `git log`/`git merge-base`-history-dependent generators that PRODUCE committed output) silently writes CORRUPT data in a shallow clone — every history-derived field collapses to the graft-boundary commit — and the remote Claude-Code-on-web container clones shallow by default, so running such a `--fix` there overwrites real content with garbage
  Details: `test-plan-index.sh` derives each shipped-plan row's "Approx. date" from `git log --follow --format=%ad --date=short -- <path>` and takes the LAST line = the first-commit date (`test-plan-index.sh:51`, `_git_first_date_at`). In a shallow clone (`git rev-parse --is-shallow-repository` → `true`; the web container clones at a shallow depth — ~52 commits this session) `git log` stops at the graft boundary, so EVERY plan's "first-commit date" resolves to the single graft commit's date. Running `bash test-plan-index.sh --fix` there rewrote all ~140 `docs/plans/INDEX.md` rows to that one graft date and I committed it — turning the deterministic, byte-stable index into uniform garbage. CI then went red on `Doc anchors + agent contract` (date/anchor drift) and the corruption could NOT self-heal: the `autosync-plan-index` job is deliberately NO-PUSH without the opt-in `PLAN_INDEX_PAT` repo secret (`doc-validation.yml:96-205` — a GITHUB_TOKEN bot push wouldn't re-trigger required checks, so by design it prints the drift diff and `exit 1`), so a corrupt COMMITTED index just stays red until the author hand-fixes and re-pushes. Recovered by restoring `docs/plans/INDEX.md` from the pre-corruption blob (`git checkout <good-rev> -- docs/plans/INDEX.md`) then `git fetch --unshallow` so later `git log` reads true history. I also initially MISDIAGNOSED the red as pre-existing upstream drift before tracing it to my own shallow-clone `--fix`. Same ROOT as the applied 2026-06-01 `Delta gates … false-flag … because CI's shallow clone defeats git merge-base` (applied.md:704) but a DISTINCT mechanism + failure mode: that one defeats `merge-base` to false-FLAG findings (read-only advisory noise); this one defeats `git log`-date to CORRUPT generated committed content via a destructive `--fix`. Two different tools bitten by the same shallow-clone root = recurrence, which argues for a shared backstop over a per-tool patch.
  Concrete next action: add a shallow-repo guard to the history-dependent generators — `test-plan-index.sh` (and any sibling that runs `git log --follow` / `git merge-base` to PRODUCE committed output) should `git rev-parse --is-shallow-repository` and, on `true`, REFUSE `--fix` (exit 2: "shallow clone — run `git fetch --unshallow` first; --fix would collapse every date to the graft boundary") and WARN under `--check`. Cheaper/parallel mitigations: have the remote web harness `git fetch --unshallow` (or `--deepen`) before any agent runs a generator, AND/OR document in the agent-rules that `--fix`-class generators are unsafe in the ephemeral shallow container. A shared `is_shallow_or_refuse()` helper sourced by the generator family closes the class in one place (the merge-base sibling would reuse it). Est ~30-45 min (guard in `test-plan-index.sh` + a bats fixture: a shallow fixture repo → `--fix` refuses; a full clone → unchanged) + ~15 min doc. Cross-ref: `test-plan-index.sh:51` (the `git log --follow` date derivation); applied.md 2026-06-01 `Delta gates … shallow clone defeats git merge-base` (the sibling-root recurrence) + applied.md:361 ("Sibling of the shallow-clone merge-base false-flag"); `doc-validation.yml:96-205` (the autosync no-PAT/no-push design that makes a corrupt COMMITTED index persist red until a human re-push — it amplifies this corruption's cost, and is correct-by-design, NOT a separate bug to file); this session's `docs/plans/INDEX.md` corruption + `git fetch --unshallow` recovery.
  Status: applied (2026-06-20 roadmap campaign — shipped #1502)
  Last-reviewed: 2026-06-20
