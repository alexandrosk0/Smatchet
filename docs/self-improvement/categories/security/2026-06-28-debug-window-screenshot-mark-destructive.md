- 2026-06-28 · orchestrator (PR-D #1566 spawn-auth) · [security] · P3 — mark the `debug.window.screenshot` MCP command Destructive (gate behind `__confirm:true`) — deferred; PR-D's path confinement already closes the arbitrary-file-write hole
  Details: `debug.window.screenshot` (and the 4 screenshot scenarios) take a caller-supplied `screenshotPath` that flows to `stbi_write_png`. Before PR-D this was an unconfined arbitrary-file-write primitive reachable over the MCP/Lua wire (#1566 class — CRITICAL C1). PR-D's C1 fix confines every wire-reachable path under `<userData>/screenshots/` via `ConfineScenarioScreenshotPath` → `ConfinePathUnderSubdir` (rejects absolute / `..` / UNC / symlinked subdir), so the command can no longer write outside the sandbox. The residual concern is purely contract-shape: a screenshot command still *writes a file* as a side effect, and the MCP Destructive convention (`__confirm:true` required for state-mutating tools) would normally flag it. Folding it in now would change the MCP tool contract (schema + every caller incl. the CI bucket-C/E drivers + scenario harness) for a hole that confinement already closed — not worth coupling into PR-D. Hence deferred, P3.
  Concrete next action: in a standalone follow-up (own PR), add the Destructive flag + `__confirm:true` requirement to `debug.window.screenshot`'s MCP schema in `Source/Plugins/Mcp/` and audit every caller (CI bucket-C/E bash drivers, scenario harness, Lua scripts) to pass the confirm token. Route through `mcp-toolsmith`. Gate on: is the extra confirm friction worth it now that confinement closes the write-escape? If no concrete demand, leave deferred. Cross-ref: PR-D C1 in `BuiltinCommands_Debug.cpp` + the 5 scenario sites; `ConfinePathUnderSubdir` in `Source/Core/.../Commands/PathConfinement.h`; plan `docs/plans/active/spawn-mcp-auth.md` § PR D.
  Status: open (deferred — confinement closes the exploitable hole; this is contract-hygiene only)
  Last-reviewed: 2026-06-28
