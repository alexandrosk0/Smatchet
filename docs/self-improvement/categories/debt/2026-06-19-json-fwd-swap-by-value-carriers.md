- 2026-06-19 · orchestrator (fan-in json-door sweep) · [debt] · P3 — `json_fwd` swap of the two remaining top-fan-in `json.hpp` carriers (`Ui/SmatchetUiSession.h` 75, `Commands/Command.h` 42) blocked on by-value members — needs boxing/pImpl, not an include swap (`json-fwd-swap-by-value-carriers`)
  Details: Post-`appcontroller-fan-in` re-measurement of which `Source/Core` headers still pull the full `<nlohmann/json.hpp>` and reach the most translation units. `AppController.h`'s json door is now CLOSED (`appcontroller-fan-in` Phase 1/2: `AppController.h:69` is `<nlohmann/json_fwd.hpp>`, all json by-reference in out-of-line decls; `Tracker/IssueCreatePipeline.h` also swapped to `json_fwd`; the direct `ITrackerIssueMutations.h` include dropped). With that closed, the two highest-fan-in headers that STILL `#include <nlohmann/json.hpp>` are **not clean `json_fwd` swaps** because both hold `nlohmann::json` **by value** (a forward-declared `nlohmann::basic_json<>` is incomplete → cannot size/lay-out a by-value member or instantiate `~vector<json>`):
    - `Commands/Command.h` (full include at `:19`; **42** direct includers incl. tests / 40 `Source/`-only) — by-value data members `CommandParameter::Default` (`:33`), `CommandError::Details` (`:61`), `CommandResult::Data` (`:69`), plus by-value returns/params `ToJson()` (`:63`), `Success(nlohmann::json)` (`:71`), `ToWireJson()` (`:78`), `BuildJsonSchema()` (`:167`).
    - `Ui/SmatchetUiSession.h` (full include at `:9`; **75** direct includers incl. tests / 61 `Source/`-only) — `std::vector<nlohmann::json>` members `UiDrawSession::Events` (`:129`) and `auditCachedEvents` (`:507`); `UiDrawSession` (`:212`) is a file-scope global (`extern UiDrawSession g_ui;` at `:928`) whose implicit ctor/dtor instantiate `~vector<json>` in every includer → complete `json` required tree-wide, so a bare include swap won't compile.
    This refreshes the now-stale `build-quality-velocity-hardening` § Root-multiplier-A item **[#2]** (struck through "`json_fwd` swap of the 3 named headers — NO-GO in isolation"), which named `AppController.h` + `Command.h` as the by-value blockers and predates `appcontroller-fan-in` shipping — the `AppController.h` half is DONE; `Command.h` remains and `SmatchetUiSession.h` is the new #1 carrier by fan-in. All line/counts verified against `origin/develop`.
  Concrete next action: box the by-value json members behind a pointer / pImpl (or relocate the json-holding structs to a `.cpp`-private home), THEN swap each header `#include <nlohmann/json.hpp>` → `<nlohmann/json_fwd.hpp>` — same proven technique as `appcontroller-fan-in` (Phase 1/4 pImpl + Phase 3 `Types/` leaf-header + `using`-alias re-export). For `Command.h` the carriers are the `CommandParameter`/`CommandError`/`CommandResult` structs; for `SmatchetUiSession.h` the two `std::vector<nlohmann::json>` event buffers (cheapest: hold each behind a `std::unique_ptr` / move the audit-event buffer into the existing session impl). Bundle with the next PR that legitimately touches each header — do NOT open a standalone churny ~75-/~42-file PR (matches the INDEX.md § 2 deferred-code-items "bundle-with-next" guidance). PCH caveat (measure, don't project): the win is **Standalone-only** — the DX12 PCH already precompiles `nlohmann/json.hpp` for the whole target (`SmatchetPchCoreDx12.h:13`, ~64% of TUs pull it) while the Standalone PCH deliberately dropped it (`SmatchetPch.h:17`); record the preprocessed-size delta on a PCH-on Standalone build before quoting a number.
  Status: open
  Last-reviewed: 2026-06-19
