- 2026-06-19 · orchestrator (fan-in json-door sweep) · [debt] · P3 — `Commands/Command.h` `json_fwd` swap blocked on by-value members (`Default`/`Details`/`Data`) — needs boxing/pImpl (`json-fwd-swap-by-value-carriers`)
  Details: Post-`appcontroller-fan-in` re-measurement found the two highest-fan-in `Source/Core` headers still pulling full `<nlohmann/json.hpp>` were `Ui/SmatchetUiSession.h` (75 includers) and `Commands/Command.h` (42), both holding `nlohmann::json` by value (a forward-declared `nlohmann::basic_json<>` is incomplete → cannot lay out a by-value member or instantiate `~vector<json>`). **`Ui/SmatchetUiSession.h` is now DONE** (this branch): swapped `:9` to `<nlohmann/json_fwd.hpp>`, kept its two `std::vector<nlohmann::json>` members (`AuditDisplayCachePayload::Events`, `UiDrawSession::auditCachedEvents`) — incomplete element type is fine for a vector member, and the out-of-line `~UiDrawSession` localises the only tree-wide `~vector<json>`. Added direct `#include <nlohmann/json.hpp>` to the 7 TUs that touch the members / construct a `UiDrawSession`: `SmatchetAuditUi.cpp`, `SmatchetUI_Layout.cpp` (the `~UiDrawSession` definer), `AppViewCommands.cpp`, `UiModeCommand.cpp`, `ViewToggleCommands.cpp` (direct json users), and the two UI tests that build a `UiDrawSession` (`grid_wheel_route_ownership`, `reset_layout_docking`). A first-party closure walk confirms `json.hpp` no longer rides the header's transitive edge; the member/ctor/dtor pattern was isolation-compiled green on g++ 13 and clang++ 18 at `-std=c++14` (pinned nlohmann v3.11.3) before push.
    **Remaining — `Commands/Command.h`** (full include at `:19`; 42 direct includers incl. tests / 40 `Source/`-only): the by-value members are *scalar* `nlohmann::json`, not vectors — `CommandParameter::Default` (`:33`), `CommandError::Details` (`:61`), `CommandResult::Data` (`:69`) — so the "keep the member, forward-declare the element" trick does NOT apply (a direct `nlohmann::json` member needs the complete type for layout). The method signatures are already `json_fwd`-safe (return-by-value / by-value param in a *declaration* only needs completeness at the definition/call site): `ToJson()` (`:63`), `Success(nlohmann::json)` (`:71`), `ToWireJson()` (`:78`), `BuildJsonSchema()` (`:167`). So the swap genuinely requires **boxing the 3 scalar members** (e.g. `std::unique_ptr<nlohmann::json>` + accessors, or pImpl), which rewrites every `.Data` / `.Details` / `.Default` site — ~109 across CLI / Palette / MCP / Lua / Unreal (`.Data` 69, `.Default` 22, `.Details` 18). Deferred (user chose "UiSession first"): high churn, foundational type, low standalone ROI. Also refreshes the stale `build-quality-velocity-hardening` § Root-multiplier-A item **[#2]** (named `AppController.h` + `Command.h`; `AppController.h` is done). All line/counts verified against `origin/develop`.
  Concrete next action: box `CommandParameter::Default` / `CommandError::Details` / `CommandResult::Data` (cheapest: `std::unique_ptr<nlohmann::json>` with small inline get/set, or move them behind a `cmd::detail` impl), update the ~109 `.Data` / `.Details` / `.Default` access sites, THEN swap `Command.h:19` → `<nlohmann/json_fwd.hpp>` and add direct `#include <nlohmann/json.hpp>` to the fallout TUs (the ones that build those structs by value). Bundle with the next PR that legitimately touches the command-system core — do NOT open a standalone ~100-file churn PR. PCH caveat (measure, don't project): the win is **Standalone-only** — the DX12 PCH already precompiles `nlohmann/json.hpp` (`SmatchetPchCoreDx12.h:13`, ~64% of TUs pull it) while the Standalone PCH dropped it (`SmatchetPch.h:17`); record the preprocessed-size delta on a PCH-on Standalone build before quoting a number.
  Status: open
  Last-reviewed: 2026-06-19
