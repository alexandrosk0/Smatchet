# Agent self-improvement — debt

> Format / categories / workflow / priority / triage: see
> [`../AGENT_SELF_IMPROVEMENT.md`](../AGENT_SELF_IMPROVEMENT.md) (index + spec).
> Sibling categories: bug (deprecated) · debt · process · tooling · infra · test · security · external-blockers · applied.
> **Product tech-debt** — internal maintainability with NO user-observable defect
> (god-object, duplication, coupling, missing abstraction, "should refactor"). A
> defect a user or a correctness/safety gate would observe is a **GitHub Issue**,
> not a debt entry — see [`../../agent-rules/issue-triage.md`](../../agent-rules/issue-triage.md).
> Live entries only. `applied` entries archive immediately to `applied.md`.

<!-- Latest first. Append new entries at the top. -->

- 2026-06-16 · orchestrator (plan-archival sweep) · [debt] · P3 — `http-fault-injection` shipped residue: cancel-token threading + doc-sweep not landed (`http-fault-injection-residue`)
  Details: `docs/plans/shipped/http-fault-injection.md` (PR #1231, commit `377c9f72`) shipped its core deliverable — `TrackerHttpRequestWithRetry` wired into the live `TrackerHttpUtils.cpp` helpers + the two new test TUs — and was archived as shipped. Two scoped sub-items did **not** land and are deferred here per "no silent residue": (1) **cancel-token threading** into the Jira/Plane search-fetch GET loops (`JiraIssueSearch.cpp` / `PlaneIssueSearch.cpp`, plan § Files-to-modify row 3) — the `cancelled` param exists on the retry engine but is passed `nullptr` at the helper call site, so interruptible backoff for long syncs is not delivered (the plan documents the worker-thread no-token fallback as acceptable, hence hardening not core); (2) the **doc-update sweep** (plan rows 8-10: `docs/guides/testing-surface.md`, `docs/plans/testing-surface-roadmap.md`, this `debt.md`) — the stale "TrackerHttpRequestWithRetry … already exercised / dead-code" framing in `testing-surface-roadmap.md` was corrected in the same sweep that filed this entry, but no dedicated D2 backlog entry existed until now.
  Concrete next action: thread a cancellation token from the sync worker into `TrackerHttpRequestWithRetry`'s `cancelled` param at the Jira/Plane search-fetch call sites so an in-flight backoff can abort on backend-switch/shutdown; add a bucket-A test that a cancelled token short-circuits the retry loop. ~1-2 h. Bundle with the next PR that touches `JiraIssueSearch.cpp` / `PlaneIssueSearch.cpp` or the offline/sync cancellation work (see the `automation-shutdown-blocking-glue` entry — same cancellation-token theme).
  Status: open
  Last-reviewed: 2026-06-16

- 2026-06-15 · orchestrator (dx12 phase 1) · [debt] · P3 — `CMakeLists.txt` dead `if(SMATCHET_EMBEDDED_IN_UNREAL)` *variable* branch (`cmake-embedded-unreal-dead-var-branch`)
  Details: `CMakeLists.txt:311` opens `if(SMATCHET_EMBEDDED_IN_UNREAL)` which tests a CMake **variable** that is never `set()` anywhere — the real Unreal-embed mechanism is the target-level compile **define** `SMATCHET_EMBEDDED_IN_UNREAL=1` on `SmatchetCore_DX12` at `CMakeLists.txt:1375` (`target_compile_definitions`). So the `:311` branch body is unreachable dead config — it can never fire from a normal configure, and reads as if a `-DSMATCHET_EMBEDDED_IN_UNREAL=ON` variable toggle exists when it does not. No build defect (dead, not wrong); a maintainability/confusion trap. Surfaced during the DX12 standalone-renderer Phase-1 work (plan `docs/plans/active/dx12-standalone-win-arm64.md` § Out of scope); flagged here per that plan's mandate, not fixed in-PR (out of Phase-1 scope — touching it risks a configure-time surprise unrelated to the renderer change).
  Concrete next action: read `:311`'s branch body, confirm it is fully subsumed by the `:1375` target-define path (or by the standalone GL default), then delete the dead `if()`/`endif()` block — OR, if any of its body is load-bearing, convert the guard to test the actual define-carrying target property instead of the phantom variable. ~20-30 min, mechanical + one full reconfigure to confirm no skew. Low priority — pure dead-code hygiene.
  Status: open
  Last-reviewed: 2026-06-15

- 2026-06-15 · orchestrator (security-audit campaign) · [debt] · P3 — automation shutdown can still extend the join by a blocking HTTP call (`automation-shutdown-blocking-glue`)
  Details: Audit finding #13's fix (PR #1245) closes the pure-Lua case — the automation count-hook now aborts on the pre-join `automationWorkerShuttingDown_` flag + a bounded per-job instruction budget (`LuaAutomationHookPolicyPure`). But an instruction-count hook only fires between Lua opcodes; it **cannot** interrupt a blocking C++ call already in flight from a binding (e.g. `JiraClient::UpdateIssueFields` → synchronous `cpr` HTTP at `Source/Core/src/Tracker/JiraIssueMutation.cpp:206`). So if shutdown is requested while one such call is mid-flight, the worker join still blocks until that call's HTTP timeout elapses. Bounded (one timeout, worker thread — not the UI thread; the UI-thread-starvation half is mitigated by architecture: automation never runs on the UI thread), so no user-visible freeze — a shutdown-latency nuisance, not a Pillar-2 freeze. Documented inline in the #1245 fix.
  Concrete next action: route the blocking tracker glue reachable from automation onto a cancellable/time-bounded path — either a `LaunchBackgroundTask`-style off-thread HTTP with a cancellation token the shutdown flag trips, or a join-timeout that abandons a stuck in-flight call. Plus a bucket-E lifecycle test (spawn a long automation, request shutdown, assert bounded exit). ~2-3 h; needs a design call on cancellation semantics first.
  Status: open
  Last-reviewed: 2026-06-15

- 2026-06-13 · orchestrator (arch-analysis adversarial verify) · [debt] · P3 — DX12/Unreal C-ABI lib-staleness signals are warnings, not hard-fails (`c-abi-staleness-soft-fail`)
  Details: Architecture-analysis adversarial-verify pass (batch 2) re-graded the "C-ABI manual versioning" risk HIGH→MEDIUM→backlog after reading live code. The standalone↔Unreal boundary (`Source/Core/include/Ui/SmatchetImGuiHostC.h`) is opaque-handle (`void*`) + flat C primitives only — **no struct layout crosses the boundary**, so the silent-layout-UB category the original risk leaned on is structurally impossible at the C-ABI layer. Two live staleness signals exist but **neither hard-fails**: (1) `SmatchetImGuiPlugin.Build.cs:234-306` `WarnIfPackagedLibsAreStale()` emits a UBT `warning:` (not a `BuildException`) when newest `Source/Core` mtime > packaged `SmatchetImGuiHost_DX12.lib` mtime; (2) `SmatchetImGuiPluginModule.cpp:99-100` logs `SmatchetHost_GetBuildTag()` (`__DATE__ __TIME__` + `SMATCHET_HOST_BUILD_TAG_REV "v2"` from `SmatchetImGuiHostC_Diagnostics.cpp:19-28`) on every `StartupModule()` but never compares it against an expected constant. So a stale packaged lib yields a warning + a log line a developer can ignore — no enforced break. ADR-0002 already covers the only struct-layout-sensitive boundary (UiDrawSession macro parity) via INTERFACE shim libs at compile time. Residual is MEDIUM-bounded: a build/dev-time staleness nuisance, no user-observable defect. Verified live (adversarial agent, 2026-06-13): see `build/arch-analysis/verify2-c-abi-manual-versioning.md`.
  Concrete next action: pick the DX trade-off — either promote `WarnIfPackagedLibsAreStale()` to throw a `BuildException` (hard-fails UBT on a stale lib; downside: blocks mid-iteration builds when only `Source/Core` comments/docs changed), OR add a runtime expected-tag compare in `StartupModule()` that `UE_LOG(Error)`s + early-returns on a `SmatchetHost_GetBuildTag()` mismatch vs a header-baked constant. Either ~30-60 min. **The warning-vs-hard-fail choice is a deliberate DX trade-off — surface to the user before implementing.**
  Status: open
  Last-reviewed: 2026-06-13

- 2026-06-13 · orchestrator (arch-analysis adversarial verify) · [debt] · P3 — Lua console `imgui.image` accepts arbitrary `http(s)://` URLs — narrow console-only SSRF, no response channel (`lua-imgui-image-url-allowlist`)
  Details: Architecture-analysis adversarial-verify pass (batch 2) **REFUTED** the broader "Lua HTTP / TOFU" risk: there is **no** general HTTP binding in the Lua API (`AppController_LuaBindingsCore.cpp:307-403` `InitLuaCore` exposes no `http`/`fetch`/`curl`/`net` table), and the only outbound path uses standard system-CA TLS, **not** TOFU (`SmatchetFieldIconRender.cpp:181-200` `HttpGetBinary` = `cpr::Get` with no `SslOptions` → libcurl default peer/host verify ON; project-wide stance confirmed at `TrackerHttpUtils.cpp:122-131`). Residual LOW defense-in-depth gap: the interactive console sandbox does **not** block `imgui` (`AppController_LuaBindings.cpp:315-362`), so `imgui.image('https://attacker/...', w, h)` from a console snippet (`AppController_LuaBindings_Draw.cpp:959-976`) reaches `HttpGetBinary` via `ResolveFieldIconAssetPath` (`AppController.cpp:2396-2421`, no scheme restriction) — an SSRF-shaped outbound fetch. Bounded: (a) requires local authenticated access to the desktop scripting console; (b) the body is decoded to a display texture only (512 KB cap), **never returned to Lua** → no exfiltration channel; (c) background-automation / MCP Lua states can't reach `imgui` at all (`PrepareFreshLuaState` omits `InitLuaUi`, `AppController_LuaBindings.cpp:1155-1162`). Verified live (adversarial agent, 2026-06-13): see `build/arch-analysis/verify2-lua-http-tofu-fetch.md`. (Borderline `[security]`; filed as `[debt]` per the user's "log to backlog" framing — defense-in-depth hardening, no live exploit.)
  Concrete next action: optionally restrict `imgui.image` to non-`http(s)` (local-asset) URLs in the Lua console sandbox, OR enforce a URL allowlist in `ResolveFieldIconAssetPath`. ~30-45 min. LOW priority — local-console-only + no response channel; only worth doing if the scripting console is ever exposed to untrusted scripts.
  Status: open
  Last-reviewed: 2026-06-13

- 2026-06-11 · orchestrator · [debt] · P3 — `git_commit_repos` (+ siblings) not in the `config.set` CLI allowlist — UI-only settable
  Details: PR #1145 (User Info window) added the `git_commit_repos`, `production_group_keyword`, `user_activity_day_window`, `max_user_changes`, and `vcs_feed_layout` config keys and exposed them in the Preferences Tracker tab (immediate-dirty). They are NOT in `ConfigSetKeyTable()`'s `kKeys` allowlist (`Source/Standalone/.../BuiltinCommands_Config.cpp:106-129`), so `config.set git_commit_repos ...` from the CLI / MCP / Lua / scenario surface is rejected — only the UI can set them. Asymmetric with peer tracker keys (domain/pat/owner/repo) which are CLI-settable. No user-observable defect (UI path works); a scriptability/parity gap. User chose to backlog rather than wire now.
  Concrete next action: add the 5 keys to `kKeys` with their cmd-key→TrackerConfig member mapping (mirror `ConfigManager.cpp:194-233`), plus a parse for the two ints + the layout enum; extend the `config.set` allowlist selftest if one pins the key set. ~30-45 min, mechanical. Verify via `config.set git_commit_repos "owner/repo"` then `config.get`.
  Status: applied (2026-06-12, feat/debt-quickwins — the 5 keys are now in `ConfigSetKeyTable()`'s `kKeys`: `gitCommitRepos`→`git_commit_repos`, `productionGroupKeyword`→`production_group_keyword`, `userActivityDayWindow`→`user_activity_day_window`, `maxUserChanges`→`max_user_changes`, `vcsFeedLayout`→`vcs_feed_layout` (hint `unified|separate`). No special int/enum parsing needed — `RunConfigSet`'s generic `nlohmann::json::parse` already coerces `"5"`→int and the layout stays a string (the 3 strings + 2 ints read back through ConfigManager's existing `kStringFields`/`kIntFields` tables). No selftest pins the key set, so none to extend. To be archived to applied.md on next triage drain.)
  Last-reviewed: 2026-06-12

- 2026-06-09 · orchestrator (dedup-clones wrap-up) · [debt] · P3 — deferred copy-paste clusters B + C from the shipped `dedup-tracker-query-and-ai-client-clones` plan (`dedup-clones-b-c-deferred`)
  Details: The DRY-dedup plan (`docs/plans/shipped/dedup-tracker-query-and-ai-client-clones.md`) shipped clusters A (query-suggest helper extraction) + D (client-header exemptions) but deferred two real clones, both grandfathered + WARN-only (no gate pressure, no incident): **(B)** the 492-token SSE-drive skeleton duplicated `AnthropicClient.cpp` ↔ `OpenAiClient.cpp` (`AiSseParser` setup → `cpr::WriteCallback` double-cancel-check → `cpr::Post` → `NetworkUsageTracker::Record` → `Flush` → cancel/transport/HTTP-redact/EOF tail) — the plan's designed fix is a shared `Source/Core/src/AiSseStream.{h,cpp}` `StreamSseRequest(...)` taking a per-provider translate callback (Ollama explicitly OUT — `AiNdjsonParser`, different arity). **(C)** `JsonToLuaImpl`/`LuaToJsonImpl` copied `AppController_LuaBindings.cpp` ↔ `AppController_LuaBindingsCore.cpp` — an *intentional* no-ImGui-isolation copy; fix is either a dependency-free leaf `LuaJsonConvert.{h,cpp}` (sol2 + nlohmann only) iff it pulls nothing unwanted, else a `SMATCHET_DEVIATION(rule=duplication; reason=intentional no-ImGui isolation)`.
  Why deferred (not done): B is user-facing AI-chat streaming error-handling — high regression-risk-to-DRY-value ratio for zero behaviour change; C is a distinct judgment call. Both premises triple-checked still-valid 2026-06-09 (clones intact, names free: `AiSseStream`/`LuaJsonConvert`).
  Concrete next action: pick up B opportunistically when the AI-client streaming code is next touched for another reason (the plan's § Approach cluster B is the ready spec); resolve C as leaf-or-exempt in a focused PR. Neither is urgent — the dup gate is WARN-first/calibration-phase. Est B ~2-3 h + manual streaming smoke; C ~1 h.
  Status: open
  Last-reviewed: 2026-06-09

- 2026-06-08 · build-doctor · [debt] · P2 — Sync layer is ImGui-header-coupled, blocking it from the Linux TSan subset (layering smell)
  Details: `Source/Core/src/Sync/TicketSyncService.cpp` calls `SmatchetToastManager::Instance().Push(...)` directly (lines 380-549), and `Source/Core/include/Ui/SmatchetToast.h` `#include`s `<imgui.h>`. So a background sync/threading TU transitively requires ImGui at both compile (header) and link (Toast's `Render()` pulls `ImGui::*` symbols). Surfaced standing up hardening #10's TSan Linux subset (ninja-tsan-linux / `SmatchetTsanTests`): the `GridLiveContext` → `TicketSyncService` path — the richest real-race surface (atomic `Backend` slot swaps, latched strong handles) — cannot link into the headless ImGui-free TSan target without dragging `ImGuiLib` (→ GLFW/X11/OpenGL on the Linux runner). The MVP subset shipped only the decoupled threading TUs (LocalCacheManager SQLite cache + Pure units). Architecture smell independent of TSan: a worker thread reaching into a UI singleton inverts the UI→service dependency direction.
  Concrete next action: decouple notification emission from the sync layer — inject an `ISyncNotifier` (or a `std::function<void(level,title,msg)>` callback) into `TicketSyncService`, with the ImGui-backed `SmatchetToastManager` as the production impl wired at bootstrap and a no-op/recording fake in tests. Then expand the TSan subset (`tests/CMakeLists.txt` tsan branch) to `GridLiveContext.test.cpp` + the sync threading tests (`TicketSyncService.test.cpp`, `OfflineQueueServiceRuntime.test.cpp`, `OfflineQueueBackendSwap.test.cpp`, `ConfigSaveConcurrency.test.cpp`). ~2-3 h; unblocks the high-value half of hardening #10.
  Status: open
  Last-reviewed: 2026-06-08

- 2026-06-07 · code-review (via orchestrator) · [debt] · P2 — per-pane column capture is session-only: an app restart re-exposes the cross-backend column leak until first focus (PR #986)
  Details: Surfaced by the PR #986 pre-merge review. The column-isolation fix captures the pane's own column set at bind time (`pane.cachedColumns*`), but that capture is session state — after a restart an unfocused cross-backend pane has no capture, `ChoosePaneColumnsSource` falls to `SharedFallback`, and the pane briefly renders the focused view's columns again until its first focus. Bounded: cosmetic, self-heals on focus. Same review also catalogued the REMAINING fallback-leak consumers to clean up as a class when per-pane view resolution lands (Slice 4): column WIDTHS applied from the fallback view's `ColumnWidths` by key match, and the grid header toolbar's view-identity text showing the focused view's name in the unfocused pane.
  Concrete next action: either persist the captured column list per pane in `smatchet_panes.json` (capture at pane-load) or — better, Slice 4 — resolve each pane's own `ViewDefinition` from its backend bucket without bucket churn, which retires the frozen-capture mechanism AND the widths/header-text leaks in one move. Track under `docs/plans/shipped/multi-grid-tabs.md` Slice 4 alongside the catalog READ routing.
  Status: partially applied — Slice 4 (PR #1009) closed THREE of the four leaks. (a) Cold-start frozen-capture hole: closed by publishing each pane context's OWN resolved `ViewDefinition` (`GridLiveContext::resolvedOwnView`, populated by the first-sync worker from the pane's backend bucket) and building the pane's column SET from it in `resolvePaneColumns` — a restarted unfocused cross-backend pane now renders ITS OWN columns, not the focused view's. (b) Column WIDTHS + (c) header-identity text: now gated on the strict `activeViewForGrid->Id == pane.viewId` ownership predicate (same discipline as the #986 sort/columns gates) — a non-owned pane uses its own widths/defaults + its own `pane.title`, never the fallback's. The per-pane catalog VALUE-read routing (item 1, "the big one") was investigated and DEFERRED — see the P2 below; shipping it as written would have been a no-op for the cross-backend target case and a staleness regression for same-backend duplicate panes.
  Last-reviewed: 2026-06-08

- 2026-06-08 · grid-engine (via orchestrator) · [debt] · P2 — per-pane catalog VALUE-read routing blocked on per-context catalog POPULATION (multi-grid Slice 4 deferral)
  Details: Slice 4's plan called for routing each pane's cell-value / option-label / display-name resolution through ITS OWN `GridLiveContext::fieldCatalog`. Implementation was prototyped (`resolvePaneCatalog`, `GetPaneAvailableFields`, `ChoosePaneCatalogSource`) then REVERTED after review found the seam has no live data source: every catalog WRITE path (`SetFieldCatalog`, `ApplyStartupFieldCatalogSnapshot`, `SetAvailableUsers`, `HandleFieldCatalogError`) targets `fieldCatalog()` = the FOCUSED context. A non-focused CROSS-backend pane's context catalog is therefore never populated (only same-backend panes get the one-time seed copy in `EnsurePaneContextLive`; cross-backend panes "fill via the focused backend-switch catalog refetch" — i.e. only on focus). So routing would have (1) fallen back to the focused catalog for the cross-backend target case (rev 0) — a no-op — and (2) routed same-backend duplicate panes to their FROZEN seed copy, going stale whenever the focused catalog refetched — a regression vs the current shared-focused-catalog read.
  Concrete next action: populate each pane context's `fieldCatalog` independently first — seed cross-backend panes from the on-disk `FieldCatalogCache` snapshot for their backend at `EnsurePaneContextLive` (off-thread, mirrors the same-backend seed) and/or have the per-pane first-sync worker fetch the catalog into the pane's context (writing to the CONTEXT, not `fieldCatalog()`), with a per-context invalidation path so same-backend panes don't go stale. THEN re-land the read routing (`resolvePaneCatalog` + `ChoosePaneCatalogSource(paneFocused, paneCatalogRevision)`, resolve-once-per-pane, never per cell). This also requires the pane's endpoint config (Domain/PlaneUrl) to be available per-backend — verify the multi-backend config plumbing exists before fetching. Until then the user-visible "unfocused cross-backend pane shows raw field values until focused" defect remains (bounded: self-heals on first focus).
  Status: open
  Last-reviewed: 2026-06-08

- 2026-06-07 · code-review (via orchestrator) · [debt] · P2 — multi-grid Slice-3 catalog WRITE-target contamination on focus-switch-during-fetch (PR #975)
  Details: Surfaced by the PR #975 pre-merge review. Per-context field-catalog workers (per-project catalog refetch, component warm/lazy fetchers in `AppController_CatalogAndFieldEdit.cpp`) resolve `fieldCatalog()` (→ `focusedContextPtr_`) at COMPLETION time, so a focus switch during the fetch lands the write in whichever context is focused when the worker finishes — not the context the fetch was kicked for. The latch-once fix shipped on #975 makes this mutex-correct (lock and object always resolve to the same context — no UB / Pillar-3 race), but the write target can still be semantically wrong. Bounded blast radius: a stale/foreign catalog snapshot in one context, self-healing on that context's next own fetch.
  Concrete next action: capture the kick-time `GridLiveContext*` (or backend key) into each catalog worker and write through THAT context (validating it is not retired), instead of re-resolving focus at completion — Slice 4 work, alongside the per-pane catalog READ routing already deferred there (see `docs/plans/multi-grid-tabs.md` § Deviations).
  Status: open
  Last-reviewed: 2026-06-07

- 2026-06-07 · orchestrator · [debt] · P2 — `NormalizeViewsBackendKey` catch-all defaults every unknown tracker to "Jira" — now load-bearing for tickets_v2/queue cache isolation; the Linear backend would silently share Jira's namespace
  Details: `Source/Core/src/Config/ConfigManager_Views.cpp:235` maps only `plane`/`github`; anything else collapses to the default "Jira" bucket. Pre-multi-grid that was a cosmetic views-bucket choice; post-#948/#951 the same function keys `tickets_v2` and the pending queues, so an unmapped 4th backend (the planned Linear) lands its rows in Jira's namespace — the exact cross-backend collision Slice 1 exists to prevent. (CR sweep CR-948-4.)
  Concrete next action: extend the map for each new tracker as part of its backend bring-up checklist AND add a guard so the catch-all cannot silently absorb a new type (e.g. LOG_WARN + a doctest case asserting every `TrackerKind` enum value has an explicit mapping — the test fails when a backend is added without updating the map). Wire into `linear-tracker-backend.md` § prerequisites. Est ~30 min.
  Status: applied (2026-06-12, feat/debt-quickwins — guard shipped. `NormalizeViewsBackendKey` now has an explicit `jira` case + LOG_WARN when a NON-empty unmapped type falls to the default bucket (empty stays a silent pre-config transient). There is no `TrackerKind` enum (backends are string-typed), so the doctest guard was adapted to the existing `KnownBackendKeys()` invariant: `ConfigManagerViews.test.cpp` asserts every `KnownBackendKeys()` entry (both its key and lowercased form) normalizes back to itself — a new backend added to `KnownBackendKeys()` without an explicit `NormalizeViewsBackendKey` case falls to "Jira" != itself and the test FAILS, catching the silent collision at test time. The map-extension-per-backend checklist note for `linear-tracker-backend.md` is the remaining bring-up doc touch (deferred to that backend's work). To be archived to applied.md on next triage drain.)
  Last-reviewed: 2026-06-12

- 2026-06-07 · grid-engine (via orchestrator) · [debt] · P2 — `OfflineQueueService` replay workers capture raw `deps_.Reader()`/`Mutations()` pointers across async (same use-after-free class fixed twice on PR #945)
  Details: The #945 ADR-0012 audit fixed two raw-backend-pointer-across-async instances in AppController and found this third, PRE-EXISTING one at `Source/Core/src/Sync/OfflineQueueService.cpp:680/:1212`: replay workers capture raw `ITrackerIssueReader*`/`ITrackerIssueMutations*` (subobjects of the swappable backend) into background tasks. A backend swap mid-replay dangles them — Pillar-3 class. Out of #945's and 1c's scope because the sound fix changes `IOfflineQueueDeps` (latched `shared_ptr` accessors instead of raw subobject getters). Multi-grid Slice 3 (N live contexts, per-context swap) RAISES the likelihood — fix before or with S3.
  Concrete next action: widen `IOfflineQueueDeps` to expose a latched `std::shared_ptr<ITrackerBackend>` (or latched reader/mutations handles), derive subobjects inside the worker from the strong handle; mirror in `FakeOfflineQueueDeps.h`; bucket-A test: swap-during-replay does not crash + replay either completes against the latched backend or no-matches. Est ~1-2 h. Owner: offline-sync.
  Status: open
  Last-reviewed: 2026-06-07

- 2026-06-07 · orchestrator · [debt] · P2 — `backend-impl-coverage-recovery`: `JiraClient` impl TUs entered the coverage denominator largely uncovered (70% → 64%); raise backend-impl coverage so the `coverage-out-of-band` label class shrinks
  Details: PR #939 linked 5 real `JiraClient` implementation TUs into `SmatchetTests` (vtable requirement for the catalog-build fixture) — absolute coverage rose, but the line-rate dropped below the 65% floor and needed the freshly-implemented `coverage-out-of-band` label (#941, postmortems.md 2026-06-07). The newly-measured backend-impl surface is real product code with thin direct coverage: the catalog fixture exercises catalog endpoints only.
  Concrete next action: extend the scripted-HTTP fixture (`JiraCatalogHttpFixture.h`) pattern to the search/mutation/user-meta paths of `JiraClient` (and later Plane/GitHub when their impl TUs get linked) until the measured rate clears 65% with headroom; then remove the label from the class. Pairs with multi-grid Slice-0 WS2's pinned mapping-edge findings. Est ~3-4 h across 2-3 sittings.
  Status: open
  Last-reviewed: 2026-06-07

- 2026-06-06 · orchestrator · [debt] · P3 — Duplicated standalone bootstrap logic between `main.cpp` and `StandaloneAppBootstrap.cpp` (code-health, DRY)
  Details: Surfaced as a `[dup] WARN` by the duplication scanner during `shrink-over-100-line-functions` Batch E (PR #898). Decomposing `StandaloneAppBootstrap::Initialize` extracted `ConfigureGlfwWindowHints` / `SetupImGuiContext` / `ApplyUserDataEnvOverride` helpers, which now textually match the parallel inline bootstrap blocks in `Source/Standalone/main.cpp` (lines ~267/521 vs `StandaloneAppBootstrap.cpp` ~275/310). **Pre-existing duplication surfaced, not introduced** — the two bootstrap paths (windowed app vs ephemeral/headless) have long carried parallel GLFW-window-hint + ImGui-context + user-data-env-override setup. WARN-only under the WARN-first DRY calibration phase (ADR-0015), so non-blocking. Related to the older `light-release-unreal-default` item 10 follow-up (`applied.md` 2026-05-25) that DRY'd part of the boot path.
  Concrete next action: extract a shared `smatchet::standalone_boot` helper (GLFW window hints + ImGui context setup + user-data-env-override) into a small `*_detail.h`/`.cpp` seam, called by both `main.cpp` and `StandaloneAppBootstrap.cpp`. ~1 h, behaviour-preserving; clears the `[dup] WARN`.
  Status: open
  Last-reviewed: 2026-06-06

- 2026-06-04 · orchestrator · [debt] · P3 — Empty `catch(...){}` + catch-all-without-`LOG_` in `Source/Standalone/` CLI paths lack `// catch-all-ok:` markers (code-health)
  Details: Surfaced by the Stop-hook whole-file lint drain (`lint-cpp-drain.sh`) when PR #847 (warnings-as-errors) trivially touched the two files; **all pre-existing** — verified zero catch lines in #847's diff (its only edits were a dead-overload removal at `CliCommandRunner.cpp:549` + a `(void)forceMcp;` at `StandaloneAppBootstrap.cpp:340`). `Source/Standalone/CliCommandRunner.cpp` has ~22 `catch (...)` without a `LOG_*` call plus 3 **empty** `catch (...) {}` (lines 967, 1012, 1508); `Source/Standalone/StandaloneAppBootstrap.cpp` has 1 empty (302) + 1 without-LOG (374). Two shapes: (a) silent `std::stoi` parse-failure ignores — a malformed `--mcp-port` / `frames` arg falls back to its default with no diagnostic (`CliCommandRunner.cpp:967`, `StandaloneAppBootstrap.cpp:302`); (b) genuinely best-effort `try { EmitErrorToStderr(env); } catch (...) {}` emit fallbacks (`CliCommandRunner.cpp:1012`, `1508`). Light zone (`Source/Standalone/` is not strict-zone gated) so the delta gate grandfathers them, and CR reviews only the diff so it won't flag them either — hence they linger. Per AGENTS.md, empty `catch (...) {}` = review CRITICAL and a catch-all needs a `// catch-all-ok: <reason>` marker or a `LOG_` call.
  Concrete next action: dedicated `Source/Standalone/` CLI-hardening PR — (a) for the `std::stoi`-ignore sites, `LOG_WARN` (or emit a structured error) on the malformed arg instead of silently defaulting; (b) for the best-effort emit-fallbacks, add `// catch-all-ok: <reason>` markers. ~1 h, mechanical, no behaviour change for valid input.
  Status: open
  Last-reviewed: 2026-06-04

- 2026-05-28 · deep-audit · [debt] · P2 — `AppController` is a ~1110-line / ~150-method god-object spanning ~9 concerns (code-health)
  Details: `Source/Core/include/AppController.h` is ~1110 lines, ~150 public methods across tracker sync, field-catalog/field-edit + editmeta caching, offline create/field-edit queues, connectivity probing, Lua automation, AI assistant, MCP activity, attachments, app-update, and host callbacks. Owns 10+ `unique_ptr` subsystems + ~10 mutexes; implemented across 10 partial-class `.cpp` files (~7.5k LOC). Highest-coupling node — the natural merge-conflict + reasoning bottleneck. Decomposition is already underway and principled (`OfflineQueueService` / `TicketSyncService` / `LuaAutomationHost` extracted behind ISP `*Deps` interfaces with fakes-for-tests). Supersedes the stale `backlog/BACKLOG_CODE_REVIEW.md` N4 (predates the friend-coupling removal). Verified (deep-audit, adversarially confirmed: header 1110 lines, 10 mutexes, 10 partial TUs). Migrated from `bug.md` (ADR-0014) — code-health, not a user-observable defect.
  Concrete next action: continue the extraction — lift the connectivity-probe FSM and the field-edit/editmeta-cache cluster into their own services behind narrow `*Deps` interfaces; group the optional host-callback setters (OpenUrl / CloseEmbeddedUi / AttachmentViewer / OpenFilePaths / RequestAppQuit) into one `HostCallbacks` struct injected at `Initialize`. Target a thin facade delegating to owned services. Multi-PR. **Sub-task surfaced by the `ilocalcache-seam` plan (2026-06-10):** relocating the queue/dead-letter result structs (`DeadLetterRestoreSummary`/`*DeleteSummary`) + the TicketSync pack types out of `AppController.h` into `Source/Core/include/Sync/OfflineQueueTypes.h` is the route to *full transitive*-include SQLite purity for the sync-service tests — `OfflineQueueService.h:32` / `TicketSyncService.h:20` `#include "AppController.h"` → `LocalCacheManager.h` → SQLiteCpp is the residual transitive conduit ADR-0020 accepts as deferred.
  Status: open
  Last-reviewed: 2026-06-10

- 2026-05-28 · deep-audit · [debt] · P3 — Three minor architecture/coupling cleanups (code-health)
  Details: (1) Extracted services still depend on `AppController`'s nested result structs: `Source/Core/include/Sync/OfflineQueueService.h:32` `#include`s the full `AppController.h` and returns `AppController::DeadLetterRestoreSummary` / `*DeleteSummary` (:65-93) — behaviour is inverted via `IOfflineQueueDeps` but the data contract still lives on the god-object, so the service header can't compile standalone. (2) `Source/Core/src/PluginHost.cpp:124` constructs `McpPlugin` by name (`make_unique<McpPlugin>(port)`), the one core spot `IPlugin` isn't fully inverted — build-gated + ADR-0002-governed + dual-target-safe, but Source/Core isn't strictly plugin-agnostic. (3) `Source/Core/src/Tracker/DefaultTrackerBackendFactory.cpp:19-25` hides a synchronous `ConfigManager::Load()` disk-read inside `Create("github")` while the Jira/Plane branches construct arg-less — asymmetric, bakes credentials into the instance. All verified (deep-audit, adversarially confirmed).
  Concrete next action: (1) relocate the queue/dead-letter result structs to `Source/Core/include/Sync/OfflineQueueTypes.h` + re-export aliases on AppController for back-compat; (2) register a `PluginHost::SetMcpPluginFactory(...)` callback wired from bootstrap (mirror `ITrackerBackendFactory`); (3) pass a `TrackerConfig` snapshot into `Create(type, const TrackerConfig&)` or have `GitHubClient` read config lazily like Jira/Plane. Each ~1 h, independent.
  Status: open
  Last-reviewed: 2026-05-28

- 2026-05-18 · debug-detective · [debt] · P3 — `SmatchetLocalizedImGui::HookDictationOnLastItem` lives inline in a hot header
  Details: Adding `[temp-debug]` instrumentation to the hook required `#include "Logger.h"` + `<unordered_map>` in `Source/Core/include/SmatchetLocalizedImGui.h` — both contagious to every TU that pulls the wrapper. Long compile churn while iterating on the temp-debug spec; non-trivial cleanup risk (one missed include leaks Logger into hot paths). Migrated from `bug.md` (ADR-0014) — a header-hygiene / compile-time concern, no user-observable defect.
  Concrete next action: split `HookDictationOnLastItem` out into a thin `Source/Core/src/SmatchetDictationHook.cpp` with the impl out-of-line behind a forward-declared free function in the header (signature unchanged: `void HookDictationOnLastItem(char*, std::size_t)`). Future debug instrumentation lives in the .cpp without touching every includer. ~1 h.
  Status: open
  Last-reviewed: 2026-05-18

- 2026-05-17 · code-review · [debt] · P2 — `LuaToJson` / `JsonToLua` + `kJsonToLuaMaxDepth` duplicated verbatim across `AppController_LuaBindingsCore.cpp` ↔ `AppController_LuaBindings.cpp`
  Details: File comment names the duplication as intentional (post-split keeps Core ImGui-free). Drift risk on the next marshalling change. Migrated from `bug.md` (ADR-0014) — duplication, no user-observable defect.
  Concrete next action: lift to a shared internal header reachable from both TUs. Surfaced by retrospective code-review sweep on PR #144.
  Status: open
  Last-reviewed: 2026-05-17

- 2026-06-11 · grid-engine · [debt] · P3 — `selectedFieldsBuf` is now write-only dead state after the #views-field-uncheck set-authority fix
  Details: The Views editor's selected-field `char[1024] selectedFieldsBuf` (`Source/Core/include/Ui/SmatchetUiSession.h:427`) was demoted to a write-only debug snapshot when the authoritative `selectedFieldSet` landed (PR for issue #1143). Zero readers / renderers remain (the 6 former `ParseCsv(selectedFieldsBuf)` authority reads all migrated to the set); `SyncSelectedFieldsBuffer` still serializes into it on every toggle purely for debugger inspection + a truncation LOG_WARN tripwire. 1 KB/session + a serialize per mutation for no functional value (fable code-review MEDIUM).
  Concrete next action: remove `selectedFieldsBuf` + `SyncSelectedFieldsBuffer` + its callers (the grid `CopyStringToBuffer` write at `SmatchetActiveProjectGridUi.cpp:789` too); drive everything off `selectedFieldSet`. ~30 min, mechanical. The TODO marker is in the header comment.
  Status: applied (2026-06-12, feat/debt-quickwins — removed `selectedFieldsBuf` (the `char[1024]` member), the `SyncSelectedFieldsBuffer` decl + def + all 5 callers (1 in LoadBuffersFromView + 4 toggle/select-all/clear sites in SmatchetViewsDashboardUi.cpp), and the grid `CopyStringToBuffer` write in SmatchetActiveProjectGridUi.cpp; everything drives off the authoritative `selectedFieldSet`. The pure `SerializeSelectedFields` helper was LEFT (tested inline in a header, harmless unused-in-prod). grep confirms zero remaining references. To be archived to applied.md on next triage drain.)
  Last-reviewed: 2026-06-12
