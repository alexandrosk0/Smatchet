- 2026-06-19 · claude (dx12-arm64 ship session) · [tooling] · P3 — `arm64-perf-baseline-commit-blocked-on-sandbox-egress`: the `ci-windows-arm64` perf baselines are bootstrapped GREEN in CI (#1424) and uploaded as a run artifact, but they cannot be FETCHED + committed from the remote-execution sandbox because the egress allowlist rejects the GitHub artifact storage host
  Details: dx12-standalone-win-arm64 Phase 3 follow-on #1424 added a `perf-baseline-bootstrap.py --host ci-windows-arm64` step to the `windows-arm64-native` leg; it seeds the six `docs/perf/baselines/*.ci-windows-arm64.json` files on a real `windows-11-arm` runner and uploads them as the `perf-arm64-baselines-<run_id>` workflow artifact (the leg is green, so the baselines are valid). To commit them, the artifact must be downloaded into the tree — but `mcp__github__download_workflow_run_artifact` returns a signed Azure blob URL (`*.blob.core.windows.net`), and `curl` against it from this sandbox returns HTTP 403 "Host not in allowlist: productionresultssa15.blob.core.windows.net. Add this host to your network egress settings to allow access." The sandbox egress policy only permits `api.github.com`-class hosts (which is why unauthenticated check-run polling works) but not the artifact blob-storage backend. So the user's explicit "fetch + commit them now" choice is not executable from here: the data exists and is correct, only the transport is blocked. This is the same egress-class limitation that blocks any artifact-content download in this environment, surfaced here for the first time on a perf-baseline commit.
  Owner decision (2026-06-19): **leave the seed files for human review** — do NOT treat this as an urgent egress-unblock. The first ARM64 baselines are a single CI sample and should land via a human-reviewed bootstrap PR (matches the dx12-standalone-win-arm64 Phase 4 row-13 design: "the future ARM64 perf job passes `--host=ci-windows-arm64` and bootstraps fresh baselines"). So this is deferred BY DESIGN, not stuck; the egress wall just confirms it would be a human commit either way.
  Concrete next action (when a human picks it up, low urgency): (b) download the `perf-arm64-baselines-<run_id>` artifact from the Actions UI and commit the six `docs/perf/baselines/*.ci-windows-arm64.json` files after eyeballing them; OR (a) if an agent is to do it, first add `*.blob.core.windows.net` (or the specific `productionresultssa15.blob.core.windows.net`) to the environment's network egress allowlist, then fetch + commit. Until the seeds land the ARM64 perf job runs against freshly-bootstrapped baselines each time (no regression gating) — acceptable for now. The ARM64 perf PATH itself is already proven by the green bootstrap run; this is purely a seed-file commit. Est ~10 min. Cross-ref: dx12-standalone-win-arm64 § Verification (actual) Phase 3 (the deferred-by-design residual); #1424 (the bootstrap step + artifact upload); applied.md 2026-06-18 `windows-11-arm-runner-for-arm64-binary-validation` (the parent item, resolved).
  Status: deferred (by design — awaiting a human-reviewed bootstrap PR; not blocking)
  Last-reviewed: 2026-06-19
