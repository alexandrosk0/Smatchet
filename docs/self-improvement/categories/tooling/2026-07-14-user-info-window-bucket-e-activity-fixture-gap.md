- 2026-07-14 · test-author · [tooling] · P2 — `user-info-window-bucket-e-activity-fixture-gap`: the User Info window's bucket-E suite (`tests/ui/user_info_window.test.cpp`, 6/6 green) covers 5 of the 7 named behaviours from `docs/plans/shipped/user-info-window.md` § Verification deterministically, but **behaviours 3 (activity Load/Reload button disabled while loading) and 4 (one-shot group-member fetch per open) are unreachable** because both live behind `SmatchetUserInfoUi::supportsActivity_`, which is `PaneSupportsActivity(paneId) == (backend->Activity() != nullptr)`. The only bucket-E backend is `FakeTrackerClient` (`tests/support/FakeTrackerClient.h`), whose `Activity()` returns `nullptr` — so `supportsActivity_` is always `false` under the harness and the Activity + Groups sections are runtime-guarded out. Same reason the **behaviour-2 `ClearPaneUserActivity` call-count spy** (fired-exactly-once-on-close-edge, never-on-a-never-opened-clear) is covered only at the observable close-edge/no-crash level, not as a backend call-count assertion.
  Concrete next action: give the bucket-E fixture an activity-capable backend — either (a) add a minimal `ITrackerActivity` implementation to `FakeTrackerClient` (scripted `FetchPaneUserActivity` / `FetchUserGroupNames` / `FetchPaneGroupMembers` results + a `ClearUserActivity` call counter exposed via a test accessor), or (b) a second fixture client that returns non-null `Activity()`. Then extend `user_info_window.test.cpp` with: (3) assert the Load button is `BeginDisabled(activityLoading_)` while a fetch is in flight (drive via the scripted-slow-fetch seam); (4) assert `launchMembersFetch` fires at most once per group per open (call-count on the fixture); and upgrade behaviour-2 to a hard `ClearPaneUserActivity`-count assertion. Est ~0.5 d once the fixture Activity() lands. Cross-ref: `docs/plans/shipped/user-info-window.md` § Verification (actual); parent entry `user-info-window-bucket-e-coverage` (this one is its activity-gated residual); roadmap PR-10.
  Status: open
  Last-reviewed: 2026-07-14
