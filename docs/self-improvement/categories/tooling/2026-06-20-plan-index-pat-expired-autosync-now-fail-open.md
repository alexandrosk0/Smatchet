- 2026-06-20 · orchestrator · [tooling] · P3 — `plan-index-pat-expired-autosync-now-fail-open`: the `PLAN_INDEX_PAT` repo secret is absent/expired, so `doc-validation.yml`'s non-required `Auto-sync plan INDEX (PR branch)` job hard-redded every `docs/plans/shipped/`-touching PR (credentialed checkout died with `could not read Username`); mitigated in #1463 by making the checkout fail-open, but the auto-sync-and-push CONVENIENCE stays lost until an admin rotates the secret.
  Details: on #1463 (plan-lock-enforcement archival — the first `shipped/`-touching PR since the PAT lapsed) the autosync job failed. Its `Checkout PR head branch` step uses `token: ${{ secrets.PLAN_INDEX_PAT || github.token }}`, and a set-but-EXPIRED PAT is truthy, so the `||` keeps it (no GITHUB_TOKEN fallback) → `fatal: could not read Username for 'https://github.com': terminal prompts disabled`. The job header already documented this failure mode but only guarded the pre-checkout SKIP path (PRs that do NOT touch `shipped/`); a `shipped/`-touching PR still reached the credentialed checkout and hard-redded. Fixed the SYMPTOM in #1463 (merge f2100d44): the checkout is now `continue-on-error` with `id: checkout`, the Set-up-Python / regen / push steps gate on `steps.checkout.outcome == 'success'`, and a `::warning::` step explains the skip — so an expired/absent PAT degrades the advisory auto-FIXER to a green no-op instead of a hard red. INDEX correctness is unaffected: the REQUIRED `test-plan-index` check (validate mode, in the `doc-anchors-and-agent-contract` job) still hard-gates drift. What remains is the ROOT cause — the secret itself is dead, so future `shipped/` archivals get no auto-synced INDEX commit; the author must run `bash agents/scripts/core/test-plan-index.sh --fix` and push by hand (exactly what #1463 did).
  Concrete next action: a repo admin rotates `PLAN_INDEX_PAT` — a fine-grained PAT scoped to this repo with `Contents: Read and write` (enough to push to an unprotected PR branch; no other scope), saved as the `PLAN_INDEX_PAT` Actions secret (see the `autosync-plan-index` job header in `.github/workflows/doc-validation.yml`). No code change needed: the fail-open guard auto-re-enables the auto-fix path once the secret is valid (`PAT_CONFIGURED` flips true). Optional hardening: add a cheap "is the PAT live?" probe step so an EXPIRED secret annotates distinctly from an ABSENT one (today both look the same to the `||` fallback).
  Cross-ref: #1463 (mitigation, merge f2100d44) + #1454 (the plan it archived); `.github/workflows/doc-validation.yml` `autosync-plan-index` job (checkout token + `PAT_CONFIGURED` gate); the required `test-plan-index` step in the `doc-anchors-and-agent-contract` job; `agents/scripts/core/test-plan-index.sh --fix`.
  Status: open
  Last-reviewed: 2026-06-20
