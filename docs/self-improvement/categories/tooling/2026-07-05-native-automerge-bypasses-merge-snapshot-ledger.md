# Agent self-improvement — tooling (entry)

- 2026-07-05 · orchestrator (recurring-findings gate campaign #1605 ship session) · [tooling] · P2 — `native-automerge-bypasses-merge-snapshot-ledger`: a PR merged via GitHub's native auto-merge appends no line to `docs/self-improvement/merge-snapshots.jsonl`, so the ADR-0017 lossless merge-time capture has a hole for exactly the merge path the standing auto-merge grant encourages.
  Details: ship-loops.md § merge-snapshot mandates the append for the two enumerated actors (in-session orchestrator REST merge, `git-janitor`), and `merge-watcher` does it in `handle_pass()` — but a session that arms **GitHub-native auto-merge** (`gh pr merge --auto` / the auto-merge API, as the #1605 session did under an explicit user "merge autonomously" grant) is none of the three: the merge fires server-side after the session may have gone idle, and nothing appends the snapshot. Observed: the ledger's newest line is 2026-06-19 (#1429) while dozens of PRs merged since — the gap is systemic, not one session's miss. The data ADR-0017 wants captured (override labels, red-check set at the merge instant) is exactly what GitHub then overwrites/strips, so retro-fill is lossy by definition.
  Concrete next action: pick one — (a) extend `merge-snapshot-append.sh` into a small post-merge sweeper (cron or `lock-cleanup.yml`-style workflow on `pull_request: closed` + merged==true) that appends a best-effort snapshot line with `mergeActor:"github-auto-merge"` and a `retro:true` field when the merge event fires, or (b) have ship-loops.md forbid native auto-merge in favor of watcher registration (which already snapshots) whenever the grant is in play. (a) keeps the native path usable; schema bump to 2 if `retro` is added.
  Status: open
  Last-reviewed: 2026-07-05
