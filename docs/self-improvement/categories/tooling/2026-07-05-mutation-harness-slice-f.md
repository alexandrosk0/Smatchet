- 2026-07-05 · orchestrator (mutation-testing pilot) · [tooling] · P2 — the mutation pilot built a small, reusable single-point-mutation harness that is a ready seed for roadmap Slice **F** (mutation-smoke / coverage-delta gate, `testing-surface-roadmap.md` — status `[new] unbacklogged`, plan-required, unstarted)
  Details: the harness drives a JSON spec of `{file, search, replace}` mutants against `SmatchetTsanTests` — for each: assert `git` tree clean → apply exact single-point edit → `cmake --build --preset ninja-tsan-linux` (incremental: 1 TU recompile + relink) → run the exe → classify KILLED/SURVIVED/BUILD_FAIL → `git checkout` revert → re-assert clean. At ~0.6 s/run + ~2–8 s incremental build, a 68-mutant sweep is a few minutes; the whole pilot ran with zero residual tree dirt. This is exactly the "flip-and-rerun mutation smoke on the hottest pure files (expose no-op assertions)" half of Slice F's scope; the pilot demonstrates it is cheap and deterministic on the doctest rig.
  Concrete next action: when Slice F is greenlit, (1) promote the harness to `scripts/dev/mutation-smoke.sh` (+ a curated per-TU mutant corpus, seeded from the equivalence-vetted mutants in `MUTATION_PILOT.md`), (2) run it advisory-nightly over the ~17 dedicated-test TUs in the TSan rig, gating on a kill-rate floor rather than per-line coverage (catches assertion rot the coverage-delta gate structurally cannot see), (3) keep the equivalent-mutant exclusion list (DT2/DT5/JQL-01/MAP-05/Labels-m3) so the floor isn't gamed by unkillable mutants. The pilot's harness + specs live in the session scratchpad; regenerate from `MUTATION_PILOT.md` diffs when productionising. Cross-ref: `MUTATION_PILOT.md` § Reproducing; `testing-surface-roadmap.md` Slice F.
  Status: open
  Last-reviewed: 2026-07-05
