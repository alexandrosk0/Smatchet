- 2026-07-04 · orchestrator (CPP_CODE_AUDIT.md remediation, #1593) · [tooling] · P2 — `AskUserQuestion` failed twice mid-session with a transport error ("Tool permission request failed: Error: Tool permission stream closed before response received"), with no retry path, forcing the orchestrator to fall back to unilateral judgment on genuinely architecturally-significant decisions
  Details: two separate findings (CPP_CODE_AUDIT.md #21 — whether to build a validity-token mechanism for a currently-unreachable dictation-buffer UAF vs. accept as documented risk; #26 — whether to rewrite `SubprocessCapture.cpp`'s fork/exec path to be fully async-signal-safe vs. accept as documented risk) were exactly the class of decision the orchestrator's own instructions say should route through `AskUserQuestion` rather than be decided unilaterally (interface changes with real regression risk, no clear "more correct" answer, low reachability today). Both calls failed identically with a stream-closed transport error before any response was recorded — not a user decline (which returns a normal "user denied" result the orchestrator can react to), but a hard tool-layer failure with no distinguishable cause and no documented retry semantics. Per the user's standing "continue autonomously" instruction, the orchestrator treated this as "asking is unavailable this turn," picked the lower-risk/non-rewrite option matching what it would have recommended as the default, and documented the reasoning inline at both code sites plus in the tracking plan doc's Deviations section — but this is a workaround, not a fix: a session that hits this failure with no fallback default in mind, or on a decision with no safe conservative option, has no path forward except guessing or stalling.
  Concrete next action: investigate why `AskUserQuestion` returned a stream-closed error rather than either succeeding or surfacing a distinguishable "tool unavailable" vs "user dismissed" result — check whether this is a client-side timeout, a malformed request (e.g. question/option payload size or shape), or an environment-specific transport issue in the remote/headless session type this ran under. If it's a transient/environment issue, document a standard retry-once-then-fall-back-to-documented-default pattern for agents so the workaround used here becomes the prescribed behavior rather than an ad hoc improvisation. If it's a payload issue, tighten `AskUserQuestion` input validation or the calling convention to avoid triggering it.
  Update (2026-07-04): the documentation half is done — `docs/agent-rules/ship-loops.md` § Escalate, don't assume now has a "When the escalation call itself fails" paragraph specifying retry-once, then pick the most conservative/reversible option and document the reasoning explicitly at the decision site (plan-doc Deviations and/or a code comment), rather than stalling or silently guessing. The root-cause investigation (why the transport call failed — client timeout, payload shape, environment-specific) is outside this repo's code/docs and remains open; this entry stays open until that's actually looked into (or someone with harness-level access confirms it's a known/transient issue).
  Status: open (fallback pattern documented 2026-07-04; root-cause investigation still open, needs harness-level access this repo doesn't have)
  Last-reviewed: 2026-07-04
