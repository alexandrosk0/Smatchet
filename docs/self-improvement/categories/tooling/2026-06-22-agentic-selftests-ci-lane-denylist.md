- 2026-06-22 · orchestrator (agentic-infra review — C1) · [tooling] · P2 — the new `Agentic self-tests` CI lane (`.github/workflows/agentic-selftests.yml` → `scripts/dev/test-all.sh --ci`) denylists a handful of suites that fail when run headless on the Linux runner; each should be hardened to run there and removed from `CI_SKIP_RE` so the lane covers the full agentic suite.
  Details: C1 wired the agentic self-test aggregator (previously run by nothing) into CI. `--ci` mode treats a missing built `Smatchet.exe` (exit 2) as a clean skip and denylists, via `CI_SKIP_RE` in `scripts/dev/test-all.sh`, the suites still red in a headless Linux env. Denylist entries, updated 2026-07-08:
    1. RETIRED — `test-merge-watcher-bats` (was 5/112) + `test-merge-watcher-integration-bats` (was 4/4). Root cause was NOT the gh seams (already stubbed): both suites asserted on the WINDOWS watcher root (`$LOCALAPPDATA/Smatchet/merge-watch`) while POSIX Python writes `$XDG_STATE_HOME/smatchet/merge-watch`; the integration setup additionally never sandboxed `XDG_STATE_HOME`, so registry state leaked into the real `$HOME/.local/state` across runs. Both bats now derive a per-OS `WATCH_ROOT` and sandbox both env vars. The 5th unit failure (`maybe_auto_act` dedup) needed the claude-on-PATH / clean-clone / live-session seams stubbed (as its sibling budget test already did).
    2. RETIRED — `test-pr-status-watch-bats`: the one red case ("--once REDs an allow-listed non-advisory failure even when non-required") was fixed by the block-on-any-red rework (PR #1619); suite is 13/13 headless.
    3. RETIRED — `test-lint-rules-bats`: the `comment_audit.py --fix HEAD` case was a TEST-FIXTURE issue, not a `--fix` gap — the fixture's lone bare `//` sat between two textual comment lines, i.e. the allowed intra-block paragraph separator `--fix` deliberately keeps; the fixture now appends a genuine 2-line blank run. A second red (scan-pr-comments clean-tree) was a real PR-numbered temporal comment that drifted into `SmatchetAiAssistantUi.cpp`; reworded. Residual (c) is settled: no `--fix` bug to split out.
    4. RETIRED — `test-bucket-lane-launch-smoke-bats`: did NOT need the Mesa GL bucket harness — the live-stub wrote placeholder bytes (`PNGSTUB`) that the dock-gap-sentinel pink-pixel scan could not stb_image-load; the stub now emits a real decodable 1x1 PNG.
    4b. OPEN — `test-guard-head-drift-bats` / `test-guard-plan-lock-bats` / `test-session-registry-bats` — one assertion each fails on the headless ubuntu CI checkout (git-worktree / PID-liveness / run-as-non-root sensitivity) but passes locally. 2026-07-08 repro attempts on a Linux container: all three suites pass in-worktree as root, AND as a non-root user in a fresh detached-HEAD clone with `setup-harness.sh claude-code` provisioned — still not reproducible. The June runner logs that held the actual `not ok` lines are no longer retained (earliest retained `agentic-selftests.yml` run is 2026-07-05, post-denylist), so the remaining path is to capture a fresh failure from a real runner (e.g. a scratch branch that temporarily un-skips the 3 suites on a non-required lane).
    5. `test-plan-index` / `test-docs` — need full git history (shallow-clone drift); already gated on full history by `doc-validation.yml`, so denylisting here loses no coverage (kept off to avoid a duplicate red).
    6. `test-doctor` — dev-env doctor demanding the Windows C++ toolchain (`cl.exe`/`clang-cl`); not an agentic gate.
  Concrete next action: (a) APPLIED by PR #1619 (block-on-any-red, 2026-07-05) — `Agentic self-tests (bats)` is in `project.config.json` § `branch_protection.required_contexts` and `setup-branch-protection.sh` was applied post-merge (`docs/plans/all-gates-blocking.md` § checklist); (b) APPLIED 2026-07-08 — denylist entries 1-4 hardened + retired from `CI_SKIP_RE` (merge-watcher watcher-root/env sandboxing, pr-status-watch re-verified green, lint-rules fixture + tree sweep, bucket-lane real-PNG stub); (c) SETTLED 2026-07-08 — the comment_audit `--fix HEAD` finding was a fixture bug, no `--fix` gap to split out; (d) capture a real runner `not ok` line for the 3 suites in 4b (scratch branch with a temporary un-skip on a non-required lane), then harden + retire them.
  Status: open (residual: (d) — the 3 runner-checkout-sensitive suites in 4b)
  Last-reviewed: 2026-07-08
