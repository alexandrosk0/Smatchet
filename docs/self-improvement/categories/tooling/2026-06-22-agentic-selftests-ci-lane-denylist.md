- 2026-06-22 · orchestrator (agentic-infra review — C1) · [tooling] · P2 — the new `Agentic self-tests` CI lane (`.github/workflows/agentic-selftests.yml` → `scripts/dev/test-all.sh --ci`) denylists a handful of suites that fail when run headless on the Linux runner; each should be hardened to run there and removed from `CI_SKIP_RE` so the lane covers the full agentic suite.
  Details: C1 wired the agentic self-test aggregator (previously run by nothing) into CI. `--ci` mode treats a missing built `Smatchet.exe` (exit 2) as a clean skip and denylists, via `CI_SKIP_RE` in `scripts/dev/test-all.sh`, the suites still red in a headless Linux env. After the wiring the lane is green at 1156 assertions with 49 skips. Open denylist entries to retire:
    1. `test-merge-watcher-bats` (5/112 fail) + `test-merge-watcher-integration-bats` (4/4 fail) — registry/state-machine + stubbed `gh`/squash-merge seams return non-zero headless (a clean writable `HOME`/`XDG_STATE_HOME` did NOT fix them, so it is not just state-dir resolution). Needs the watcher seams stubbed/mocked so the state-machine tests run without live `gh`.
    2. `test-pr-status-watch-bats` (1/13: "--once REDs an allow-listed non-advisory failure even when non-required") — needs a `gh`/API fixture.
    3. `test-lint-rules-bats` (1/57: "comment_audit.py --fix strips a new blank-comment run but keeps prose AND code-like", `tests/bats/lint_rules.bats:537`) — `comment_audit.py --fix HEAD` did not strip the bare `//` in the fixture; determine whether `--fix` has a real gap or the test's `git`-diff setup is the issue. The same lint rules are still gated by `test-lint-rules.sh` in `doc-validation.yml`, so coverage is not lost meanwhile.
    4. `test-bucket-lane-launch-smoke-bats` (1/4) — needs the Mesa GL bucket harness.
    4b. `test-guard-head-drift-bats` / `test-guard-plan-lock-bats` / `test-session-registry-bats` — one assertion each fails on the headless ubuntu CI checkout (git-worktree / PID-liveness / run-as-non-root sensitivity) but passes locally and does NOT reproduce under detached-HEAD or identity-stripped local runs; the rest of each suite passes. `test-lint-hook-split` was in this bucket too until the lane began running `setup-harness.sh claude-code` (it needs the provisioned `.claude/hooks`); the same provisioning did NOT fix these 3, so the cause is elsewhere — needs the actual `not ok` line from a runner (the failing blocks sit at the top of the CI log, past `tail`).
    5. `test-plan-index` / `test-docs` — need full git history (shallow-clone drift); already gated on full history by `doc-validation.yml`, so denylisting here loses no coverage (kept off to avoid a duplicate red).
    6. `test-doctor` — dev-env doctor demanding the Windows C++ toolchain (`cl.exe`/`clang-cl`); not an agentic gate.
  Concrete next action: (a) APPLIED by PR #1619 (block-on-any-red, 2026-07-05) — `Agentic self-tests (bats)` is in `project.config.json` § `branch_protection.required_contexts` and `setup-branch-protection.sh` was applied post-merge (`docs/plans/all-gates-blocking.md` § checklist); (b) retire denylist entries 1-4 as each suite is made headless-runnable; (c) consider folding the comment_audit `--fix HEAD` finding (entry 3) into its own bug if confirmed a real `--fix` gap.
  Status: open (residual: (b) + (c))
  Last-reviewed: 2026-07-07
