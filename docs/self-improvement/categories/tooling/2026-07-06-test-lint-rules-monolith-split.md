- 2026-07-06 · orchestrator (agentic-infra audit 2026-07) · [tooling] · P2 — `test-lint-rules.sh` (~139 KB) and `merge-gates.sh` (~97 KB) are monoliths an agent cannot review as diffs
  Details: the two most load-bearing harness scripts — the strict-zone lint scanner and the merge-gate poller — plus `build-and-test.yml` (~132 KB) are each effectively unreviewable in one sitting and are exactly the files an agent most needs to understand before trusting or extending a gate. The lint scanner at least carries `--selftest` + bats; the workflow has nothing equivalent. The shipped `split-scripts-build-vs-agentic` plan separated the *trees*, not the monolith internals. AGENTIC_INFRA_AUDIT.md finding C2.
  Progress 2026-07-08: the `test-lint-rules.sh` half LANDED — decomposed from 2267 lines into a ~1030-line entry point (CLI, zone config, mode dispatch, unchanged contract) + 12 per-rule-family sourced modules under `agents/scripts/project/lint-rules.d/` (fail-closed explicit load list; modules resolve relative to the script so the `--diff` base-worktree scan keeps using the current scanner). Behaviour-preserving: every mode's output diffed byte-identical before/after (incl. `--selftest`, `--catalog`, `--full`, all `--scan-*`, `--diff origin/develop`); shell-lint target set + build-and-test.yml path-routing arms extended to cover `lint-rules.d/`; bats added for the fail-closed load + script-relative module resolution.
  Concrete next action: the `build-and-test.yml` reviewability gap (the last of the three C2 legs) — the workflow still has nothing equivalent to a `--selftest` + bats net. Effort M (remaining).
  Note (2026-07-13, agentic-infra-audit-review): the `merge-gates.sh` half (1506 lines, no `--selftest`, guarded by 164 `tests/bats/merge_gates.bats` cases) was DELIBERATELY NOT attempted in the Linux-container session — its only correctness net is "bats green at each step" and `bats` is absent there (the C3 environment-parity gap). Left open for a `windows-dev`/`ci` tier so the byte-identical-behaviour claim could be proven, not asserted.
  Progress 2026-07-13 (windows-dev): the `merge-gates.sh` half LANDED. Decomposed from 1506 → 1245 lines into a thin entry point + two fail-closed sourced modules under a new `agents/scripts/core/merge-gates.d/` (explicit load list, script-relative via `BASH_SOURCE`, mirroring `lint-rules.d/`): `00-common.sh` (block-on-any-red allow-list constant + prompt-shim lazy-source + standalone `gh_pr_ready_idempotent`) and `10-gate-filter.sh` (the 31-field GATE_FILTER jq projection, relocated VERBATIM into a single-quoted `_MG_GATE_FILTER_TEMPLATE` global the entry point copies byte-for-byte). `tests/bats/merge_gates.bats` held **164/164** green at each extraction step; every CLI mode diffed byte-identical stdout+stderr+exit vs pristine `origin/develop`. The four gate-condition verdicts (CI/CR/Bugbot/user-comments) stayed INLINE — they are one indivisible stateful poll loop sharing ~15 per-poll locals + the `nudge_coderabbit` closure + cross-iteration streak counters, so threading them through a function boundary is the subtle-behaviour risk the split must not take; the win is measured at the source (the two biggest reviewable chunks — the 218-line jq program + the top-level helpers — are now named modules). The `#1428` freshness self-check stayed in the entry-point function so `hash-object "${BASH_SOURCE[0]}"` still resolves to `merge-gates.sh`. Coupled gate fixed same-PR: `test-oob-label-impl.sh` (which greps the `$downgraded` jq region + label bindings) extended to fall back to the sibling module; its 9-case selftest + real-tree parity pass. **`--selftest` bonus NOT added** — the file stays `source`-able (no top-level `exit`), so a `--selftest` would need a guarded direct-invocation path; deferred as its own small item if wanted.
  Concrete remaining action: `build-and-test.yml` reviewability gap only (the merge-gates + lint-rules legs are both done).
  Status: open (build-and-test.yml leg only; test-lint-rules + merge-gates legs DONE)
  Last-reviewed: 2026-07-13
