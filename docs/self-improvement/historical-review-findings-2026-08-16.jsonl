{"batch": "batch-20-redo", "file": "docs/self-improvement/historical-review-findings.md", "fix": "Reclassify #1831 (updater revocation checking disabled) and #1840 (consent TOCTOU) as userVisible:true per ADR-0014 and open GitHub Issues, or state explicitly in the Batch 19 header why a signature-revocation bypass is out of user-visible scope.", "line": 378, "pr": 1878, "problem": "ADR-0014 classification gap in the Batch 19 entry: line 370 declares \"0 user-visible ... NO GitHub Issues; backlog only\", yet two of the five recorded findings are shipped-product security defects rather than internal debt. #1831 records that AttachmentAppUpdateService sets WTD_REVOKE_NONE, so the auto-updater accepts a known-revoked signing cert (logged LOW, userVisible:false); #1840 records a Lua consent TOCTOU that can execute unapproved script bytes, and the ledger itself hedges it as an elevation candidate. Neither enters the Issue tracker, so both survive only as prose in this ledger.", "severity": "MEDIUM", "sha": "8a2e2457", "userVisible": false}
{"batch": "batch-20-redo", "file": "Source/Core/src/TicketGridSortPure.cpp", "fix": "In ParseWholeDouble, reject non-finite results (return false unless std::isfinite(out)); in CompareNumericValues decide with `if (da < db) return -1; if (db < da) return 1; return 0;` instead of `da != db`.", "line": 99, "pr": 1879, "problem": "CompareNumericValues' double path accepts NaN: strtod parses \"nan\"/\"NaN\" (and \"inf\") as a whole-string match, so for a \"number\"-typed column with two NaN cells `da != db` is true and `(da < db)` is false, returning +1 for BOTH Compare(a,b) and Compare(b,a). That comparator is fed straight into std::stable_sort in Source/Core/src/Ui/SmatchetActiveProjectGridTable.cpp:205, violating strict weak ordering (UB / nondeterministic grid order).", "severity": "MEDIUM", "sha": "2d347dbf", "userVisible": true}
{"batch": "batch-20-redo", "file": "Source/Core/src/TicketGridSortPure.cpp", "fix": "Short-circuit at the top of IsTrackerDateOrDateTimeField with `if (kTimeTrackingFieldIds.count(fieldId)) return false;` (the set is already file-local), mirroring the ordering CompareFieldValuesForSort already uses.", "line": 165, "pr": 1879, "problem": "The date-like word heuristic `idLower.find(\"time\") != npos` matches every time-tracking id the same file deliberately special-cases (timespent, timeestimate, timeoriginalestimate, aggregate*), so IsTrackerDateOrDateTimeField returns true for duration columns. Callers then treat them as dates: TicketGridModel.cpp:204 sets IsDateLike, and SmatchetGridUiSupport.cpp:359 routes copy through DisplayValueForTrackerDateField for a raw seconds value.", "severity": "MEDIUM", "sha": "2d347dbf", "userVisible": true}
{"batch": "batch-20-redo", "file": "Source/Core/src/TicketGridSortPure.cpp", "fix": "Reject up front when the string is empty or `std::isspace((unsigned char)s[0])`, and for the double path reject a leading \"0x\"/\"0X\" prefix (or use a stricter parse), so the comment's contract holds.", "line": 33, "pr": 1879, "problem": "Both helpers are documented as a \"whole-string numeric parse\" but only check the end pointer, so strtoll/strtod's own leading-whitespace skip and sign handling make \" 5\" and \"+5\" parse as numeric, and strtod additionally accepts C99 hex floats (\"0x10\" sorts as 16 on number-typed columns while the int64 path rejects it). Sorting silently disagrees with the displayed text.", "severity": "LOW", "sha": "2d347dbf", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/core/test-agent-contract.sh", "fix": "Split the two cases: only `continue` when the agent legitimately has no claude-code hint block at all; when either key is present-but-partial, or when both are absent on a non-README agent, record a mismatch (e.g. `model_mismatch+=(\"$base: no model: key — agent will inherit the session model\")`) instead of skipping.", "line": 440, "pr": 1881, "problem": "Check 15 fails OPEN on the case it exists to catch: `[[ -z \"$hint_model\" ]] && continue` skips any agent whose harness-hints claude-code model is missing, and the check never asserts that a top-level `model:` exists at all. Check 5 skips on the same condition (line 186), so an agent file with NO model keys passes every gate and silently inherits the opus-class session model — the exact regression this PR was written to prevent. The check's own header comment (lines 427-431) says \"Both must exist and agree\".", "severity": "HIGH", "sha": "2966edc7", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/core/test-agent-contract.sh", "fix": "Anchor the extraction to the claude-code block, e.g. `awk '/^ claude-code:/{c=1;next} c&&/^ [^ ]/{c=0} c' \"$f\" | grep -m1 -E '^ model:'` (same fix applies to the check-5 copy at lines 184-185).", "line": 439, "pr": 1881, "problem": "`awk '/^harness-hints:/,/^---/' | grep -E \"^ model:\" | head -1` is not scoped to the `claude-code:` sub-block — it takes the first 4-space-indented `model:` under harness-hints regardless of harness. `.coderabbit.yaml` (lines 156-158, also touched by this PR) explicitly documents `harness-hints.<harness>:` blocks such as `harness-hints.claude:` as intentional, so the moment any agent lists a second harness before `claude-code:`, check 15 compares the top-level `model:` against the wrong harness and emits a false PASS or a false FAIL. The awk range also runs to EOF if the frontmatter terminator is missing.", "severity": "MEDIUM", "sha": "2966edc7", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/_shared/skills/git-cleanup-procedures/SKILL.md", "fix": "Seed the default before doubling: `MERGE_GATES_MAX_POLLS=$(( ${MERGE_GATES_MAX_POLLS:-90} * 2 ))`.", "line": 160, "pr": 1881, "problem": "The verbatim \"Keep waiting\" branch does `MERGE_GATES_MAX_POLLS=$((MERGE_GATES_MAX_POLLS*2))`. The variable is normally UNSET (merge-gates.sh:345 supplies the default 90), so the arithmetic evaluates to 0 and exports `MERGE_GATES_MAX_POLLS=0` — set-and-non-empty, so `${MERGE_GATES_MAX_POLLS:-90}` no longer defaults, and `TIMEOUT_SECONDS` derives to 0 (merge-gates.sh:350). The re-poll returns immediately non-zero and `|| exit 1` abandons the merge. The user-facing option \"Keep waiting (double MAX_POLLS)\" therefore aborts instead of waiting.", "severity": "MEDIUM", "sha": "2966edc7", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-sidecar.py", "fix": "Use `n * (n - 1) // 2` in the complexity string (and fix the bats comment), or count ordered comparisons consistently on both sides.", "line": 537, "pr": 1884, "problem": "The emitted `complexity` string compares the pivot-tournament budget (unordered: n + k(n-k) + C(k,2)) against `n*(n-1)`, the ORDERED all-pairs count. Round-robin is C(n,2)=n(n-1)/2, so the printed savings are inflated 2x and are false for small N: at N=4,k=2 the schedule needs 9 comparisons vs 6 real round-robin pairs, yet the string claims '9 vs 12'. tests/bats/verifier_sidecar.bats:98 enshrines the same wrong baseline in its comment.", "severity": "MEDIUM", "sha": "37041397", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/agent-rules/verifier-sidecar.md", "fix": "Point the link at `../plans/active/llm-verifier-sidecar.md`.", "line": 3, "pr": 1884, "problem": "Broken doc cross-ref: `[../plans/llm-verifier-sidecar.md](../plans/llm-verifier-sidecar.md)` resolves to docs/plans/llm-verifier-sidecar.md, which does not exist — the plan lives at docs/plans/active/llm-verifier-sidecar.md.", "severity": "LOW", "sha": "37041397", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-sidecar.py", "fix": "Update both comments to docs/plans/active/llm-verifier-sidecar.md.", "line": 9, "pr": 1884, "problem": "Header comment `# Plan: docs/plans/llm-verifier-sidecar.md.` points at a non-existent path (real: docs/plans/active/llm-verifier-sidecar.md). Same stale pin at tests/bats/verifier_sidecar.bats:13.", "severity": "LOW", "sha": "37041397", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-sidecar.py", "fix": "Catch `OSError` and `UnicodeDecodeError` in load_json and re-raise as VerifierError so every bad-input path exits 2.", "line": 143, "pr": 1884, "problem": "load_json catches only FileNotFoundError and JSONDecodeError, and main() catches only AssertionError/VerifierError, so a directory path, an unreadable file, or a non-UTF-8 file escapes as a raw traceback with exit 1 — violating the documented exit-code contract (0 success / 2 malformed input or usage). Verified: `verifier-sidecar.py aggregate docs` prints an IsADirectoryError traceback.", "severity": "LOW", "sha": "37041397", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-produce.py", "fix": "Anchor the letter parse: only accept the response when the stripped text is a single A-T letter (or the first token after stripping punctuation is one char in _LETTER_SET); otherwise fall through to the numeric parse and raise ProduceError if that fails too.", "line": 178, "pr": 1885, "problem": "extract_scalar scans EVERY character of the response text and returns on the first one that happens to fall in A-T, with no anchoring. Any preamble silently yields a wrong score instead of an error: \"Score: B\" returns 'S' -> 0.053 (near-worst) rather than B -> 0.947; \"The answer\" returns 'T' -> 0.0. Scalar mode is exactly the path used for backends prone to a short preamble, so candidates get silently mis-ranked with no failure signal.", "severity": "HIGH", "sha": "6094b058", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-produce.py", "fix": "Accumulate in probability space: out[letter] = log(exp(out[letter]) + exp(lp)) (or math.logaddexp) instead of max().", "line": 161, "pr": 1885, "problem": "When two top_logprobs entries normalize to the same letter (e.g. tokens \"A\" and \" A\"), the code keeps max(logprob) instead of summing the probability mass. The letter's true mass is p1+p2, so the distribution the whole sidecar consumes (expectation, variance, uncertainty) is systematically understated for any backend that tokenizes the letter with a leading space.", "severity": "MEDIUM", "sha": "6094b058", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-produce.py", "fix": "Either invert the 1-20 branch to match the letter rank convention ((GRANULARITY - num) / (GRANULARITY - 1)) or document explicitly in the docstring that bare numbers are higher-is-better while letters are ranks.", "line": 189, "pr": 1885, "problem": "Polarity mismatch between the two scalar scales. The letter scale is a rank where A (position 1) = 1.0 = best, but the bare-number 1-20 branch maps 1 -> 0.0 and 20 -> 1.0, i.e. higher-is-better. A model that answers \"1\" meaning rank 1 (best) is scored as worst.", "severity": "LOW", "sha": "6094b058", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-produce.py", "fix": "Require https:// whenever api_key is non-empty and the host is not localhost/127.0.0.1, or at least LOG/warn once when sending a bearer token over http://.", "line": 205, "pr": 1885, "problem": "HttpTransport accepts plain http:// endpoints and then unconditionally attaches the Authorization: Bearer VERIFIER_API_KEY header, so the key travels in cleartext to any non-localhost http:// base URL.", "severity": "LOW", "sha": "6094b058", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/bats/verifier_produce.bats", "fix": "Use `run bash -c 'set -o pipefail; \"$PY\" \"$PRODUCE\" ... | \"$PY\" \"$SIDECAR\" aggregate -'`.", "line": 78, "pr": 1885, "problem": "The pipe test runs `bash -c 'producer ... | sidecar aggregate -'` without `set -o pipefail`, so only the sidecar's exit code is asserted. A producer that crashes mid-stream is masked as long as the sidecar happens to exit 0.", "severity": "LOW", "sha": "6094b058", "userVisible": false}
{"batch": "batch-20-redo", "file": ".github/workflows/tsan-linux-nightly.yml", "fix": "Update the comment to `docs/plans/shipped/mutation-smoke-gate.md`.", "line": 92, "pr": 1886, "problem": "Step comment points readers at `docs/plans/mutation-smoke-gate.md`, but this same PR moved the plan to `docs/plans/shipped/mutation-smoke-gate.md` (see the Archive note in the plan). The path is dead at origin/develop.", "severity": "LOW", "sha": "f90156a8", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/test-mutation-smoke.sh", "fix": "Point the comment at `docs/plans/shipped/mutation-smoke-gate.md`.", "line": 3, "pr": 1886, "problem": "Header comment cross-refs the pre-move plan path `docs/plans/mutation-smoke-gate.md`, which no longer exists (plan lives in `docs/plans/shipped/`).", "severity": "LOW", "sha": "f90156a8", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/self-improvement/categories/applied.md", "fix": "Rewrite both cites as `docs/plans/shipped/mutation-smoke-gate.md`.", "line": 15, "pr": 1886, "problem": "Archived entry cites `docs/plans/mutation-smoke-gate.md` (also at line 19) — broken after this PR relocated the plan to `docs/plans/shipped/`.", "severity": "LOW", "sha": "f90156a8", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-calibrate.py", "fix": "After float(v), reject non-finite values and enforce sane bounds (min_samples >= 1 integer; max_brier/max_ece/min_auc within [0,1]) with CalibrationError, mirroring the isfinite check already in _unit().", "line": 356, "pr": 1887, "problem": "resolve_policy() accepts any float for min_samples/max_brier/max_ece/min_auc with no finiteness or range validation. Python's json.loads accepts bare NaN, and every comparison against NaN is False, so a policy block containing NaN (or max_brier:1.0 / min_auc:0.0 / min_samples:-1) silently clears all four promotion checks and makes eligible_for_blocking true — a fail-open on the very gate that authorizes advisory->blocking promotion.", "severity": "MEDIUM", "sha": "7dc1a6e6", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-calibrate.py", "fix": "Raise CalibrationError(f\"{field} must be in [0,1]\") when v is outside the range (allow a tiny float-noise epsilon), rather than max/min clamping.", "line": 108, "pr": 1887, "problem": "_unit() silently clamps out-of-range values into [0,1] instead of rejecting them. A labels file with outcome:2, or a producer bug emitting score:1.7 / -0.5, is coerced to the nearest legal value; clamping can only shrink the (score-outcome) gap, so malformed calibration data quietly improves Brier/ECE and pushes the report toward eligible-for-blocking.", "severity": "MEDIUM", "sha": "7dc1a6e6", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-calibrate.py", "fix": "Catch OSError (superset of FileNotFoundError) in load_json and re-raise as CalibrationError so IO failures land on the documented exit 2.", "line": 95, "pr": 1887, "problem": "load_json() catches only FileNotFoundError and JSONDecodeError; other OSErrors from _read()'s open() (directory path, permission denied, unreadable mount) escape main()'s except (AssertionError, CalibrationError) as an uncaught traceback and the process exits 1. The header documents IO error as exit 2, and exit 1 is the --gate \"NOT eligible for blocking\" signal — a CI gate job cannot distinguish an unreadable input from a genuine not-eligible verdict.", "severity": "MEDIUM", "sha": "7dc1a6e6", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-calibrate.py", "fix": "Factor the raw per-bin means into a helper that reliability() rounds for display and ece() consumes unrounded.", "line": 201, "pr": 1887, "problem": "ece() sums over reliability()'s rows, whose mean_score and empirical_rate are already round(...,4) display values. The gate metric is therefore derived from presentation-rounded numbers and silently changes if reliability()'s rounding is ever adjusted.", "severity": "LOW", "sha": "7dc1a6e6", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-calibrate.py", "fix": "Add --policy/--threshold flags (or read a policy object from the --labels file) and apply them on the join path too.", "line": 383, "pr": 1887, "problem": "The --scores/--labels join path hard-codes dict(DEFAULT_POLICY) and DEFAULT_THRESHOLD with no override, while the single-file path honours the input's \"policy\" and \"threshold\". docs/agent-rules/verifier-sidecar.md:101 documents the join as the end-to-end loop, so the recommended path cannot tune promotion thresholds at all.", "severity": "LOW", "sha": "7dc1a6e6", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-produce.py", "fix": "Wrap the record write in its own try/except OSError that warns to stderr without masking the in-flight exception, and validate the --record path before starting the run.", "line": 521, "pr": 1887, "problem": "The --record write sits in a finally block; if open(args.record,'w') fails (bad directory, read-only path) the OSError replaces whatever exception produce() raised and is not caught by main()'s except (AssertionError, ProduceError, FileNotFoundError), producing a traceback and exit 1 instead of the documented exit 2 — and the original produce failure is lost.", "severity": "LOW", "sha": "7dc1a6e6", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/ui/ai_prefs_autosave_flow.test.cpp", "fix": "Make the cleanup unconditional: wrap SetTestOverride(nullptr) / showPreferences reset in a small RAII scope guard (C++14 struct with a destructor) constructed right after SetTestOverride(&MakeStubGated), so it runs even when IM_CHECK short-circuits the test body.", "line": 318, "pr": 1888, "problem": "On the deadline-expiry path IM_CHECK aborts/returns from the TestFunc before the following AiClientFactory::SetTestOverride(nullptr) and Drain() run. The global factory override (&MakeStubGated) and g_ui.showPreferences=true then leak into subsequent tests, and the still-in-flight worker/queued PublishProbeResult outlives the test — reintroducing exactly the teardown race this PR removed, just on the failure branch.", "severity": "MEDIUM", "sha": "d8ef9486", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/ui/ai_prefs_autosave_flow.test.cpp", "fix": "Use the same RAII cleanup guard for the override reset and dispatcher drain in this variant.", "line": 270, "pr": 1888, "problem": "Same unconditional-cleanup gap in the sibling VerifyOnSave_TestConnection variant: if the 5s spin times out, IM_CHECK returns before SetTestOverride(nullptr) and Drain(), leaking the factory override and leaving a queued callback in flight at teardown.", "severity": "MEDIUM", "sha": "d8ef9486", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/ui/ai_prefs_autosave_flow.test.cpp", "fix": "Replace the numeric pins with symbol references (e.g. \"the PostToMainThread(PublishProbeResult) tail of TriggerProbe\" and \"the cancel guard at the top of PublishProbeResult\") in Source/Core/src/AiPrefsTestConnection.cpp so the comment cannot drift.", "line": 304, "pr": 1888, "problem": "Stale line pins in the explanatory comment. It cites \"AiPrefsTestConnection.cpp:143-147\" for the always-posts-back call and \"AiPrefsTestConnection.cpp:91-93\" for the cancel short-circuit; at HEAD the PostToMainThread call is Source/Core/src/AiPrefsTestConnection.cpp:138-140 and the cancel guard is line 31 (91-93 is an unrelated onDelta lambda). The file path is also given without its Source/Core/src/ prefix.", "severity": "LOW", "sha": "d8ef9486", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-endpoint.py", "fix": "Validate length too: `if len(fixed) != 1 or fixed not in LETTERS: raise EndpointError(...)`.", "line": 286, "pr": 1889, "problem": "`if fixed not in LETTERS` is a SUBSTRING test against the string \"ABCDEFGHIJKLMNOPQRST\", not a single-letter membership test. `--fixed \"\"`, `--fixed AB`, `--fixed CDE` all pass validation and then `LETTERS.index(fixed)` (line 85) silently resolves to the first letter of the run (empty string -> index 0 -> 'A'). The documented contract (\"--fixed must be one of A-T\", exit 2) is not enforced, so a typo'd demo/smoke run quietly pins the wrong letter instead of erroring.", "severity": "MEDIUM", "sha": "3330c79a", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-endpoint.py", "fix": "Read and discard Content-Length bytes before sending the 404 (or send `Connection: close` on the error path).", "line": 177, "pr": 1889, "problem": "The 404 branch of do_POST replies and returns WITHOUT draining the request body from self.rfile. With `protocol_version = \"HTTP/1.1\"` (line 159) the connection is keep-alive, so the unread body bytes are re-parsed as the next request line, desyncing the connection (client sees a garbage 400/hang) for any POST to a wrong path that carries a payload — e.g. a caller pointing VERIFIER_BASE_URL at a base that yields /v1/v1/chat/completions.", "severity": "MEDIUM", "sha": "3330c79a", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-endpoint.py", "fix": "Add a trailing `except Exception as exc: self._send(500, {\"error\": {\"message\": str(exc)}})`.", "line": 186, "pr": 1889, "problem": "Only EndpointError is caught around parse_request/chat_completion. Any other exception (e.g. a non-dict `messages` element chain, or a TypeError from odd JSON shapes) escapes do_POST, so BaseHTTPRequestHandler aborts the connection instead of returning a 5xx — the client sees a connection reset rather than a diagnosable error.", "severity": "LOW", "sha": "3330c79a", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/bats/verifier_endpoint.bats", "fix": "Use `run bash -c \"set -o pipefail; ...\"` in the piped tests.", "line": 67, "pr": 1889, "problem": "`run bash -c \"... produce ... | ... sidecar aggregate -\"` without `set -o pipefail`, so `$status` only reflects the sidecar. A producer that dies mid-run (non-zero exit) is masked as long as the sidecar tolerates the truncated stream; same shape at lines 89, 95 and 107. Weakens the live-path assertion these tests exist to make.", "severity": "LOW", "sha": "3330c79a", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-produce.py", "fix": "Only treat a single-letter dist as degenerate when the raw top_logprobs list itself was short/uninformative (e.g. pass the pre-filter token count into is_degenerate), or drop the len<2 rule and rely solely on the placeholder/flat-value checks.", "line": 269, "pr": 1891, "problem": "is_degenerate() treats len(dist) < 2 as degenerate, but `dist` is not the raw top_logprobs — extract_logprobs() filters it down to A-T letter tokens only. A healthy, highly confident backend whose top-20 contains just one letter token (remaining slots being whitespace/punctuation/word-piece variants) yields len(dist)==1, so --strict aborts the entire run with \"backend returned degenerate logprobs\" on a perfectly good response. The DeepSeek recipe added in the same PR puts --strict in the copy-paste command, making this the default path.", "severity": "HIGH", "sha": "0c68611b", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-produce.py", "fix": "Detect the placeholder pattern by magnitude/clustering instead of an absolute cut (e.g. require values far below -1000, or require the non-top values to be identical placeholders), and/or only warn rather than abort when the top token is itself a plausible logprob.", "line": 274, "pr": 1891, "problem": "`all(v <= _DEGENERATE_LOGPROB for v in values[1:])` with _DEGENERATE_LOGPROB = -100.0 flags a legitimate but very peaked distribution (e.g. A=-0.0001, B=-105) as degenerate; real logprobs can legitimately fall below -100 for a near-deterministic single-token answer. Under --strict this hard-fails a valid run.", "severity": "MEDIUM", "sha": "0c68611b", "userVisible": false}
{"batch": "batch-20-redo", "file": "scripts/dev/verifier-produce.py", "fix": "Wrap the flush in its own try/except, log the write failure via stderr, and let the original exception propagate.", "line": 520, "pr": 1891, "problem": "The record-flush in the `finally` block is unguarded: if opening/writing args.record raises (unwritable path, bad directory), that IOError propagates and replaces the in-flight ProduceError, so the real cause (\"degenerate logprobs\", HTTP error, etc.) is lost and the exit code path in main() no longer matches it (IOError is not in the caught tuple, so it escapes as a traceback instead of exit 2).", "severity": "LOW", "sha": "0c68611b", "userVisible": false}
{"batch": "batch-20-redo", "file": "Source/Plugins/Mcp/McpPlugin.cpp", "fix": "Replace RegisterToolsListRoute()'s body with res.set_content(nlohmann::json{{\"tools\", BuildToolCatalogJson()}}.dump(), \"application/json\") so all surfaces share the one builder.", "line": 963, "pr": 1893, "problem": "BuildToolCatalogJson() was added as the \"single source\" for the tool catalog (header doc McpPlugin.h:102-106 claims the catalogs \"can never drift\"), but the pre-existing REST route RegisterToolsListRoute() (McpPlugin.cpp:517-541) still builds a byte-for-byte duplicate of the same registry + run_lua + Lua-tool assembly. GET /mcp/tools/list and the JSON-RPC tools/list + smatchet://commands paths are now two independent copies, so the next catalog change silently diverges for REST clients.", "severity": "MEDIUM", "sha": "19c35a64", "userVisible": false}
{"batch": "batch-20-redo", "file": "Source/Plugins/Mcp/McpPlugin.cpp", "fix": "Emit {\"code\", -32002} with message \"Resource not found\" plus data.uri, and update the assertion at tests/ui/mcp_resources.test.cpp:174.", "line": 999, "pr": 1893, "problem": "resources/read on an unrecognized URI returns JSON-RPC -32602 (Invalid params). MCP reserves -32002 (\"Resource not found\") for this case; clients branching on -32002 to refresh their resource list will instead treat it as a malformed-request bug.", "severity": "LOW", "sha": "19c35a64", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/ui/mcp_resources.test.cpp", "fix": "POST tools/list in the same test and assert the parsed result.tools array equals the parsed contents[0].text array.", "line": 158, "pr": 1893, "problem": "The stated invariant of the PR is that resources/read and tools/list cannot drift, but case 3 only asserts the read body contains \"contents\" and \"inputSchema\" — it never compares the resource payload against a tools/list response, so a regression reintroducing a separate catalog path would still pass.", "severity": "LOW", "sha": "19c35a64", "userVisible": false}
{"batch": "batch-20-redo", "file": "Source/Core/src/Persistence/LocalCacheManager.cpp", "fix": "Bound the whole retry by a wall-clock deadline (e.g. give up after ~500ms total) instead of only an attempt count, and/or lower the per-attempt busy_timeout while retrying; alternatively make the UI-thread caller (UpdateCachedCommentCount) route the cache write off the render thread so no sleep_for ever runs on the main thread.", "line": 186, "pr": 1894, "problem": "RunWriteTxnWithBusyRetry blocks the calling thread with std::this_thread::sleep_for between attempts, and each of the 5 attempts can additionally sit up to the armed busy_timeout (setBusyTimeout(5000) in ApplyWalPragmas) before SQLite returns BUSY. Worst-case SaveTicket/SaveTickets latency goes from ~5s to ~25s + 375ms of sleeps, with no total-deadline cap. This is not purely a background path: AppController::UpdateCachedCommentCount -> UpdateTicket -> Cache->SaveTicket is documented (AppController_CatalogAndFieldEdit.cpp:852) as \"Called on the UI thread from the modal's main-thread post-back\", so under real cache contention the retry loop stalls the frame pump with no progress cue. Each attempt also holds stmtMutex_ for its full BEGIN..COMMIT, serializing every other statement on the instance.", "severity": "HIGH", "sha": "b472830e", "userVisible": true}
{"batch": "batch-20-redo", "file": "Source/Core/include/Tracker/CollaborationPreconditionPure.h", "fix": "In CollaborationErrorToVoidResult, fall back to a non-empty message when err.Detail is empty, e.g. `return VoidResult::Err(err.Detail.empty() ? std::string(\"Tracker collaboration request failed.\") : err.Detail);` (and update the empty-Detail test to assert the fallback text). Same treatment for the read-side CollaborationResultToResult mapping.", "line": 58, "pr": 1896, "problem": "CollaborationErrorToVoidResult maps a failing TrackerError with an empty Detail to VoidResult::Err(\"\") (the PR's own test at CollaborationPreconditionPure.test.cpp:108 pins this). Downstream the empty message is indistinguishable from success: the watch-self caller in Source/Core/src/Tracker/TrackerGridFieldDisplay.cpp:232-233 encodes success as an empty std::string (`r.has_value() ? std::string() : r.error()`) and the tooltip at line 242 only reports failure when `!watchSelfError.empty()`, so a backend error with no Detail is silently swallowed and the user believes the watch succeeded.", "severity": "MEDIUM", "sha": "50352045", "userVisible": true}
{"batch": "batch-20-redo", "file": "docs/plans/active/refactor-quickwins-parser-lua-service.md", "fix": "Add ExtractAdfTextToStream (and re-verify every other file-local callee of the retained NormalizeTrackerObjectValue chain) to the _detail.h declaration list; grep the core-file survivors for anon-namespace symbols before partitioning.", "line": 39, "pr": 1898, "problem": "Commit-3 cross-TU visibility inventory is incomplete. The plan names only JsonLooksLikeJiraTimetracking as needing a _detail.h declaration (\"FormatTrackerTimetrackingDisplay ... needs nothing\"), but ExtractAdfTextToStream is file-local (defined at Source/Core/src/Tracker/TrackerFieldValueParser.cpp:376, no declaration in the public header) and is called at :958 from NormalizeTrackerObjectValue, which the plan keeps in the core file while moving \"ADF walkers\" to _CommentsAdf.cpp. The split as specified does not compile/link.", "severity": "MEDIUM", "sha": "9410f4fb", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/refactor-quickwins-parser-lua-service.md", "fix": "State that each table entry must set handled only on a non-empty result (empty => keep probing), and that the comments/ADF early branches stay outside the table; pin behavior with the existing TrackerFieldValueParser doctest suites before/after.", "line": 40, "pr": 1898, "problem": "The table-ization design mischaracterizes NormalizeTrackerObjectValue. Only 2 of ~10 probes have the `bool& handled` shape (NormalizeParentRefObject :897, NormalizeIdObject :1002); the rest are inline contains()/is_string() branches (comments array :951, doc/ADF :951-959, accountId :963, displayName :970, value+child :976, value :988, name :994) that deliberately FALL THROUGH when the extracted string is empty. A {name, fn} array iterated first-match-wins that treats a match as terminal changes output for empty-string cases.", "severity": "MEDIUM", "sha": "9410f4fb", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/refactor-quickwins-parser-lua-service.md", "fix": "Re-locate the parallel token list at HEAD and repin, or drop the line numbers and identify the list by symbol name plus the dup-baseline clone hashes.", "line": 30, "pr": 1898, "problem": "Stale line pins for the commit-1 X-macro work. At HEAD, Source/Core/src/Ui/SmatchetTheme.cpp:541-547 is a block of HighContrast ImGuiCol assignments and Source/Core/include/Ui/SmatchetTheme.h:78-82 is the ApplyUiDensityScale doc comment — neither is the \"theme-token list declared and defined in parallel\" the commit targets. Same pins repeated at line 55.", "severity": "MEDIUM", "sha": "9410f4fb", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/refactor-quickwins-parser-lua-service.md", "fix": "Correct the path to Source/Plugins/Whisper/HotkeyParse.cpp and pre-mark that site as grandfathered (Plugins -> Core/Tracker header edge) rather than a mechanical dedup target.", "line": 28, "pr": 1898, "problem": "HotkeyParse.cpp is listed under Source/Core/src/ but at HEAD it lives at Source/Plugins/Whisper/HotkeyParse.cpp — a different module. Making that site include Source/Core/include/Tracker/TrackerError.h is precisely the new cross-subsystem include edge the same bullet's DAG guard (and the line-96 \"DRY refactor coupling independent subsystems = review CRITICAL\" risk) warns against, but the plan does not flag it because it believes the site is in Core.", "severity": "MEDIUM", "sha": "9410f4fb", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/refactor-quickwins-parser-lua-service.md", "fix": "Replace the line range with the specific clone hashes for the TrackerError.h/StringUtil.h pairs, which are stable across baseline regeneration.", "line": 27, "pr": 1898, "problem": "The dup-baseline citation is stale: at HEAD docs/high-integrity/dup-baseline.md:16-18 are AppController.h/FieldEditPipelineService rows and :41-59 are JiraClient/PlaneClient/GitHubClient rows — none of them the TrackerError.h or StringUtil.h re-inline pairs this commit targets. No clone hashes are given for these rows, so the \"locate via the dup-baseline hashes\" fallback (line 53) does not apply here.", "severity": "LOW", "sha": "9410f4fb", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/refactor-quickwins-parser-lua-service.md", "fix": "Correct both paths.", "line": 29, "pr": 1898, "problem": "Two more wrong source paths: Source/Core/src/Ai/AiEndpointSanitize.cpp is actually Source/Core/src/AiEndpointSanitize.cpp, and Source/Core/src/IssueDraft.cpp is actually Source/Core/src/Tracker/IssueDraft.cpp (repeated at lines 53-54).", "severity": "LOW", "sha": "9410f4fb", "userVisible": false}
{"batch": "batch-20-redo", "file": ".github/workflows/plan-lock-gate.yml", "fix": "Replace with `uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1` to match the other 28 workflows; optionally add a SHA-pin assertion to agents/scripts/core/test-workflow-yaml.sh so drift fails CI.", "line": 39, "pr": 1902, "problem": "This Dependabot bump pinned actions/checkout to the full SHA 3d3c42e5aac5ba805825da76410c181273ba90b1 in all 28 other workflows, but here it wrote the mutable tag `uses: actions/checkout@v7.0.1`. Verified repo-wide: this is the ONLY unpinned third-party action in .github/workflows, and no gate enforces SHA pinning (test-workflow-yaml.sh has no pin check), so nothing will catch it. A moved/retagged v7.0.1 executes attacker-controlled code inside the plan-lock hard-net gate job.", "severity": "HIGH", "sha": "8e91dafb", "userVisible": false}
{"batch": "batch-20-redo", "file": "Source/Core/src/TicketFieldEditor.cpp", "fix": "Move the submit off the render path: kick the SubmitWorklog call into a background task (LaunchBackgroundTask/std::async like the sibling watcher/comment posters), keep an 'in-flight' flag in s_ActiveWorklogState that disables Save and draws a progress cue, and apply the VoidResult (close popup or set ErrorMsg) via PostToMainThread on completion.", "line": 1067, "pr": 1903, "problem": "HandleWorklogSave() is called straight from the ImGui render path (RenderTimeTrackingModal -> HandleWorklogSave, TicketFieldEditor.cpp:1226) and calls app.SubmitWorklog(), which synchronously does ConfigManager::Load() plus a blocking HTTP POST (backend->Collaboration()->AddWorklog). The whole UI freezes for the duration of the Jira round-trip with no spinner/progress cue — a Pillar-1 violation, and on a slow/hung tracker the app appears hung.", "severity": "CRITICAL", "sha": "4c1f7e7d", "userVisible": true}
{"batch": "batch-20-redo", "file": "Source/Core/src/Commands/Builtin/BuiltinCommands_TicketMutations.cpp", "fix": "Reject non-positive seconds up front with CommandResult::Failure(ErrorCode::InvalidArgument, \"seconds must be > 0\"), and either append the \"%ds\" remainder to the h/m string or document the truncation to minute granularity.", "line": 103, "pr": 1903, "problem": "ticket.add_worklog does not validate the 'seconds' arg. args.value(\"seconds\", 0) defaults to 0 when the caller omits it, and a negative value is also accepted; the formatter then falls to the final branch and submits timeSpent=\"0s\" (or \"-30s\") to the tracker, producing an opaque backend rejection instead of a clear argument error. The formatter also silently drops the seconds remainder (e.g. 3630s -> \"1h\", 90s handled but 3690s -> \"1h 1m\" loses 30s).", "severity": "MEDIUM", "sha": "4c1f7e7d", "userVisible": true}
{"batch": "batch-20-redo", "file": "Source/Core/src/Ui/AnnotateAnalysisUi_Modals.cpp", "fix": "Mirror the sibling sites: `gerr = r.error().empty() ? \"Group lookup failed.\" : r.error();`", "line": 271, "pr": 1904, "problem": "The Result-unpack here takes `gerr = r.error()` with no empty-message fallback, unlike the two sibling sites this same PR added in SmatchetUserInfoUi.cpp (lines 266/294, which use `r.error().empty() ? \"Group lookup failed.\" : r.error()`). The PR's own seam test (CollaborationPreconditionPure.test.cpp:139) asserts a backend error with an empty Detail still maps to an Err, so gerr can legitimately be empty; the post-back then guards with `if (State().profileGroups.empty() && !gerr.empty())`, so that failure renders as an empty group list with no error shown — a silent failure in the user-profile modal.", "severity": "LOW", "sha": "1af13407", "userVisible": true}
{"batch": "batch-20-redo", "file": "Source/Core/src/AppController_CatalogAndFieldEdit.cpp", "fix": "Change the L784 message to \"Tracker backend is not initialized.\" to match the sibling reads (and update any test pinning the literal).", "line": 784, "pr": 1905, "problem": "SearchUsersByQuery's backend-absent error is hardcoded to \"Jira backend is not initialized.\" while its two siblings introduced by the same change (FetchIssueWatchers L715, FetchIssueVotes L762) say \"Tracker backend is not initialized.\". Smatchet supports Linear/GitHub backends, so a non-Jira user hitting this path (users.search command, JQL autocomplete, annotate assign resolve) sees a wrong tracker name.", "severity": "LOW", "sha": "bc1c5030", "userVisible": true}
{"batch": "batch-20-redo", "file": "docs/plans/active/build-quality-velocity-hardening.md", "fix": "Update the line-239 status bullet to say Tier 1 and Tier 2 are complete (2026-07-23) and that remaining #21 is Tier 3 (structural blockers), Tier 4 (scenario void+outErr test sweep), Whisper adjacents and Tracker-internal residuals; keep the dated log rows unchanged.", "line": 239, "pr": 1908, "problem": "Live Sprint-2 status line (not a dated log row) still describes remaining #21 work as \"Tier-1/2 clean flips ≈14–19 Core sites\". The later 2026-07-23 implementation-log rows at lines 189–190 state Tier 1 and Tier 2 are both CLEARED, so this status bullet is stale doc-vs-doc drift and misdirects the next agent to already-done work.", "severity": "LOW", "sha": "", "userVisible": false}
{"batch": "batch-20-redo", "file": "Source/Core/src/AttachmentAppUpdateService.cpp", "fix": "Replace the numeric pin with a symbol reference, e.g. \"twin of the capped writer in CheckForAppUpdate (kMaxUpdateCheckBytes)\", so the marker survives future line shifts.", "line": 159, "pr": 1911, "problem": "The new SMATCHET_DEVIATION(rule=duplication) marker points at \"the marked sites at :513\", but at HEAD line 513 is blank — the twin capped-download site's comment block starts at 514 and its own SMATCHET_DEVIATION marker is at 518. This PR inserted the DownloadedAttachment struct + prologue above it, shifting the target. (The twin's reciprocal pin \"mirrors the capped download at :155\" at line 516 is likewise now off: the cap constant is at 161.)", "severity": "LOW", "sha": "a6ae7aab", "userVisible": false}
{"batch": "batch-20-redo", "file": "README.md", "fix": "Change the link target to `docs/plans/shipped/linear-tracker-backend.md`.", "line": 30, "pr": 1912, "problem": "Broken cross-ref: the Linear plan link points at `docs/plans/linear-tracker-backend.md`, which does not exist at HEAD — the plan was moved to `docs/plans/shipped/linear-tracker-backend.md` (see docs/plans/INDEX.md:78). The README link 404s on GitHub.", "severity": "LOW", "sha": "df2bc0e7", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/duration-parser-unification.md", "fix": "Pick one: either state that `Empty` sorts to a fixed absolute end (drop the \"same side as the grid's empty handling\" clause), or add `int sortDirection` to CompareTimeTrackingValues and stop claiming the signature is untouched.", "line": 34, "pr": 1913, "problem": "Spec is unimplementable as written: it requires the sort adapter to map `Empty` \"to the same side as the grid's empty handling\", but that side is `sortDirection`-dependent in CompareFieldValuesForSort (Source/Core/src/TicketGridSortPure.cpp:124-134), while step 3 (line 57) explicitly keeps `CompareTimeTrackingValues(const std::string&, const std::string&)`'s signature, which receives no direction. The same line also fixes an absolute order (`Parsed < Empty < Unparseable`), contradicting direction-dependence.", "severity": "MEDIUM", "sha": "36e1b8bf", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/duration-parser-unification.md", "fix": "Pin explicitly that the Unparseable sentinel is strictly greater than the reconciled saturation ceiling (and, per line 34, that the Empty sentinel sits strictly between), and add a test asserting saturated != unparseable.", "line": 46, "pr": 1913, "problem": "Overflow \"saturates at the shared ceiling and stays `Parsed`\" while line 31 maps `Unparseable` to \"a stable sentinel ... sorted to one end\", but nothing requires the sentinel to be distinct from the saturation ceiling. Today's code already returns kMaxDurationSeconds on overflow (TicketGridDurationSortPure.cpp:118), so the obvious \"sort last = max\" sentinel would compare equal to a genuinely saturated duration, collapsing the two buckets the plan exists to separate.", "severity": "MEDIUM", "sha": "36e1b8bf", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/duration-parser-unification.md", "fix": "Either add `s` (seconds) to the accepted unit set, or call out in § Risks that `\"30s\"` becomes Unparseable and widen the re-pin range to `:40-47` with per-line justification.", "line": 60, "pr": 1913, "problem": "The re-pin list for tests/Core/TicketGridDurationSortPure.test.cpp cites `:40,43-47` and enumerates only garbage cases, but line 46 in that range is `CHECK(ParseDurationToSecondsForSort(\"30s\") == 30)` — a currently-correct parse that the plan's closed grammar (w/d/h/m plus bare integer, line 41) turns into `Unparseable`, and line 41's `CHECK(\"3h 30m left\" > 0)` sits outside the cited range yet also changes meaning. Silently regressing an `s` suffix is a real behaviour loss, not a garbage re-pin.", "severity": "MEDIUM", "sha": "36e1b8bf", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/duration-parser-unification.md", "fix": "Change 10804 to 12604 (or restate as \"10800 + 1800 + 4 fabricated seconds\").", "line": 44, "pr": 1913, "problem": "Arithmetic error in a stated current-behaviour claim: `\"3h 30m left\"` is said to yield 10804 today. Tracing ParseDurationUnitsSum gives 10800 (3h) + 1800 (30m) + 4 junk seconds = 12604; the quoted number drops the 30m term entirely.", "severity": "LOW", "sha": "36e1b8bf", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/core/small_helper_audit.py", "fix": "Either allow `>` as a declarator prefix (a call `a > Foo(b)` is a far rarer shape than a template return type), or add the template-return skip to § Accepted blind spots and pin it with a selftest assertion the way the pointer-return case already is.", "line": 101, "pr": 1914, "problem": "`>` is in `_NOT_A_DECLARATOR_PREFIX`, so `extract_helpers` skips every free helper whose return type ends in a template argument list (`std::vector<std::string> SplitCsv(const std::string&) { ... }` — verified: returns []). That is a large, common class in this tree, and the docstring's § Accepted blind spots documents only the raw-pointer-return skip, so the census silently under-reports with no record of why.", "severity": "MEDIUM", "sha": "41cf8786", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/core/small_helper_audit.py", "fix": "Add `}` (and `)`) to `_NOT_A_DECLARATOR_PREFIX`, or require the token before the name to be an identifier/`&`/`*`-free type token rather than merely not-in-the-denylist.", "line": 101, "pr": 1914, "problem": "`}` is absent from `_NOT_A_DECLARATOR_PREFIX`, so a brace-block macro invocation at namespace scope that follows a previous definition's closing brace is extracted as a function definition (verified: `void A() { ... }\\nMY_MACRO(thing) { ... }` yields a helper named `MY_MACRO`, 52 tokens). Three such blocks with the same normalized body would form a bogus clone group.", "severity": "LOW", "sha": "41cf8786", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/core/dead_export_audit.py", "fix": "Let OSError propagate (main already maps exceptions to exit 2, the documented infra-error contract) instead of degrading to an empty file body.", "line": 151, "pr": 1914, "problem": "`_read` swallows OSError and returns \"\", so an unreadable reference file contributes zero occurrences. A symbol whose only caller lives in that file is then reported dead — which breaks the module docstring's stated guarantee that a false positive \"would need a caller that is textually absent, which C++ cannot produce\", and this detector's output is what drove the 8 deletions in this same PR.", "severity": "LOW", "sha": "41cf8786", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/core/test-markdown-links.sh", "fix": "Drop the hard-coded 9/14 counts from the header (the gate prints the live grandfathered count itself) or update them to match the current baseline.", "line": 44, "pr": 1914, "problem": "Header comment pins stale literals: it states the baseline holds \"9 distinct `file::href` KEYS, which `--all` reports as 14 line-level OCCURRENCES\". The committed docs/high-integrity/markdown-link-baseline.md now holds 6 entries (\"## dangling links (6)\" / \"grandfathered: 6\").", "severity": "LOW", "sha": "41cf8786", "userVisible": false}
{"batch": "batch-20-redo", "file": "PRIVACY.md", "fix": "Qualify the sentence, e.g. \"If you configure no services, Smatchet makes no network requests other than the automatic update check described under Updates (which you can disable).\"", "line": 45, "pr": 1916, "problem": "Self-contradiction in the privacy policy: \"If you configure no services, Smatchet performs no network requests during normal use.\" contradicts the doc's own summary (line 11-13) and the Updates section (line 94-97), which state the automatic startup update check contacts GitHub by default (UpdateCheckEnabled defaults to true in Source/Core/include/Config/ConfigManager.h:536, fired from SmatchetUI.cpp:538). As written the policy understates outbound traffic for a default install.", "severity": "MEDIUM", "sha": "bc477273", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/shipped/gate-blind-spot-sweep.md", "fix": "Restore the original root-relative filenames in the shipped plan's Context/Slice-3 text (they describe the pre-move tree) and add a one-line \"(relocated to docs/guides/ by the 2026-08-02 root declutter)\" note, instead of silently rewriting the paths.", "line": 59, "pr": 1917, "problem": "The relocation sed-rewrote paths inside a SHIPPED (historical) plan, producing text that now contradicts the live gate. Step 17 reads \"extend the `is_active_md` target set to include top-level user-facing guides (`docs/guides/cli.md`, `docs/guides/lua.md`, `docs/guides/mcp.md`, ...)\", but the current `test-markdown-links.sh` root-file tuple deliberately does NOT list them (they are covered by the `docs/**` branch), and the new bats test `tests/bats/markdown_links.bats:170` asserts exactly that. Same shape at line 15, which now says the scan covers `docs/**` and, in the same sentence, that `docs/guides/cli.md` is \"top-level\" and out of scope. A future reader following this plan would re-add tuple entries the gate no longer wants. Note `docs/design/rename-blame-to-annotate.md:55-58` (also in this commit's context) states historical records must not be rewritten.", "severity": "LOW", "sha": "a0490ff2", "userVisible": false}
{"batch": "batch-20-redo", "file": "tools/repo-health/generate.py", "fix": "Make these two collectors fail loudly: raise (or emit a WARN and a non-zero exit) when the named audit doc is absent or the headline regex does not match, rather than returning an empty dict.", "line": 90, "pr": 1917, "problem": "`collect_coverage()` / `collect_audit()` (line 103) read the relocated audit docs through `read()`, which returns `''` on any OSError. A missing/moved `docs/audits/*.md` yields an empty regex match and an empty dict, so the repo-health dashboard silently drops the coverage and audit-findings tiles with no error and no non-zero exit. `docs/STRUCTURE.md:60` documents this hazard (\"degrades the dashboard silently rather than failing — grep for read(\\\"docs/\") but nothing guards it, so the next reorg repeats the failure mode undetected.", "severity": "LOW", "sha": "a0490ff2", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/bats/capture_intent.bats", "fix": "Normalise before matching (e.g. strip spaces around `==`/`!=` into a canonical form in the flatten step, `sed -E 's/[[:space:]]*(==|!=)[[:space:]]*/ \\1 /g'`) and/or assert the absence of the whole class: reject `user.login` appearing anywhere other than the known `!= 'dependabot[bot]'` clause.", "line": 375, "pr": 1924, "problem": "The guard assertion `[[ \"$if_expr\" != *\"user.login ==\"* ]]` is whitespace-sensitive. It is meant to stop the intent gate from being narrowed into a positive login allow-list (which would silently disable the gate for everyone else), but a valid GitHub expression written without spaces around the operator (`github.event.pull_request.user.login=='someone'`) — or via `contains(...)` / `startsWith(...)` on the login — passes the assertion untouched. The sibling `user.type` assert on line 374 has no such gap; only this one is operator-spacing dependent.", "severity": "LOW", "sha": "c396e1c9", "userVisible": false}
{"batch": "batch-20-redo", "file": "Source/Core/src/Ui/SmatchetPlanDocViewerUi.cpp", "fix": "Mirror KickScriptLoad: build the future into a local inside try/catch, LOG_ERROR + surface a message on failure, and only assign `s.loadFuture` / set `s.loadInFlight = true` after the launch succeeded.", "line": 238, "pr": 1928, "problem": "StartLoadSelected latches `s.loadInFlight = true;` BEFORE the `std::async` launch on line 239. `std::async` can throw (std::system_error when threads are exhausted, std::bad_alloc), and there is no try/catch — the throw escapes into the ImGui frame and, if caught anywhere upstream, leaves loadInFlight permanently true with an invalid loadFuture, so PollLoadResult early-outs forever and the plan-doc viewer never loads another document for the rest of the session. The sibling site added by this same PR (LuaConsolePlugin::KickScriptLoad, lines 305-326) explicitly documents and avoids exactly this ordering.", "severity": "HIGH", "sha": "08cdde44", "userVisible": true}
{"batch": "batch-20-redo", "file": "Source/Core/src/Ui/SmatchetPlanDocViewerUi.cpp", "fix": "Clear `s.loadInFlight = false;` before `get()`, and wrap the `get()` in `try { ... } catch (const std::exception& ex) { LOG_ERROR(...); s.body = <error text>; return; }`.", "line": 254, "pr": 1928, "problem": "PollLoadResult calls `s.loadFuture.get()` (line 254) BEFORE clearing `s.loadInFlight` (line 255) and without a try/catch. A worker exception (bad_alloc on the up-to-1 MiB buffer, an ifstream exception) is rethrown here on the render thread: the future is consumed but loadInFlight stays latched true, permanently wedging the viewer. LuaConsolePlugin::PollScriptLoad (lines 365-385) clears its flag before get() and catches, with a comment naming this precise hazard; the viewer was not given the same treatment.", "severity": "HIGH", "sha": "08cdde44", "userVisible": true}
{"batch": "batch-20-redo", "file": "Source/Plugins/LuaConsole/LuaConsolePlugin.cpp", "fix": "On the name-mismatch bail, set `scriptLoadRefusedReason_` (e.g. \"The script never loaded — reselect it before saving.\") and clear reloadNoticeName_, so Save stays gated while the editor holds a blank buffer that matches no loaded file.", "line": 397, "pr": 1928, "problem": "In PollScriptLoad the `if (result.name != selectedScriptName_) return;` bail leaves the editor blank (StartLoadSelectedScriptIntoEditor blanked it and re-baselined diskSnapshot_ onto the blank) with scriptLoadInFlight_ false and scriptLoadRefusedReason_ empty (KickScriptLoad cleared it) — so SaveCurrentScript's two blank-buffer guards (lines 432 and 439) are both open. Reachable: select A -> read kicked; RefreshScriptList/SyncSelectionToList clears the selection because A vanished from disk; result A lands and is dropped here; a later SyncSelectionToList tick auto-selects the first script B; Save then writes the empty buffer over B, truncating a real script. Every other non-applying outcome in this function (TooLarge, worker throw, launch failure) sets scriptLoadRefusedReason_ for exactly this reason.", "severity": "HIGH", "sha": "08cdde44", "userVisible": true}
{"batch": "batch-20-redo", "file": "docs/self-improvement/postmortems.md", "fix": "Repin to `msvc-build-onboarding-hardening.md:86`, or drop the `:<line>` suffix in favour of the section anchor (§ Deviations from plan), which cannot drift.", "line": 286, "pr": 1929, "problem": "Stale line-pin: the entry cites `docs/plans/shipped/msvc-build-onboarding-hardening.md:85` as the § Deviations claim line, but at HEAD line 85 is the `## Deviations from plan` heading and the quoted claim sits on line 86 — off by one. Ironic in a postmortem whose thesis is that unanchored/unverifiable claims escape.", "severity": "LOW", "sha": "d77a1fdc", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/self-improvement/postmortems.md", "fix": "Note the #1955 port inline and either widen the pathspec (`git log -S 'vcvars' -- scripts/dev`) or pin the command to a pre-port sha so the evidence stays reproducible.", "line": 286, "pr": 1929, "problem": "Evidence chain no longer reproduces: the cited command `git log -S 'vcvars' -- scripts/dev/local/build_standalone.ps1 scripts/dev/build_standalone.ps1` names paths that do not exist at HEAD (the PowerShell build scripts were ported to bash in PR #1955, 0a78d77c), and `Get-VsWherePath (:71-83)` pins into that deleted file. The command now returns empty because the pathspecs match nothing, not because the capability was absent — the stated proof is self-confirming rather than verifiable.", "severity": "LOW", "sha": "d77a1fdc", "userVisible": false}
{"batch": "batch-20-redo", "file": ".github/workflows/release.yml", "fix": "Reword to \"Read as $PARAM_NOTES in the bash script below, never interpolated into the script body.\"", "line": 195, "pr": 1930, "problem": "Surviving comment on the PARAM_NOTES env says notes are \"Read via $env: below\", but this step is `shell: bash` and reads the value as ${PARAM_NOTES:-} / \"$PARAM_NOTES\". `$env:` is PowerShell syntax carried over from the neighbouring signing step, so the comment misdescribes the mechanism it documents.", "severity": "LOW", "sha": "314c0bc1", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/self-improvement/postmortems.md", "fix": "Change `--selftest` to `--baseline` in that sentence (or reword to \"the gate's `--baseline` mode re-derives this baseline; `--selftest` only pins the classifier against fixtures\").", "line": 292, "pr": 1931, "problem": "Doc-vs-code drift on the gate's own interface: the § Preventing gate text states \"The gate's `--selftest` re-derives this baseline rather than hardcoding it\". In agents/scripts/core/test-plan-claim-anchors.sh, `--selftest` runs fixture assertions against a synthetic mktemp tree via SMATCHET_PLAN_BASE (see the `--selftest — fixture assertions on the classifier` header at :50 and the block at :98-135) and never touches docs/high-integrity/plan-claim-anchor-baseline.md. It is `--baseline` that re-derives and rewrites the baseline (:49, :735-741). A reader following this postmortem would run the wrong flag and conclude the 38/30/25 figures are self-verifying when they are not.", "severity": "MEDIUM", "sha": "0ac0f723", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/self-improvement/postmortems.md", "fix": "Re-anchor to the historical evidence, e.g. pin the sha the claim was true at (`scripts/dev/local/build_standalone.ps1:71-83` @ da36b45f) or note the file was retired by #1955 and cite the bash successor.", "line": 286, "pr": 1931, "problem": "Stale citation anchors: the § What escaped paragraph pins `Get-VsWherePath` (:71-83) in `scripts/dev/local/build_standalone.ps1` and names `scripts/dev/build_standalone.ps1`. Neither path exists at develop — PR #1955 (0a78d77c, \"port the build + local-dev PowerShell scripts to bash\") deleted build_standalone.ps1, so the line-pin is unverifiable and the file reference dangles. Ironic given this is the postmortem that introduced the citation-anchor gate.", "severity": "LOW", "sha": "0ac0f723", "userVisible": false}
{"batch": "batch-20-redo", "file": "QUICKSTART.md", "fix": "Update the intro sentence to name the actual shell, e.g. \"Windows, Git Bash (ships with Git for Windows) — no Visual Studio Developer Command Prompt, no MSYS2\", keeping the PowerShell fences for the winget prerequisite steps only.", "line": 3, "pr": 1933, "problem": "Surviving intro line promises the whole flow runs in \"Windows, ordinary PowerShell — no Visual Studio Developer Command Prompt, no MSYS2\", but the build step it leads into (line 31-38, since rewritten) is a bash fence running `./build.sh` and explicitly says \"Run it from Git Bash\". A first-run user follows the intro, stays in PowerShell, and `./build.sh` fails immediately — the exact failure the quickstart exists to prevent.", "severity": "MEDIUM", "sha": "2c087df8", "userVisible": false}
{"batch": "batch-20-redo", "file": "Source/Core/src/Tracker/TrackerSetupPure.cpp", "fix": "Gate on actual first-run: add `|| cfg.BackendHasBeenReachable` to the early-return guard so the unlock can only fire on an install that has never latched reachability; add a bucket-A case for the veteran-enables-read-only path.", "line": 71, "pr": 1935, "problem": "ApplyVerifiedSaveUnlock is documented as the FIRST-RUN unlock but has no first-run condition — it fires on any session where ReadOnlyMode is true and the pin matches. A veteran (BackendHasBeenReachable already true) who ticks \"Read-only mode\" (SmatchetPreferencesUi.cpp:289 writes d.cfg directly), presses Test connection (green -> pin set), then Save & Sync has their deliberate write-protection silently cleared, with only a LOG_INFO and no UI cue. The one-shot pin consumption (a4133112) only closed the re-enable-AFTER-save ordering, not enable-then-probe-then-save.", "severity": "HIGH", "sha": "ec7321cf", "userVisible": true}
{"batch": "batch-20-redo", "file": "Source/Core/src/Tracker/TrackerSetupPure.cpp", "fix": "Mix the field length into the digest (e.g. hash s.size() as 8 bytes after the field) instead of a constant XOR, and correct the comment.", "line": 20, "pr": 1935, "problem": "The field terminator `h ^= 0xFFULL; h *= kFnvPrime;` is byte-identical to hashing a 0xFF byte, so it is a separator, not a length delimiter as the comment claims. A field containing byte 0xFF is indistinguishable from a field boundary: CredentialFingerprint({\"a\\xFF\",\"b\"}) == CredentialFingerprint({\"a\",\"\\xFFb\"}). Dormant in practice (0xFF is not valid UTF-8), but the comment overstates the guarantee.", "severity": "LOW", "sha": "ec7321cf", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/ui/tracker_first_run_setup.test.cpp", "fix": "Make the null-controller path fail closed (IM_CHECK(app != nullptr)) or mark the test skipped via an engine-visible mechanism so the driver does not report a green run for zero real assertions.", "line": 110, "pr": 1935, "problem": "Both bucket-E tests return early with only a ctx->LogInfo(\"SKIP: ...\") when SmatchetActiveUiTestAppController() is nullptr. The ImGui Test Engine counts an early return as PASS, so scripts/dev/test-ui-tracker-first-run-setup.sh reports passed=2 failed=0 and exits 0 while nothing was exercised — the driver's zero-run floor cannot catch it because the count is non-zero.", "severity": "LOW", "sha": "ec7321cf", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/bats/fail_open_gate_cluster.bats", "fix": "Assert the major version in the probe: `\"$c\" -c 'import sys; sys.exit(0 if sys.version_info[0] >= 3 else 1)'`, and/or reorder candidates to `python3 python py` to match the other seven call sites.", "line": 33, "pr": 1936, "problem": "The exec-probe `\"$c\" -c \"\"` only proves the binary runs, not that it is Python 3. Combined with the candidate order here (`python` tried FIRST), a box where `python` is Python 2 selects it, PYTHON is exported to the drivers, the `skip` guard never fires, and the py3-only driver parsing fails/produces nothing — the same class of silent miss the probe was added to close.", "severity": "MEDIUM", "sha": "f423605a", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/core/issue-sweep.sh", "fix": "Make the probe version-aware: `\"$c\" -c 'import sys; sys.exit(0 if sys.version_info[0] >= 3 else 1)'`.", "line": 62, "pr": 1936, "problem": "Probe accepts any runnable interpreter, including Python 2 (`python -c \"\"` exits 0). On a py3-less box PY is set, the `[ -n \"$PY\" ]` hard-exit guard on line 64 never fires, and the embedded py3 program runs under py2 — its output is captured into `records` without an exit-status check, so the sweep reports zero records rather than failing.", "severity": "MEDIUM", "sha": "f423605a", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/project/migrate-bugs-to-issues.sh", "fix": "Add a `sys.version_info[0] >= 3` assertion to the probe expression.", "line": 38, "pr": 1936, "problem": "Same probe weakness as issue-sweep.sh: exec-validation does not exclude Python 2, so a py2-only host passes the guard on line 40 and the classifier yields 0 records — exactly the failure mode the comment on lines 32-35 claims to prevent.", "severity": "MEDIUM", "sha": "f423605a", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/bats/lint_rules.bats", "fix": "Move `_resolve_py` into a shared helper (e.g. tests/bats/helpers/*.bash plus a sourced snippet for the two scripts) and have all call sites use it.", "line": 36, "pr": 1936, "problem": "The identical 4-line exec-validating probe is copy-pasted into 8 files (2 shell scripts + 6 bats suites) with three different candidate orders and three different variable names (PY / PYBIN / PYTHON). `_resolve_py` here is the only factored version and it is file-local, so any future fix (e.g. the py2 guard above) must be applied in eight places.", "severity": "LOW", "sha": "f423605a", "userVisible": false}
{"batch": "batch-20-redo", "file": "Source/Core/src/Ui/SmatchetAboutUi.cpp", "fix": "Route it through the shim like its siblings — `ImGui::IsPopupOpen(kAboutPopupId, ImGuiPopupFlags_AnyPopupLevel)` (adding an IsPopupOpen forwarder to SmatchetLocalizedImGui.h that applies WindowTitleFromSource), or compare against `::ImGui::GetID(SmatchetLocalization::WindowTitleFromSource(kAboutPopupId))`.", "line": 456, "pr": 1937, "problem": "The dismissal guard calls raw `::ImGui::IsPopupOpen(kAboutPopupId, ...)` while the open/begin calls two lines earlier go through the localized shim (`ImGui` is #defined to SmatchetLocalizedImGui, whose OpenPopup/BeginPopupModal run WindowTitleFromSource). `kAboutPopupId` == \"About Smatchet\" has a catalog entry ({\"window.about\", \"About Smatchet\", \"À propos de Smatchet\"}), so under a non-English locale the popup is registered under the translated ID and this raw lookup hashes the English literal — it returns false unconditionally. The very protection the adjacent comment describes (do not clear state when a deeper popup owns the frame) is therefore dead in that locale: showAbout is cleared and the cached snapshot released while the modal is still open.", "severity": "MEDIUM", "sha": "fce0951c", "userVisible": true}
{"batch": "batch-20-redo", "file": "tests/Core/AboutInfo.test.cpp", "fix": "Keep the pure assertions (ParseDepManifest / FormatAboutGitLine / BuildAboutReportText over synthetic AboutInfo) in the doctest and move the real-GatherAboutInfo smoke assertions — including the dep-manifest-present case — to tests/bats/about_buildinfo.bats, which already drives the config-touching surface.", "line": 172, "pr": 1937, "problem": "Three doctest cases call the real `GatherAboutInfo(...)` (lines 172, 183, 200), which runs `ConfigManager::Load()` / `GetUserDataDirectory()` / `GetConfigPath()` — on-disk I/O, which the tests/** instruction forbids in the pure-logic doctest lane. It passes only because GatherAboutInfo degrades gracefully when no config exists, so it is a latent flake on any machine/runner with an unexpected config rather than a hard failure. (Self-documented as open debt in docs/self-improvement/categories/debt/2026-08-04-about-modal-cr-deferrals.md, still unfixed at HEAD.)", "severity": "MEDIUM", "sha": "fce0951c", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/project/test-about-buildinfo-bats.sh", "fix": "Count skips separately (`grep -cE '^ok [0-9]+ .*# skip'`) and either subtract them from PASSED or, when running with CI set, fail if every case skipped.", "line": 45, "pr": 1937, "problem": "PASSED is counted with `grep -cE '^ok [0-9]+'`, and bats emits skipped tests as `ok N # skip ...`. When cmake is absent every case in about_buildinfo.bats self-skips, so PASSED=8/FAILED=0, the PASSED-floor guard at line 53 is satisfied and the wrapper exits 0 — a green report with zero actual coverage. Intentional for a cmake-less dev box, but it means a CI image that loses cmake silently reports the About codegen gate as passing.", "severity": "LOW", "sha": "fce0951c", "userVisible": false}
{"batch": "batch-20-redo", "file": "tests/Core/AboutInfo.test.cpp", "fix": "Guard the second-to-last check with `REQUIRE(text.size() >= 2);`.", "line": 141, "pr": 1937, "problem": "`CHECK(text[text.size() - 2] != '\\n');` is only guarded by `REQUIRE(!text.empty())`. A one-character report would index `text[(size_t)-1]`, an out-of-bounds read on std::string::operator[]. Dormant today because BuildAboutReportText always emits multiple lines.", "severity": "LOW", "sha": "fce0951c", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/shipped/about-dialog-help-menu.md", "fix": "Change `scripts/dev/worktree.ps1` to `scripts/dev/worktree.sh`.", "line": 42, "pr": 1937, "problem": "The git-worktree row points at `scripts/dev/worktree.ps1`, which does not exist in the tree — only `scripts/dev/worktree.sh` (and `worktree-prune.sh`). The corresponding comment in cmake/SmatchetGenerateBuildInfo.cmake names worktree.sh, so the doc is the drifted copy.", "severity": "LOW", "sha": "fce0951c", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/core/test-agent-contract.sh", "fix": "Run the selftest inline at the start of --check (e.g. run_selftest >/dev/null || exit 1 before the check loop), or add an explicit --selftest step in .github/workflows/doc-validation.yml and scripts/dev/test-docs.sh.", "line": 72, "pr": 1938, "problem": "The --selftest regression guard added by this PR is never executed automatically. scripts/dev/test-all.sh and scripts/dev/test-docs.sh enroll the script via the test-*.sh glob with no args, and .github/workflows/doc-validation.yml:347 runs it with no args — all resolve to ${1:---check}. No caller in the repo passes --selftest, so the 4-byte-UTF-8 locale assertions (lines 49-68) are dormant: a future edit dropping LC_ALL=C from grep_fixed reintroduces the Git-Bash false-negative with nothing failing in CI.", "severity": "MEDIUM", "sha": "640a1abf", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/core/test-shell-lint.sh", "fix": "Require the exec-check to be an invocation of a python-ish token: e.g. match `(\\\"?\\\\$\\\\{?(var|_p|PY[A-Z]*)\\\\}?\\\"?|['\\\"]?python[0-9.]*['\\\"]?)[[:space:]]+(-c|--version|-V)` within the window (still allowing the canonical probe-one-var/validate-another shape) instead of a bare `-c` anywhere.", "line": 465, "pr": 1939, "problem": "Rule 9 shape (a) suppresses the violation whenever PY_EXEC_RE ('[[:space:]](-c|--version|-V)([[:space:]]|$)') matches ANYWHERE in the 8-line window, unanchored to the probed candidate. Common unrelated tokens inside a loop body — `bash -c`, `grep -c`, `sort -c`, `xargs`/`tar -c`, or any `--version` of another tool — silently exempt a genuinely resolve-only python picker, so this blocking whole-tree gate fails open on exactly the silent-wrong class it exists to catch.", "severity": "MEDIUM", "sha": "96d58332", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/py-probe-exec-validation-gate.md", "fix": "Update §Approach/§Risks/§Out of scope to describe the widened shape (b) and the blessed lib/resolve-py.sh resolver, and drop the 'single-candidate guards are not flagged / ~15 backlogged' claims.", "line": 53, "pr": 1939, "problem": "Doc-vs-code drift on the shipped gate's scope. The plan states rule 9 fires ONLY on the >=2-candidate picker and that a single-candidate `command -v python3 || exit 2` hard-require guard is 'not flagged' (also §Risks:133-136 'residual ~15 backlogged', §Out of scope:165, §Deviations:178-183, and line 42 'no shared shell library exists'). At HEAD shape (b) is WIDENED to flag ANY literal python-name probe outside agents/scripts/core/lib/resolve-py.sh (which now exists), and tests/bats/shell_lint.bats asserts the single-candidate guards at fixture lines 28 and 34 DO fire. A reader of this still-'active' plan gets a materially wrong picture of a blocking gate's scope.", "severity": "MEDIUM", "sha": "96d58332", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/py-probe-exec-validation-gate.md", "fix": "Repin to lines 14 and 20/22/28/30/34, or describe the sites without hardcoding line numbers.", "line": 191, "pr": 1939, "problem": "Stale line pins in §Deviations: 'the bad fixture now reports both line 11 (shape a) and line 17 (shape b)'. In tests/fixtures/shell_lint/known-bad-9-py-probe.sh at HEAD the shape-(a) probe is line 14 and the shape-(b) sites are lines 20, 22, 28, 30, 34 (as asserted by shell_lint.bats).", "severity": "LOW", "sha": "96d58332", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/plans/active/py-probe-exec-validation-gate.md", "fix": "Fill the implementation log, flip Status to shipped, move to docs/plans/shipped/py-probe-exec-validation-gate.md and update inbound references.", "line": 3, "pr": 1939, "problem": "Plan is shipped (rule 9, fixtures, bats cases and all 10 'Files to modify' entries are live at origin/develop) but is still **Status**: active under docs/plans/active/ with §Implementation log left as '_(filled post-ship)_', contradicting its own §Archive instruction (line 197) to move to docs/plans/shipped/.", "severity": "LOW", "sha": "96d58332", "userVisible": false}
{"batch": "batch-20-redo", "file": "agents/scripts/core/test-shell-lint.sh", "fix": "Extend the window to the loop's matching `done` (or widen to ~20 lines) rather than a fixed 8-line span.", "line": 462, "pr": 1939, "problem": "The shape-(a) detection window is a hardcoded 8 lines from the `for` header (`sed -n \"${lno},$((lno + 8))p\"`). A candidate loop whose `command -v \"$c\"` probe (or whose exec-validation) sits further into the body is silently skipped — another quiet miss for a binary, no-WARN-tier gate.", "severity": "LOW", "sha": "96d58332", "userVisible": false}
{"batch": "batch-20-redo", "file": "docs/self-improvement/categories/tooling/2026-08-04-gate-scripts-resolve-only-python-probes.md", "fix": "Flip to `Status: resolved` with the resolving PR/sha, bump Last-reviewed, and drop or refresh the stale line pins (`:20`, `:26`, `:22`, `:77`, `:411`, `:16` in the Remaining-sites and Cross-ref blocks, lines 9-11 and 23-25) — none of those line numbers point at a `command -v python` probe any more.", "line": 26, "pr": 1940, "problem": "Backlog entry still reads `Status: open` / `Last-reviewed: 2026-08-04`, but the work it describes is fully done at HEAD: all six cited gate scripts now call the exec-validating `resolve_py()` (check-pr-intent.sh:22, sort-applied-md.sh:28, test-autonomous-debug-loop.sh:24, test-agent-contract.sh:79, test-lint-hook-split.sh:338, test-skill-load-log.sh:18), the resolver was hoisted to agents/scripts/core/lib/resolve-py.sh as suggested, and test-shell-lint.sh (~:432/:478) already enforces routing through it. A stale open item invites an agent to redo completed work.", "severity": "MEDIUM", "sha": "fd523b48", "userVisible": false}
{"batch": "batch-20-redo", "file": ".github/workflows/release.yml", "fix": "Add workflow- or job-level `env: SCCACHE_DIR: ${{ github.workspace }}\\.sccache` to release.yml, mirroring build-and-test.yml; or drop the .sccache cache steps if the GHA backend is intended.", "line": 143, "pr": 1883, "problem": "The two sccache steps cache `${{ github.workspace }}\\.sccache`, but release.yml never sets SCCACHE_DIR (build-and-test.yml sets it workflow-level at line 24 and documents the local-disk backend as the deliberate choice). sccache therefore writes elsewhere (its default dir, or the GHA backend the action enables), so the cached path is never populated: restore/save are no-ops (actions/cache warns on the empty path) and every signed release compiles cold against a 60-minute job timeout.", "severity": "MEDIUM", "sha": "748da9aa", "userVisible": false}
{"batch": "batch-20-redo", "file": ".github/workflows/release.yml", "fix": "In the same pwsh guard (with the password also in that step's env), Write-Error + exit 1 when [string]::IsNullOrWhiteSpace($env:SMATCHET_SIGN_PFX_PASSWORD), so a bad secret fails in the first minute.", "line": 174, "pr": 1883, "problem": "The 'Decode signing certificate' step fails fast only on a missing SMATCHET_SIGN_PFX_BASE64; SMATCHET_SIGN_PFX_PASSWORD is never validated. release-github.sh configure_signing() (scripts/publish/release-github.sh:395) accepts an empty password silently, so an unset/rotated-away password secret is only discovered when signtool runs, after the full ~40-minute release build.", "severity": "LOW", "sha": "748da9aa", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/SmatchetPreferencesUi_General.cpp", "fix": "Resolve currentPref (and the two displayed paths) once on the section/page open-edge and cache it in UiDrawSession — the same pattern drawPreferencesTrackerTab already uses via RefreshCachedProjectsSnapshotOnOpen — and refresh the cached value only after a successful ConfigManager::SetStoragePreference.", "line": 247, "pr": 1941, "problem": "DrawGeneralStorageSection re-resolves the storage preference every frame: ConfigManager::GetStoragePreference(runtimeAssetDir, kDefaultPref) does a FileExists() stat plus an ifstream open + line-by-line parse of smatchet_storage_mode.txt (ConfigManager_PathUtils.cpp:623-657), and GetUserDataDirectory()/GetStoragePreferenceFlagPath() run again at :300-301. This is synchronous disk I/O on the ImGui render path for every frame the General > Storage section is expanded — AGENTS.md Quality Pillar 2 states 'sync I/O on UI thread = code-review CRITICAL'. The section's own help text advertises the runtime-asset dir may be a network share or source-controlled tree, exactly where the per-frame stat+read stalls the UI thread.", "severity": "CRITICAL", "sha": "c7fb2236", "userVisible": true}
{"batch": "batch-21", "file": "tests/ui/prefs_schema_coverage.test.cpp", "fix": "Either add the label assertion (record label alongside id, diff against LabelEn with the documented shortcuts.keyboard.bindings exemption) or delete the label-check claim from PreferencesSchema.h:16-17, PreferencesSchema.cpp:334-335 and the plan so the contract matches the guard.", "line": 163, "pr": 1941, "problem": "The drift guard only diffs id sets (observed vs declared). It never compares a widget's rendered label with its descriptor's LabelEn, yet three surviving contracts assert it does: PreferencesSchema.h:16-17 ('LabelEn ... is both the search haystack seed and the drift guard's label check'), PreferencesSchema.cpp:334-335 ('the drift guard's label check exempts this id'), and the shipped plan's Q2/§Verification. A widget renamed while keeping its id therefore drifts silently — the label is what feeds the search haystack, so the setting becomes unfindable with nothing red, which is the exact 'invisible search break' ADR-0023 calls the guard load-bearing for.", "severity": "MEDIUM", "sha": "c7fb2236", "userVisible": false}
{"batch": "batch-21", "file": "docs/plans/shipped/preferences-ia-resegmentation-and-search.md", "fix": "Drop the '(n) badges are kept' clause (or record it as dropped) — or add the badge to ComposeNavLabel using d.prefsFilter.CategoryMatchCount while filtering.", "line": 338, "pr": 1941, "problem": "§ Deviations states 'The (n) badges from Q8 are kept', but the shipped nav rail renders no match-count badge: SmatchetPreferencesUi_Shell.cpp ComposeNavLabel composes only '<Title>[ *]###<navId>', and CategoryMatchCount is consumed solely by CategoryIsEmptyUnderFilter for the BeginDisabled test. The post-implementation record claims a UX affordance the code does not have.", "severity": "LOW", "sha": "c7fb2236", "userVisible": false}
{"batch": "batch-21", "file": "docs/plans/shipped/preferences-ia-resegmentation-and-search.md", "fix": "Add a § Deviations row: the tab-local filter box survives as the inactive-global-query fallback (the global query takes over the row filter when active), and there is no Shortcuts-local (N of M) readout.", "line": 212, "pr": 1941, "problem": "§ Slices slice-3 says the Shortcuts tab-local '###kbSearch' box and its 'static std::string filter' are **deleted** and that the Shortcuts '(N of M)' readout counts dynamic row matches. Shipped code keeps both: SmatchetPreferencesUi_Keybindings.cpp still owns s_kbFilter and draws InputTextWithHint(\"###kbSearch\", ...) whenever the global query is inactive, and the only readout is the global 'showing N / M' in DrawPrefsSearchReadout. Neither divergence is recorded in § Deviations, so the shipped plan misdescribes the code (AGENTS.md § Plan revision after implementation).", "severity": "LOW", "sha": "c7fb2236", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/postmortems.md", "fix": "Re-pin to the current range (~:255-320) or drop the numeric pin in favour of a symbol/marker reference (e.g. `DATE_RE` / `resolve_date`), so the citation survives future edits.", "line": 259, "pr": 1944, "problem": "Stale line-pin: the entry cites `agents/scripts/core/test-plan-index.sh:122-143` as the git-log-based date derivation. At HEAD that range is selftest fixture code; the actual `git log --follow` derivation now lives around lines 261-320 (the marker-based fix this postmortem proposed has since landed and shifted the file).", "severity": "LOW", "sha": "51bb18ec", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/postmortems.md", "fix": "Update or de-number these citations (name the guard/fallback instead of pinning lines); note in the entry that the proposed `<!-- plan-date: -->` marker fix has since landed, which is what moved them.", "line": 270, "pr": 1944, "problem": "Stale line-pins: cites the shallow-clone guard at `:45`, `:105` and the staged-rename sibling-tier fallback at `:135-143`. At HEAD only `:45` still lands on the shallow guard; `:105` and `:135-143` are now selftest code, and the staged-rename fallback sits near :316.", "severity": "LOW", "sha": "51bb18ec", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/setup-env.sh", "fix": "Fill the row's pacman/apt/brew cells (e.g. `bats` / `bats` / `bats-core`) so pkgs_for resolves it without npm.", "line": 156, "pr": 1946, "problem": "The `bats` package row maps only to npm (`-` for pacman/apt/brew), yet check-required-tools.sh:64 documents working installs `pacman -S bats` and `brew install bats-core`. On a host without npm (MSYS2, or macOS with brew only), this REQUIRED tool is classified 'no automatic install' and setup-env exits 1 even though the native manager could provide it.", "severity": "MEDIUM", "sha": "", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/setup-env.sh", "fix": "Compute SUDO only when non-root and sudo exists (`SUDO=; [[ $EUID -ne 0 ]] && have sudo && SUDO=sudo`), and run `$SUDO apt-get update` once before the first install.", "line": 204, "pr": 1946, "problem": "apt branch hardcodes `sudo apt-get install -y` with no prior `apt-get update`. In the documented `--yes` 'CI / fresh-clone bootstrap' case the run is usually root in a container with no `sudo` binary, so every apt install fails with 127; a stale index likewise 404s.", "severity": "MEDIUM", "sha": "", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/setup-env.sh", "fix": "Reword to name only shellcheck as the tool the probe does not cover.", "line": 326, "pr": 1946, "problem": "Comment claims 'shellcheck and bats are lint / test-harness tools it [the probe] never looks for', but bats is a REQUIRED row in check-required-tools.sh:64. Stale rationale misdescribing probe coverage.", "severity": "LOW", "sha": "", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/setup-env.sh", "fix": "Delimit the usage block with a sentinel (print from line 2 until the first non-comment line, or an explicit end marker) instead of fixed line numbers.", "line": 49, "pr": 1946, "problem": "`--help` renders the header with a hardcoded line pin `sed -n '2,30p' \"$0\"`. Any edit to the header comment silently truncates or over-prints help output, with no gate catching the drift.", "severity": "LOW", "sha": "", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Change `(:162-164)` to `(:167-175)`, or drop the numeric pin and cite the `GIT_BIN = _resolve_bin(\"git\", ...)` symbol instead so it cannot drift.", "line": 2279, "pr": 1947, "problem": "Stale line-pin: the resolution text claims `git` gained the WindowsApps-shim treatment at `merge-watcher.py` `:162-164`. At origin/develop those lines are the bash candidate tuple (`C:\\msys64\\usr\\bin\\bash.exe`, the `os.path.exists` check, `BASH_BIN = candidate`); the GIT_BIN resolution actually lives at :167-175. The adjacent pin `merge-watcher.py:157` is correct, so the reader trusts the range and lands on the wrong code.", "severity": "LOW", "sha": "2584205d", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/font_asset_resolve.bats", "fix": "Pin discovery in resolve()/setup: export GIT_CEILING_DIRECTORIES=\"${BATS_TEST_TMPDIR}\" (the main/linked fixtures inside it still resolve) before invoking cmake -P.", "line": 104, "pr": 1948, "problem": "The \"resolves empty outside a git tree\" test assumes BATS_TEST_TMPDIR is not inside any git checkout. git rev-parse discovery walks upward, so when TMPDIR is redirected into the workspace (common on CI/MSYS), the module resolves the enclosing repo's assets/fonts/fa-solid-900.ttf (which CI fetches) and the assertion on line 109 fails — an environment-dependent false failure.", "severity": "MEDIUM", "sha": "2602340e", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/font_asset_resolve.bats", "fix": "Drop the output redirection and assert the fixture, e.g. `[ -e \"${LINKED}/.git\" ]` at the end of make_trees.", "line": 48, "pr": 1948, "problem": "`git worktree add ... >/dev/null 2>&1` swallows failure. If worktree creation fails, ${LINKED} never exists and the \"resolves empty when neither worktree has the TTF\" test (line 95) still passes for the wrong reason — a false PASS.", "severity": "LOW", "sha": "2602340e", "userVisible": false}
{"batch": "batch-21", "file": "cmake/SmatchetFontAssets.cmake", "fix": "On non-zero rc, retry without --path-format=absolute and absolutize the result via get_filename_component(... ABSOLUTE BASE_DIR sourceDir), or emit a STATUS noting the git-version limitation.", "line": 45, "pr": 1948, "problem": "`rev-parse --path-format=absolute` requires git >= 2.31; on an older git the command exits non-zero, the function returns \"\" and the linked-worktree fallback silently disappears with no diagnostic distinguishing it from \"TTF genuinely absent\".", "severity": "LOW", "sha": "2602340e", "userVisible": false}
{"batch": "batch-21", "file": "assets/fonts/README.md", "fix": "Point the sentence at `Source/Standalone/CMakeLists.txt`.", "line": 28, "pr": 1948, "problem": "Doc says `CMakeLists.txt` adds the copy_if_different POST_BUILD step, but that step lives in Source/Standalone/CMakeLists.txt (root CMakeLists.txt only include()s cmake/SmatchetFontAssets.cmake) — stale cross-ref for anyone following the drop-in instructions.", "severity": "LOW", "sha": "2602340e", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/SmatchetAboutUi.cpp", "fix": "Hoist a function-local static std::vector<char> scratch buffer (single-threaded UI thread) and assign into it, or keep one reusable buffer in AboutDrawCtx, instead of constructing a fresh vector per row per frame.", "line": 70, "pr": 1950, "problem": "SelectableValue() heap-allocates a std::vector<char> copy of the value on every call, and it is called once per fact row plus once per dependency, i.e. ~15-20 allocations every frame the About modal is open — a per-frame heap allocation on the ImGui render path. The copy also defeats the ReadOnly fast path's whole point since ImGui never writes to the buffer.", "severity": "LOW", "sha": "1401376b", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/SmatchetAboutUi.cpp", "fix": "Use SmatchetLocalization::TranslateSource(\"unknown\") (and add the row to the localization table) for the placeholder.", "line": 68, "pr": 1950, "problem": "The empty-value placeholder literal \"unknown\" is emitted straight into the InputText buffer, bypassing TranslateSource — every other chrome string in this TU is localized (and pinned by AboutLocalization.test.cpp), so a French user sees an untranslated \"unknown\" for any missing fact.", "severity": "LOW", "sha": "1401376b", "userVisible": true}
{"batch": "batch-21", "file": "scripts/dev/lockfile.py", "fix": "Capture rc = subprocess.call(...); if rc < 0: rc = 128 + (-rc) before returning, so signal deaths mirror the shell/flock convention.", "line": 122, "pr": 1951, "problem": "subprocess.call() returns a NEGATIVE value (-N) when the child is terminated by a signal on POSIX; main() returns it straight to sys.exit(), which masks it to (256-N)&255 (e.g. SIGKILL -> 247). flock(1), which this script's docstring advertises itself as a drop-in stand-in for (see the 'flock equivalences' table, lines 25-28), propagates 128+N (137). Callers that key off the child's exit status see a bogus code.", "severity": "LOW", "sha": "5df45025", "userVisible": false}
{"batch": "batch-21", "file": "docs/harness/claude-code/hooks/lint-syntax-both.py", "fix": "Distinguish \"no first-party diagnostics\" from \"compilation never reached first-party code\": if a TU's returncode!=0 and 100% of its lines matched FP patterns/banner, record an inconclusive result and emit a distinct non-zero-or-WARN outcome naming the missing `ninja-iter-clang` preset, instead of silently dropping the target.", "line": 199, "pr": 1952, "problem": "The new bare-filename banner filter plus the widened system-header allow-list turn the dual-target syntax gate into a fail-open: when a compile aborts at the first system header (the documented no-VS-Developer-shell case), every produced line is FP-filtered or is the banner, `real_lines` is empty, no failure is appended, and the hook exits 0 — reporting PASS for a run that never parsed a single first-party token. The PR's own self-improvement entry records that an injected `undeclared_symbol_probe()` returns rc=0.", "severity": "HIGH", "sha": "2df7bb4e", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Commands/Scenarios/UserInfoScreenshotScenarios.cpp", "fix": "Either keep the cfg-field unwind inline in OnFinish (only the g_ui view/identity pins need deferring), or drain the pending queue on shutdown as well; and correct the ScenarioCaptureQuiesce.h contract comment, which currently claims the deferred callbacks touch only session-scoped state.", "line": 168, "pr": 1952, "problem": "Deferring `restoreState()` via QueuePostCaptureRestore makes the unwind conditional on another frame being drawn, but restoreState restores PERSISTED config fields (cfg.GitHubPat, GitCommitRepos, GitHubOwner, GitHubRepo, UiMode, VcsFeedLayout, WhisperSetupCompleted), not just session state. ScenarioCaptureQuiesce.h:57-61 asserts the opposite (\"every field they restore is session-scoped, which is harmless\") and explicitly blesses a process exiting before the drain. An in-process `scenario.run` whose app exits (or saves config) before the next frame persists the cleared GitHub PAT/repos and WhisperSetupCompleted=true to the user's config.", "severity": "MEDIUM", "sha": "2df7bb4e", "userVisible": true}
{"batch": "batch-21", "file": "Source/Core/include/Commands/CommandPaletteUi.h", "fix": "Either gate the setter on `open_` (making the comment true) or reword the comment to \"latched; consumed on the next Draw while open\".", "line": 40, "pr": 1952, "problem": "Doc-vs-code drift: `FocusFilterInput()` is documented as \"a no-op while the palette is closed\", but it unconditionally sets `focusFilterPending_`, and CommandPaletteUi::Draw returns at the `if (!open_)` early-out without clearing the latch — so the request survives until the palette is next drawn open and then steals window focus there.", "severity": "LOW", "sha": "2df7bb4e", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/tooling/2026-08-05-dual-target-syntax-hook-vacuous-without-clang-preset.md", "fix": "Update the reference to docs/harness/claude-code/hooks/lint-syntax-both.py (optionally noting it is installed into .claude/hooks/ by harness setup).", "line": 9, "pr": 1952, "problem": "Cross-ref points at `.claude/hooks/lint-syntax-both.py`, which is not a tracked path in this repo (no `.claude/` files are tracked). The canonical, tracked copy of the script edited by this same PR is docs/harness/claude-code/hooks/lint-syntax-both.py.", "severity": "LOW", "sha": "2df7bb4e", "userVisible": false}
{"batch": "batch-21", "file": "CMakeLists.txt", "fix": "Require content as well as existence: after `math(EXPR SMATCHET_ICON_BYTE_COUNT ...)`, fall into the else-branch (WARNING + SMATCHET_ICON_AVAILABLE 0) when `SMATCHET_ICON_BYTE_COUNT` is 0 — e.g. `if(EXISTS ... )`/`file(READ ...)` then `if(_smatchet_icon_nibbles GREATER 0)` gating the AVAILABLE=1 assignment.", "line": 565, "pr": 1954, "problem": "`if(EXISTS \"${_smatchet_icon_src}\")` treats a zero-byte smatchet.ico as present: SMATCHET_ICON_AVAILABLE is set to 1 with SMATCHET_ICON_BYTES empty, so the generated header emits `static const unsigned char kSmatchetIconIcoBytes[] = { };` — a zero-size array, which is a hard compile error on MSVC (C2466). That defeats the block's own stated contract (\"A missing icon is a WARNING, never a FATAL_ERROR ... falls back to a glyph\"): an empty/corrupt-to-zero asset breaks the build instead of degrading to the glyph.", "severity": "LOW", "sha": "1f4a6b74", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/include/Persistence/SmatchetIcoDecode.h", "fix": "Either weaken the doc to \"never reads outside bytes[0,byteCount) and reports all format errors via return/outError; may throw std::bad_alloc\", or make it true by catching std::bad_alloc around the resize and returning false with an out-of-memory outError.", "line": 27, "pr": 1954, "problem": "The documented contract says DecodeIcoToRgba \"Never throws\", but the implementation calls `outRgba.resize(rowBytes * height)` (up to 512x512x4 = 1 MiB) and assigns std::string error literals — both can throw std::bad_alloc. The only caller, TryGetAboutIconTexture (Source/Core/src/Ui/SmatchetAboutIcon.cpp), is invoked from the ImGui render path with no try/catch, so the doc claim is what a UI-path caller is relying on.", "severity": "LOW", "sha": "1f4a6b74", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/local/test-build-wrapper.sh", "fix": "Assert a positive outcome too: capture rc and require 0 (the fake cmake exits 0), or require the output to contain \"build-standalone: cmake --preset\" so the test fails when the script never reaches the configure step.", "line": 353, "pr": 1955, "problem": "Test 9 (test_preflight_no_overreach) is a pure negative assertion: it only checks that \"cl.exe is not on PATH\" is absent from the output, never that the run reached the intended point. Any earlier failure makes it pass vacuously — build-standalone.sh calls smatchet_configure_preset_binary_dir, which needs python, but the test forces PATH=\"$fake_bin:/usr/bin:/bin\"; in Git Bash on Windows (the target platform) python is not under /usr/bin, so the script dies with \"no working python interpreter\" and the test still reports PASS. It would not catch a regression that re-broadened the msvc/clang pre-flight.", "severity": "MEDIUM", "sha": "0a78d77c", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/local/test-build-wrapper.sh", "fix": "Register the created paths with the existing EXIT trap (e.g. append to a global CREATED array cleaned in the trap handler), or make run-standalone.sh's sibling roots overridable so the test writes nothing under $REPO_ROOT/build.", "line": 101, "pr": 1955, "problem": "test_run_standalone_sibling_table writes fake executables into the developer's REAL build tree ($REPO_ROOT/build/ninja-debug-msvc/Smatchet.exe and .../ninja-iter-msvc/Smatchet.exe) rather than a sandbox. Cleanup is inline at the end of the function; the file's only trap is `trap 'rm -rf \"$TMP_ROOT\"' EXIT`, which does not cover these paths. A Ctrl-C, timeout or kill mid-test leaves a 2-line `#!/bin/sh / exit 0` stub named Smatchet.exe in a real preset build dir, which run-standalone.sh will then resolve and launch as the app.", "severity": "MEDIUM", "sha": "0a78d77c", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/verify.sh", "fix": "Capture the child's status and branch: rc>=2 -> report an infra failure and propagate that code; rc==1 -> the current violations message.", "line": 92, "pr": 1955, "problem": "The header documents comment_audit.py's contract as \"exit 1 = violations, >=2 = infra failure\", but the handler treats every non-zero identically: it prints \"comment-noise delta gate found violations\" and die()s with exit 1. An infra failure (bad base ref, missing git, python traceback) is misreported to the author as a lint violation, and verify.sh's own exit code stops distinguishing the two for callers such as build-and-run.sh --verify.", "severity": "LOW", "sha": "0a78d77c", "userVisible": false}
{"batch": "batch-21", "file": "project.config.schema.json", "fix": "Drop build-msvc-asan.sh from the reader list, leaving scripts/dev/with-msvc-env.sh as the single documented consumer.", "line": 38, "pr": 1955, "problem": "Stale doc-vs-code drift in the msvc_toolset_pin description: it claims the key is \"Read by scripts/dev/with-msvc-env.sh (bash) and scripts/dev/local/build-msvc-asan.sh\". This same PR rewrote build-msvc-asan.sh to delegate the whole vswhere -> pinned-toolset -> vcvars dance to with-msvc-env.sh (its own header, lines 4-7); it no longer reads the pin — grep for msvc_toolset_pin in that script matches only the comment.", "severity": "LOW", "sha": "0a78d77c", "userVisible": false}
{"batch": "batch-21", "file": "docs/plans/active/agentic-infra-audit-campaign-2026-06.md", "fix": "Update the path to `docs/plans/shipped/kill-powershell-minimize-toolchain.md`.", "line": 267, "pr": 1956, "problem": "Broken cross-ref: cites `docs/plans/kill-powershell-minimize-toolchain.md`, but the plan lives at `docs/plans/shipped/kill-powershell-minimize-toolchain.md` (no file at the cited path).", "severity": "LOW", "sha": "1b6d4938", "userVisible": false}
{"batch": "batch-21", "file": "docs/plans/active/agentic-infra-audit-campaign-2026-06.md", "fix": "Point the row at `docs/plans/shipped/kill-powershell-minimize-toolchain.md`.", "line": 276, "pr": 1956, "problem": "Same broken cross-ref in the already-known appendix table row HP-01: `docs/plans/kill-powershell-minimize-toolchain.md` does not exist (it is under `docs/plans/shipped/`).", "severity": "LOW", "sha": "1b6d4938", "userVisible": false}
{"batch": "batch-21", "file": "docs/plans/deferred/cursor-vexp-coexistence.md", "fix": "Re-pin to `setup-harness.sh#L527`, or drop the line number and cite the function name `setup_cursor()` so the reference cannot drift again.", "line": 11, "pr": 1956, "problem": "Stale line-pin: the deep link `setup-harness.sh#L462` is claimed to be the `exit 1` for `.cursor/rules` being a file, but at HEAD that check is at lines 527-532 (`setup_cursor()` starts at 522); line 462 is inside the unrelated AGENTS.md verify block. The companion pin `#L199` (gen_subsystem_claude_shims) is still correct.", "severity": "LOW", "sha": "1b6d4938", "userVisible": false}
{"batch": "batch-21", "file": "scripts/publish/test-installer-smoke.sh", "fix": "Prefix both invocations with `MSYS_NO_PATHCONV=1` (or a local `native_exec` helper), and extend msys_argv_switches.bats to cover `$INSTALLER`/`$uninstaller`/`signtool` call sites in tracked *.sh.", "line": 168, "pr": 1957, "problem": "Bare `\"$INSTALLER\" /VERYSILENT /SUPPRESSMSGBOXES /NORESTART` (and the identical uninstall call at line 115) invoke a native Windows exe with `/switch` args from Git Bash without `MSYS_NO_PATHCONV=1`. MSYS rewrites `/VERYSILENT` into `C:/Program Files/Git/VERYSILENT`, so Inno drops the silent switches and shows the interactive wizard — the smoke test hangs (or dies with \"Installer exited non-zero\") on any headless/CI run. This is the exact bug class the same PR introduced `native_exec()` for in release-github.sh, and the later `tests/bats/msys_argv_switches.bats` gate only inspects `$ISCC_EXE`/`$SIGN_TOOL`/`cmd.exe` sites, so these two survive unguarded. Note line 199 already uses `taskkill //F //PID`, showing the escaping was known.", "severity": "HIGH", "sha": "1ec9fb0c", "userVisible": false}
{"batch": "batch-21", "file": "scripts/publish/test-installer-smoke.sh", "fix": "Run it as `MSYS_NO_PATHCONV=1 signtool verify /pa /v ...` and distinguish a signtool invocation error (non-Authenticode failure) from a genuine NotSigned verdict.", "line": 135, "pr": 1957, "problem": "`signtool verify /pa /v \"$(winpath \"$path\")\"` is likewise unguarded against MSYS argument translation: `/pa` and `/v` are rewritten to paths, signtool errors out, and the branch reports `Status=\"NotSigned\"` for a correctly signed installer. `--check-signatures` therefore produces a false negative on every signed release bundle.", "severity": "MEDIUM", "sha": "1ec9fb0c", "userVisible": false}
{"batch": "batch-21", "file": "scripts/publish/release-github.sh", "fix": "Also refuse a resolved OUT_DIR that is $REPO_ROOT itself or an ancestor/parent of it (and refuse `.`/`..` outright) before calling rm -rf.", "line": 218, "pr": 1957, "problem": "`new_clean_directory` `rm -rf`s its target and only refuses the drive root; combined with line 575, a relative `--out-dir` is resolved under $REPO_ROOT, so `--out-dir .` (or `--out-dir ..`) resolves to the repo root / its parent and the very first preflight step deletes the working tree.", "severity": "MEDIUM", "sha": "1ec9fb0c", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/worktree.sh", "fix": "Re-baseline only the caller's own entry by default (match CLAUDE_SESSION_ID / SMATCHET_JANITOR_SELF_SESSION against the entry basename), and require an explicit flag (e.g. --all) to touch sibling entries.", "line": 218, "pr": 1958, "problem": "cmd_resync rewrites EVERY registry entry in the tree to the current branch/sha, with no self-filter. Running `worktree.sh resync` in the shared integration tree silently re-baselines other LIVE sessions' entries, so guard-head-drift.sh stops denying for them — the drift that already happened under those siblings is erased. git-janitor.sh solves the same problem by self-excluding via CLAUDE_SESSION_ID (documented in docs/agent-rules/process-rules.md line 87); resync has no equivalent.", "severity": "HIGH", "sha": "cf622ad6", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/worktree.sh", "fix": "Preserve the entry's existing ts when re-baselining (only branch= and sha= should change); refresh ts only for the caller's own entry.", "line": 219, "pr": 1958, "problem": "cmd_resync writes ts=$now into every entry, resurrecting dead sessions. sr_entry_is_live falls back to ts-freshness for non-authoritative (legacy ppid=1) entries and sr_prune_dead_stale requires ts-staleness before reaping a provably-dead entry — so a resync makes dead siblings count as live for another 30 min and resets their stale clock, meaning periodic resyncs keep dead entries permanently unreapable.", "severity": "MEDIUM", "sha": "cf622ad6", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/worktree.sh", "fix": "Only accept a directory argument when it is absolute (or canonicalise it with `cd \"$SLUG\" && pwd` before use), and skip/refuse --delete-branch when the argument was a path rather than a slug.", "line": 266, "pr": 1958, "problem": "cmd_rm's `if [ -d \"$SLUG\" ]` tests the argument relative to the caller's CWD, but the value is then passed to `git -C \"$REPO_ROOT\" worktree remove \"$SLUG\"`, which resolves relative paths against REPO_ROOT — a relative name that exists in CWD but not under REPO_ROOT targets the wrong path (or nothing). It also bypasses assert_safe_slug, and --delete-branch then derives branch_name=\"feat/$SLUG\" from that same string, producing a nonsense branch name (e.g. `feat/C:/Dev/trees/foo`) when a path was passed.", "severity": "MEDIUM", "sha": "cf622ad6", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/worktree.sh", "fix": "On fetch failure either fall back to the local `$BASE` ref (matching the message) or abort; if the stale origin ref is intentionally used, print how far behind it is instead of claiming the local branch was used.", "line": 172, "pr": 1958, "problem": "On a failed fetch cmd_new prints \"(fetch failed — using local $BASE)\" but the next lines still prefer `origin/$BASE` whenever that ref exists, so the new worktree is silently branched off a stale remote-tracking ref while the user was told a local branch was used — the exact stale-base pitfall docs/harness/SETUP.md § Worktree base warns about.", "severity": "MEDIUM", "sha": "cf622ad6", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/worktree.sh", "fix": "Fetch origin/develop (or verify the ref exists and warn when it doesn't) before classifying worktrees as merged.", "line": 297, "pr": 1958, "problem": "cmd_prune's `merge-base --is-ancestor \"$branch\" origin/develop` runs with no fetch and with stderr/exit status swallowed, so it silently reports nothing when origin/develop is absent, and reports \"MERGED -> safe to remove\" against a possibly days-stale origin/develop.", "severity": "LOW", "sha": "cf622ad6", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/new-session.sh", "fix": "Derive the created path from worktree.sh (e.g. have `worktree.sh new` print a machine-readable path on its last line and capture it) instead of recomputing TREES_ROOT.", "line": 57, "pr": 1958, "problem": "TREES_ROOT default `C:/Dev/trees` is duplicated here and in worktree.sh. If the two ever diverge, the post-create `[ -d \"$TARGET\" ]` check fails and the launcher dies with \"aborting launch\" after worktree.sh already created the worktree, leaving an orphan tree behind.", "severity": "LOW", "sha": "cf622ad6", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/new-session.sh", "fix": "Print the header via a range terminated by content (e.g. `sed -n '2,/^$/p'` on the comment block) or move usage into a heredoc like run-with-procdump.sh's usage().", "line": 32, "pr": 1958, "problem": "`--help` does `sed -n '2,25p'` but the header comment ends at line 22; lines 23-25 are blank plus `set -uo pipefail`, so the help output prints script code. A stale line-pin that drifts on every header edit.", "severity": "LOW", "sha": "cf622ad6", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/run-with-procdump.sh", "fix": "Validate the value at parse time (`case \"$2\" in ''|*[!0-9]*) echo ... >&2; exit 2 ;; esac`) instead of silently ignoring it.", "line": 84, "pr": 1958, "problem": "`[ \"$AUTOCYCLE_PANES\" -gt 0 ] 2>/dev/null` swallows the error for a non-numeric `--autocycle-panes foo`, so SMATCHET_AUTOCYCLE_PANES is never exported and the run proceeds looking normal — the user believes the hands-free repro harness is active when it is not.", "severity": "LOW", "sha": "cf622ad6", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/local/package-unreal-plugin-msvc.sh", "fix": "Canonicalize before comparing: resolve the target with `cd \"$1\" 2>/dev/null && pwd -P` (or realpath -m) and reject any path still containing a `..` segment, then prefix-compare the canonical forms.", "line": 118, "pr": 1959, "problem": "assert_inside_repo() guards rm -rf with a plain string-prefix `case \"$target\" in \"$root\"/*)` and never canonicalizes `..`. A relative --build-dir is prefixed with REPO_ROOT at line 104, so `--build-dir ../../../Windows --force-configure` yields `<repo>/../../../Windows`, which still starts with $root and passes the guard; reset_build_dir then rm -rf's a directory outside the repo. winpath() is the only normalizer and it is an identity function whenever cygpath is absent (documented fallback, lines 87-93).", "severity": "MEDIUM", "sha": "0b4e1035", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/local/rebuild-testproject-plugin.sh", "fix": "Define die() before the source and use `. \"$REPO_ROOT/scripts/common/smatchet-cmake-common.sh\" || die \"could not source smatchet-cmake-common.sh\"`, matching line 57.", "line": 17, "pr": 1959, "problem": "`. \"$REPO_ROOT/scripts/common/smatchet-cmake-common.sh\"` is sourced with no `|| die` and before die() is defined (line 54). With `set -uo pipefail` and no `set -e`, a missing/broken common file does not stop the script; smatchet_python then fails inside read_engine_association and the user gets the misleading error \"no EngineAssociation in <path> - pass --engine-root.\" The sibling source at line 57 is correctly guarded.", "severity": "LOW", "sha": "0b4e1035", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/local/package-unreal-plugin-msvc.sh", "fix": "Drop the hardcoded home-dir default; when SMATCHET_UNREAL_PROJECT_ROOT/_FILE is unset and no flag is given, die with a message naming the env var and flag instead.", "line": 28, "pr": 1959, "problem": "Developer-specific absolute default baked into a committed script: PROJECT_ROOT defaults to \"C:/Users/alexk/Documents/Unreal Projects/TestProject\" (same pattern at build-deploy-and-open-unreal.sh:17 and rebuild-testproject-plugin.sh:19). Any other developer running without --project-root/env hits \"target Unreal project folder not found\" pointing at a stranger's home directory.", "severity": "LOW", "sha": "0b4e1035", "userVisible": false}
{"batch": "batch-21", "file": "docs/harness/SETUP.md", "fix": "Change the reference to a real link: [`docs/plans/shipped/kill-powershell-minimize-toolchain.md`](../plans/shipped/kill-powershell-minimize-toolchain.md).", "line": 79, "pr": 1960, "problem": "Stale cross-ref: the new § Windows-only shims points readers at `docs/plans/kill-powershell-minimize-toolchain.md`, but the plan lives at `docs/plans/shipped/kill-powershell-minimize-toolchain.md` (verified: the un-shipped path does not exist). It is a code span, not a markdown link, so test-markdown-links.sh does not catch it.", "severity": "LOW", "sha": "a30630af", "userVisible": false}
{"batch": "batch-21", "file": "docs/plans/shipped/msvc-build-onboarding-hardening.md", "fix": "Update the path to `docs/plans/shipped/kill-powershell-minimize-toolchain.md`.", "line": 77, "pr": 1960, "problem": "Same stale path: `docs/plans/kill-powershell-minimize-toolchain.md` (since shipped) — the file was moved to `docs/plans/shipped/` by this very sweep, so the annotation and the path contradict each other.", "severity": "LOW", "sha": "a30630af", "userVisible": false}
{"batch": "batch-21", "file": "docs/design/separate-agents-repo.md", "fix": "Re-pin both occurrences to setup-harness.sh:339-345, or drop the line numbers and cite the `link_file \".claude/hooks/agent-token-log.py\"` call site by name so the reference cannot drift again.", "line": 100, "pr": 1960, "problem": "Stale line-pin. The doc claims token-tracking hard links are created at `agents/scripts/core/setup-harness.sh:140-143`; at HEAD lines 140-143 are inside `link_agents()` (agent .md linking). The token-tracking `link_file` calls are at setup-harness.sh:342-345. Same wrong pin repeated at line 184 (Risk 4).", "severity": "LOW", "sha": "a30630af", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/postmortems.md", "fix": "Update line 176 to `(P2, process — shipped in PR #2018, `e8805608`).` and archive the corresponding entry into `categories/applied.md` so the linked slug resolves.", "line": 176, "pr": 1961, "problem": "Filed-as status drift: preventing gate (b) — evidence-gate the `cr-out-of-band` drop — is still recorded as an open item \"(P2, process).\", while sibling (a) on line 174 carries \"shipped in PR #1953, `ec41fe86`\". (b) actually shipped in PR #2018 (`e8805608`): `override_is_moot()` in agents/scripts/core/postmortem-owed.sh now has a `cr-out-of-band)` arm keyed on `gate_conclusion \"$pr\" 'CR findings'`, exactly as prescribed. The ledger now understates completion, and the entry slug `postmortem-owed-cr-override-denoise-hides-wedged-gate` appears nowhere in categories/applied.md.", "severity": "LOW", "sha": "e15992b3", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/postmortems.md", "fix": "Replace the numeric line pins with symbol references (`core_scoped_only_trigger()` / `pr_touches_core_cpp()`), which survive edits, per the repo's no-stale-line-pin convention.", "line": 139, "pr": 1961, "problem": "Stale line-pins: `postmortem-owed.sh:156-163` (and `:149-155` on line 140) no longer point at the `cr-out-of-band` scope-gating code. At origin/develop those line ranges are the `ABSENT_GRACE_SECONDS` run-creation grace-window block; the described logic now lives around lines 394-412 (`core_scoped_only_trigger`/`pr_touches_core_cpp`) and 473-482 (`override_is_moot` cr arm).", "severity": "LOW", "sha": "e15992b3", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/SmatchetUI.cpp", "fix": "Keep the focus request armed until selectDockedTab actually resolves a window: either clear requestUserInfoFocus/requestPreferencesFocus only when FindWindowByName() succeeded (have selectDockedTab return bool), or re-arm for one extra frame when the window did not yet exist.", "line": 1111, "pr": 1962, "problem": "selectDockedTab() is called BEFORE the window's Begin(), and the focus request is consumed on that same frame (g_ui.requestUserInfoFocus cleared at :1117; the Preferences twin at SmatchetPreferencesUi.cpp:194 clears d.requestPreferencesFocus at :202/:211). ImGui only creates the ImGuiWindow inside Begin(), so on the first-ever open of the window in a process FindWindowByName() returns nullptr, selectDockedTab early-returns as a no-op, and the request is gone by the next frame — the exact 'comes up behind its sibling and stays there' failure the PR fixes can still occur on the first open when the sibling tab (Preferences / User Info) was created first.", "severity": "MEDIUM", "sha": "a7fa2a32", "userVisible": true}
{"batch": "batch-21", "file": "docs/self-improvement/categories/debt/2026-08-06-scenario-runner-ticks-twice-per-frame.md", "fix": "Add a `Status: shipped <date>` line (or move/close the entry) and refresh the two line pins.", "line": 1, "pr": 1962, "problem": "Live debt entry describes an issue that has since been fixed: the second ScenarioRunner::Tick in RunRenderLoop is gone (StandaloneAppBootstrap.cpp:680-691 now documents its removal), leaving one tick site. Both line pins are also stale — SmatchetUI.cpp:642 is now :672, and StandaloneAppBootstrap.cpp:685 no longer contains a Tick call. The entry carries no `Status:` line (3 of 6 debt entries do), so it reads as open.", "severity": "LOW", "sha": "a7fa2a32", "userVisible": false}
{"batch": "batch-21", "file": "tests/ui/docked_tab_focus.test.cpp", "fix": "Emit a machine-detectable skip marker and assert in CI that the DockedSibling variant actually ran (e.g. fail the lane if the primary variant skipped on a host known to support docking), so a permanently-skipping guard is visible.", "line": 131, "pr": 1962, "problem": "The PRIMARY variant self-skips with ctx->LogInfo and no failure when the host never realizes a tab bar (and again at :107 when docking is disabled). Under the Mesa headless bucket-E lane that is the likely path, so the regression guard for the imgui#2304 workaround can report pass while asserting nothing; only variant 2 (the null-safety no-op case) is deterministic.", "severity": "LOW", "sha": "a7fa2a32", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/msys_argv_switches.bats", "fix": "Mirror assert_guarded: after the scan, fail with \"no cmd.exe /switch call site found — test is stale\" if the pre-filter matched zero lines, or fold a known-good fixture (a guarded sample line) into the scan so the classifier is proven live.", "line": 97, "pr": 1963, "problem": "The \"every cmd.exe invocation\" gate has no positive control. Running its own pre-filter (`grep -nE '(^|[^#]*[^\"'[:alnum:]])cmd\\.exe[[:space:]]+/[A-Za-z]'` minus comment lines) over `git ls-files '*.sh'` at HEAD matches ZERO lines — the three cmd.exe call sites this PR guarded were since replaced by scripts/common/unreal-batch.sh's temp-.bat approach. So the test now passes unconditionally with an empty offender set, and unlike assert_guarded it has no zero-hit \"test is stale\" failure. Any future drift in how cmd.exe is spelled (`\"$COMSPEC\" /c`, `cmd //c`, a variable expansion) silently keeps it green.", "severity": "MEDIUM", "sha": "9e296697", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/msys_argv_switches.bats", "fix": "Strip the `NNN:` prefix first, then classify on the code portion only (e.g. cut at the first unquoted `#` and re-test the remainder) instead of glob-matching `cmd.exe` anywhere after a `# `.", "line": 90, "pr": 1963, "problem": "Comment classifier exempts real code: the `case` arm `*\\#\\ *cmd.exe*` skips any line where `cmd.exe` appears after a `# ` anywhere on the line, so a genuinely unguarded `cmd.exe /c foo # falls back to cmd.exe` is silently exempted. The sibling arm `\\#*` is dead code — `$line` always arrives prefixed with `NNN:` from `grep -n`, so it can never match a leading `#`.", "severity": "MEDIUM", "sha": "9e296697", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/msys_argv_switches.bats", "fix": "Match the exe expansion as a command word after any of `^`, `(`, `$(`, `&&`, `||`, `|`, `;`, `if`/`while`/`then`, rather than only at line start, and keep the same mandatory-guard-prefix re-anchor for each match.", "line": 39, "pr": 1963, "problem": "assert_guarded only recognises call sites whose exe expansion is the FIRST word on the line (`^[[:space:]]*(guard)?\"\\$EXE\"[[:space:]]`). An unguarded invocation written as `out=$(\"$SIGN_TOOL\" verify ...)`, `if \"$ISCC_EXE\" ...; then`, after `&&`/`|`, or continued from a previous line via `\\` is invisible to the scan; the test still passes because the existing line-start sites are guarded. That is a fail-open on exactly the class of new code the gate exists to catch.", "severity": "MEDIUM", "sha": "9e296697", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/msys_argv_switches.bats", "fix": "Run the negative case with a scrubbed environment, e.g. `run env -u MSYS_NO_PATHCONV -u MSYS2_ARG_CONV_EXCL python -c ...`, so the test controls the variable it is measuring.", "line": 117, "pr": 1963, "problem": "The Windows-only behavioural test asserts that an unguarded `/DMyAppVersion=0.1.0` IS mangled. If `MSYS_NO_PATHCONV` (or `MSYS2_ARG_CONV_EXCL`) happens to be exported in the ambient environment — plausible in a Git Bash shell or CI step that sets it globally — line 119 becomes a false RED with a misleading message.", "severity": "LOW", "sha": "9e296697", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/lib/review-ack.sh", "fix": "Use `git diff HEAD --name-only --diff-filter=d -- ...` and `git diff HEAD --numstat -- ...` in the branch branches of ra_changed_files / ra_changed_lines so the substantive test covers exactly what the fingerprint hashes.", "line": 129, "pr": 1964, "problem": "Branch-mode asymmetry: ra_fingerprint (line 117) hashes `git diff HEAD` (staged + unstaged), but ra_changed_files (129) and ra_changed_lines (142) use bare `git diff` (unstaged only). Staged-but-uncommitted first-party C++ is therefore invisible to the substantive test, so pre-ship.sh's push gate reports \"N/A — diff is not substantive\" for a large or strict-zone diff that is merely `git add`-ed, fail-opening the push-side review gate.", "severity": "MEDIUM", "sha": "65b2b1b9", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/lib/review-ack.sh", "fix": "Capture the python rc / detect an empty zones array and emit the same fail-closed sentinel (e.g. \"(strict-zone list unreadable — project.config.json)\") instead of returning no hit.", "line": 169, "pr": 1964, "problem": "ra_strict_hit fails OPEN when python works but the strict-zone list cannot be read: `python -c \"...json.load(open('project.config.json'))['lint']['zones']['strict']...\" 2>/dev/null || true` swallows a missing/renamed/malformed project.config.json or a moved key, leaving `zones` empty so no path ever matches and every sub-threshold strict-zone edit N/A-passes silently. This contradicts the deliberate #1116 fail-CLOSED handling three lines above for the no-python case.", "severity": "MEDIUM", "sha": "65b2b1b9", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/lib/review-ack.sh", "fix": "Keep deletions for strict-zone detection (drop the filter in ra_changed_files, or run a second `--diff-filter=D` pass) while still skipping them where a readable path is needed.", "line": 126, "pr": 1964, "problem": "`--diff-filter=d` drops deletions from ra_changed_files, so removing a file inside a `lint.zones.strict` prefix is never a strict-zone hit. A strict-zone file shorter than REVIEW_LINE_THRESHOLD lines can be deleted and committed with no review ack.", "severity": "LOW", "sha": "65b2b1b9", "userVisible": false}
{"batch": "batch-21", "file": "scripts/common/unreal-batch.sh", "fix": "Check the write and bail: `{ ...; } > \"$script\" || { rm -f \"$script\"; echo 'run_unreal_batch: could not write the temporary batch file.' >&2; return 1; }`, and additionally verify the file is non-empty (`[ -s \"$script\" ]`) before executing it.", "line": 45, "pr": 1965, "problem": "The generated wrapper is written with an unchecked redirection `{ ...; } > \"$script\"`. If the write fails or truncates (full/read-only TMPDIR), the .bat is empty or partial, cmd runs it, and it exits 0 — the caller's `|| die` never fires and the build is reported as succeeding without ever running. That is exactly the silent false-PASS the file's header comment says it exists to prevent.", "severity": "MEDIUM", "sha": "c3d7be8b", "userVisible": false}
{"batch": "batch-21", "file": "scripts/common/unreal-batch.sh", "fix": "Escape percent signs when writing the argument: replace `%` with `%%` (e.g. `printf ' \"%s\"' \"${arg//%/%%}\"`) for both the bat path on line 38 and each arg on line 41.", "line": 41, "pr": 1965, "problem": "Arguments are emitted verbatim into the batch body (`printf ' \"%s\"' \"$arg\"`) with no cmd-level escaping. cmd.exe performs `%VAR%` expansion on the line at execution, so any argument containing `%` (legal in Windows paths, and common in UBT/UAT switches) is silently rewritten or blanked before it reaches the .bat — a quoting helper silently corrupting arguments is the one failure it must not have.", "severity": "MEDIUM", "sha": "c3d7be8b", "userVisible": false}
{"batch": "batch-21", "file": "scripts/common/unreal-batch.sh", "fix": "Register cleanup immediately after mktemp, e.g. a RETURN/EXIT-scoped `trap 'rm -f \"$script\"' EXIT INT TERM` in a subshell, or have the caller-side wrapper clean stale smatchet-unreal-*.bat files.", "line": 49, "pr": 1965, "problem": "`rm -f \"$script\"` runs only on the normal path. Unreal builds are long; a Ctrl-C or any signal during `\"$script\"` leaves the throwaway .bat behind in TMPDIR, accumulating on every interrupted build.", "severity": "LOW", "sha": "c3d7be8b", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/include/Ui/SmatchetWindowExpand.h", "fix": "Rewrite the fourth bullet to describe the shipped behaviour: the slot is reserved via ReserveTabBarSlot/TabItemSpacing, with the only remaining exposure being the first frame after a node first queues a button (the amend re-enters an already-laid-out bar). Same drift exists in docs/plans/shipped/window-expand-button.md lines 56-62.", "line": 42, "pr": 1966, "problem": "The header's \"four limits this deliberately does NOT solve\" block still declares the full-tab-bar overlap unfixed (\"the button overlaps the last tab and takes its clicks... A real fix has to shrink BarRect (or claim a section) rather than draw over it\"), but the shipped implementation does claim a section: ReserveTabBarSlot (SmatchetWindowExpand.cpp:192-198) submits a trailing ImGui::TabItemSpacing through DockNodeBeginAmendTabBar precisely to shrink the central section. A reader of the contract header is told a live click-stealing bug exists that the code already mitigates, and the real residual (one frame of overlap on first submission) is not stated here.", "severity": "MEDIUM", "sha": "1f42859f", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/SmatchetWindowExpand.cpp", "fix": "Make the transition ini-safe: either write the pre-expand WindowExpandSaved placement into the window's ImGuiWindowSettings entry when the override is applied, or drop the expansion (replay the saved placement) during shutdown before ImGui::DestroyContext saves, so the persisted state is the home slot.", "line": 258, "pr": 1966, "problem": "The per-frame pin (SetNextWindowDockID(0)/SetNextWindowPos/Size on WorkPos/WorkSize) IS the window's live state, and ImGui's settings writer snapshots Pos/Size/DockId off the live window (omitting DockId= while 0). Quitting the app while a window is expanded therefore persists it to imgui.ini as floating + fullscreen; on relaunch SmatchetUI_Layout's pendingReDockWindows force-redocks it to its DEFAULT slot, silently discarding the user's customised dock placement for that window. No test bucket can observe it (documented in docs/self-improvement/categories/test/2026-08-05-no-bucket-e-shutdown-relaunch-primitive.md).", "severity": "MEDIUM", "sha": "1f42859f", "userVisible": true}
{"batch": "batch-21", "file": "docs/plans/shipped/window-expand-button.md", "fix": "Replace the closing paragraph with the actual merge state of #1966 (merged) and the current state of #1984, or drop it now that the plan is filed under shipped/.", "line": 313, "pr": 1966, "problem": "The plan closes with \"Neither PR is merged. ... no merge authorisation exists for #1966 or #1984\", while the document itself carries `Status: shipped`, lives under docs/plans/shipped/, and #1966 is merged into develop. The status literal and the closing paragraph contradict each other, so the doc misreports the shipped state of the feature.", "severity": "LOW", "sha": "1f42859f", "userVisible": false}
{"batch": "batch-21", "file": "docs/agent-rules/verifier-sidecar.md", "fix": "Retarget the link to `../plans/shipped/verifier-scored-code-review-gate.md` (and update the visible label to match).", "line": 63, "pr": 1970, "problem": "Broken relative markdown link: `[docs/plans/verifier-scored-code-review-gate.md](../plans/verifier-scored-code-review-gate.md)` resolves to docs/plans/verifier-scored-code-review-gate.md, which does not exist at HEAD — the plan was later moved to docs/plans/shipped/verifier-scored-code-review-gate.md.", "severity": "MEDIUM", "sha": "48c84ecf", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/lib/review-ack.sh", "fix": "Update all four comment references to docs/plans/shipped/verifier-scored-code-review-gate.md.", "line": 50, "pr": 1970, "problem": "Stale plan path in comments — `docs/plans/verifier-scored-code-review-gate.md` no longer exists (now docs/plans/shipped/...). Same stale path also at agents/scripts/core/review-ack.sh:32, agents/scripts/core/test-verifier-review-gate-bats.sh:14, and tests/bats/verifier_review_gate.bats:6.", "severity": "LOW", "sha": "48c84ecf", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/review-ack.sh", "fix": "After parsing, reject `verdict_file` when action=check with exit 2; and in the bare `--verdict` branch reject an argument starting with `--`.", "line": 158, "pr": 1970, "problem": "`--verdict`/`--verdict=` is parsed in the shared arg loop but only consumed on the `record` path; `review-ack.sh --check --verdict f.json` silently accepts and ignores the flag instead of erroring, which is the same class of silent-degradation the surrounding comments (lines 168-174) were written to prevent. Related: bare `--verdict --branch` swallows the next flag as the filename and then fails as rc 2 (infra, WARN+allow) rather than a usage error.", "severity": "LOW", "sha": "48c84ecf", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/tooling.md", "fix": "Update both citations (lines 11 and 12) to `docs/plans/shipped/verifier-scored-code-review-gate.md`.", "line": 11, "pr": 1972, "problem": "Broken cross-ref: the entry cites `docs/plans/verifier-scored-code-review-gate.md` (again on line 12), but at HEAD the plan lives at `docs/plans/shipped/verifier-scored-code-review-gate.md` — no file exists at the cited path.", "severity": "LOW", "sha": "684cc2ab", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/tooling.md", "fix": "Rewrite the Details/next-action to mark (1) shipped (traces now recorded by pre-ship.sh), narrow the open work to (2) durable trace sink + label source and (3) the periodic `verifier-calibrate.py` report, and bump Last-reviewed.", "line": 11, "pr": 1972, "problem": "Doc-vs-code drift on a factual claim: the entry asserts \"slice 2 (the `pre-ship.sh` wiring that actually drives `verifier-produce.py` against an independent backend) is unshipped, so in practice no verdict is generated at all\". At HEAD `scripts/dev/pre-ship.sh` (preship_produce_verdict, ~lines 333-371) already pipes `verifier-produce.py --record <trace> | verifier-sidecar.py aggregate`, and `test-verifier-preship-wiring-bats.sh` covers it; the plan is filed under docs/plans/shipped/. Next-action item (1) is done, yet the row still reads Status: open with (1) outstanding, so a reader picking this up re-does shipped work and mis-scopes the remaining gap (label source + periodic calibrate report).", "severity": "MEDIUM", "sha": "684cc2ab", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/pre-ship.sh", "fix": "Make the scored candidate text the same two-diff concatenation `ra_fingerprint branch` hashes (append `git diff HEAD` output), or refuse to produce a verdict when `git diff HEAD -- <RA_CPP_GLOBS>` is non-empty and fall back to a presence-only ack with a WARN.", "line": 352, "pr": 1974, "problem": "Scope mismatch between what the verifier scores and what the ack certifies. The producer scores only `git diff base_ref...HEAD` (committed history), but the fingerprint the verdict is pinned to — `ra_fingerprint branch` (review-ack.sh:116-117) — is `git diff base...HEAD` PLUS `git diff HEAD` (uncommitted working tree). So uncommitted first-party C++ edits are never seen by the verifier, yet the resulting score/hard_veto is recorded against a fingerprint that covers them; on the gate path (line 678) that verdict then certifies a diff the model never read.", "severity": "MEDIUM", "sha": "35e1f4a2", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/pre-ship.sh", "fix": "Give preship_produce_verdict a distinct rc for 'nothing to score' (e.g. rc 2 from the empty-diff branch) and suppress the WARN for it, warning only on real backend/pipeline failure.", "line": 626, "pr": 1974, "problem": "The 'no usable verdict' WARN cannot distinguish backend failure from the legitimate empty-diff case. preship_produce_verdict returns 1 both when the backend is down and when the inline Python exits 1 because the branch diff is empty (line 357-358), so an ordinary empty-diff ack against a healthy backend emits `WARN — verifier backend configured but produced no usable verdict`. That is a routine false alarm on a gate whose whole degrade story rests on the WARN being meaningful.", "severity": "LOW", "sha": "35e1f4a2", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/project/test-cr-finding-gate-bats.sh", "fix": "Exclude skip directives from the pass count (e.g. `grep -E '^ok [0-9]+' | grep -vc '# skip'`) and add an explicit expected-test-count floor (`[ $((PASSED+FAILED)) -ge <N> ]`) so a partially-parsed or fully-skipped run fails instead of going green.", "line": 46, "pr": 1977, "problem": "PASSED is counted with `grep -cE '^ok [0-9]+'`, which also matches bats TAP skip lines (`ok 3 name # skip ...`). A run where every test skips (missing jq, the known bats 'unknown test name' silent-skip under a non-UTF-8 locale) yields PASSED>0, FAILED=0, RC=0 — the PASSED>0 floor at line 54 is satisfied and the gate reports green having verified nothing. The floor also has no expected-count minimum, so a suite that parses only 1 of its ~10 tests passes.", "severity": "MEDIUM", "sha": "d02c1fed", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/cr_finding_gate.bats", "fix": "Assert the real check's full predicate against the fixture, mirroring the positive test at line 147 (both the `always() && steps.eval.outcome != 'success'` grep and the `state=pending` grep must fail on $tmp), and build the fixture by removing the whole fallback step rather than only lines containing the assertion string.", "line": 160, "pr": 1977, "problem": "The `selftest: a workflow with no fallback poster is detected` test is tautological: it builds the fixture with `grep -v 'state=pending' \"$WF\" > \"$tmp\"` and then asserts `! grep -q \"state=pending\" \"$tmp\"`. That holds for any input by construction — it never exercises the real detector, and it would pass identically if $WF had no fallback poster at all. The file header claims negative fixtures 'prove the checks fire rather than passing vacuously'; this one does not.", "severity": "MEDIUM", "sha": "d02c1fed", "userVisible": false}
{"batch": "batch-21", "file": ".github/workflows/cr-finding-gate.yml", "fix": "Make Resolve PR fail the job when it cannot resolve head sha/base (drop the `|| echo \" \"` swallow, or set an explicit `resolved=false` output and fail), so the operator gets a red run instead of a green run that silently posts nothing.", "line": 144, "pr": 1977, "problem": "The fallback poster is gated on `steps.pr.outputs.base == 'develop' && steps.pr.outputs.sha != ''` — the same outputs the Evaluate step needs. The Resolve PR step swallows API failures (`gh api ... 2>/dev/null || echo \" \"`) and always exits 0, so a transient GitHub API failure there leaves sha/base empty: Evaluate is skipped, the fallback is skipped, the job reports SUCCESS, and no status is posted at all — the StatusContext keeps its stale value and the PR wedges. That is precisely the wedge class this PR exists to close, left open on the resolve path.", "severity": "MEDIUM", "sha": "d02c1fed", "userVisible": false}
{"batch": "batch-21", "file": ".github/workflows/cr-finding-gate.yml", "fix": "Retry briefly and then `exit 1` on a failed fallback POST so the run goes red and the wedge is visible/re-runnable, instead of exiting 0 on a violated invariant.", "line": 154, "pr": 1977, "problem": "The fallback status POST failure is downgraded to a warning (`|| echo \"WARN: fallback status POST failed\" >&2`) and the step (and job) still exit 0. The workflow's stated invariant is 'it always leaves a status behind'; when the POST fails the invariant is violated but the run is reported green, so nothing surfaces the wedge.", "severity": "LOW", "sha": "d02c1fed", "userVisible": false}
{"batch": "batch-21", "file": ".github/actions/cr-finding-gate/action.yml", "fix": "Either reserve an estimated pass cost in the guard (e.g. `SECONDS + POLL_INTERVAL_SECONDS + POLL_PASS_BUDGET`) or correct the comment to state that the budget bounds sleeps plus completed passes only, and keep the headroom assertion in the bats suite explicit about it.", "line": 518, "pr": 1977, "problem": "The loop-exit guard `[ $(( SECONDS + POLL_INTERVAL_SECONDS )) -lt \"$deadline\" ] || break` reserves only the sleep interval, not the `decide()` pass that follows it — contrary to its own comment ('another interval plus the pass after it would not fit in the budget'). Actual worst-case step time is POLL_BUDGET_SECONDS plus one full GraphQL round-trip, so the wall-clock budget the ordering test pins is not the true bound. Currently masked by the 180 s budget vs 6 min step timeout headroom.", "severity": "LOW", "sha": "d02c1fed", "userVisible": false}
{"batch": "batch-21", "file": "docs/agent-rules/review-panels.md", "fix": "Rewrite present-tense (\"the `review-panel` block in `project.config.json`, read by `agents/scripts/core/run-review.sh`\") and either add a `## Roster` section or repoint project.config.json's `_doc` at `§ Reviewer identity: the launcher names the file`.", "line": 27, "pr": 1978, "problem": "Stale future tense + dangling cross-ref: surviving text says the roster is a `review-panel` block in `project.config.json` \"when the Phase 2 port lands\", but Phase 2 shipped (docs/plans/shipped/absorb-whip-process.md Status: shipped, PRs #1978-#1982) and the block exists today. That block's `_doc` points at \"docs/agent-rules/review-panels.md § Roster\" — review-panels.md has no `## Roster` heading, so the config's only pointer to its own spec resolves to nothing.", "severity": "MEDIUM", "sha": "8e133e76", "userVisible": false}
{"batch": "batch-21", "file": "docs/agent-rules/review-panels.md", "fix": "Rewrite present-tense naming `agents/scripts/core/lib/review-guard.sh` and state the two contracts as invariants the guard upholds rather than constraints on a future port.", "line": 104, "pr": 1978, "problem": "\"The guard tool ports in Phase 2; these two contracts ... bind the port\" is stale — the guard shipped as `agents/scripts/core/lib/review-guard.sh` (sourced by run-review.sh:67, covered by test-review-guard-bats.sh). Readers are told a shipped, load-bearing gate is still hypothetical.", "severity": "LOW", "sha": "8e133e76", "userVisible": false}
{"batch": "batch-21", "file": "docs/agent-rules/work-items.md", "fix": "Redirect to `4-resolution-*.md` (or the amended artifact itself), keeping `4-review-*.md` problems-only.", "line": 172, "pr": 1978, "problem": "\"Record significant changes and their rationale in the `4-review-*.md` files\" contradicts the same doc (line 118-119: each review file \"records only problems ... no praise, status summaries\") and review-panels.md § Output file template (\"Problems only ... no restatement\"). `4-review-*.md` are also reviewer-owned panel outputs, not the cascading agent's; `4-resolution-*` is the addresser artifact where rationale belongs.", "severity": "MEDIUM", "sha": "8e133e76", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/work_item_lint.py", "fix": "Only apply the transient exemption inside item folders, e.g. `if rel.startswith(f\"{ITEMS}/\") and TRANSIENT_LEAF.match(rel.rsplit(\"/\", 1)[-1]): continue`. Add a regression case asserting a dangling id in docs/agent-rules/review-panels.md still fires.", "line": 283, "pr": 1979, "problem": "The TRANSIENT_LEAF exclusion is applied to EVERY tracked .md in the repo, not just files inside docs/work/items/. Its stated rationale (\"review/resolution files are transient scratch, deleted when the round closes\") only holds inside an item folder, but the leaf regex ^(\\d+-)?(close-)?(review|resolution)- matches durable docs too — docs/agent-rules/review-panels.md is silently exempted from the dangling-citation sweep today, and any future review-*.md / resolution-*.md durable doc would be as well. Unanchored exclude token = citation gate fails open on exactly the doc that describes the panel.", "severity": "MEDIUM", "sha": "d2d4835f", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/test_work_item_lint.py", "fix": "Before printing the canonical line, fail when result.testsRun == 0: print \"Passed: 0 Failed: 1\" plus a message on stderr and sys.exit(1).", "line": 474, "pr": 1979, "problem": "No zero-run floor. If unittest discovers zero tests (renamed/removed classes, a discovery regression) result.testsRun==0, the script prints \"Passed: 0 Failed: 0\" and exits 0 — green with nothing run. This is the same fail-open shape Z that test-review-guard-bats.sh:52 and test-run-review-bats.sh:52 added in this very PR explicitly guard against, and the wrapper scripts/dev/test-work-item-lint.sh:31 adds no floor either, so test-docs.sh and doc-validation.yml would both pass.", "severity": "MEDIUM", "sha": "d2d4835f", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/run-review.sh", "fix": "Use `\"${SKIPPED[@]+\"${SKIPPED[@]}\"}\"` (or guard each loop with `[ \"${#SKIPPED[@]}\" -gt 0 ]`, as lines 488/523 already do for MISSING/STRAYS).", "line": 397, "pr": 1979, "problem": "`for s in \"${SKIPPED[@]}\"` (also lines 198 and 267) expands a possibly-empty array under `set -u`. On bash < 4.4 an empty \"${arr[@]}\" is an unbound-variable error, which is fatal in a non-interactive shell — the launcher would abort immediately after spawning the legs whenever no harness was skipped. The script otherwise advertises macOS support (the BSD `stat -f %m` probe at line 416), where the system bash is 3.2.", "severity": "LOW", "sha": "d2d4835f", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/work_item_lint.py", "fix": "Use one shared digit spec in both patterns (e.g. \\d{1,4}), or normalise via a single ID_CORE constant referenced by ID_PAT and LEDGER_DEF_PATS.", "line": 64, "pr": 1979, "problem": "ID_PAT requires exactly 2-3 digits (\\d{2,3}) while LEDGER_DEF_PATS accept \\d+. A ledger entry outside that width (DEF-1, BL-1234) is registered as defined but citations of it are never scanned, so a genuinely dangling one-digit or four-digit id is invisible to the sweep. The two patterns silently disagree on what an id is.", "severity": "LOW", "sha": "d2d4835f", "userVisible": false}
{"batch": "batch-21", "file": "docs/agent-rules/review-panels.md", "fix": "Repoint to `docs/plans/shipped/absorb-whip-process.md`, preferably as a markdown link so the docs gate guards it.", "line": 8, "pr": 1980, "problem": "Stale doc cross-ref: the surviving line cites `docs/plans/absorb-whip-process.md`, but that plan now lives at `docs/plans/shipped/absorb-whip-process.md` (docs/plans/ holds only INDEX.md + active/deferred/shipped). It is bare prose, not a markdown link, so test-docs.sh's link check does not catch it.", "severity": "LOW", "sha": "5423edbf", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/panel_verdicts.py", "fix": "Add `--authoring-harness <tag>` to the header example at line 16 so it matches the § Independence note and the two rule docs.", "line": 16, "pr": 1981, "problem": "The header's canonical pipeline example (lines 16-18) invokes the adapter without `--authoring-harness`, but the same file's independence note (lines 45-51) and docs/agent-rules/review-panels.md + address-review-feedback SKILL.md now state the flag is \"not optional in practice\" — omitting it silently scores the author's own legs. Doc-vs-code drift inside the script that teaches the pipeline.", "severity": "LOW", "sha": "3d123185", "userVisible": false}
{"batch": "batch-21", "file": "docs/work/closed/01-spawn-honors-timeout.md", "fix": "Update the path to `docs/plans/shipped/absorb-whip-process.md` (Phase 5).", "line": 8, "pr": 1982, "problem": "Broken cross-ref: the surviving line points at `docs/plans/absorb-whip-process.md`, which does not exist at HEAD — the plan moved to `docs/plans/shipped/absorb-whip-process.md`.", "severity": "LOW", "sha": "60f1db66", "userVisible": false}
{"batch": "batch-21", "file": "docs/work/closed/01-spawn-honors-timeout.md", "fix": "Drop the line numbers (cite the files/functions only) or mark them explicitly as pre-fix pins.", "line": 14, "pr": 1982, "problem": "Stale line pin: the doc cites the pre-fix spawn call site as `CliDispatch.cpp:476`; the ScenarioWaitMs call site at HEAD is CliDispatch.cpp:579 (CliCommandRunner.cpp:122 still matches). A reader following the pin lands on unrelated code.", "severity": "LOW", "sha": "60f1db66", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/SmatchetAiAssistantUi.cpp", "fix": "Record the fallback side only when a dock write actually lands (inside the targetDockId != 0 branch that consumes the pending flags) and keep it as a session-only override on UiDrawSession instead of persisting, so the stored preference returns once a real secondary node exists. Alternatively cut a real kSecondarySideBar node in the default layout, or delete the constant and the swap feature.", "line": 1496, "pr": 1984, "problem": "ApplyAssistantDocking rewrites and persists the user's AssistantPanelOnSecondarySide preference whenever the requested side bar is not live. The block runs every frame and is not gated on assistantPendingSideSwap/needsReDock, so it fires even when no dock write happens (mouse held, or FirstUseEver already satisfied). Since kSecondarySideBar is cut by no DockBuilder call and is absent from the embedded default ini (per the PR's own debt note), an existing config with AssistantPanelOnSecondarySide=true is silently flipped to false on the first frame and ScheduleConfigSaveDetached persists it. The setting is lost durably, and the swap button is BeginDisabled in exactly that state, so the user cannot set it back.", "severity": "MEDIUM", "sha": "e8713063", "userVisible": true}
{"batch": "batch-21", "file": "docs/self-improvement/categories/debt/2026-08-07-dock-node-id-slot-liveness-followups.md", "fix": "Re-pin the five references to their HEAD lines, or drop the numbers and cite the enclosing functions (ApplyAssistantDocking, PrepareMcpWindowLayout, PrepareLuaWindowLayout, prepareTopLevelWindow) so the entry cannot drift again.", "line": 8, "pr": 1984, "problem": "Stale line pins. At HEAD the EnsureDockSlotAlive call sites are SmatchetAiAssistantUi.cpp:1485 (entry says 1484), SmatchetMcpServerUi.cpp:96 with its rationale comment at 93-95 (entry says 94-100 and 91-93), SmatchetPerfUi.cpp:230 (entry says 228-229), LuaConsolePlugin.cpp:70 (entry says 66-67, which is the function signature). Only SmatchetUI_Layout.cpp:118-119 is accurate.", "severity": "LOW", "sha": "e8713063", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/postmortems.md", "fix": "Drop the `:240-251` range from the link (link the file/function by name only), or repin to the current `has_entry()` definition (~line 347) — unpinned function-name references don't rot.", "line": 68, "pr": 1985, "problem": "Stale line-pin: the entry cites `postmortem-owed.sh:240-251` as the location of `has_entry()`. At origin/develop `has_entry()` is defined at line 347; lines 240-251 are the unrelated expected-present non-required-check allow-list block. A reader following the pin lands on the wrong logic.", "severity": "LOW", "sha": "79701968", "userVisible": false}
{"batch": "batch-21", "file": "agents/_shared/skills/address-review-feedback/SKILL.md", "fix": "Amend the sentence to cover both rc-3 causes, e.g. \"If the adapter exits 3, no leg was independent — every leg was either your own harness or had an unreadable harness tag — and none vetoed. Check stderr: on 'harness tag unreadable', rename the leg to 5-review-<round>-<harness>-<model>.md and re-run before falling back to a presence-only ack.\"", "line": 86, "pr": 1987, "problem": "The rc-3 explanation is incomplete and misdirects the operator: it states \"If the adapter exits 3, every leg was your own harness and none vetoed\". panel_verdicts.py also returns 3 when a leg's filename does not match the launcher grammar — harness_of() returns None, apply_independence() drops it with \"harness tag unreadable\", and with no other legs main() returns 3 (covered by tests/bats/panel_verdicts.bats \"unreadable harness tag counts as NOT independent (fail closed)\"). Following this doc, the addresser records a presence-only ack and closes the round, silently discarding a real independent leg whose only defect was a misnamed file. docs/agent-rules/review-panels.md states both causes correctly, so only the skill (the file actually executed step-by-step) drifts.", "severity": "MEDIUM", "sha": "34526b5c", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/include/Tracker/TrackerBackendKind.h", "fix": "Make DefaultTrackerBackendFactory.cpp switch on smatchet::tracker::BackendIndexFromType(trackerType) so there is genuinely one mapping, or soften the comment to \"mirrored by\" (as tests/Core/TrackerBackendKind.test.cpp already says) and add a test pinning both sites to the same answer.", "line": 15, "pr": 1989, "problem": "The header asserts \"One shared rule, so the two cannot disagree again\", but DefaultTrackerBackendFactory.cpp (lines 17-28) still carries its own independent ToLowerAsciiCopy + string-compare ladder and never calls BackendIndexFromType. Only the UI side was de-duplicated, so the exact drift this PR fixed (UI drawing one backend's credential rows while the factory builds another client) can reappear as soon as either list gains or renames a backend.", "severity": "MEDIUM", "sha": "c00197d4", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/PreferencesFilter.cpp", "fix": "Compare folded bytes on the fly without materializing a copy, or fold each row's label/commandId/hotkeys once into the cached index alongside entry.Haystack instead of re-folding per frame.", "line": 59, "pr": 1989, "problem": "ContainsLower folds the whole haystack into a fresh std::string on every call. It is invoked per keybinding row per frame (SmatchetPreferencesUi_Keybindings.cpp RowMatchesFilter: label + commandId + every hotkey, driven from both the row loop and AnyKeybindingRowMatchesQuery), so an active Preferences search causes hundreds of heap allocations per frame on the ImGui render path — and the new accent pass adds a second scan on top of ToLowerAsciiCopy.", "severity": "LOW", "sha": "c00197d4", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/historical-review-findings.md", "fix": "Amend the Batch 20 header to state coverage as 53 of 60 merged PRs and list the 7 merge-commit PRs as unswept, or re-run them and update the counts. Cross-reference docs/self-improvement/categories/tooling/2026-08-16-historical-review-worklist-misses-merge-commit-prs.md.", "line": 16, "pr": 1991, "problem": "Batch 20's header asserts \"The frontier is now #1–#1940 contiguous; the next sweep resumes from #1941\" and \"53 reviewed\", but the work-list was built by scraping `(#N)` off develop squash subjects, which silently omits PRs that landed as true merge commits. GitHub's merged list for #1878–#1940 has 60 PRs, not 53: #1883, #1919, #1920, #1921, #1923, #1927, #1932 were never in the work-list (all release-publishing pipeline code — release.yml / release-github.sh — hence never survivor-reviewed). The contiguity claim is false and a later reader will skip that range forever.", "severity": "MEDIUM", "sha": "213881dd", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/historical-review-findings.md", "fix": "Add a dated superseding note on line 54 (e.g. \"— superseded by Batch 20 below/above; resume from #1941\") rather than leaving the stale bold imperative standing.", "line": 54, "pr": 1991, "problem": "The targeted-pass section still says in bold \"#1877–#1940 (~60 PRs) remains entirely unswept and the next sweep still resumes from #1877\", directly contradicting the Batch 20 section added in the same commit 40 lines above (\"frontier is now #1–#1940 contiguous; the next sweep resumes from #1941\"). Two live, bolded, mutually exclusive resume pointers in the one document whose job is telling the next runner where to resume.", "severity": "LOW", "sha": "213881dd", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/SmatchetAutocompleteUi.cpp", "fix": "Add #include \"Tracker/TrackerBackendKind.h\" directly to each .cpp that calls IsPlaneBackendType (include-what-you-use), keeping the _detail.h include as incidental rather than load-bearing.", "line": 220, "pr": 1992, "problem": "New call sites of smatchet::tracker::IsPlaneBackendType were added in SmatchetAutocompleteUi.cpp (220, 284), SmatchetViewsDashboardUi.cpp (435) and SmatchetViewsDashboardUi_widgets.cpp (247, 325, 384), but the declaring header \"Tracker/TrackerBackendKind.h\" was only added to SmatchetViewsDashboardUi_detail.h. All four TUs compile solely on that transitive include, so pruning or reordering includes in _detail.h breaks them.", "severity": "LOW", "sha": "be6732dc", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/SmatchetPreferencesUi.cpp", "fix": "Invalidate at the single source of binding mutation rather than at each UI call site: have `MarkKeybindingsDirty()` (or the quick-bind path in SmatchetUI.cpp:1054) also set `d.prefsKeybindRowsMatchDirty = true`, or key the cache off a monotonically-bumped keybindings revision counter compared each frame in drawPreferencesWindow.", "line": 1196, "pr": 1993, "problem": "The keybinding-row match scan was reduced from every-frame to an edge-triggered cache whose invalidation set is incomplete. `prefsKeybindRowsMatchDirty` is set only in the Preferences keybindings editor (SmatchetPreferencesUi_Keybindings.cpp:430), but `d.cfg.Keybindings` is also mutated outside that editor — `quickBind_.Draw(d.cfg)` in SmatchetUI.cpp:1054 (toolbar / command-palette quick-bind) calls only `MarkKeybindingsDirty()`. With Preferences open under an active query, a quick-bind that adds/changes a matching hotkey leaves `prefsKeybindRowsMatchQuery` stale until the user edits the query or the language changes: the Shortcuts section stays hidden (or stays visible with zero matching rows) and the \"showing N of M\" readout disagrees with the rows.", "severity": "MEDIUM", "sha": "bcd1e90d", "userVisible": true}
{"batch": "batch-21", "file": "Source/Core/include/Tracker/NewIssueInheritFields.h", "fix": "Add `if (kind == kBackendGitHub) { return cfg.NewIssueInheritFieldIdsGitHub; }` and flip the pinning expectation in tests/Core/NewIssueInheritFields.test.cpp:72-83; or, if the behaviour change needs its own decision, disable/label the GitHub inherit-fields row in Preferences so the UI stops offering a setting nothing consumes.", "line": 25, "pr": 1994, "problem": "NewIssueInheritFieldIdsFor() has no kBackendGitHub branch, so a GitHub config falls through to cfg.NewIssueInheritFieldIds (the Jira list). TrackerConfig::NewIssueInheritFieldIdsGitHub is loaded (ConfigManager_Load.cpp:498), saved (ConfigManager_Save.cpp:100) and edited in Preferences (SmatchetPreferencesUi.cpp:243/981), but no draft path reads it — the user's \"New issue: inherit fields from last row (GitHub)\" setting is accepted, persisted and silently ignored. The PR documents this as a deliberately preserved known gap and pins it in tests, but it remains a live user-visible defect.", "severity": "MEDIUM", "sha": "d665b65e", "userVisible": true}
{"batch": "batch-21", "file": "agents/scripts/core/lib/script-freshness.sh", "fix": "Treat a local-only glob match as 'ahead', not 'unknown': in the glob branch, when the develop blob is missing for a locally-matched file, drop that file from BOTH fingerprints (set _matched=true, do not set SCRIPT_FRESHNESS_INCOMPLETE) so the remaining declared files still decide fresh/stale. Keep the INCOMPLETE blanking only for the literal-relpath branch and for a locally-MISSING declared file. Add a bats case mirroring `glob: a file develop has and the checkout LACKS reads stale` for the reverse direction.", "line": 98, "pr": 1996, "problem": "script_freshness_fingerprint_one sets SCRIPT_FRESHNESS_INCOMPLETE when EITHER side is missing, so a glob-matched file that exists LOCALLY but not on origin/develop (i.e. a newly added file on the working branch) blanks BOTH fingerprints and forces `unverifiable` — which warn_if_script_stale prints NOTHING for. Reproduced: in a repo declaring `entry.sh` + `rules.d/*.sh`, adding an untracked `rules.d/99-local-only.sh` AND drifting `entry.sh` away from origin/develop yields verdict=unverifiable and zero output, instead of the `stale` WARN. pre-ship.sh declares `agents/scripts/project/lint-rules.d/*.sh`, so any branch that adds one lint rule silently disables the whole freshness caveat — the false-green class this detector exists to prevent, one direction over from the develop-only case the same commit deliberately fixed to report `stale`.", "severity": "HIGH", "sha": "79891e09", "userVisible": false}
{"batch": "batch-21", "file": "docs/agent-rules/merge-gates.md", "fix": "Reword to \"when any file in its declared gate-logic set (`merge-gates.sh`, both `merge-gates.d/` modules, and `lib/script-freshness.sh`) differs from `origin/develop`\".", "line": 152, "pr": 1996, "problem": "Doc-vs-code drift on the freshness guard's declared set: the line states block mode refuses GATES_PASSED when \"this `merge-gates.sh`'s on-disk blob differs from `origin/develop`'s\", but merge-gates.sh fingerprints four files (merge-gates.sh, merge-gates.d/00-common.sh, merge-gates.d/10-gate-filter.sh, lib/script-freshness.sh). A reader auditing the guard would conclude a stale merge-gates.d module is not covered.", "severity": "LOW", "sha": "79891e09", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/archive-backlog-entry.sh", "fix": "Make the residual check independent of the fix parser (e.g. re-scan with a deliberately loose matcher: any occurrence of the entry basename inside a ](...) or [id]: target, not just targets that normpath-resolve to the entry), and move the inbound WRITE after the refusal point (scan, refuse, then fix) so exit 3 cannot leave referrers rewritten.", "line": 393, "pr": 1998, "problem": "The \"VERIFY the guarantee rather than assume it\" residual re-scan is vacuous: inbound-scan and inbound-fix share the identical fix()/rewrite_links parser, so any link shape the fix pass missed is missed identically by the re-scan, and every shape it did rewrite is already gone from disk. residual is therefore always empty and exit 3 is unreachable — the documented protection against \"a link shape the parser missed would otherwise be orphaned silently at exit 0\" does not exist. Worse, if it ever did fire it would exit 3 after line 347's inbound-fix already rewrote referrers on disk, contradicting the Step-0 invariant at line 321 (\"everything that can REFUSE, before anything is written\") and leaving a half-mutated, unstaged tree.", "severity": "MEDIUM", "sha": "53843cc8", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Update to the actual count (26), or drop the numeral and describe coverage qualitatively so it cannot go stale as tests are added.", "line": 3637, "pr": 1998, "problem": "Doc-vs-code drift on a counted literal: the archived entry states \"Nineteen tests in archive_backlog_entry.bats\", but the same commit shipped 26 @test cases in tests/bats/archive_backlog_entry.bats (all 26 still alive at develop). The follow-on claim \"reverting the fix fails 8 of them\" is pinned to that stale count.", "severity": "LOW", "sha": "53843cc8", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/postmortem-owed.sh", "fix": "Guard both loops: for _r in ${ROWS[@]+\"${ROWS[@]}\"}; do — or wrap them in [ \"${#ROWS[@]}\" -gt 0 ].", "line": 726, "pr": 1998, "problem": "for _r in \"${ROWS[@]}\" (and the judging loop at 773) expands a possibly-empty array under set -euo pipefail. When gh pr list returns nothing (offline, auth failure, empty window) ROWS is empty; on bash < 4.4 that is an unbound-variable hard error, turning a clean \"nothing to judge\" into a non-zero exit on the --blocking SessionStart path. The repo guards this exact shape elsewhere (archive-backlog-entry.sh:367 uses ${touched[@]+\"${touched[@]}\"}) and line 762 guards ROWS with a count check, so the convention is applied inconsistently here.", "severity": "LOW", "sha": "53843cc8", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/archive_backlog_entry.bats", "fix": "Add a non-zero floor on extracted targets before the dangling check (count them and assert e.g. >= 4), matching the zero-run floor the wrapper applies at test-archive-backlog-entry-bats.sh:20.", "line": 106, "pr": 1998, "problem": "The \"every re-depthed link actually resolves\" test — described as \"the assertion that would have caught the original bug outright\" — passes vacuously when its grep -o '](\\([^)]*\\))' applied.md produces no output: the while body never runs, missing stays 0, and [ \"$missing\" -eq 0 ] succeeds. A regression that dropped or mangled all link syntax during re-depth would make this test green rather than red.", "severity": "LOW", "sha": "53843cc8", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/test-plan-claim-anchors.sh", "fix": "Tighten the anchor alternatives: require the sha to be preceded by a backtick/word-boundary AND contain at least one digit (e.g. r'\\b(?=[0-9a-f]{7,40}\\b)[0-9a-f]*\\d[0-9a-f]*\\b'), and require the ':\\d+' form to follow a file-like token (r'[\\w./\\\\-]+\\.[A-Za-z0-9]+:\\d+\\b') rather than any character. Add selftest cases for the two false-anchor strings above.", "line": 459, "pr": 1999, "problem": "ANCHOR_RE = r':\\d+\\b|#\\d{2,}|\\b[0-9a-f]{7,40}\\b' accepts non-evidence as a citation, so the gate fails OPEN. The hex-sha alternative matches any 7+ letter English word drawn from a-f (\"defaced\", \"acceded\", \"effaced\"), and ':\\d+' matches any clock time / ratio / port. Verified: \"- the retry policy already existed and defaced the queue\" and \"- the cache already had a 10:30 refresh window\" both pass unflagged, which is exactly the unverified-delivery-claim class this gate exists to stop.", "severity": "MEDIUM", "sha": "8997b18b", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/test-plan-claim-anchors.sh", "fix": "Anchor the file-header test instead of the generic prefix: treat a line as a header only when `raw.startswith(\"+++ \")` AND path parsing succeeded for that diff stanza (or track that the '---'/'+++' pair is at the top of a stanza), so a content line beginning '++' is counted and increments lineno.", "line": 704, "pr": 1999, "problem": "added_lines()'s hunk parser drops any added line whose CONTENT starts with '++' — git renders it as '+++...', which fails `not raw.startswith(\"+++\")` — and because the `lineno += 1` lives inside that same elif, the line counter is never advanced. Every added line after it in the hunk is then recorded at a line number one too low, so a genuinely new unanchored claim can be scoped to the wrong line and silently escape diff-scope (or an untouched line be flagged).", "severity": "LOW", "sha": "8997b18b", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/test-plan-claim-anchors.sh", "fix": "Compute the committed-side scope against the working tree in one step (`git diff --unified=0 origin/develop -- <plan paths>`, which already includes uncommitted changes) instead of unioning two differently-based diffs, and make an origin/develop lookup failure exit non-zero with a 'cannot resolve base — run --all' message rather than returning an empty scope.", "line": 710, "pr": 1999, "problem": "Diff scope unions added_lines(\"origin/develop...HEAD\") with added_lines(\"HEAD\"), but the first set's line numbers are relative to the HEAD blob while scan() reads the WORKING-TREE file. Any uncommitted edit that inserts or deletes lines above a committed claim shifts it, so the committed claim's real line is no longer in `allowed` and the gate passes locally. Also, when origin/develop is unavailable (fresh/shallow clone) added_lines returns an empty set on CalledProcessError and the pre-push run silently scopes nothing.", "severity": "LOW", "sha": "8997b18b", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/test-gate-selftests.sh", "fix": "Make the quote optional and drop the `--` requirement in both the grep prefilter and the awk command-position test: match `\"?\\$\\{?(_SCRIPT_PATH|SCRIPT_PATH|0)\\}?\"?([[:space:]]|$)` at the start of a stripped segment (still after the keyword/env/redirection strip, so bash/sh/. prefixes keep passing). Add two selftest fixtures — unquoted exec and non-dash argument — to the assembled-fixture block near line 259, and either widen or footnote the header/backlog wording.", "line": 60, "pr": 2002, "problem": "The raw-self-exec rule only matches a DOUBLE-QUOTED path followed by an argument starting with `--`: SELF_EXEC_RE (line 60) and the awk match at line 132 both require `\"$_SCRIPT_PATH\"` plus `[[:space:]]+--`. Two equally-broken shapes ride through: an UNQUOTED exec (`$_SCRIPT_PATH --check`) and a NON-dash argument (`\"$_SCRIPT_PATH\" check`). Both hit the same 126 Permission denied on a mode-100644 script, so the vacuous-negative hole the gate exists to close is still reachable. Verified against HEAD: a synth tree with those two shapes exits `PASS — all 2 --selftest-exposing scripts assert a failure case` (rc=0), while the quoted `--` form is correctly flagged. No live instance in the tree today, so the gap is dormant, but the gate's own header (lines 48-59) and the backlog outcome note in docs/self-improvement/categories/applied.md line 4119 both claim it blocks \"raw self-exec of $_SCRIPT_PATH/$SCRIPT_PATH/$0\" without stating the restriction.", "severity": "MEDIUM", "sha": "e2b6a3f8", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/record-review-verdict.sh", "fix": "Prune stale markers when stamping — e.g. after writing, delete `review-verdict-*` files older than N days (or whose sha is no longer reachable via `git cat-file -e`), in `_record` before the write.", "line": 57, "pr": 2003, "problem": "Every recorded verdict writes a new `$GIT_DIR/review-verdict-<full-sha>` file and nothing ever prunes them; a long-lived clone accumulates one zero-value file per push/amend indefinitely (and pre-push stage E never removes a consumed marker).", "severity": "LOW", "sha": "4a4ec8e9", "userVisible": false}
{"batch": "batch-21", "file": "scripts/git-hooks/pre-push", "fix": "Accumulate all unmarked refs into a newline-joined list (e.g. `rv_missing=\"$rv_missing\\n ${rv_ref#refs/heads/} @ ${rv_sha:0:12}\"`) and print the full set in the refusal heredoc.", "line": 332, "pr": 2003, "problem": "Stage (E)'s loop assigns `rv_missing_sha`/`rv_missing_ref` without breaking, so on a multi-ref push with several unmarked refs the refusal names only the LAST unmarked ref; the author stamps that one, re-pushes, and gets blocked again on the earlier one.", "severity": "LOW", "sha": "4a4ec8e9", "userVisible": false}
{"batch": "batch-21", "file": ".github/actions/cr-finding-gate/action.yml", "fix": "Make the marker check atomic against the post — e.g. re-fetch comments immediately before the POST and abort if a marker for this SHA appeared, or serialize the nudge behind a per-PR lock (a dedicated concurrency group on a separate nudge job, since the gate job itself must stay concurrency-free).", "line": 152, "pr": 2004, "problem": "The \"once per head\" nudge dedup is check-then-act: maybe_nudge_full_review fetches all issue comments, checks for the marker, then POSTs. cr-finding-gate.yml deliberately has NO concurrency block (lines 50-66), and bursts of pull_request_review_comment / issue_comment events are documented to fire many runs within one second. Two or more concurrent runs on the same head can all read the comment list before any of them posts, so each posts its own `@coderabbitai full review` — the once-per-head cap and the NUDGE_BUDGET count are both raceable, and the failure mode is exactly the rate-limit-slot burn the guard rails were written to prevent.", "severity": "MEDIUM", "sha": "2d83c5f3", "userVisible": false}
{"batch": "batch-21", "file": ".github/actions/cr-finding-gate/action.yml", "fix": "Derive the expected author at runtime (e.g. `gh api user --jq .login`, mapping to `<login>[bot]` for app tokens) instead of hard-coding, or assert the token identity once and fail loudly on mismatch; add a bats case covering a non-`github-actions[bot]` poster.", "line": 131, "pr": 2004, "problem": "Both the once-per-head dedup and the budget cap only count markers whose comment author login is the literal `github-actions[bot]`. That login is a consequence of the `github-token` input happening to be `${{ github.token }}` (workflow line 129); with a PAT or GitHub App token the nudge would post under a different login, its own markers would never match, and the gate would re-post a nudge on every run for the same head with the budget cap permanently inert. Nothing in the action or the bats suite pins that coupling.", "severity": "MEDIUM", "sha": "2d83c5f3", "userVisible": false}
{"batch": "batch-21", "file": ".github/actions/cr-finding-gate/action.yml", "fix": "Collapse to a single grep with no downstream short-circuit, e.g. `! printf '%s' \"$body\" | grep -qiE 'actionable[^0-9]*[0-9]'`, and mirror the change in tests/bats/cr_finding_gate.bats verdict() plus the pinning grep at line 621.", "line": 470, "pr": 2004, "problem": "The shape-3 negative guard `! printf '%s' \"$body\" | grep -i 'actionable' | grep -q '[0-9]'` runs under `set -o pipefail` (line 45). `grep -q` exits on its first match, so if the upstream `grep -i actionable` still has output queued it dies on SIGPIPE (141) and the pipeline reports non-zero — which `!` turns into \"no digit found\", accepting a format-drifted findings header (`Actionable comments: 3`) as a clean pass and posting success. Needs >64 KiB of `actionable`-matching lines to trigger, but it is a fail-open in the gate's own safety guard.", "severity": "LOW", "sha": "2d83c5f3", "userVisible": false}
{"batch": "batch-21", "file": ".github/actions/cr-finding-gate/action.yml", "fix": "Widen the positive signal to the set of CR review-section headings that only appear on a submitted review body (nitpick / duplicate / outside-diff-range comments), keeping the digit-bearing `actionable` negative guard as the fail-closed half; add bats fixtures for the additional shapes.", "line": 469, "pr": 2004, "problem": "The shape-3 positive discriminator recognises only a `nitpick comments` section. CodeRabbit also emits header-less clean bodies whose only section is `Duplicate comments` or `Outside diff range comments`; those still take the fail-closed `return 1` path at line 480 and reproduce the exact permanent wedge this change fixes, since (per the ledger entry at docs/self-improvement/categories/applied.md:4208) `@coderabbitai full review` re-wedges rather than resolving it.", "severity": "LOW", "sha": "2d83c5f3", "userVisible": false}
{"batch": "batch-21", "file": ".github/actions/cr-finding-gate/action.yml", "fix": "Emit a one-line `echo \"CR finding gate: nudge suppressed (<reason>)\"` before each `return 0` guard.", "line": 150, "pr": 2004, "problem": "Every suppression path in maybe_nudge_full_review (`no clean evidence`, `busy newer than clean`, `marker already present`, `budget exhausted`, `comment fetch failed`) is a bare `return 0` with no log line, while the post path echoes. When a PR wedges on PENDING there is no way to tell from the CI log which guard fired, on a function whose whole purpose is unattended self-healing.", "severity": "LOW", "sha": "2d83c5f3", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Tracker/GitHubFixtureBackend.cpp", "fix": "Route the GitHub constructor through LoadFixtureJson(j) like Plane/Linear (keeping its owner/repo-hint mapping and, if the wording matters, its loadError_ text), and delete the file-local ReadFileToString.", "line": 37, "pr": 2005, "problem": "GitHubFixtureBackend now derives from TrackerFixtureBackendBase but still uses its own local ReadFileToString + ParseBoundedOrDiscarded prologue instead of the base's LoadFixtureJson(). Two divergent fixture-load paths survive (two unbounded-slurp deviation sites, two different open-failure diagnostics), so a future hardening of the bounded-parse caps or of the slurp would land on Plane/Linear only and silently miss GitHub.", "severity": "LOW", "sha": "ada9ac37", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Tracker/LinearFixtureBackend.cpp", "fix": "Keep one single-line marker directly above the class with the intended owner; drop the other, leaving the rationale prose above it.", "line": 52, "pr": 2005, "problem": "Two SMATCHET_DEVIATION(rule=duplication) markers now sit back-to-back on the same construct (LinearFixtureBackendFactory) with conflicting metadata: the reflowed original claims owner=security-audit, the newly added one owner=tracker. Duplicate exemption records for one clone make ownership/revisit auditing ambiguous.", "severity": "LOW", "sha": "ada9ac37", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Diagnostics/BugReportService.cpp", "fix": "Add a const unsigned char*/size overload of Base64Encode in StringUtil.h (the std::string overload delegating to it) and have Base64EncodeBytes pass in.data()/in.size() with no intermediate copy.", "line": 86, "pr": 2005, "problem": "Base64EncodeBytes now materializes a full std::string copy of the attachment byte buffer before encoding (std::string(in.begin(), in.end())). The previous in-place encoder walked the vector directly, so peak memory for a minidump/screenshot upload grows by an extra whole-file copy.", "severity": "LOW", "sha": "ada9ac37", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/merge-gates.sh", "fix": "Treat the anchor and the streak as one unit: after the regex validation, `[ -n \"$outage_since\" ] || outage_streak=0` (and likewise drop the streak when outage_head is empty), so a probe never runs against a window narrower than the streak it claims to summarise.", "line": 419, "pr": 2006, "problem": "The three carried outage fields are validated independently: a malformed/absent MERGE_GATES_PRIOR_OUTAGE_SINCE is silently blanked while MERGE_GATES_PRIOR_OUTAGE_STREAK is kept. With a carried streak already at/near OUTAGE_POLLS and since=\"\", line 1205 re-stamps the anchor at `date -u` now, so the repo-wide probe at line 1208 asks \"any runs created in the last ~0 seconds?\" — total_count is essentially always 0, producing a false `ESCALATE: ACTIONS UNAVAILABLE` / return 7. Exit 7 is the deliberately no-skip halt, so a healthy PR is wedged with a wrong diagnosis. All three are documented operator-seedable env seams (header lines 140-147), so the inconsistent-pair state is reachable outside the watcher's atomic write.", "severity": "MEDIUM", "sha": "6a2bb053", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Update the 2026-08-13 fix-(3) paragraph: window = absence-streak start (re-anchored on a confirmed-alive probe); MERGE_GATES_OUTAGE_POLLS must be a positive integer, 0 is rejected with return 3 (no disable switch); drop the stale case count/list or match the bats section.", "line": 4363, "pr": 2006, "problem": "Ledger drift introduced in the same commit that shipped the code it describes. The fix-(3) update says the probe window is `actions/runs?created=>=<poll start>` (the shipped code anchors at the absence-STREAK start and re-anchors after a confirmed-alive probe — merge-gates.sh:1205/1228), says \"0 disables\" (merge-gates.sh:406-409 REJECTS 0 with return 3, and tests/bats/merge_gates.bats:2552 pins that), and describes the suite as \"8 cases … / disabled / knob validation\" when there is no disabled case and the outage section holds ~13.", "severity": "LOW", "sha": "6a2bb053", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/infra/2026-08-05-merge-watcher-liveness-unmonitored.md", "fix": "Update to \"polls up to 20 s and requires 3 consecutive Running samples (~6 s sustained)\".", "line": 103, "pr": 2007, "problem": "Doc-vs-code drift on a numeric literal: the entry says the installer \"polls up to 15 s\", but merge-watcher-install-autostart.ps1:186 sets a 20 s deadline and the health verdict requires 3 CONSECUTIVE Running samples (~6 s sustained), which the doc omits entirely.", "severity": "LOW", "sha": "a7d35e0c", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/merge-watcher-install-autostart.ps1", "fix": "Reset $preLogLen = 0 after Start-ScheduledTask (truncation is the real baseline), compare mtime instead of length, or make the action append with `>>`.", "line": 210, "pr": 2007, "problem": "The \"Log is growing\" cue compares the post-start log length against $preLogLen captured before Start-ScheduledTask, but the registered action redirects with `>` (line 126), truncating daemon.log on every start. On any re-install over an existing log the new length restarts at 0 and rarely exceeds the old length, so the growth confirmation is effectively never printed even for a healthy daemon.", "severity": "LOW", "sha": "a7d35e0c", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/postmortems.md", "fix": "Make the link text match the target, e.g. `[CR finding gate wedges on an empty-body CodeRabbit review](categories/applied.md#cr-finding-gate-wedges-on-an-empty-body-coderabbit-review)`.", "line": 173, "pr": 2010, "problem": "Cross-ref link text is a stale path (`categories/process/2026-08-05-cr-finding-gate-empty-body-review-wedge.md`) that no longer exists — the entry was archived into `categories/applied.md`, which is where the href points. Reader sees a filename absent from the tree.", "severity": "LOW", "sha": "586ee54e", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Point it at the archived entry's heading anchor within applied.md instead of the bare file path.", "line": 4747, "pr": 2010, "problem": "`[adaptive-ratelimit](applied.md)` is a self-link from inside applied.md to the whole ~5600-line file with no anchor, so the referenced entry is unlocatable.", "severity": "LOW", "sha": "586ee54e", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Change \"six cases\" to \"nine cases\", or drop the number and reference the \"§ cancelled-while-pending\" block so no stale count is pinned.", "line": 5001, "pr": 2012, "problem": "Doc-vs-code drift on a counted literal: the applied-status note claims \"six cases in tests/bats/merge_gates.bats (§ cancelled-while-pending)\", but the section this same commit added contains nine @test cases (merge_gates.bats lines 2451, 2462, 2477, 2490, 2504, 2513, 2521, 2530, 2539). The CodeRabbit round added the substring-match and full-first-page cases without updating the count.", "severity": "LOW", "sha": "7f0a293b", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/SmatchetToolbarUi.cpp", "fix": "Pass the button's own args: BoundHotkeyDisplayAll(cfg.Keybindings.Bindings, b.CommandId, b.ArgsJson) — matching what DispatchButton actually dispatches.", "line": 279, "pr": 2013, "problem": "The toolbar tooltip switched from BoundHotkeyDisplay(bindings, commandId) to BoundHotkeyDisplayAll(bindings, commandId, \"{}\"). The old call used the command-id-only rule (prefer the \"{}\" row, otherwise fall back to ANY row for that command); the new call uses semantic args matching against a hardcoded \"{}\" with no fallback. ToolbarButton carries its own ArgsJson (ToolbarConfig.h:31) and it is ignored here, so a customized toolbar button whose command is bound with non-default args (e.g. view.toggle.performance {\"action\":\"show\"}, view.sidebar.primary {\"action\":\"toggle\"}) now shows no shortcut at all where it previously showed one.", "severity": "MEDIUM", "sha": "8579fd47", "userVisible": true}
{"batch": "batch-21", "file": "Source/Core/src/Ui/ImGuiHotkey.cpp", "fix": "Track that shift was implied (bool impliedShift) and clear out.shift when a subsequent KeyFromToken hit replaces out.key, unless an explicit \"shift\" token was seen.", "line": 238, "pr": 2013, "problem": "The implied shift from a shifted spelling is never cleared when a later main key overrides it. \"Ctrl+Plus+B\" tokenizes to [ctrl, plus, b]: \"plus\" sets key=Equal and shift=true, then \"b\" sets key=B but shift stays true — the spec silently parses as Ctrl+Shift+B. The header contract says only \"the last main key wins\", and the sibling regression case (\"Ctrl+A+B\") is pinned in tests while this one is not.", "severity": "LOW", "sha": "8579fd47", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/SmatchetHotkeyCapture.cpp", "fix": "Cache the conflict result alongside the editor's existing mutated/dirty flag and recompute only when the binding table changes.", "line": 168, "pr": 2013, "problem": "ComputeKeybindingRowConflicts is called once per frame from the Keyboard Shortcuts editor (SmatchetPreferencesUi_Keybindings.cpp:361) and re-parses every combo of every binding plus allocates 2 output string vectors and a std::vector<ParsedRow> of 2 vectors per row — roughly 3n heap allocations and ~30 ParseImGuiHotkey calls (each of which itself allocates several strings) on the render path every frame the tab is visible. It is cheaper than the per-row scan it replaced, but the work is still fully redundant between frames.", "severity": "LOW", "sha": "8579fd47", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/Ui/ImGuiHotkey.cpp", "fix": "Compare tok == sk.token directly (the table is lowercase by construction); drop the per-candidate LowerAscii allocation.", "line": 154, "pr": 2013, "problem": "ShiftedKeyFromToken compares with LowerAscii(sk.token), allocating a std::string for each of the 4 kShiftedKeys entries on every non-modifier token lookup, even though the table literals (\"+\", \"plus\", \"_\", \"underscore\") are already lowercase and the incoming token is documented as arriving lower-cased.", "severity": "LOW", "sha": "8579fd47", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/include/Config/KeybindingsConfig.h", "fix": "Repoint all seven references to docs/plans/shipped/keybindings-multi-combo.md.", "line": 23, "pr": 2013, "problem": "Stale doc cross-reference: the comment points at docs/plans/keybindings-multi-combo.md, which now lives at docs/plans/shipped/keybindings-multi-combo.md. The same dead path survives in tests/Core/ConfigMigration.test.cpp:571, tests/Core/ImGuiHotkey.test.cpp:155 and :398, tests/Core/KeybindingsConfig.test.cpp:45, and tests/ui/keybindings_editor_rebind.test.cpp:252 and :493.", "severity": "LOW", "sha": "8579fd47", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/cr_finding_gate.bats", "fix": "Add `[ -f \"$WF\" ]` (or a positive anchor such as `grep -q '^name: CR finding gate' \"$WF\"`) at the top of the test before the negated greps, so a missing/renamed workflow fails the pin instead of satisfying it.", "line": 141, "pr": 2014, "problem": "The new \"workflow has NO concurrency block\" pin is a pure negative assertion (`run grep ...; [ \"$status\" -ne 0 ]`) with no proof the file exists or is non-empty. `WF` is a hardcoded path set in setup() with no existence check, so if cr-finding-gate.yml is ever renamed/moved, grep exits 2 (file not found) and BOTH assertions pass vacuously — the pin the comment claims cannot go inert does exactly that. Same for the cancel-in-progress grep at line 143.", "severity": "MEDIUM", "sha": "", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Re-shape the block to the canonical bullet form (`- 2026-08-06 · orchestrator · [tooling] · P1 — …` with indented Details/Resolution continuation lines), or archive via `agents/scripts/core/archive-backlog-entry.sh`, which emits that form.", "line": 5057, "pr": 2016, "problem": "The archived entry was appended as a heading-form block (`## [P1] …` + `**Date**: 2026-08-06`) with no `- YYYY-MM-DD · …` entry line. `agents/scripts/core/sort-applied-md.sh` splits entries only on `^- (\\d{4}-\\d{2}-\\d{2}) `, so lines 5057-5115 are absorbed as continuation of the *previous* bullet entry (`- 2026-08-04 …` at line 4921). A re-sort/`--check` run therefore files this 08-06 entry under the 08-04 date group, breaking the file's stated 'sorted by original surface date, latest first' contract and silently mis-dating the record.", "severity": "MEDIUM", "sha": "452e4875", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Append `Status: applied` + `Last-reviewed: 2026-08-15` to the entry (or re-do the archival through `archive-backlog-entry.sh`, which also re-depths relative links and repoints inbound referrers).", "line": 5115, "pr": 2016, "problem": "The archived entry ends at the 'Generalisable lesson' section with no `Status: applied` and no `Last-reviewed:` line, unlike sibling archived entries. applied.md's header states 'Closed entries. Archive moves immediately on Status → applied', and `archive-backlog-entry.sh` normally rewrites `Status: …` to `Status: applied (flipped at archival)` — its absence shows the block was hand-appended rather than run through the script, so the entry still reads as an unresolved `[P1]` to any status scan or future reconcile pass.", "severity": "LOW", "sha": "452e4875", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/merge-gates.d/10-gate-filter.sh", "fix": "Do not reuse the build-need allow-list as a review-safety allow-list. Bind a separate predicate for this arm, e.g. `all(test(\"^(docs/|backlog/|.*[.]md$)\"))` (drop `agents/scripts/`), or additionally require the skip comment to be newer than the head commit's pushedDate.", "line": 140, "pr": 2017, "problem": "$pureDocs reuses the is-pure-docs-diff.sh allow-list, which includes `agents/scripts/` — shell/gate code, not docs. It is then used as the ONLY guard on the comment-surface terminal-pass arm of $crreviewskipped (line 181), whose stated purpose is to stop a STALE CodeRabbit skip comment (comments persist across pushes) from fast-passing a fresh CODE push. A PR touching only `agents/scripts/**` (including merge-gates.sh / merge-gates.d/*.sh themselves) is classified pureDocs, so a leftover `## Review skipped` comment terminal-PASSes the CR bucket for an unreviewed gate-script push. Fail-open on the highest-leverage code class in the repo.", "severity": "HIGH", "sha": "c85b3521", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/merge-gates.d/10-gate-filter.sh", "fix": "Invert to an allow-list: terminal-pass only on the reason texts known to be terminal (path filters / docs-only / trivial diff / draft), and let every unrecognised \"Review skipped: ...\" description fall through to the NONE grace-then-pass, which is slow but never wedges.", "line": 175, "pr": 2017, "problem": "The StatusContext arm of $crreviewskipped terminal-PASSes on any description matching \"review skipped\" minus a deny-list of five observed reason strings (too many files / manual review required / available on request / rate.?limit / limit reached). A terminal-PASS decision keyed on an exhaustive deny-list of free-text vendor strings fails OPEN for every unlisted non-terminal reason (e.g. \"Review skipped: quota exceeded\", \"usage limit\", or a description truncated by GitHub's 140-char status limit before the reason text) — the PR fast-passes the CR gate instead of waiting.", "severity": "MEDIUM", "sha": "c85b3521", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Either implement the precedence arm, or split the entry: archive the shipped part and re-file the unshipped precedence rule as an open backlog item.", "line": 5165, "pr": 2017, "problem": "The ledger entry filed in applied.md (i.e. archived as applied) lists three concrete next actions; only (1) narrow $crskip and part of (3) test pins shipped in this PR. Deliverable (2) — let the required `CR findings (0 actionable)` StatusContext SUCCESS pass the CR bucket ahead of the `case NONE` chain — has no precedence arm anywhere in merge-gates.sh / merge-gates.d (grep finds only rollup name matches at 10-gate-filter.sh:246,262). Residual work is now invisible because the entry is in the applied archive.", "severity": "MEDIUM", "sha": "c85b3521", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Repoint the cross-refs at merge-gates.d/10-gate-filter.sh (or drop the line numbers and cite the `$crskip` / `$crreviewskipped` binding names), and give the entry its own heading rather than nesting it under the preceding entry's lesson section.", "line": 5179, "pr": 2017, "problem": "Cross-refs in this entry pin `agents/scripts/core/merge-gates.sh` at :552-554 ($crskip), :564-569 ($crreviewskipped), :937-980, :1201-1202, :1164-1167 — but the jq filter was already split into agents/scripts/core/merge-gates.d/10-gate-filter.sh at merge time, so every pin was stale when merged. The entry is also appended directly under an unrelated P1 entry's `### Generalisable lesson` section with no heading of its own, so heading-scoped doc tooling attributes it to the wrong entry.", "severity": "LOW", "sha": "c85b3521", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/postmortem-owed.sh", "fix": "Split the branch by evidence source: on the snapshot path keep the cr-out-of-band trigger unconditionally (schema 2 carries no per-context conclusion, so there is no authoritative evidence to drop on), and apply the gate_conclusion 'CR findings' test only to live-fallback triggers, matching the documented override_is_moot rule. Add a bats case for snapshot cr-out-of-band + live rollup re-run SUCCESS still KEEPS.", "line": 982, "pr": 2018, "problem": "The new de-noise branch calls gate_conclusion (a LIVE rollup read) on snapshot-derived triggers too, violating the invariant stated above override_is_moot: the moot-filter runs on the LIVE-fallback path ONLY, never on a snapshot-derived trigger, where re-judging against the lossy live rollup could drop a real load-bearing override. A snapshot that recorded overrideLabels=[cr-out-of-band] with redChecks=[] on a non-Core diff is now silently dropped whenever anyone re-ran the CR context green after merge, so the detector under-reports a genuine gate escape (fail-open nudge). The added bats case (postmortem_owed.bats:496) only covers the still-PENDING variant, so the over-drop path is untested.", "severity": "MEDIUM", "sha": "e8805608", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Re-pin to the current HEAD ranges (core_scoped_only_trigger ~402-409, pr_touches_core_cpp ~412-415, gate_conclusion ~437-455, override_is_moot ~465-486) or drop the numeric pins and cite function names only, which is drift-proof.", "line": 5196, "pr": 2018, "problem": "Every line-pin in the new applied entry is stale and was already wrong at the commit that added it: it cites core_scoped_only_trigger (:156-163), pr_touches_core_cpp (:167-170), gate_conclusion (:187-202), override_is_moot (:212-232 / :227-230) and the de-noise comment (:149-155), but at e8805608 those are at 402, 413, 433 and 458 respectively (~250 lines off). Anyone following the cross-refs lands in unrelated code.", "severity": "LOW", "sha": "e8805608", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Append ' Status: applied (flipped at archival)' and ' Last-reviewed: 2026-08-15' to the entry.", "line": 5225, "pr": 2018, "problem": "The new archive entry ends at Cross-ref: with no Status: and no Last-reviewed: line, both mandatory per the Format block in docs/self-improvement/AGENT_SELF_IMPROVEMENT.md and present on sibling archived entries (e.g. the 2026-08-04 entry below carries 'Status: applied (flipped at archival)' + 'Last-reviewed: 2026-08-04'). An entry in applied.md with no applied marker cannot be distinguished from an unresolved one by a reader or a sweep.", "severity": "LOW", "sha": "e8805608", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/lib/resolve-py.sh", "fix": "Split the canonicalization so the failure is observable, e.g. `local _rp_dir; _rp_dir=\"$(cd \"$(dirname \"$_rp_path\")\" 2>/dev/null && pwd)\" || continue; [ -n \"$_rp_dir\" ] || continue; _rp_path=\"$_rp_dir/$(basename \"$_rp_path\")\"`.", "line": 59, "pr": 2019, "problem": "The relative-path canonicalization `_rp_path=\"$(cd \"$(dirname \"$_rp_path\")\" 2>/dev/null && pwd)/$(basename \"$_rp_path\")\" || continue` has a dead `|| continue`: an assignment's exit status is that of the LAST command substitution (`basename`, always 0), so a failing `cd` is never detected. Verified: the branch yields `_rp_path=\"/python3\"` and resolve_py then `printf`s it and `return 0` — handing every caller a non-existent absolute interpreter path that fails at the first `\"$PY\" ...` invocation, which is exactly the silent-wrong-pick class this lib exists to prevent.", "severity": "MEDIUM", "sha": "a63d2166", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/test-shell-lint.sh", "fix": "Anchor the exemption to the canonical repo-relative path, e.g. `case \"${script#./}\" in agents/scripts/core/lib/resolve-py.sh) return 0 ;; esac` (compare against the repo-root-relative path the linter already computes for `emit`).", "line": 483, "pr": 2019, "problem": "Rule 9's self-exemption is an unanchored path-suffix glob (`*/lib/resolve-py.sh) return 0`), not the one blessed path. Any file whose path merely ends in `lib/resolve-py.sh` (e.g. a copy-pasted `scripts/dev/lib/resolve-py.sh` or a worktree/vendored duplicate) is silently exempted from SHELL_LINT_PY_PROBE — the gate would not flag a second, wrongly re-implemented resolver, defeating the \"one blessed home\" invariant the rule enforces.", "severity": "LOW", "sha": "a63d2166", "userVisible": false}
{"batch": "batch-21", "file": "agents/core/code-review.md", "fix": "Replace the scope command with the two-dot/working-tree form: `git diff \"$(git merge-base origin/develop HEAD)\"` (merge-base -> working tree). Drop the false contrast against `origin/develop...HEAD` or restate it as `A...B` == committed-only in both spellings.", "line": 37, "pr": 2020, "problem": "The no-arg review scope is documented backwards. `git diff origin/develop...` is IDENTICAL to `git diff origin/develop...HEAD` (git substitutes HEAD for the omitted right side), so it is merge-base -> HEAD, committed-only. Verified in-repo: with README.md dirty, both forms omit it; only `git diff $(git merge-base origin/develop HEAD)` shows it. The doc asserts the opposite (\"merge-base -> working tree, so unstaged hunks are in scope\") and calls the HEAD form the one that \"silently drops them\". A reviewer following this contract reviews a diff that drops every unstaged hunk while being told it cannot — the exact fail-open the sibling `MM` rule at lines 43-48 was added to close. It also breaks the `--intent-to-add` claim on lines 39-40: intent-to-add files appear in worktree diffs, not in a commit-to-commit `A...B` diff.", "severity": "HIGH", "sha": "acccaddb", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Cite the symbol without the line number (`grep -n SMATCHET_TEST_WITHMSVC_EXIT scripts/dev/local/test-build-wrapper.sh`), per the repo's no-line-pin doc convention.", "line": 5259, "pr": 2020, "problem": "Stale line-pin: the entry cites `scripts/dev/local/test-build-wrapper.sh:186` as where `SMATCHET_TEST_WITHMSVC_EXIT=` is set. At HEAD that symbol lives at lines 174/178/196; line 186 is unrelated sandbox-stub code.", "severity": "LOW", "sha": "acccaddb", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/debt/2026-08-07-dock-node-id-slot-liveness-followups.md", "fix": "Make the link text match the target — e.g. `see the archived entry in [`categories/applied.md`](../applied.md) (2026-08-07 id-existence-checks-need-containment)` — so the rendered text never names a nonexistent file.", "line": 58, "pr": 2020, "problem": "Link text and href disagree: the text names `../process/2026-08-07-id-existence-checks-need-containment.md` but the href points at `../applied.md`. The href resolves, so link-checkers stay green while the rendered text advertises a path that does not exist. Same shape survives at `docs/self-improvement/categories/test/2026-08-07-booted-app-or-skip-fails-open.md:41`, and at `applied.md:5343` and `:5392`.", "severity": "LOW", "sha": "acccaddb", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/with-msvc-env.sh", "fix": "`unset SMATCHET_MSVC_STATUS_FILE SMATCHET_MSVC_ENV_FAIL_RC` immediately before `exec \"$@\"` (line 279), so the sentinel channel is scoped to this wrapper instance; then the comment's proof-of-ownership claim is actually true.", "line": 65, "pr": 2021, "problem": "SMATCHET_MSVC_STATUS_FILE (and SMATCHET_MSVC_ENV_FAIL_RC) are left in the environment across the tail `exec \"$@\"`, so the wrapped command and every descendant inherit the sentinel path. The adjacent surviving comment (lines 60-63) asserts the opposite as a guarantee — \"nothing it does can reach this function — sentinel presence is proof the failure was ours\". Any nested invocation of with-msvc-env.sh from inside the wrapped build (e.g. scripts/dev/local/build-msvc-asan.sh, or a future recursive wrapper call) would write `toolchain-missing` to the caller's sentinel file, and build.sh would print the \"no usable MSVC toolchain\" install hints for an ordinary nested failure — reintroducing exactly the false-diagnosis class the out-of-band channel was added to eliminate.", "severity": "MEDIUM", "sha": "378858b6", "userVisible": false}
{"batch": "batch-21", "file": "docs/agent-rules/build.md", "fix": "Amend to \"directly when `cl.exe` is already on PATH or `--run-only` was passed (launching a built exe needs no toolchain), otherwise through `scripts/dev/with-msvc-env.sh`\".", "line": 14, "pr": 2021, "problem": "The rewritten § Entry point sentence states build.sh \"delegates to scripts/dev/local/build-and-run.sh — directly when `cl.exe` is already on PATH, otherwise through `scripts/dev/with-msvc-env.sh`\". build.sh:103 also delegates directly when `--run-only` is passed with no compiler (`if [ \"$HAVE_CL\" -eq 1 ] || [ \"$RUN_ONLY\" -eq 1 ]`), so the documented \"otherwise\" is wrong for that branch.", "severity": "LOW", "sha": "378858b6", "userVisible": false}
{"batch": "batch-21", "file": "Source/Standalone/CliSpawn.cpp", "fix": "Assign `outChild->logPath` only where the handle/fd opened successfully (Windows: inside the `else` at :323; POSIX: create/probe the log in the parent before fork, or leave logPath empty when open fails).", "line": 300, "pr": 2022, "problem": "`outChild->logPath = logPath;` is set before the log file is actually created, so it is populated even when capture fails (Windows CreateFileA -> INVALID_HANDLE_VALUE at :319, POSIX open() -> fd<0 at :391). That violates the documented contract in CliCommandRunner_Internal.h:106 (\"\" = capture unavailable): CliDispatch.cpp:151-153 prints `[spawn] child stdout/stderr -> <path>` and the new child-died envelope emits `hint: read its captured output: <path>` (CliDispatch.cpp:330-332) pointing at a file that does not exist, sending the investigator to a dead end on exactly the crash path this PR added.", "severity": "MEDIUM", "sha": "0be6c68f", "userVisible": false}
{"batch": "batch-21", "file": "Source/Standalone/CliDispatch.cpp", "fix": "Test `ec` after `file_size` and return false when it is set, rather than relying on the error sentinel comparing greater than zero.", "line": 255, "pr": 2022, "problem": "`ResultFileNonEmpty` discards its `std::error_code`: `fs::file_size(path, ec)` returns `static_cast<uintmax_t>(-1)` on failure, which is `> 0`, so a directory at outPath or a file unlinked between the `exists` and `file_size` calls is reported as a ready result. The wait then returns kExitOk and the downstream bounded read emits a vaguer OpenFailed envelope instead of the correct timeout/child-died outcome.", "severity": "LOW", "sha": "0be6c68f", "userVisible": false}
{"batch": "batch-21", "file": "Source/Standalone/CliExitCodes.cpp", "fix": "Document `child-died` and its exit-4 mapping plus the `childExitCode`/`logTail` envelope fields in docs/guides/cli.md.", "line": 43, "pr": 2022, "problem": "`child-died` is a new agent-facing `error.code` on the `--spawn` path, but docs/guides/cli.md (the stable agent contract: exit-code table at :108-118, error-envelope section at :128) never mentions it, so agents branching on error codes have no documented way to tell a crashed child (exit 4) from a generic handler error.", "severity": "LOW", "sha": "0be6c68f", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/bucket_lane_launch_smoke.bats", "fix": "Mirror required_context_parity.bats: try python3, then python, then py, resolve the working interpreter into a var and use it for the heredoc; skip only when none can `import yaml`.", "line": 255, "pr": 2023, "problem": "The lane-shape invariant test (the only enforcement that lane-integrity is bucket-C's sole blocking step) probes only `python3`. The repo's established pattern (tests/bats/required_context_parity.bats:25-28) probes python3/python/py because the bats runner is Windows/MSYS where `python3` frequently does not exist. On such a runner this test always `skip`s, so the property the PR added it to enforce is silently unenforced everywhere.", "severity": "MEDIUM", "sha": "683cb5ac", "userVisible": false}
{"batch": "batch-21", "file": "tests/bats/bucket_lane_launch_smoke.bats", "fix": "Add `[ -s \"$SMATCHET_GOLDEN_REPORT_FILE\" ]` plus a row-count assertion (or a `grep -q \"$(printf '\\tpass\\t')\"` guard as in the git-date test) before the while loop.", "line": 200, "pr": 2023, "problem": "The 'undatable golden reports -' test loops over the report file with `[ \"$verdict\" = \"pass\" ] || continue` and never asserts the file is non-empty or has N rows. If report_row regressed to emitting nothing at all, the loop body never runs and the test passes green — a fail-open test for the exact behaviour it pins. (The sibling tests at lines 116 and 149 do carry such a guard.)", "severity": "MEDIUM", "sha": "683cb5ac", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/test-screenshot-diff.sh", "fix": "Degrade instead of dying: `if ! : > \"$SMATCHET_GOLDEN_REPORT_FILE\" 2>/dev/null; then echo \"WARN: cannot write golden report; disabling\" >&2; unset SMATCHET_GOLDEN_REPORT_FILE; fi`, and append with `>> \"$SMATCHET_GOLDEN_REPORT_FILE\" || true` in report_row.", "line": 159, "pr": 2023, "problem": "The verdict-report writes are unguarded under `set -euo pipefail`: the up-front truncate `: > \"$SMATCHET_GOLDEN_REPORT_FILE\"` (line 160) and the append in report_row (line 236) abort the whole driver if the path is unwritable or its directory is missing. The truncate runs before any scenario, so the lane-status sentinel is never written and the blocking `Lane-integrity` CI step fails closed — a pure reporting facility gains blocking power, the exact property this PR asserts it must never have (and the property the epoch digits-only guard at line 227-230 protects against elsewhere).", "severity": "MEDIUM", "sha": "683cb5ac", "userVisible": false}
{"batch": "batch-21", "file": ".github/workflows/build-and-test.yml", "fix": "Move lines 1146-1151 back to immediately above the `Lane-integrity — bucket-C did not pass-nothing` step at line 1055.", "line": 1146, "pr": 2023, "problem": "Orphaned comment: this 6-line block describes the `Lane-integrity — bucket-C did not pass-nothing` step, but that step now sits at line 1055 (moved above the new report steps) with no comment of its own, while the block was left behind directly above `Surface bucket-C spawned-child log` — where it runs straight into that step's own 'Observability' comment. A future editor reads the child-log step as the lane's teeth and the real teeth as uncommented.", "severity": "LOW", "sha": "683cb5ac", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/test-screenshot-diff.sh", "fix": "Append `|| true` to the command substitution (as done for the analogous `ls -t | head -1 || true` in the workflow at line 1173).", "line": 384, "pr": 2023, "problem": "`linf=\"$(printf '%s' \"$diff_out\" | sed -n '...' | head -1)\"` runs under `set -euo pipefail`; if `head -1` exits before sed finishes, sed dies on SIGPIPE and pipefail propagates the failure into the assignment, aborting the driver mid-scenario (no rows for remaining scenarios, no lane-status sentinel, blocking lane-integrity reds). The following `[ -n \"$linf\" ] || linf=\"-\"` shows the intent was graceful degradation.", "severity": "LOW", "sha": "683cb5ac", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/test-screenshot-diff.sh", "fix": "Set age only when the formatted date succeeded, e.g. `if [ \"$mtime\" = \"-\" ]; then age=\"-\"; else age=$(( ... )); fi`.", "line": 232, "pr": 2023, "problem": "When `epoch` is set but both `date` forms fail to format it, `mtime` falls back to '-' while `age` is still computed as a number on the next line, emitting a row like `- 57`. The driver's own header (lines 69-72) and the bats assertion (`-|-`) document the date/age pair as moving together.", "severity": "LOW", "sha": "683cb5ac", "userVisible": false}
{"batch": "batch-21", "file": "Source/Core/src/AppController_PaneContexts.cpp", "fix": "Erase by pane id across all backend keys at retirement (scan paneOwnedTicketIds_ for the '\\n'+paneId suffix), or re-key/drop the pane's entry inside GridLiveContext::SetCacheBackendKey when the key changes.", "line": 848, "pr": 2024, "problem": "ForgetPaneOwnedTicketIds() keys the erase with ctx.CacheBackendKeyCopy() read AT RETIREMENT TIME, but the entry was stored under whatever backend key was live when PublishOwnedTicketIds ran. SwapBackendIfTrackerChanged (TicketSyncService.cpp:665 -> SetCacheBackendKey) re-stamps a pane's key at runtime, so a pane that switched tracker kind leaves an orphan paneOwnedTicketIds_ entry under the OLD key. Nothing else erases it, and CollectTicketIdsRetainedByOtherContexts prefix-scans that namespace with no liveness check, so those ids pin cache rows against every sibling pane's stale sweep permanently (tickets_v2 rows never reclaimed; ghost rows keep resurfacing in siblings' namespace-wide reads).", "severity": "MEDIUM", "sha": "964a05d8", "userVisible": true}
{"batch": "batch-21", "file": "Source/Core/src/AppController_CatalogAndFieldEdit.cpp", "fix": "Make the admit an atomic append under paneOwnedIdsMutex_ (e.g. an AddPaneOwnedTicketId(key, paneId, id) that re-reads the map entry inside the lock) instead of writing back a stale copied vector.", "line": 100, "pr": 2024, "problem": "Non-atomic read-modify-write of the pane owned-id set: `owned` is snapshotted at line 64, then a full SQLite `Cache->GetAllTickets` read and a mutex acquisition happen before SetPaneOwnedTicketIds writes `owned + admitId` back at line 100. Any PublishOwnedTicketIds that lands in that window (sync-session finalize) is silently clobbered and the pane's recorded set reverts to the pre-sync ids — the next RefreshLocalData then filters the freshly-synced rows out of the grid and stops pinning them against a sibling's stale deletion. AppController.h:1206 explicitly documents this path as worker-invoked, so the write is not UI-thread-serialized by contract.", "severity": "MEDIUM", "sha": "964a05d8", "userVisible": true}
{"batch": "batch-21", "file": "Source/Core/src/AppController_CatalogAndFieldEdit.cpp", "fix": "Distinguish 'never synced' from 'set forgotten' (e.g. keep a per-pane hasSyncedOnce flag or a tombstone entry) and render empty rather than the sibling union for a revived pane.", "line": 65, "pr": 2024, "problem": "The pane-scoping filter is skipped whenever the recorded set is empty, falling back to the whole shared namespace. That is not only the true cold-start case: RetireHiddenPaneContexts calls ForgetPaneOwnedTicketIds after the 30 s hidden grace, so a pane the user returns to gets a fresh context with an EMPTY recorded set and the bootstrap refresh repopulates its grid with every sibling pane's tickets until its own sync finishes — exactly the cross-pane row leak F2/F3 was meant to close.", "severity": "MEDIUM", "sha": "964a05d8", "userVisible": true}
{"batch": "batch-21", "file": "Source/Core/src/SmatchetTicketChangeNotifications.cpp", "fix": "Erase the pane's entry on pane retirement/close, or bound the map (it is process-lifetime state on the notification path).", "line": 72, "pr": 2024, "problem": "The per-pane dedup guard is a function-static std::map<std::string,std::string> that is only ever inserted into (operator[] at line 74) and never erased when a pane is retired or removed. Every pane id seen in the process lifetime retains a signature string forever.", "severity": "LOW", "sha": "964a05d8", "userVisible": false}
{"batch": "batch-21", "file": "docs/plans/active/pane-stale-delete-collision.md", "fix": "Correct the path to Source/Core/include/Sync/TicketSyncService.h.", "line": 54, "pr": 2024, "problem": "'Files to modify' cites `Source/Core/include/TicketSyncService.h`, which does not exist — the header is `Source/Core/include/Sync/TicketSyncService.h` (correctly spelled at line 67 of the same table). Broken cross-ref in an active plan.", "severity": "LOW", "sha": "964a05d8", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Append ` Status: applied (2026-08-16 — both fixes landed in PR #2025: cpp-rules.md single-line rule + the `[dup] hint:` diagnostic)` and ` Last-reviewed: 2026-08-16` to the entry, matching the neighbouring entries' shape; or re-run `agents/scripts/core/archive-backlog-entry.sh` for future archivals instead of moving text by hand.", "line": 5496, "pr": 2025, "problem": "The entry moved into applied.md by this PR has no `Status:` and no `Last-reviewed:` line — it ends at the `Related:` paragraph (line 5535). Every other applied.md entry carries `Status: applied (...)` (337 occurrences) plus `Last-reviewed:`, which is what `archive-backlog-entry.sh` writes automatically (it rewrites `Status: open` -> `Status: applied (flipped at archival)`). This entry was hand-moved, bypassing that script, so an archived-and-fixed item is indistinguishable from an unresolved one and the review-rotation `Last-reviewed` bookkeeping has nothing to sort on.", "severity": "MEDIUM", "sha": "95e37591", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/applied.md", "fix": "Retarget the pin to `dup_audit.py:354-412` (covering `_has_dup_deviation` through `_suppressed`), or drop the line numbers and cite the function names only, since backlog pins are not mechanically checked.", "line": 5498, "pr": 2025, "problem": "Stale line-pin: the entry cites [`dup_audit.py:353-381`] as the source of the described mechanics, but the same commit inserted `INEFFECTIVE_DEVIATION_WINDOW` / `_ineffective_dup_deviation` at lines 361-384, pushing `_suppressed` (the function the paragraph actually explains) to line 386 — outside the cited range. The pin now points at the new diagnostic helper instead.", "severity": "LOW", "sha": "95e37591", "userVisible": false}
{"batch": "batch-21", "file": "docs/agent-rules/cpp-rules.md", "fix": "Label the block, e.g. end the sentence with a period and precede the fence with `Correct shape:` (and optionally add a second fence marked `Broken — does NOT suppress:` showing the wrapped marker).", "line": 86, "pr": 2025, "problem": "The paragraph ends \"...reports a bare `FAIL` that reads as if the *reason text* were wrong when only its *shape* is:\" — the trailing colon introduces the fenced block at lines 88-93 as the failure case, but that block actually shows the CORRECT shape (overflow prose on comment lines ABOVE, `SMATCHET_DEVIATION(...)` on one line directly above the symbol). A reader skimming for \"what not to do\" copies the good example as the bad one.", "severity": "LOW", "sha": "95e37591", "userVisible": false}
{"batch": "batch-21", "file": "scripts/dev/pre-ship.sh", "fix": "Make the concurrent path non-mutating or serialized: either compute the fingerprint over untracked files without touching the index (e.g. hash `git diff HEAD` plus explicit `git diff --no-index /dev/null <untracked>` output), or wrap the ita register/restore pair in a repo-scoped lock (`flock \"$repo_root/.git/preship.lock\"`) so only one pre-ship process holds the ita registration at a time. Also check the `git add --intent-to-add` exit status explicitly instead of letting `set -e` kill the run mid-gate.", "line": 300, "pr": 2027, "problem": "The protocol this PR introduces (ship-loops.md step 1b) mandates running `pre-ship.sh` and the reviewer's `pre-ship.sh --review-fingerprint` CONCURRENTLY, but both processes mutate the shared git index: `preship_ita_untracked` runs `git add --intent-to-add` (line 295) and installs an EXIT trap that runs `git restore --staged` (line 300). `ra_fingerprint branch` hashes `git diff HEAD`, which includes ita entries, so whichever process exits first un-registers the untracked files out from under the other. Result with any new untracked .cpp present: (a) the reviewer stamps a fingerprint that OMITS the brand-new file while step 1c `--ack-review` re-registers it and computes a different one -> permanent 'artifact is stale' wedge, or the reverse ordering -> an ack whose fingerprint covers a diff the review never saw (the exact hole the ita registration exists to close); (b) index.lock contention makes `git add` fail, and `set -euo pipefail` (line 44) aborts the whole gate. The comment at 305-307 asserts `--review-fingerprint` is 'read-only', which is false — it writes the index.", "severity": "MEDIUM", "sha": "7840e30e", "userVisible": false}
{"batch": "batch-21", "file": "agents/core/code-review.md", "fix": "Document the base-ref-carrying form in step 7, e.g. `FP=\"$(bash scripts/dev/pre-ship.sh --review-fingerprint \"$BASE\")\"`, and note that it must match the base ref the gate/ack are run with.", "line": 87, "pr": 2027, "problem": "Step 7 tells the reviewer to stamp with `FP=\"$(bash scripts/dev/pre-ship.sh --review-fingerprint)\"` — no base ref, so it always uses the default `origin/develop`. When the ship loop runs the gate against a non-default base (`pre-ship.sh <base-ref>` / `--ack-review <base-ref>`, both supported), the ack's fingerprint is computed over a different diff than the artifact's and `--ack-review` refuses with 'artifact is stale', which is unactionable from the message.", "severity": "LOW", "sha": "7840e30e", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/test-markdown-links.sh", "fix": "Extend the sibling-label alternation to the label set actually used in docs/self-improvement/categories/** (add Resolution|Recurrence|Impact|Verification|Cause|Owner|Follow-up|Evidence), or invert the rule: close on ANY `^\\s{0,3}[A-Z][A-Za-z-]+:` that is not itself a proposal opener.", "line": 419, "pr": 2028, "problem": "_PROPOSAL_CLOSE_RE only terminates a proposal block on Status|Last-reviewed|Cross-ref|Details|Related|Mechanics or a `- YYYY-MM-DD` entry header. The backlog also uses Resolution: (285 occurrences), Recurrence:, Impact:, Verification:, Cause:, Owner:, Follow-up:. A block opened by `Concrete next action:` therefore stays open across those labels, so every repo-path code span cited under them is silently exempted from the new check — a fail-open in exactly the label a stale citation is likely to sit in.", "severity": "MEDIUM", "sha": "ee2f9a44", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/test-markdown-links.sh", "fix": "Strip surrounding backticks/whitespace from cand before matching (cand = span_text.strip().strip('`').strip()), or recurse strip_code_spans over the captured content.", "line": 373, "pr": 2028, "problem": "_CODE_SPAN_RE captures group(2) as raw inner content, so a doubled-backtick span (`` `scripts/x.sh` ``) yields \"`scripts/x.sh`\" — with leading/trailing backticks — which the anchored _REPO_PATH_SPAN_RE can never match. The motivating example in applied.md:5542 is written in exactly that form, so the citation shape that prompted the rule is one it cannot see.", "severity": "LOW", "sha": "ee2f9a44", "userVisible": false}
{"batch": "batch-21", "file": "agents/scripts/core/test-markdown-links.sh", "fix": "Compute one hunk set in worktree coordinates — e.g. a single `git diff -U0 $(git merge-base origin/develop HEAD)` against the working tree — instead of unioning two diffs taken against different baselines.", "line": 455, "pr": 2028, "problem": "_added_lines unions hunk line numbers from `git diff -U0 origin/develop...HEAD` (coordinates in the HEAD blob) with `git diff -U0 HEAD` (coordinates in the worktree blob), then applies the union to the file as read from the worktree. With uncommitted edits that shift line counts, the develop...HEAD numbers land on the wrong worktree lines, so a newly-added code span can fall outside the scoped set and go unchecked in --diff mode.", "severity": "LOW", "sha": "ee2f9a44", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/process/2026-08-16-watcher-autoregister-bypasses-merge-consent.md", "fix": "Update the three pins to `docs/agent-rules/merge-gates.md:101`, or drop the numeric pin and cite the quoted clause text (which is stable) instead.", "line": 25, "pr": 2031, "problem": "Stale line-pin: the entry pins the offending consent clause to `docs/agent-rules/merge-gates.md:84` (also repeated at doc lines 92 and 115, where step 4 of \"Concrete next action\" instructs a future fixer to edit that exact line). At HEAD the quoted text \"…OR any PR registered with `smatchet-merge-watcher`\" lives at merge-gates.md:101; line 84 is an unrelated CodeRabbit rate-limit paragraph. A fixer following the instruction literally edits the wrong line.", "severity": "MEDIUM", "sha": "3dc6695f", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/process/2026-08-16-watcher-autoregister-bypasses-merge-consent.md", "fix": "Repin to `settings.json.tmpl:190`, `:206`, or cite the hook command string rather than line numbers.", "line": 18, "pr": 2031, "problem": "Stale line-pin: the hook is said to be wired at `docs/harness/claude-code/settings.json.tmpl:145` and `:161` (repeated at doc line 114). At HEAD the two `autoregister-pr.sh` hook entries are at tmpl lines 190 and 206; line 145 is a `pretool-edit-p4-lock-check.sh` timeout and 161 is the `\"PostToolUse\"` key.", "severity": "LOW", "sha": "3dc6695f", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/process/2026-08-16-watcher-autoregister-bypasses-merge-consent.md", "fix": "Change all three pins to `smatchet-merge-watcher.md:103`.", "line": 60, "pr": 2031, "problem": "Stale line-pin (off by one): `smatchet-merge-watcher.md:102` is cited three times (doc lines 60, 91, 117) as the \"Explicit owner transfer on register\" decision. At HEAD line 102 is decision 4 (\"Daemon foreground default + `--background` opt-in\"); the owner-transfer decision is line 103. Doc line 91 tells an implementer to reuse the ownership-transfer notice \"that `smatchet-merge-watcher.md:102` already specifies\".", "severity": "LOW", "sha": "3dc6695f", "userVisible": false}
{"batch": "batch-21", "file": "docs/self-improvement/categories/process/2026-08-16-watcher-autoregister-bypasses-merge-consent.md", "fix": "Quote the real entry title (or just cite `applied.md:665` / the date + PR numbers #1406/#1414/#1415).", "line": 71, "pr": 2031, "problem": "Cross-ref cites a quoted title that does not exist: the 2026-06-19 entry is quoted as \"Intent gate bypassed via non-poller merge\", but the actual entry in `categories/applied.md:665` reads \"block-allowlisted `Intent section` gate is bypassed by every non-poller merge path\". The quoted string is unfindable by grep — it appears nowhere in the repo except this line.", "severity": "LOW", "sha": "3dc6695f", "userVisible": false}
{"batch": "batch-21", "file": "docs/CONTEXT.md", "fix": "Mark the two unbuilt commands as planned in the CONTEXT.md entry (e.g. \"`debug.log_tail` and `debug.dump_self` (planned — see the linked plan)\"), or drop them from the surface list until slices 2/3 land.", "line": 122, "pr": 2032, "problem": "The canonical glossary defines \"Live-process evidence\" as evidence obtained \"over the command surface: log read-back (`debug.log_tail`), an on-demand self-minidump (`debug.dump_self`), `perf.snapshot`, `debug.lua_eval`\" — present tense, no planned/unimplemented marker. Neither `debug.log_tail` nor `debug.dump_self` exists: `grep -rn 'log_tail\\|dump_self' Source/` returns only unrelated QuickCreate config keys, and the registry only carries 11 `debug.*` commands (crash, dock.dump, dock.reset, grid.edit-burst, log, lua_eval, lua_log_test, mcp_status, thread_dump, window.resize, window.screenshot). An agent following CONTEXT.md will dispatch an unknown command. The owning plan is still `active`/unstarted.", "severity": "MEDIUM", "sha": "caba777f", "userVisible": false}
{"batch": "batch-21", "file": "docs/plans/active/autonomous-debug-live-evidence.md", "fix": "Move the mitigation to the dispatch entry point: switch `mutex_` to `std::timed_mutex` (or add a `try_lock`-based lookup fast path) in `CommandRegistry`, and add `Source/Core/include/Commands/CommandRegistry.h` + `CommandRegistry.cpp` to § Files to modify with the sizing that implies — or downgrade the bullet to an accepted residual risk.", "line": 104, "pr": 2032, "problem": "The § Risks mitigation for the registry-lock risk is not implementable as written. The bullet itself states the MCP worker \"blocks in `Contains()` at `CommandRegistry.cpp:54` before reaching any handler\", then prescribes \"a `try_lock`-with-timeout probe on the dump path\" — the handler never runs, so a probe inside it cannot break that block. Additionally `CommandRegistry::mutex_` is a plain `std::mutex` (`Source/Core/include/Commands/CommandRegistry.h:81`), which has no timed try_lock, and `CommandRegistry.{h,cpp}` is absent from § Files to modify, so the stated \"cheap; do it in slice 3\" scope is wrong.", "severity": "MEDIUM", "sha": "caba777f", "userVisible": false}
{"batch": "batch-21", "file": "docs/plans/active/autonomous-debug-live-evidence.md", "fix": "Require a visible cue on the palette entry (spinner/modal or a deferred worker-thread dump with progress) rather than an annotation, or state explicitly that the annotation exempts both call paths and record the accepted UI-freeze in the Pillar-2 exception list — do not describe a path-selective annotation the scanner cannot express.", "line": 86, "pr": 2032, "problem": "The Pillar-2 callout accepts a \"multi-hundred-millisecond stall\" on the ImGui thread (minidump write reachable from the Command Palette) with no visible cue, and pre-authorizes silencing the gate: \"If the static scanner flags it, the fix is a `PILLAR2_WORKER_ONLY` annotation on the *palette* path only.\" `scripts/dev/pillar2-scan.sh` matches a `/* PILLAR2_WORKER_ONLY */` + `// est-latency:` comment pair within 3 lines of the offending call site in the source — there is no palette-vs-MCP discrimination, so the only expressible annotation sits on the single handler call site and exempts the UI-reachable path outright. Combined with line 97 resolving the visible-cue bucket-E harness by \"diagnostic-only scope\", the plan directs a future implementer to ship a >100 ms unqueued UI freeze with the Pillar-2 gate suppressed.", "severity": "MEDIUM", "sha": "caba777f", "userVisible": false}
{"batch": "batch-21", "file": "docs/plans/active/autonomous-debug-live-evidence.md", "fix": "Change \"4 of 10\" to \"4 of 11\".", "line": 50, "pr": 2032, "problem": "Self-contradicting count: line 50 says `docs/guides/cli.md` \"documents 4 of 10 registered `debug.*` commands\" while line 34 in the same plan says \"5 of the 11 `debug.*` commands\". The registry has 11 (`debug.grid.edit-burst` lives in `BuiltinCommands_Perf.cpp` and is easy to miss when grepping only `BuiltinCommands_Debug.cpp`). The drift figure is the input to the cli-guide-registry-parity gate plan cited on the same line.", "severity": "LOW", "sha": "caba777f", "userVisible": false}
{"batch": "batch-22", "file": "agents/scripts/core/merge-gates.sh", "fix": "Distinguish the two branches in the message: when the refusal is the sentinel (no label event found for an active label), tell the operator to RE-APPLY the label and then re-run; keep the \"re-apply cannot help\" wording only for the genuine label-after-run stale case. Cheapest: have the filter emit the sentinel case as a distinct marker in field 33 so the poll loop can print the right advice.", "line": 754, "pr": 2033, "problem": "Stale-override WARN gives inverted remediation for the fail-closed truncated-timeline branch. 10-gate-filter.sh:59-64 sets $testsAt/$perfAt to the sentinel \"9999-12-31T23:59:59Z\" when the timeline is PRESENT but carries no LabeledEvent for an ACTIVE override label (the `last:100` window truncated it). No run can ever satisfy `(.completedAt // .startedAt) >= \"9999-...\"`, so the gate is permanently wedged — yet the WARN tells the operator to `gh run rerun` / push a commit and states \"re-applying the label only moves the label-time later and cannot help\". Re-applying is in fact the ONLY escape (it puts a LabeledEvent back inside the last:100 window), and 10-gate-filter.sh:56-58 says exactly that, so the two texts contradict each other.", "severity": "MEDIUM", "sha": "915ea692", "userVisible": false}
{"batch": "batch-22", "file": "docs/audits/AGENTIC_INFRA_AUDIT.md", "fix": "Update both links (lines 6 and 7) to `../plans/shipped/agentic-infra-audit-campaign-2026-06.md`.", "line": 7, "pr": 2033, "problem": "Broken cross-reference introduced by this PR's plan move. Lines 6 and 7 link to `../plans/agentic-infra-audit-campaign-2026-06.md`, but the same commit flipped that plan to `shipped` and moved it to `docs/plans/shipped/agentic-infra-audit-campaign-2026-06.md` (see docs/plans/INDEX.md:155 and the plan's own line 224). Both links now 404.", "severity": "MEDIUM", "sha": "915ea692", "userVisible": false}
{"batch": "batch-22", "file": "docs/audits/AGENTIC_INFRA_AUDIT.md", "fix": "Repoint mutation-testing-pilot.md to the `shipped/` path; replace the two vanished plan links with the tracker that actually exists (or de-link them to plain text).", "line": 173, "pr": 2033, "problem": "Additional dead plan links on surviving lines: `../plans/subagent-eval-agentic-coverage.md` (lines 162 and 172) and `../plans/testing-surface-roadmap.md` do not exist at any path, and `../plans/mutation-testing-pilot.md` now lives at `../plans/shipped/mutation-testing-pilot.md`.", "severity": "LOW", "sha": "915ea692", "userVisible": false}
{"batch": "batch-22", "file": "docs/plans/active/pane-add-dock-tab.md", "fix": "Set `> **Status**: shipped — archived <date>; see § Implementation log.`, add `## Implementation log`, git mv the file to docs/plans/shipped/, and run `bash agents/scripts/core/test-plan-index.sh --fix`.", "line": 3, "pr": 2034, "problem": "Plan shipped (PR merged, user-verified) but the doc is still in docs/plans/active/ with free-text status `**Status**: implemented, visually verified by the user (2026-08-16)`. process-rules.md § Archive on revision requires flipping to `> **Status**: shipped` and git mv to docs/plans/shipped/ + test-plan-index.sh --fix in the same PR. Because the literal is `implemented`, agents/scripts/core/plan-archival-owed.sh (which keys on the value immediately after `Status:`) never fires — the archival backstop fails open and the plan sits stale in active/, where lock/roadmap tooling reads it as in-flight. The doc also omits the mandatory `## Implementation log` section.", "severity": "MEDIUM", "sha": "", "userVisible": false}
{"batch": "batch-22", "file": "Source/Core/src/Ui/SmatchetActiveProjectGridUi.cpp", "fix": "Derive the fallback rect per pane (cascade-offset by pane index, or keep the pane's own position and only clamp size/on-screen) instead of one shared centered rect for all \"grid-pane\" windows.", "line": 213, "pr": 2034, "problem": "Every extra pane passes the same unregistered layout key \"grid-pane\" to repairTopLevelWindow. The unregistered key correctly skips the re-dock arm, but still reaches DefaultLayoutRectFor(\"grid-pane\", 420, 300), which falls through to the centered-default rect. With d.layoutForceDefaultsFrames > 0 (Reset Layout), or when several floating extra panes are off-screen/degenerate, every floating extra pane is snapped to the identical centered rect and they stack exactly on top of one another — the same one-key-for-N-windows class this PR fixed for docking, left in place for rect repair.", "severity": "LOW", "sha": "", "userVisible": true}
{"batch": "batch-22", "file": "Source/Core/src/Commands/Builtin/BuiltinCommands_Debug.cpp", "fix": "Keep the inline path only for non-UI sources: when ctx.Source is the palette/UI, run the write on a worker thread and return an async/pending result, or at minimum show a modal busy cue, so the render loop is never blocked by MiniDumpWriteDump.", "line": 280, "pr": 2035, "problem": "debug.dump_self runs MiniDumpWriteDump inline on whatever thread dispatched it. That is correct for CLI/MCP, but the command palette dispatches synchronously on the UI thread (CommandPaletteUi::drainPendingDispatch called from the palette draw at CommandPaletteUi.cpp:254), so a palette invocation suspends every thread and blocks the render loop for the whole dump write (hundreds of ms to seconds) with no progress cue. The plan itself notes the Pillar-2 scanner fires here.", "severity": "HIGH", "sha": "52640de2", "userVisible": true}
{"batch": "batch-22", "file": "Source/Core/src/Commands/Builtin/BuiltinCommands_Debug.cpp", "fix": "Prune agent-dumps/ to the N newest *.dmp after a successful write (mirroring CrashSink's rotation), or age-out files older than a fixed window.", "line": 263, "pr": 2035, "problem": "<userData>agent-dumps/ has no retention at all. Dumps were moved out of <userData>crashes/ precisely because CrashSink rotates that dir to the 5 newest *.dmp, but no replacement rotation was added — every debug.dump_self call writes a new ondemand-<epochMs>-<pid>.dmp that is never pruned, so an agent polling a wedged instance accumulates unbounded multi-MB minidumps in the user's data directory.", "severity": "MEDIUM", "sha": "52640de2", "userVisible": false}
{"batch": "batch-22", "file": "Source/Standalone/SmatchetCrashHandler.cpp", "fix": "Write fixed literal/snprintf-formatted messages into a preallocated buffer instead of allocating inside the noexcept provider.", "line": 240, "pr": 2035, "problem": "WriteSelfDumpWin32 is declared noexcept (and SelfDump.h states the provider must not throw), yet it builds errOut via std::string concatenation and std::to_string and constructs a std::unique_lock. A throwing allocation inside a noexcept function calls std::terminate — in a hang-diagnosis path that kills the process it was called to explain.", "severity": "LOW", "sha": "52640de2", "userVisible": false}
{"batch": "batch-22", "file": "tests/Commands/BuiltinCommandsDispatch.test.cpp", "fix": "Reword to 'writes into <userData>agent-dumps/ under a clock+pid-derived name with no caller-supplied path'.", "line": 353, "pr": 2035, "problem": "Comment claims dump_self 'only writes into the app's own crash dir from a fixed name', contradicting both shipped behaviour and the assertions in the test two cases above: the path is <userData>agent-dumps/ (explicitly not the crash dir) and the filename embeds epochMs+pid, so it is not fixed.", "severity": "LOW", "sha": "52640de2", "userVisible": false}
{"batch": "batch-22", "file": "docs/self-improvement/historical-review-findings.md", "fix": "File the 25 userVisible rows as GitHub Issues (`gh issue create` with bug + P0–P3 + area labels per ADR-0014), backfilling the Issue number into each jsonl row, or state explicitly in each batch header why the CRITICAL/data-loss rows are out of ADR-0014 scope.", "line": 24, "pr": 2039, "problem": "25 `userVisible` findings across the two new batches (line 24: \"12 userVisible findings are Issue candidates under ADR-0014 and are NOT yet filed\"; line 169: the same for 13 more) are recorded as prose only. ADR-0014 states \"A code-review finding about a real product bug is a GitHub Issue, not a backlog entry\", and the set includes a Pillar-1 UI freeze (#1903 TicketFieldEditor.cpp:1067, blocking HTTP POST on the render path) and a data-loss defect (#1928 LuaConsolePlugin.cpp:397, blank buffer truncating a real script on Save). The same ledger's own batch-19 row (jsonl pr 1878, line 378) already records this exact non-elevation as a defect, so this is a recurrence: the findings exist nowhere in the tracker and die with the ledger.", "severity": "MEDIUM", "sha": "4775648d", "userVisible": false}
{"batch": "batch-22", "file": "docs/self-improvement/historical-review-findings.md", "fix": "State the join key as `pr` + `line` (+ tier-less basename) in both index preambles, or extend the test-plan-ref-integrity.sh carve-out to the index blocks of historical-review-findings.md so the ledger can carry the same tier-ful path the jsonl records.", "line": 59, "pr": 2039, "problem": "The internal-debt index says it is \"keyed by `pr` + `file` + `line`\" against historical-review-findings-2026-08-16.jsonl (same claim at line 203), but 13 index rows were rewritten to the plan-ref gate's move-proof tier-less form while the jsonl kept the real tier-ful path — e.g. index line 72 `docs/plans/autonomous-debug-live-evidence.md` vs jsonl `docs/plans/active/autonomous-debug-live-evidence.md` (also lines 81, 134-137, 205, 208, 211, 234-236). A file-keyed lookup for those rows finds nothing. Verified: ledger and jsonl agree on 161/161 batch-21 and 79/79 batch-20-redo rows on every other field; only these 13 paths disagree. Same forced rewrite also means a finding whose defect IS a moved plan path cannot be stated verbatim in ledger prose — the reason the jsonl got its carve-out in test-plan-ref-integrity.sh:18-28.", "severity": "LOW", "sha": "4775648d", "userVisible": false}
{"batch": "batch-22", "file": "agents/scripts/core/merge-watcher.py", "fix": "Emit a distinct state (e.g. GATES_PASSED_UNAUTHORIZED) from the consent-gate return, add it to NOTIFY_STATES so the user gets exactly one \"PR #N is green and waiting on `merge-watch authorize N`\" toast, keep it out of AGENT_EVENT_END_STATES (or document that END consumers must branch on merge_action), and back off the poll interval once an entry is green+unauthorized so it stops burning gh API quota indefinitely.", "line": 3196, "pr": 2041, "problem": "An unauthorized entry whose gates go green has no notification path. GATES_PASSED is deliberately absent from NOTIFY_STATES (only merged PRs used to reach it), so handle_pass's new consent-gate early return sets merge_action=\"skipped: not authorized to merge\" and the entry silently re-polls merge-gates.sh (plus the lifecycle probe and _pr_is_draft) every cycle forever, never notifying and never terminating. DRAFT_UNAUTHORIZED got a one-shot toast (merge-watcher.py:1343); the far more common green-but-unauthorized park did not. Worse, GATES_PASSED is in AGENT_EVENT_END_STATES, so `merge-watch await --until=terminal` reports \"no further agent action\" for a PR that never merged — a consumer that honours the documented END-state contract without also reading merge_action is told the wrong thing.", "severity": "MEDIUM", "sha": "35260e38", "userVisible": false}
{"batch": "batch-22", "file": "docs/harness/claude-code/hooks/autoregister-pr.sh", "fix": "Capture the register exit code, and branch the systemMessage: registered watch-only on 0; on 1 re-read the real state (`merge-watch status <pr>` AUTH column) and report registered/authorized accordingly; on any other code report that registration FAILED with the captured message instead of claiming success.", "line": 57, "pr": 2041, "problem": "The systemMessage asserts \"auto-registered PR #N WATCH-ONLY (not authorized to merge)\" unconditionally. The register call on line 52 is wrapped in `|| true` and its exit code is never inspected, so a total failure (cwd not a git repo, python resolution succeeded but the CLI errored, registry lock contention) still reports the PR as registered — and if the entry already exists with authorized:true, cmd_register exits 1 and the message falsely claims the PR is NOT authorized to merge. ship-loops.md:199 (added in this same change) makes the registered/authorized state a mandatory, accurate line in the ship report, and this hook's output is the agent's primary source for it.", "severity": "MEDIUM", "sha": "35260e38", "userVisible": false}
{"batch": "batch-23", "file": "agents/scripts/core/unwatched-pr-nudge.sh", "fix": "Use the guarded expansion `${runner[@]+\"${runner[@]}\"}` (or an explicit if/else: `timeout \"$tmo\" gh ...` when timeout exists, plain `gh ...` otherwise) so the empty-array case is safe on bash 3.2.", "line": 82, "pr": 2067, "problem": "fetch_rows expands an empty array `\"${runner[@]}\"` under `set -u` when `timeout` is absent; on bash < 4.4 (stock macOS /bin/bash 3.2, a platform the script's own comment targets) this is a fatal `runner[@]: unbound variable` error that aborts the script with stderr noise and non-zero exit during the SessionStart hook \u2014 violating the header's own 'always silent exit 0 / never wedge SessionStart' contract. The `2>/dev/null` on the gh line covers only gh's stderr, not the shell's.", "severity": "MEDIUM", "sha": "24f2fdcf", "userVisible": false}
{"batch": "batch-23", "file": "agents/scripts/core/historical-review-worklist.sh", "fix": "Feed comm lexically-sorted input: build AUTH_FILE/SCRAPE_NUMS with plain `sort -u` (or `comm <(sort \"$AUTH_FILE\") <(sort \"$SCRAPE_NUMS\")`), keeping `sort -n` only for display; alternatively compute set differences in awk keyed on the number.", "line": 164, "pr": 2068, "problem": "MISSING/EXTRA are computed with `comm -23`/`comm -13` over files sorted with `sort -n -u`, but comm requires lexical order. On any range spanning a digit-width boundary (999/1000; later 9999/10000) with any auth/scrape difference, comm mis-merges: verified locally that AUTH={999,1000} vs SCRAPE={1000} yields MISSING={999,1000} \u2014 PR 1000 is spuriously MISSING, so the repair loop runs merge_commit_for on a squash PR, finds nothing, and the script exits 2 (false FAIL) on a healthy range; a resolvable spurious-missing PR instead gets duplicate work-list units. comm also emits 'not in sorted order' warnings into the coverage stderr and exits 1 (ignored inside $()).", "severity": "MEDIUM", "sha": "5dbb39ff", "userVisible": false}
{"batch": "batch-23", "file": "agents/scripts/core/historical-review-worklist.sh", "fix": "Paginate the authority (`gh api --paginate repos/{owner}/{repo}/pulls?state=closed&base=develop` filtered on non-null merged_at, or raise --limit above the merged-PR count with a headroom assert), and hard-fail (exit 2) when the smallest PR number returned by gh is > LO, i.e. the window demonstrably does not reach the requested range.", "line": 141, "pr": 2068, "problem": "The gh authority is `gh pr list --state merged --base develop --limit 900` (newest-created 900), but the repo already has ~1959 merged PRs (1900 squash subjects + 59 merge commits on develop). A range fully below the 900-PR window exits 2 (fail-closed), but a range straddling the cutoff proceeds with a silently truncated authority: squash PRs below the cutoff surface only as a 'stale authority' EXTRA WARN yet still emit units, while a merge-commit PR below the cutoff is in neither the truncated authority nor the scrape \u2014 invisible again with exit 0 and an authoritative-looking coverage triple. That re-opens the exact #1878-#1940 blind-spot class this gate was written to close, for any historical re-run (e.g. --range 1000 1200 today).", "severity": "MEDIUM", "sha": "5dbb39ff", "userVisible": false}
{"batch": "batch-23", "file": "agents/scripts/core/historical-review-worklist.sh", "fix": "Validate before shifting: for each value-taking flag check `[ $# -ge N ] || { echo usage >&2; exit 2; }` (or shift individually per consumed token: `shift; LO=\"${1:-}\"; shift; ...` with explicit empty-value checks), so a malformed invocation exits 2 per the header contract.", "line": 70, "pr": 2068, "problem": "A value-taking flag missing its value hangs the script instead of the documented exit-2 usage error: `--range 100` (or `--merged-list`/`--against` as the last token) makes `shift 3`/`shift 2` fail with count-out-of-range; under `set -uo pipefail` without `set -e` the positional params are left unchanged and the `while [ $# -gt 0 ]` loop re-parses the same flag forever \u2014 confirmed infinite busy-loop spamming shift errors to stderr. In the unattended sweep workflows this wedges a session until timeout rather than failing loudly.", "severity": "MEDIUM", "sha": "5dbb39ff", "userVisible": false}
{"batch": "batch-23", "file": "backlog/DEEP_REVIEW_2026-07-07.md", "fix": "Rewrite the paragraph to past tense (e.g. \"DR29 was the last item closed (2026-08-18, PR #2121); its scope had shrunk to ...\") or prefix it with a superseded marker pointing at the closing block, matching the doc's existing supersede convention.", "line": 35, "pr": 2069, "problem": "Surviving prose \"**DR29 is the only item not closed**\" directly contradicts the status-snapshot table three lines above it, which (updated by PR #2121) records \"13 \u2705 (DR29 closed 2026-08-18 by PR #2121)\". PR #2121 closed DR29 and updated the table + added a closing block, but left this paragraph reading as if DR29 were still open.", "severity": "LOW", "sha": "1eecef2d", "userVisible": false}
{"batch": "batch-23", "file": "backlog/DEEP_REVIEW_2026-07-07.md", "fix": "Update the section to state 33/33 shipped and mark the DR29 paragraph as executed-by-PR-#2121 (or fold it into the provenance <details> block), so the ledger's \"current\" sequencing shows zero open items.", "line": 421, "pr": 2069, "problem": "The \"Sequencing (current \u2014 2026-08-16)\" section still says \"The single remaining item is **DR29**\" and prescribes the hoist-helpers change as open work, but that exact change shipped in PR #2121 (2026-08-18) per the closing block earlier in the same file \u2014 the section's \"current\" framing is stale.", "severity": "LOW", "sha": "1eecef2d", "userVisible": false}
{"batch": "batch-23", "file": "docs/plans/shipped/branch-protection-config-completeness.md", "fix": "Change the link to `coverage-sanitizer-required-contexts.md` (same-directory relative path).", "line": 102, "pr": 2070, "problem": "Broken relative link: `../coverage-sanitizer-required-contexts.md` resolves to docs/plans/coverage-sanitizer-required-contexts.md, which does not exist \u2014 the target lives at docs/plans/shipped/coverage-sanitizer-required-contexts.md (both plans are now in shipped/).", "severity": "LOW", "sha": "30d09b2d", "userVisible": false}
{"batch": "batch-23", "file": "agents/scripts/core/setup-branch-protection.sh", "fix": "Update the comment path to docs/plans/shipped/branch-protection-config-completeness.md.", "line": 32, "pr": 2070, "problem": "Header comment cites `docs/plans/branch-protection-config-completeness.md`, but the plan was archived to `docs/plans/shipped/branch-protection-config-completeness.md` in this same PR \u2014 the cited path does not exist at HEAD.", "severity": "LOW", "sha": "30d09b2d", "userVisible": false}
{"batch": "batch-23", "file": "tests/bats/setup_branch_protection.bats", "fix": "Update the comment path to docs/plans/shipped/branch-protection-config-completeness.md.", "line": 11, "pr": 2070, "problem": "Same stale citation as the script header: the bats WHY-comment points at `docs/plans/branch-protection-config-completeness.md`, which moved to `docs/plans/shipped/` and no longer exists at the cited path.", "severity": "LOW", "sha": "30d09b2d", "userVisible": false}
{"batch": "batch-23", "file": "agents/scripts/core/test-workflow-yaml.sh", "fix": "Move the SHA-pin block above the PYYAML_OK/ACTIONLINT_OK soft-pass exit (or change that branch to skip only the YAML-parse loop instead of exit 0), so the grep-only pin assertion always runs.", "line": 67, "pr": 2080, "problem": "The SHA-pin assertion (lines 67-107) is pure grep and needs neither PyYAML nor actionlint, but it sits after the pre-existing soft-pass early-exit (no validator installed -> 'cannot validate' -> exit 0), so on a dev machine without PyYAML/actionlint the pin check is silently skipped too. CI runners ship PyYAML so the merge-gate enforcement still fires; the gap is local-only.", "severity": "LOW", "sha": "93b065d6", "userVisible": false}
{"batch": "batch-23", "file": "Source/Core/src/Ui/SmatchetAiAssistantUi.cpp", "fix": "In the FirstUseEver branch, derive assistantSideFallback from where the panel is ACTUALLY docked (e.g. after Begin, compare the live window's DockNode id against kSecondarySideBar) instead of from targetDockId; or only write the fallback when the dock write can actually apply (window not yet created / SetWindowDockAllowFlags still allows FirstUseEver), leaving the previous fallback in place otherwise.", "line": 1520, "pr": 2084, "problem": "In ApplyAssistantDocking's steady-state branch, d.assistantSideFallback is recomputed as if SetNextWindowDockID(targetDockId, ImGuiCond_FirstUseEver) landed, but that write is a no-op for a window restored from imgui.ini (ImGui strips FirstUseEver from SetWindowDockAllowFlags for ini-created windows, and after the first applied dock write). When the preferred side's dock node becomes live while the panel sits ini-docked on the other side (the exact #2048 recovery path: pref=secondary, prior-session fallback saved the panel to primary, then a layout reset / newer default ini re-cuts the secondary bar), the fallback resets to -1 while the panel never moves \u2014 AiAssistantEffectiveOnSecondary then reports the wrong side, the swap button label/tooltip describe where the panel is not, and the first swap click is a dead no-op (it re-docks the panel to where it already is). This violates the survivor comment's own contract that the override is recorded 'only on a frame that really issues a dock write'.", "severity": "MEDIUM", "sha": "b703b68e", "userVisible": true}
{"batch": "batch-23", "file": "Source/Core/src/TicketFieldEditor.cpp", "fix": "Track outstanding submits in a container (e.g. std::set<std::string> of in-flight issue ids) instead of one string; post-back erases its own id. Seeding and the HandleWorklogSave gate query the set.", "line": 1136, "pr": 2085, "problem": "Single-slot in-flight latch: `s_WorklogSubmitInFlightIssueId = issueId;` unconditionally overwrites an outstanding different-ticket latch. Save on A -> Cancel -> open B -> Save (latch := B) -> re-open A: WorklogSubmitOutstandingFor(B, A) is false, SubmitInFlight seeds false, Save re-enables while A's POST is still running -> a second worklog for one intent, the exact duplicate class this PR fixed, reopened via a two-ticket interleaving.", "severity": "MEDIUM", "sha": "1b7c9255", "userVisible": true}
{"batch": "batch-23", "file": "Source/Core/src/TicketFieldEditor.cpp", "fix": "In the stale branch, when s_ActiveWorklogState.Initialized && s_ActiveWorklogState.IssueId == issueId, also set s_ActiveWorklogState.SubmitInFlight = false before returning (the POST the seed represented has completed).", "line": 1157, "pr": 2085, "problem": "Re-open the SAME ticket mid-POST: open seeds SubmitInFlight=true (line 1048) and burns a fresh Gen (line 1054), so when the post-back lands, the latch is cleared (1148) but CallbackIsStale fires on the Gen mismatch and returns after the toast \u2014 the SubmitInFlight=false release at line 1170 never runs for the re-opened instance. Save stays disabled with a perpetual 'Saving worklog...' cue until the dialog is closed and re-opened; after a failed POST this blocks the user's retry.", "severity": "MEDIUM", "sha": "1b7c9255", "userVisible": true}
{"batch": "batch-23", "file": "Source/Core/src/TicketFieldEditor.cpp", "fix": "Add the three key rows with French translations to the worklog block in Source/Core/src/SmatchetLocalization.cpp.", "line": 1161, "pr": 2085, "problem": "Localization keys worklog.toast.saved_title, worklog.toast.failed_title (lines 1161/1165) and worklog.cancel_inflight_tooltip (line 1369) are used via SmatchetLocalization::T with English fallbacks but were never added to the SmatchetLocalization.cpp table (only worklog.saving was) \u2014 these strings always render in English in the French locale, unlike every sibling worklog.* string.", "severity": "LOW", "sha": "1b7c9255", "userVisible": true}
{"batch": "batch-23", "file": "Source/Core/src/Tracker/TrackerGridFieldDisplayPure.cpp", "fix": "Localize at the render sink instead of the model: in RenderWorklogField, for the no-work case emit the literal as a translation source (e.g. BeginTooltip + ImGui::TextUnformatted(\"No work logged yet. Click to log work.\") like RenderTimeSpentButton), or add a flag to WorklogCellModel so the renderer picks the localized literal; append the 'Click to log work / edit estimates.' hint through a translating sink as well.", "line": 416, "pr": 2088, "problem": "Worklog-cell tooltips are baked as English literals in the pure model (lines 416/423 and the composed '\\n\\nClick to log work / edit estimates.' at 431) and rendered via SmatchetLocalizedImGui::SetTooltip(\"%s\", model.tooltip) \u2014 the wrapper translates only the format string (\"%s\" is a lookup miss), so fr-FR users see English tooltips on the worklog cell while the adjacent timespent cell shows the same text in French (it passes the literals as translation sources via TextUnformatted/Text, hitting the existing worklog.none / worklog.total table entries). Contradicts the PR's own 'two entry points cannot drift apart' intent; the French rows are unreachable from the worklog cell.", "severity": "LOW", "sha": "c12f395c", "userVisible": true}
{"batch": "batch-23", "file": "agents/scripts/project/lint-rules.d/00-common.sh", "fix": "Add a case arm '[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]*) return 0 ;;' (8 leading digits + anything) in revisit_datelike_but_invalid() so ISO-basic-with-trailing-junk classifies as a calendar attempt and fails closed, matching the Python prefix semantics; add a fixture line (revisit=20270811typo) to deviation-revisit-malformed.cpp and bump the bats grep -c count 7 -> 8; adjust the 'bare YYYYMMDD' wording in the comment and cpp-rules.md to cover the junk-suffixed form.", "line": 125, "pr": 2090, "problem": "revisit_datelike_but_invalid()'s ISO-basic arm is an exact-8-digit glob ([0-9]x8, whole-string case match), so a value like revisit=20270811typo (8 digits + trailing junk) misses every attempt arm and falls to the slug branch \u2014 a permanent, never-expiring exemption (fail-open). This is internally inconsistent (dashed-date-with-junk 2027-08-11typo and quarter-with-junk 2026-Q1typo both fail closed in the same function) and diverges from the reference CALENDARISH_RE in test-plan-claim-anchors.sh:523, whose \\d{8} via .match is a prefix match that fails closed on the identical value \u2014 while surviving comments in BOTH files (00-common.sh ~:113-116, test-plan-claim-anchors.sh ~:519-522, which explicitly lists '20270811typo' as failing closed) claim the two enforcers classify identically.", "severity": "MEDIUM", "sha": "5020f235", "userVisible": false}
{"batch": "batch-23", "file": "docs/self-improvement/categories/tooling/2026-08-16-cr-findings-pending-statuscontext-wedges-merge-gates.md", "fix": "Refresh the four line pins to the current 10-gate-filter.sh locations, or replace them with the stable anchor names ($blocking definition, $downgraded definition, failing-count vs pending-count emit lines) so the entry survives future line drift.", "line": 5, "pr": 2094, "problem": "The entry's line pins into agents/scripts/core/merge-gates.d/10-gate-filter.sh have rotted after PRs #2107/#2120 reshuffled the file: $blocking pinned at :93-97 is now ~:138-142, $downgraded pinned at :137-141 is now ~:186, and the failing/pending counts pinned at :279/:280-282 are now :324/:326-328 \u2014 the 'Concrete next action (1)' instruction to apply the subtraction 'at :280' now points into the CR rate-limit comment block. The described defect itself is still live (verified: :324 subtracts $downgraded from failures, :326-328 pending count does not), so Status: open is accurate.", "severity": "LOW", "sha": "e9890e0d", "userVisible": false}
{"batch": "batch-23", "file": "tests/bats/bucket_lane_launch_smoke.bats", "fix": "Replace with an explicitly checked form, e.g. `run grep -q \"$(printf '\\tfail\\t')\" \"$SMATCHET_GOLDEN_REPORT_FILE\"; [ \"$status\" -ne 0 ]` (or bats >=1.5 `run ! grep ...` + status check); do the same at line 346 so it doesn't depend on last-command position.", "line": 321, "pr": 2096, "problem": "Mid-test negated assertion `! grep -q \"$(printf '\\tfail\\t')\" \"$SMATCHET_GOLDEN_REPORT_FILE\"` is inert: bash `set -e` (which bats runs tests under) ignores the status of a `!`-negated pipeline, and later commands follow it, so if the driver regressed to emit a generic `fail` row alongside `fail-dimension` the test still passes. The sibling negation at line 346 only works because it happens to be the last command of its test function.", "severity": "MEDIUM", "sha": "5d2502ed", "userVisible": false}
{"batch": "batch-23", "file": "Source/Standalone/StandaloneBoot_detail.cpp", "fix": "On the rc==0 path, still invoke the contract (which reports 'no capture was recorded' via the actual<=0 branch) or at minimum disarm expectedCaptureWidth/Height before returning, so the write failure fails the armed contract instead of misattributing it to a later capture.", "line": 152, "pr": 2096, "problem": "WriteCapturePng returns early on stbi_write_png failure before ReportCaptureSizeContract runs, so an armed scenario capture-size contract is neither failed (no capture actually landed) nor disarmed; it leaks onto the next unrelated capture (e.g. a bug-report shot), which then logs a spurious contract-violation ERROR naming the scenario's sizes.", "severity": "LOW", "sha": "5d2502ed", "userVisible": false}
{"batch": "batch-23", "file": "Source/Core/include/Ui/FieldPreviewLinePure.h", "fix": "Test blankness without materializing the line (`value.find_first_not_of(\" \\t\", start) < end`, npos-safe since end==size on the last line) and build the result once via `out.Text.assign(value, start, end - start);` \u2014 one allocation, matching the old erase-based code.", "line": 48, "pr": 2097, "problem": "FirstVisibleLine copies the chosen line twice on the per-cell render path: `const std::string line = value.substr(start, end - start)` allocates, then `out.Text = line;` copy-assigns again (the const local also blocks a move \u2014 house 'move on last use' rule). Runs once per drawn one-line cell per frame; the plan's Pillar-1 callout claims 'no allocation beyond the one substr the old code already did', which the surviving code exceeds by one alloc+copy for any non-SSO preview line.", "severity": "LOW", "sha": "688f4a4f", "userVisible": false}
{"batch": "batch-23", "file": "scripts/dev/check-golden-provenance.sh", "fix": "Stamp only goldens the bootstrap run actually wrote: have the driver emit a captured-basenames list that --emit consumes, or make --emit fail on a PNG whose basename has no matching scenario; alternatively add an orphan-golden sweep to the bootstrap job before the manifest step.", "line": 145, "pr": 2100, "problem": "--emit stamps EVERY *.png in the golden dir, but test-screenshot-diff.sh --bootstrap only overwrites goldens for scenarios in its SCENARIOS list. An orphaned golden (scenario retired/renamed) keeps its old developer-GPU bytes yet gains a manifest row, becoming falsely CI-native \u2014 the exact laundering the guard exists to prevent. The workflow comment (build-and-test.yml 1373-1378) claims set -euo pipefail closes this hole, but it only closes it for scenarios the driver actually runs.", "severity": "MEDIUM", "sha": "b5d08950", "userVisible": false}
{"batch": "batch-23", "file": "scripts/dev/check-golden-provenance.sh", "fix": "In --strict mode treat total==0 as a failure (exit 1) or as misconfiguration (exit 2), mirroring the missing-dir case; add a bats case pinning it.", "line": 189, "pr": 2100, "problem": "--strict exits 0 when the golden dir exists but contains zero PNGs (total=0, missing empty \u2014 vacuous pass). Once the CI call site graduates to --strict, a misconfigured SMATCHET_GOLDEN_DIR or a wiped golden dir passes the gate: a dormant fail-open in a gate script.", "severity": "LOW", "sha": "b5d08950", "userVisible": false}
{"batch": "batch-23", "file": ".github/workflows/build-and-test.yml", "fix": "Have install-mesa-gl expose its MESA_VERSION as an action output and set GOLDEN_MESA from that output, removing the second (and third) literal.", "line": 1383, "pr": 2100, "problem": "GOLDEN_MESA: \"24.2.5\" hardcodes the Mesa version, duplicating MESA_VERSION pinned in .github/actions/install-mesa-gl/action.yml (and the literal in docs/agent-rules/golden-image-approval.md line 35). A Mesa bump in the action silently makes every future PROVENANCE.tsv row record the wrong renderer version \u2014 the manifest column whose whole point is telling the reviewer what produced the bytes.", "severity": "LOW", "sha": "b5d08950", "userVisible": false}
{"batch": "batch-23", "file": "tests/bats/golden_provenance.bats", "fix": "Either make the test read the actual flag from the CI call site (grep build-and-test.yml for --strict and, when present, assert --list-missing is empty) or reword the comment to what the test really checks (a smoke run against the committed repo state).", "line": 128, "pr": 2100, "problem": "The comment (lines 123-124) claims this test \"fails loudly the day someone flips the CI call to --strict before the goldens are actually CI-native\", but the test invokes the guard WITHOUT --strict and asserts only exit 0 plus a substring that always prints \u2014 both hold regardless of CI-native count, so the test cannot fail in that scenario (it only fails if the golden dir vanishes).", "severity": "LOW", "sha": "b5d08950", "userVisible": false}
{"batch": "batch-23", "file": "docs/plans/shipped/git-ref-plan-locks.md", "fix": "Update row #2's Status cell to DONE 2026-08-18 (rotation verified via green workflow run), keeping the 90-day trigger column so the next expiry (~2026-11-16) is still tracked.", "line": 13, "pr": 2102, "problem": "Ops-table row #2 still says 'FIRED 2026-08-16 \u2014 ACTION REQUIRED' for the LOCK_RENDER_PAT rotation, but the rotation already happened: a later commit's comment in .github/workflows/locks-render.yml (\"first green run after the 2026-08-18 rotation\") confirms the new PAT is live. The operator status table advertises a resolved incident as still open, inviting a needless re-rotation or misreading of on-call state.", "severity": "MEDIUM", "sha": "d996d837", "userVisible": false}
{"batch": "batch-23", "file": "scripts/dev/check-golden-provenance.sh", "fix": "On IHDR-read failure fall back to the literal 'unknown' instead of $EMIT_GEOM (and/or try python3 before python: \"${PYTHON:-$(command -v python3 || echo python)}\"); drop the GOLDEN_GEOMETRY fallback plumbing entirely since per-file reading made it obsolete.", "line": 131, "pr": 2105, "problem": "geometry_of()'s failure fallback stamps $EMIT_GEOM (workflow-pinned GOLDEN_GEOMETRY=1920x1009) when the python IHDR read fails (no `python` on PATH, truncated PNG). For the two 480x1009 narrow user-info goldens this records a factually wrong geometry \u2014 the exact 'a record that states the wrong number is worse than one that states none' failure the script's own header comment (lines 48-50) says the column must never exhibit. Dormant today: the Windows CI bootstrap runner has python, and manifest_has verifies only sha+basename so the column is informational.", "severity": "LOW", "sha": "5439459c", "userVisible": false}
{"batch": "batch-23", "file": "agents/scripts/core/cr-oob-review-backfill.sh", "fix": "Flatten the body inside jq before emitting, e.g. `\"\\(.created_at)\\t\\(.body|gsub(\"\\n\";\" \"))\"`, or select the first matching comment in jq (`[.[]|select(...)]|first`) instead of relying on `head -1` over raw multi-line output.", "line": 316, "pr": 2119, "problem": "Cooldown extraction is silently inert for multi-line CR replies: the jq emits created_at + the raw body (newlines printed literally), so the record spans multiple lines; `head -1` keeps only the body's FIRST line, and the python `re.search(\"available in (\\\\d+) minutes\")` misses the phrase unless CR happens to put it on line 1 of the reply (the doc's own quoted reply shows it later). No cooldown row is ever emitted, so the CR-named-cooldown guard never fires \u2014 same 'data-layer guard goes inert silently' class as the 2026-08-18 incident, and unreachable by --selftest/bats (both inject past the data layer). Consequence when live: a request fired inside a CR-named cooldown is dropped and the slot burned (local window arithmetic still throttles, so bounded).", "severity": "MEDIUM", "sha": "eec3e1f0", "userVisible": false}
{"batch": "batch-23", "file": "agents/scripts/core/cr-oob-review-backfill.sh", "fix": "Capture the gh exit status separately and HOLD (return non-zero / distinct verdict) on a fetch error instead of conflating it with a genuine zero review count.", "line": 338, "pr": 2119, "problem": "owes_review treats a failed `gh api` fetch as 'owes a review': errors are suppressed (2>/dev/null), empty output makes `grep -c .` = 0, and `[ \"${n:-0}\" -eq 0 ]` returns success, so an unknown state is read as permission to fire \u2014 contrary to the script's own scanfail principle (a failed lookup must HOLD). Blast radius bounded: the marker re-check in post_request prevents duplicates, so the cost is one wasted one-per-45-min quota slot on a PR that may already carry a review.", "severity": "LOW", "sha": "eec3e1f0", "userVisible": false}
{"batch": "batch-23", "file": "docs/self-improvement/cr-oob-review-backfill.md", "fix": "Qualify the line, e.g. 'runs the poller on a */15-equivalent schedule (currently disabled pending the dry_run re-arm \u2014 see \u00a7 The 2026-08-18 duplicate-comment incident)'.", "line": 36, "pr": 2119, "problem": "Doc-vs-code drift: the surviving line states the workflow 'runs the poller on a `*/15`-equivalent schedule', but at origin/develop the cron in cr-oob-review-backfill.yml is commented out (disabled after the 2026-08-18 duplicate-comment incident, as the same doc's later section records) \u2014 the doc's opening contradicts its own incident section until the schedule is re-armed.", "severity": "LOW", "sha": "eec3e1f0", "userVisible": false}
{"batch": "batch-23", "file": "docs/agent-rules/merge-gates.md", "fix": "Correct the pin to the actual counts ('plus four for the WARN') or, more durably, drop the numeric counts and just name the pinned behaviours (cross-suite fires / same-suite silent / advisory silent / non-required non-advisory fires) so the pin can't rot on the next test addition.", "line": 263, "pr": 2120, "problem": "Stale test-count pin: the line claims the section is 'Pinned by tests/bats/merge_gates.bats (six dedup cases plus two for the WARN)', but this same commit shipped FOUR WARN tests (its own commit message says '217/217 merge_gates cases (4 new)'), and HEAD carries seven dedup-pinning non-WARN cases (lines 1168-1335 of merge_gates.bats). The count was wrong at merge time and remains wrong.", "severity": "LOW", "sha": "d63a7009", "userVisible": false}
{"batch": "batch-23", "file": "Source/Core/include/Lua/LuaHookGuard.h", "fix": "Update the path to docs/plans/shipped/lua-recorded-cmd-list.md.", "line": 15, "pr": 2121, "problem": "Comment cites docs/plans/active/applied/lua-recorded-cmd-list.md, but docs/plans/active/applied/ no longer exists \u2014 the plan moved to docs/plans/shipped/lua-recorded-cmd-list.md (broken cross-ref for anyone chasing the 100000-instruction budget rationale).", "severity": "LOW", "sha": "adddaa3e", "userVisible": false}
{"batch": "batch-23", "file": "tests/Lua/LuaTimeout.test.cpp", "fix": "Point line 11 at docs/plans/shipped/lua-recorded-cmd-list.md and drop or refresh the CMakeLists line-pin on line 15 (cite the block by name, e.g. 'CMakeLists.txt Lua-as-C++ / LUAI_THROW = throw block', not by line number).", "line": 11, "pr": 2121, "problem": "Same header comment block carries two stale pins: line 11 cites the dead docs/plans/active/applied/lua-recorded-cmd-list.md path (plan now at docs/plans/shipped/), and line 15 pins the Lua-as-C++ LUAI_THROW block at 'CMakeLists.txt:336-343' \u2014 that block now sits at ~CMakeLists.txt:920-970 (lines 336-343 are the PCH option today).", "severity": "LOW", "sha": "adddaa3e", "userVisible": false}
{"batch": "batch-23", "file": "tests/support/LuaHostFixture.h", "fix": "Keep the preceding hook_.reset() (old hook must tear down before the new one installs), then assign hook_ = std::make_unique<smatchet::lua::LuaHookGuard>(state_, count); instead of reset(new ...).", "line": 90, "pr": 2121, "problem": "hook_.reset(new smatchet::lua::LuaHookGuard(state_, count)) uses raw new, violating the project RAII rule (unique_ptr + make_unique); it escapes the no-raw-new gate only because the gate's sweep roots are Source/* and exclude tests/.", "severity": "LOW", "sha": "adddaa3e", "userVisible": false}
{"batch": "batch-23", "file": "Source/Core/src/FileIo.cpp", "fix": "Add a LOG_WARN before the early return (e.g. \"FileIo: Utf8ToWide failed for lock '%s' \u2014 proceeding without exclusive access\") so the branch matches the documented best-effort contract and the other failure paths.", "line": 70, "pr": 2122, "problem": "Win32 ScopedFileLock::Acquire() silently proceeds unlocked when Utf8ToWide(lockPath_) returns empty (invalid-UTF-8 path): `if (wLock.empty()) return;` emits no LOG_WARN, contradicting the FileIo.h contract that \"every acquisition failure ... is logged\" \u2014 the one degrade branch with zero observability.", "severity": "LOW", "sha": "32feeaff", "userVisible": false}
{"batch": "batch-23", "file": "Source/Core/src/Ui/SmatchetActiveProjectGridCells.cpp", "fix": "Track a bool found in the pane loop and return without calling UpdateCachedCommentsFromThread when the captured pane id is no longer present in dPtr->gridPanes.", "line": 145, "pr": 2123, "problem": "KickCommentsTooltipFetch post-back: the gridPanes loop only performs the backendKey mid-flight-swap check when the captured pane id is found; if the pane was removed mid-flight while focusedPaneId is stale-equal to the captured id, the loop falls through and UpdateCachedCommentsFromThread still writes \u2014 skipping the live-backendKey re-check the contract comment (lines 108-111) promises, so a same-keyed row in a different backend's snapshot could be cross-contaminated.", "severity": "LOW", "sha": "f7829c65", "userVisible": false}
{"batch": "batch-23", "file": "agents/scripts/core/cr-oob-review-backfill.sh", "fix": "Capture gh api's exit status separately from the grep count; on lookup failure return a distinct 'unknown' outcome (or return 1 / emit scanfail) so the caller HOLDs instead of concluding the PR owes a review \u2014 mirroring marker_scan's scanfail semantics.", "line": 336, "pr": 2126, "problem": "owes_review fails OPEN: when the paginated reviews lookup errors (gh api fails, stderr discarded), grep -c yields 0 so n=0 and the function returns 'owes a review' \u2014 a failed read is treated as 'no reviews exist', the exact failed-lookup-as-absence class this PR was written to stop. The marker-based already_requested guard does not cover a backlog PR that CR already reviewed but never carried a marker, so a transient API failure can post a spurious review request to an already-reviewed PR (bounded to one, since the marker then lands).", "severity": "MEDIUM", "sha": "84081db7", "userVisible": false}
{"batch": "batch-23", "file": "Source/Core/src/Sync/TicketSyncService.cpp", "fix": "Reword the comment to 'any fetch that kept at least one ticket resets the streak; an empty partial fetch leaves it untouched' (matching NoteFullSyncEmptiness's actual reset condition).", "line": 497, "pr": 2147, "problem": "Comment says 'a partial fetch leaves it untouched', but NoteFullSyncEmptiness resets the empty streak on ANY non-empty result (if (!keptNothing)) regardless of fullSyncCompleted \u2014 a non-empty partial fetch does reset the streak. Code behavior is the safe/conservative choice (non-empty evidence contradicts emptiness); the comment is what's wrong.", "severity": "LOW", "sha": "fb0cdefa", "userVisible": false}
{"batch": "batch-23", "file": "Source/Core/src/Tracker/JiraUserAndMeta.cpp", "fix": "Advance startAt by the number of values actually returned per page (accumulate a per-chunk fetched count from FetchJiraUserArray \u2014 e.g. return the page's array size via an out-param, or read the page bean's maxResults) instead of page * kMaxIdsPerCall; tighten the multi-page test so the second scripted page only matches the startAt a correctly-advancing client would send (the clamped-page-size value, e.g. 1 in the existing fixture).", "line": 534, "pr": 2149, "problem": "FetchUsersByAccountIds pagination advances startAt by the requested page size (page * kMaxIdsPerCall = 128) instead of the count of results the server actually returned. The comment (lines 517-519) claims a server that clamps the page size below maxResults still yields every user, but if Jira clamps user/bulk to e.g. 50, page 0 returns users 0-49 with isLast:false, the next request jumps to startAt=128, and users 50-127 are silently never fetched \u2014 their account ids stay unnamed hashes in the readable echo. The multi-page test (tests/Core/JiraUserAndMetaHttp.test.cpp:350) masks this: its handler returns the second page for any non-zero startAt, never asserting the follow-up startAt is correct.", "severity": "MEDIUM", "sha": "aab05760", "userVisible": true}
{"batch": "batch-23", "file": "agents/scripts/core/test-agent-contract.sh", "fix": "Frontmatter-scope the top-level extraction (e.g. an awk that only scans between the opening and closing `---`, mirroring has_claude_code_block), and add a selftest fixture with a body `model:` line at column 0 that must stay a reported finding, not a silent skip.", "line": 106, "pr": 2150, "problem": "model_verdict extracts the top-level model via a whole-file `grep -m1 -E \"^model:\"` while the PR frontmatter-scoped every other extractor for exactly this leak class: a column-0 `model: ...` line in an agent file's prose/example body (outside the frontmatter) is taken as the top-level key, so a no-block agent with no real model key satisfies `[[ -n \"$top\" ]]` at line 111 and is silently skipped \u2014 the same invented-value fail-open shape fixtures (g)/(h) pin for the hint side, and a false-drift report when a real frontmatter model disagrees with a body line matched first.", "severity": "MEDIUM", "sha": "0bf557c0", "userVisible": false}
{"batch": "batch-23", "file": "Source/Core/src/Ui/SmatchetGridHeaderUi.cpp", "fix": "Either skip the pane's current backend in the picker loop (the caret tooltip already says \"different tracker backend\"), or honor targetViewId in the same-backend apply branch (set dup.viewId = ResolveNewPaneView(...) and skip the snapshot inherit when targetViewId differs from src->viewId).", "line": 224, "pr": 2154, "problem": "The \"##PaneNewOnBackend\" popup lists every credentialed backend including the pane's OWN backend, and its view submenu arms paneAddRequest with targetBackendKey == src->backendKey plus a specific targetViewId. ApplyPaneAddAndCloseRequestsCore (SmatchetGridPaneWindows_detail.cpp, crossBackend = !targetBackendKey.empty() && targetBackendKey != src->backendKey) only honors targetViewId on the crossBackend branch; the same-backend branch copies src->viewId and inherits the snapshot, so picking a specific saved view of the current backend silently opens a duplicate of the CURRENT view \u2014 the user's view selection is dropped.", "severity": "MEDIUM", "sha": "10b0711c", "userVisible": true}
