# QA Mini — Plan

A fabricated mini-item for `scripts/dev/test_work_item_lint.py`. Never a real work item.

## QA

### Boot

Setup: fresh launch.

- Launch the app => main menu appears (durable)
- Resize the window => layout reflows (durable)
- Toggle the debug overlay => overlay hides, no crash
