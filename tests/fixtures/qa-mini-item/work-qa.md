# QA — standing manual smoke checklist

Fixture copy of a standing checklist for `scripts/dev/test_work_item_lint.py`. The two durable Boot
steps below are the drained cores from the mini-item's sheet — **step core only**, no citation,
matching the standing-doc convention (git blame holds provenance).

## Boot
- Launch the app => main menu appears
- Resize the window => layout reflows
