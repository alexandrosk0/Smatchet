{
  "schemaVersion": 1,
  "caseId": "cr-false-positive-trap",
  "agent": "code-review",
  "description": "Seed calibration case: a diff that LOOKS risky (a raw pointer, a manual loop, a narrowing cast) but is correct in context. Verifies code-review does NOT over-report — the genuine finding count is zero. Guards against a prompt edit that trades recall for a flood of false positives.",
  "repoRef": "fdccba25",
  "baseBranch": "develop",
  "files": [
    "Source/Core/src/Commands/FuzzyMatch.cpp"
  ],
  "toolPosture": "read-only",
  "delegationPacket": "Review the staged diff to Source/Core/src/Commands/FuzzyMatch.cpp for correctness bugs. The change rewrites the scoring loop. Report only genuine correctness bugs, each with file:line, a severity (critical/high/medium/low/info), and a one-line summary. If there are no genuine bugs, say so explicitly.",
  "dimensions": [
    { "key": "correctness", "kind": "judge", "description": "Did the agent correctly conclude there are no genuine bugs, rather than inventing false positives?" },
    { "key": "finding_count", "kind": "objective", "check": "finding_count", "description": "Reference finding count is 0 — any reported finding lowers this score." }
  ],
  "referenceOutcome": {
    "expectedFindingCount": 0,
    "allowedSeverities": ["critical", "high", "medium", "low", "info"],
    "expectedFindings": [],
    "notes": "Seed calibration case for the MVP contract proof — replace with a harvested real trace in Phase 2. The correct outcome is zero findings; the finding_count objective check penalises over-reporting, and the judge dimension rewards an explicit 'no genuine bugs' conclusion."
  }
}
