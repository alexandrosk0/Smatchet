# Robolectric config for SmatchetActivity JVM unit tests (WS5 item 17,
# docs/plans/mobile-mvp-completion.md). Pin the emulated SDK to the
# module's compileSdk (34) so Robolectric fetches one matching android-all jar
# instead of probing per-test — keeps the offline CI classpath deterministic.
sdk=34
